# Documentation Hub

This is the single source of truth for the product.

## Sections
- `planning/` Sequenced backlogs (e.g. orchestrator-full-sweep → executor)
- `decisions/` Product and release verdicts / decision log
- `product/` Feature and behaviour documentation
- `user/` End-user guides
- `journeys/` End-to-end user journeys
- `technical/` Architecture and implementation notes
- `api/` API behaviour and contracts
- `data/` Schemas, entities, validation, and integrity rules
- `ui/` UI patterns, states, and design behaviour
- `testing/` Test plans, cases, and regression coverage
- `security/` Auth, permissions, and security controls
- `operations/` Internal processes and support guidance
- `decisions/` Product and technical decision logs

## Recently shipped

| Date | Feature | Docs updated |
|------|---------|--------------|
| 2026-04-18 | **Release-gate cleanups: legacy EmptyState deletion (R1), mobile cook-timer analytics parity (R2)** — the two small residuals from the `post-feature-expansion-audit-2026-04-18-verification.md` CONDITIONAL GO verdict. **R1:** deleted `src/app/components/EmptyState.tsx` (the pre-M5 empty-state component with the `{ icon, title, description, ctaLabel, onCtaClick, secondaryCtaLabel, onSecondaryCtaClick }` prop shape) and migrated its only remaining consumer (`src/app/components/ShoppingList.tsx`) to the shared `src/app/components/suppr/empty-state.tsx` primitive. CTA preserved as an `<Button variant="outline" />` in the new `action` slot; the legacy `empty_state_cta_clicked` analytics fire is preserved with its pre-migration `{ title, ctaLabel }` payload. Grep confirmation: `grep "components/EmptyState"` on `src/` returns zero. Mobile `apps/mobile/components/EmptyState.tsx` untouched (already the new-primitive mirror). **R2:** mobile `apps/mobile/app/cook.tsx` was silent on three analytics events web has fired since Batch 3.8. Added `cook_mode_opened { recipeId, stepCount }` on mount and `recipe_timer_started { recipeId }` when the timer starts. `recipe_timer_completed` is **intentionally not fired** on mobile — the mobile timer is a count-up stopwatch with no natural completion event (the user always presses Stop); firing on Stop would conflate "user cancelled" with web's "countdown hit zero". Mobile `recipe_timer_started` payload omits `seconds` for the same reason: no pre-set duration exists at start time, and emitting a placeholder would poison the dashboard. Divergence documented inline in `cook.tsx` and pinned by a regression test (`apps/mobile/tests/unit/cookAnalyticsParity.test.ts`) so a future "fix the missing event" PR surfaces this reasoning before landing. Tests: +3 web (`tests/unit/shoppingListEmptyState.test.tsx` — renders new primitive, fires `empty_state_cta_clicked` with legacy payload, hides when items exist), +5 mobile (cook analytics parity — event + import structural checks). Web 92 files / 1086 tests green, mobile 6 files / 67 tests green; web tsc + mobile tsc clean. Zero user-facing behaviour change beyond the two new mobile events firing where web already did. | `DOCUMENTATION_HUB.md`, `planning/post-feature-expansion-audit-2026-04-18-verification.md`, `planning/sweep-2026-04-executor-backlog.md` |
| 2026-04-18 | **Audit L4 + L5 — Eat-again dismiss clock-rollback + slot-name single-source-of-truth** — two correctness/robustness fixes shipped together, zero user-facing behaviour change. **L4 (F6 in backlog):** previous dismiss rule stored only today's dateKey under `"suppr-eat-again-dismissed"` (localStorage / AsyncStorage). A device clock rollback (travel across time zones, device wobble, manual date change) could resurrect the banner on the same real-world day. New shared helper `src/lib/nutrition/eatAgainDismiss.ts` stores `{ dateKey: "YYYY-MM-DD", dismissedAt: ISO-8601 }` under v2 key `"suppr-eat-again-dismissed-v2"`. `shouldShowEatAgain(stored, now)` hides the banner when **either** (a) `stored.dateKey === today's local date` OR (b) `|now - dismissedAt| < 12h` (clock-rollback safety net). `migrateLegacyDismiss(v1Raw, now)` lifts the v1 bare-dateKey string into a v2 record stamped at `now` so an upgrade never loses the user's dismiss; `readDismissState(v2Raw, v1Raw, now)` picks v2 first, falls back to v1. Both `NutritionTracker.tsx` (web, localStorage) and `apps/mobile/app/(tabs)/index.tsx` (mobile, AsyncStorage) consume the shared helper — mobile also runs an opportunistic v1 → v2 background migration write the first time it reads. 27 new vitest cases pin the storage shape, same-day rule, rollback window, invalid-`now` degrade path, migration success + rejection paths, and the full end-to-end "dismiss → later query" flow (same day inside + outside window, next-day past window, cross-midnight rollback). **L5 (F7 in backlog):** new shared helper `src/lib/nutrition/mealSlots.ts` exports `MEAL_SLOTS = ["Breakfast", "Lunch", "Dinner", "Snacks"] as const`, the `MealSlot` type, `isMealSlot(s)` strict guard, and `normaliseMealSlot(raw)` tolerant resolver (case-insensitive + trim + `"Snack"` → `"Snacks"` migration). Comparison sites swept to the helper: `computeEatAgainForSlot` in `src/lib/nutrition/foodHistory.ts` (`slot.trim().toLowerCase() === mSlot.trim().toLowerCase()` → `normaliseMealSlot(slot) === normaliseMealSlot(m.name)`); `normaliseSlot` in `src/lib/nutrition/savedMeals.ts` (4-way `===` chain → `isMealSlot(s)`); `handleOpenSaveCombo` + mobile save-combo dialog opener (4-way `===` chains → `isMealSlot`); mobile `apps/mobile/app/(tabs)/planner.tsx` `swapMeal` slot-target branch (four `slotName.toLowerCase() === "..."` calls → single `normaliseMealSlot(slotName)` + branch on canonical) and the recipe-fit filter; `journalSlotFromMealTypes` in `apps/mobile/app/recipe/[id].tsx` (inline 4-tuple + `.toLowerCase()` find → `normaliseMealSlot(mealType[0])`). UI rendering literals (headers, icons-by-slot lookups, Quick Add labels, `<Text>Breakfast</Text>` etc.) intentionally kept as-is per spec — only *comparison* sites moved. Test-data literals in `tests/unit/*.ts` intentionally untouched. 11 new vitest cases pin `MEAL_SLOTS` contents + order, `isMealSlot` strict acceptance / rejection (including legacy `"Snack"` rejected at strict, accepted at tolerant), and `normaliseMealSlot` on lowercase / UPPERCASE / Title-case / whitespace / legacy singular / unrelated strings / non-string inputs. Totals: +38 new test cases; web + mobile tsc clean; full vitest run 91 files / 1083 tests green. | `DOCUMENTATION_HUB.md`, `technical/components.md`, `planning/sweep-2026-04-executor-backlog.md` |
| 2026-04-18 | **Audit M11 — ship the 4 deferred component / CRUD tests (G8, G16, F1, F2)** — the four heavy tests deferred from earlier batches now have real implementations. **G8 (week-start-day round-trip):** new shared helper `src/lib/nutrition/weekStartDayClient.ts` (`loadWeekStartDay` / `saveWeekStartDay` / `isWeekStartDay`) backs both web `Settings.tsx` and mobile `apps/mobile/app/(tabs)/more.tsx` — a single canonical `update({ week_start_day }).eq("id", uid)` shape that cannot drift. 15 vitest cases across web + mobile pin Monday-tap / Sunday-tap payloads, select-based hydration, unknown-value fallback, and error propagation. **F1 (favourites CRUD):** `tests/unit/favoriteFoodsClient.test.ts` exercises `listFavorites` / `addFavorite` / `removeFavorite` / `isFavorite` against a chainable supabase stub — including the PG 23505 unique-violation recovery path (read the existing row, return it as success) and the duplicate-key message fallback when no `code` is present. Replaces the deferred NOTE at the bottom of the pre-existing `favoriteFoods.test.ts`. **F2 (dialog render):** RTL render tests for `CopyMealDialog` + `DuplicateDayDialog` pin source-day exclusion, deduping, quick-range chip expansion, date-range inclusive expansion, reversed ranges disabling confirm, and the zero-meal-source edge. **G16 (fit-this-in live reactivity):** `foodSearchFitThisIn.test.tsx` mounts `<FoodSearch>` with a seeded custom food (via `vi.mock` on `customFoodsClient`) + a stubbed `fetch`, clicks through to the portion preview, and asserts the "If you log this" projection updates 100g → 50g → 200g in lock-step with `projectRemaining(...)`, flipping to `+N over` framing when the kcal budget is exceeded. Vitest config gains `esbuild.jsx = "automatic"` so components that don't explicitly `import React` (e.g. `copy-meal-dialog.tsx`) render cleanly under the automatic JSX runtime. **Mobile deferred (infrastructure-blocked):** `@testing-library/react-native` is not installed on `apps/mobile`; RNTL counterparts for G16 mobile and F2 mobile cannot land until the package + jest-expo preset are added. Backlog rows for G16 / F2 flag this explicitly. Totals: +43 new test cases; web 1045 + mobile 62 green; both tsconfigs clean. | `DOCUMENTATION_HUB.md`, `testing/test-plan.md`, `planning/sweep-2026-04-executor-backlog.md`, `planning/post-feature-expansion-audit-2026-04-18.md` |
| 2026-04-18 | **Audit M7 — replace `window.prompt` / `window.confirm` with themed dialogs** — every use of the unthemed browser prompts on web was swept out and replaced with shadcn `Dialog` / `AlertDialog` primitives. Three new reusable components under `src/app/components/suppr/`: `DestructiveConfirmDialog` (single destructive-confirm primitive — `AlertDialog` + destructive-variant button + `"This can't be undone."` description pattern), `RenameSavedMealDialog` (themed saved-meal rename with inline `normaliseSavedMealName` validation — trim + 80-char cap centralised in `src/lib/nutrition/savedMeals.ts` as `SAVED_MEAL_NAME_MAX_LENGTH` / `normaliseSavedMealName(raw)`), and `TextPromptDialog` (generic themed single-text-input prompt for named-slot create/rename flows). Call sites converted: **`quick-add-panel`** saved-meal rename + delete, **`FoodSearch`** custom-food delete, **`Settings`** "Delete local data & sign out" + two-stage account deletion (three confirms), **`MealPlanner`** new-plan / rename-plan / delete-plan + apply-template / leftover-clearing swap / keyboard-accessible Move fallback, **`today-meals-section`** meal-row Delete overflow item, **`plan-templates-dialog`** template delete action, and **`RecipeDetail`** copy-link fallback (now surfaces the URL via a 15-second `toast.message` instead of `window.prompt`). `grep window\\.(prompt\\|confirm)\\s*\\(` on `src/` returns zero after the sweep (only comment references remain). Behaviour preserved: rename still renames, delete still deletes, destructive flows still double-confirm. Mobile untouched — `Alert.prompt` / `Alert.alert` are the native iOS patterns and stay. Radix `AlertDialog` gives focus trap, labelled title + description, `Escape` / overlay-click cancel handling, and destructive-variant Delete buttons out of the box. Web tsc clean, mobile tsc clean, 1045 vitest tests still green. | `DOCUMENTATION_HUB.md`, `technical/components.md`, `ux/patterns.md` |
| 2026-04-18 | **Today progressive disclosure (audit M4)** — the Today screen on web and mobile previously stacked 10 cards top-to-bottom on first run, making it hostile to brand-new users. The audit's Biggest Structural Risk. M4 lands four additive gates — **no card or feature is removed**, nothing is forced off for an existing user who had interacted with it. (1) **Hydration & stimulants card**: visible once `target_water_ml > 0` OR the user has logged any water / caffeine / alcohol (directly or via a meal that carries `waterMl`). First-run fallback is a tiny "Track hydration?" link that reveals the card on tap — no state is written until the user logs. (2) **Quick Add panel**: collapsed behind a single "Quick add" CTA above the Meals section on both platforms. Default collapsed on first run; user's last open/closed choice persists per device under `suppr-quick-add-collapsed-v1` (localStorage on web, AsyncStorage on mobile). The mobile FAB → Previous full-screen overlay is preserved as the power-user path. (3) **Steps & activity card**: visible once `steps_by_day` or `activity_burn_by_day` is non-empty (i.e. Apple Health / Google Fit has synced at least once). First-run fallback is a "Connect health" link — mobile opens the Health Sync screen, web reveals the card so the user can log steps manually. (4) **Adaptive TDEE hint gate**: documented for future hint UI — visibility tied to the same `getEffectiveTDEE` threshold (medium/high confidence) OR ≥ 14 logged days, so any hint we ship later agrees with the calorie budget. All rules live in the shared pure helper `src/lib/nutrition/todayProgressiveDisclosure.ts` so web and mobile cannot drift. Result: a first-run user sees **only** Day strip + Calorie ring + Remaining macros + Meals section + collapsed Quick Add CTA. Every other card reveals on state or on explicit manual tap. Returning users whose state already passes the gates keep all their cards — the gates are sticky. Accessibility labels preserved; nothing silent. 23 new vitest unit tests pin `isHydrationCardVisible` / `isStepsCardVisible` / `isAdaptiveTdeeHintVisible` / `parseQuickAddCollapsed` / `serializeQuickAddCollapsed` behaviour across fresh-user, logged-water, synced-health, and returning-user paths. Web tsc + mobile tsc + full vitest suite (88 files / 1043 tests) clean. | `DOCUMENTATION_HUB.md`, `product/overview.md`, `journeys/food-tracking.md`, `technical/components.md`, `ux/patterns.md`, `testing/test-plan.md` |
| 2026-04-18 | **Audit M3 + M5 + M6 polish batch** — three independent post-feature-expansion polish items landed together. **M3 (Bulk insert for Duplicate-day range):** web `useNutritionJournalState` gains `addLoggedMealsForDate(dayKey, meals[])` that batches the fresh ids + does a single `insert([...rows])` to `nutrition_entries` and returns the inserted rows. `duplicateDay`, `duplicateDayToDateRange`, and `copyMealToDateRange` all route through it so a 7-day × 4-meal duplicate collapses from **28 sequential single-row inserts to 7 batched inserts** (one per target day). Analytics fires **ONE** `food_logged { count, batched: true, source: "duplicate_day" | "copy_meal" }` event per batch — not N events. Mobile parity: `insertClonedRowsIntoDay` (already bulk-per-day) now returns the insert count, and the three range/duplicate callers accumulate it into a single batched `food_logged` event matching web. Single-meal `addLoggedMealForDate` is unchanged — still a primitive, still fires `food_logged { calories, fromPlanner }` once per log. New unit test file `tests/unit/nutritionJournalBulkInsert.test.tsx` pins: (a) exactly 1 Supabase insert per target day with an **array** payload, (b) exactly 1 `food_logged` event per batch with `count` equal to total inserted rows, (c) no-op inputs (same-day, empty source) don't insert and don't fire events, (d) single-meal path still uses single-row insert. **M5 (Shared EmptyState primitive):** single `<EmptyState icon title description action />` per platform (`src/app/components/suppr/empty-state.tsx` on web, `apps/mobile/components/EmptyState.tsx` on mobile). `title` accepts rich React children so callers preserve existing inline emphasis without forking. Replaced 3 noisiest empty-state sites on both platforms — Favourites tab, Frequent tab, and My meals tab (signed-out + no-combos) — with identical strings on web and mobile (copy preserved verbatim; only the rendering moved into the primitive). Pre-audit strings were not broken, only stylistically inconsistent, so M5 did not rewrite them. New `tests/unit/emptyState.test.tsx` locks the primitive's shape (title / description / action / icon / rich children / className passthrough / icon aria-hidden). **M6 (Card radii audit):** picked `rounded-card` (web, `var(--radius-card) = 1rem / 16px`) and `Radius.lg` (mobile, `16`) as the single card-shell radius across the product. No new tokens. Replaced drift sites in `src/app/components/suppr/` (today-streak-insight-card, recipe-notes-card ×2, weekly-recap-card ×2 including inline stat tiles, today-week-view ×5, today-steps-card, today-dashboard-macro-tiles ×4, remaining-macros-bar, today-activity-bonus-card, macro-card, hydration-stimulants-card — **20 sites**). Mobile replaced card-shell drift in `RemainingMacrosBar` (container), `QuickAddPanel` (saved-meal row + history row), and `MoveMealSheet` (destination row) — **4 sites**. Buttons, pills, badges, chips, inputs, dropdown cells, and camera frames intentionally left untouched per the M6 scope. `docs/ux/patterns.md` gains "Empty States" and "Card radius" sections covering when the primitive / token applies and when it does not. Zero user-facing behaviour change beyond (a) Duplicate-range feels faster on large ranges, (b) empty-state tabs look consistent, (c) card corners all match at 16px. | `DOCUMENTATION_HUB.md`, `technical/components.md`, `ux/patterns.md` |
| 2026-04-18 | **Audit M8 + M9 + M10 polish batch** — three low-risk hygiene fixes landed together. **M8**: `src/app/components/suppr/hydration-stimulants-card.tsx` `Row` subcomponent's hand-rolled overflow menu (`menuOpen` state + `onMouseLeave` close, no keyboard support, no focus trap) replaced with the canonical shadcn `<DropdownMenu>` / `<DropdownMenuTrigger>` / `<DropdownMenuContent>` / `<DropdownMenuItem>` pattern — same single "Reset today" action, now with `role="menu"`, focus trap, arrow-key navigation, and `Escape`-to-close for free via Radix. Mobile hydration card (RN `Modal`-based sheet per audit guidance) untouched. **M9**: two new theme tokens `--stimulant-caffeine` (`#8b5cf6` violet) and `--stimulant-alcohol` (`#f59e0b` amber) added to `src/styles/theme.css` light + dark blocks and mirrored in `apps/mobile/constants/theme.ts` as `StimulantColors`. Both hydration cards now route caffeine + alcohol colours through the tokens — `grep -E '#[0-9a-fA-F]{3,6}'` on both card files returns zero hits. Hex values unchanged; just tokenised. **M10**: canonical AI-source constants `AI_VOICE_SOURCE = "AI voice"` and `AI_PHOTO_SOURCE = "AI photo"` plus the `aiLoggingSourceLabel(source)` helper landed in `src/lib/nutrition/aiLogging.ts`. Both voice + photo commit paths (web `NutritionTracker.tsx`, mobile `app/(tabs)/index.tsx`) call the helper so new rows write the canonical human-readable form. `isAiSourcedFoodHistoryItem` in `foodHistory.ts` stays permissive — legacy rows (`"voice"`, `"ai_voice"`, `"ai_photo"`) still surface the AI badge in the Quick Add Recent tab. Historical rows are not migrated — backwards-compat via the detector is sufficient. 13 new tests (3 label contract + 10 detector backwards-compat) pinned in `tests/unit/aiLogging.test.ts` + `tests/unit/foodHistory.test.ts`. No user-facing behaviour change on any of the three items. | `DOCUMENTATION_HUB.md`, `technical/components.md`, `ux/brand-tokens.md`, `ux/patterns.md` |
| 2026-04-18 | **Today composition split (audit H3)** — `NutritionTracker.tsx` was 3,142 LOC and mobile `app/(tabs)/index.tsx` was 3,831 LOC after H1 landed. Both were the single most-touched files in the product. Every per-card / per-section JSX block on Today is now its own file under `src/app/components/suppr/today-*.tsx` (web) or `apps/mobile/components/today/Today*.tsx` (mobile). `NutritionTracker.tsx` → 1,619 LOC. Mobile Today → 2,777 LOC. Zero behaviour change: every existing analytics firing point, `aria-label` / `accessibilityLabel`, test, and state flow preserved. Web sub-cards: `today-date-header`, `today-hero-ring`, `today-eat-again-banner`, `today-streak-insight-card`, `today-fasting-pill`, `today-steps-card`, `today-activity-bonus-card`, `today-week-view`, `today-dashboard-macro-tiles`, `today-quick-log-strip`, `today-meals-section`, `today-complete-day-dialog`, `today-add-meal-dialog`, `today-barcode-dialog`. Mobile sub-cards: `TodayDateHeader`, `TodayHeroRing`, `TodayFastingPill`, `TodayEatAgainBanner`, `TodayStreakInsightCard`, `TodayActivityCard`, `TodayActivityBonusCard`, `TodayWeekView`, `TodayMealsSection`, `TodayCompleteDayModal`, `TodayFabSheet`, `TodayEditMealModal`, `TodayNutrientsModal`, `TodayDashboardMacroTiles`, `TodayQuickLogStrip`, `TodayDeficitInsight`, `TodayPlannedMealsCard`, `TodayAddFoodForm`. Both monster files are now composition roots — new Today behaviour lands in a sub-card, not inside the root. Pre-existing hook + effect + state logic stays in the root (flagged in the H3 report as a deliberate "do not over-engineer" boundary). Aspirational targets were <1000 LOC web + <1500 LOC mobile; the mobile root retains ~2000 LOC of pre-JSX hooks (HealthKit sync, profile load, fasting timer, celebration triggers) which would require a custom-hook extraction to trim further and was out of scope for this audit. | `DOCUMENTATION_HUB.md`, `technical/components.md` |
| 2026-04-18 | **Badge primitive unified (audit H2)** — single `<Badge variant=… />` primitive per platform (`src/app/components/suppr/badge.tsx` on web, `apps/mobile/components/Badge.tsx` on mobile) now owns every compact pill label that used to be rendered as an inline span / `<View>+<Text>`. Variants: `neutral / info / warn / pro / ai / added / override / leftover / custom / freeze`. One size, padding, radius, weight and tracking — variants swap colour only. Default `aria-label` / `accessibilityLabel` per semantic variant (pro / override / leftover / freeze / ai / added / custom). Replaced call sites: web `RecipeDetail` (Override + Added row badges), `FoodSearch` (Custom), `MealPlanner` (Leftover tile), `quick-add-panel` (AI on Recent tab), `voice-log-dialog` + `photo-log-dialog` (AI estimate), `NutritionTracker` (Freezes available on streak insight). Mobile `FoodSearchModal` (Custom), `recipe/verify` (Override + Added), `planner.tsx` (Leftover), `VoiceLogSheet` + `PhotoLogSheet` (AI estimate), `app/(tabs)/index.tsx` (Freezes available). Divergent inline `px-1.5 py-0.5 / text-[9px] / text-[10px]` styles retired. `docs/ux/patterns.md` adds a "Badges" section with the full variant table. See also: amendment under the 5.13 Voice/Photo row below. | `DOCUMENTATION_HUB.md`, `technical/components.md`, `ux/patterns.md` |
| 2026-04-18 | **Mobile planner Move action (batch 3.10 parity fix, audit C2)** — the web 3.10 drag-drop + keyboard "Move" fallback shipped in April 2026 without a mobile counterpart. Audit C2 flagged the gap (zero grep hits for Move / moveMeal on mobile). This lands the mobile entry point: long-press any meal row in `apps/mobile/app/(tabs)/planner.tsx` → medium-impact haptic → `Alert.alert` action sheet (Move / Swap / Delete / Cancel) → new `apps/mobile/components/MoveMealSheet.tsx` destination picker (every day × slot in the current week, source cell marked `FROM` and disabled, every destination exposed as `accessibilityRole="button"` with `accessibilityLabel="Move to {DayName} {Slot}"`). Tapping a destination runs the shared `moveMealInPlan` helper — two-way swap when the target has a meal, empty-slot replace otherwise, destination slot label always preserved. Parent-of-leftovers meals trigger a factual confirm Alert ("This will remove N leftover meals.") and run `markLeftoversOnSwap` before the move. Delete replaces the slot with an empty placeholder and recomputes totals. All flows persist through the same relational-tables-with-legacy-JSONB-fallback path the generate flow uses. Analytics: `meal_moved_in_plan { fromSlot, toSlot, crossDay }` now fires from mobile as well as web. Zero duplication — no new logic tests needed (`leftoversPlanner.test.ts` already covers `moveMealInPlan`). No shame copy anywhere. | `DOCUMENTATION_HUB.md`, `product-roadmap.md`, `technical/components.md`, `mobile_qa_uat_test_plan.md` |
| Apr 2026 | **Voice logging + AI photo logging (Pro, batch 5.13)** — two hands-free Pro-tier ways to log a meal. Voice: press-and-hold the mic on Today (`VoiceLogDialog` / `VoiceLogSheet`) → transcript → `/api/nutrition/voice-log` (GPT-4o-mini parse + verified-nutrition pipeline) → review list where every item shows a confidence dot (0.75 / 0.5 thresholds) and an "AI estimate" badge with inline-editable macros → "Log all" commits. Photo: Snap chip → `PhotoLogDialog` / `PhotoLogSheet` using `<input type="file" capture>` / `expo-image-picker` → `/api/nutrition/photo-log` (GPT-4o vision + verified pipeline) → same review flow. Low-confidence items (<0.5) get amber border, `role="alert"` "please verify" note, and the commit button becomes "Log anyway" — nothing ever auto-logs. Pro gate is factual, not pushy: free + Base users see a "Voice/AI photo logging is a Pro feature. Upgrade to use it." dialog (web) or route to `/paywall?from=voice_log|photo_log` (mobile); no countdowns, no dark patterns. Logged rows carry `source: "AI voice"` / `"AI photo"` and surface with a subtle "AI" badge in the Quick Add Recent tab. Shared helper `src/lib/nutrition/aiLogging.ts` (`classifyConfidence`, `isLowConfidence`, `aggregateTotals`, `averageConfidence`, `sanitiseAiItem`, `sanitiseAiItems`) owns sanitisation + classification + totals across both platforms so the behaviour cannot drift. New analytics: `voice_log_started`, `voice_log_committed { itemCount, avgConfidence }`, `voice_log_paywalled`, `ai_photo_log_started`, `ai_photo_log_committed`, `ai_photo_log_paywalled`. 37 new unit tests cover the helper. Deferred: Whisper-based server audio upload; native Android mic permission UX polish. **Legacy cleanup (2026-04-18, audit C4)** — removed unreachable pre-5.13 voice/photo code from `apps/mobile/app/(tabs)/index.tsx`: `handleVoiceLog`, `handlePhotoLog`, `submitVoiceTranscript`, `photoAnalyzing` / `voiceInputOpen` / `voiceInputText` state, the photo-analyzing overlay + voice-text fallback modal JSX, and the now-unused `VOICE_LOG_NATIVE_BUILD_HINT` import. Audit confirmed no call sites remained after the Snap/Voice chips + FAB were rewired to `handleOpenVoiceLog` / `handleOpenPhotoLog`. `apps/mobile/lib/voiceLog.ts` is preserved — still imported by `VoiceLogSheet.tsx` for native speech recognition. Net `-287` LOC on Today (4,419 -> 4,132). **Amendment (2026-04-18, audit H2):** the "AI estimate" and "AI" badges introduced by 5.13 now render through the shared `<Badge variant="ai" />` primitive (see separate row above). Same strings, consistent pill shape with Override / Added / Custom / Leftover / Freeze. | `DOCUMENTATION_HUB.md`, `product/overview.md`, `product-roadmap.md`, `journeys/food-tracking.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md`, `ux/patterns.md` |
| Apr 2026 | **Siri Shortcuts deep links (batch 5.12 — widget deferred)** — Three `suppr://` deep links cover Siri / Shortcuts-app automation without a Swift Intent extension: `suppr://log/water?ml=250`, `suppr://fast/start?hours=16`, `suppr://today/remaining`. **Audit 2026-04-18 C5: the iOS home-screen widget is NOT shipped — no native WidgetKit extension is in the Xcode project; the snapshot writer writes a file no extension currently reads. Kept the `widgetSnapshot.ts` infra in place (ready for when a native target is added) but removed "widget" from the shipped claim.** See details on Siri links below. [Legacy 5.12 description preserved for reference] — Suppr writes a compact "today snapshot" (kcal consumed / target, protein / carbs / fat remaining, active fast + target hours, `updatedAt`) to AsyncStorage + a shared App Group-accessible file on every macro / fast change (debounced 500 ms), so a native iOS widget extension (deferred to real Xcode work) can render the calorie ring + remaining macros on Home + Lock screens. Three `suppr://` deep links cover Siri / Shortcuts-app automation without a Swift Intent extension: `suppr://log/water?ml=250` (defaults to 250, clamped 1..5000), `suppr://fast/start?hours=16` (defaults to 16, clamped 1..48), `suppr://today/remaining` (opens Today). URLs are parsed with the WHATWG `URL` class (no regex), a present-but-non-numeric param rejects, the scheme is case-insensitive, and malformed input never throws. A single-slot "latest-wins" pending-action queue (`lib/siriPending.ts`, 5-min TTL) bridges `_layout.tsx` → Today so the layout stays provider-free. Water actions flow through the existing `addWaterMl`; fast actions append to `profiles.fasting_sessions` and never stack a second active session. VoiceOver announcement fires immediately via `AccessibilityInfo.announceForAccessibility`. Shared helpers `src/lib/nutrition/widgetSnapshot.ts` + `siriDeepLinks.ts` (pure); mobile wrappers `apps/mobile/lib/widgetSnapshot.ts` (AsyncStorage + best-effort `expo-file-system`) + `siriDeepLinks.ts` (re-export) + `siriPending.ts`. No new native packages installed — iOS widget-extension wiring and `react-native-siri-shortcut` donation are deferred and documented. Analytics `widget_snapshot_updated` + `siri_action_invoked { kind }`. Tests: 45 mobile (12 widgetSnapshot + 33 siriDeepLinks) + 9 shared (3 + 6). | `DOCUMENTATION_HUB.md`, `product/overview.md`, `product-roadmap.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md`, `journeys/shortcuts-and-widgets.md` (new), `user/shortcuts-and-widgets.md` (new) |
| Apr 2026 | **Streak freeze + Weekly recap card + push (batch 4.11)** — retention-friendly streak protection plus an end-of-week recap moment. (a) Users hold a small budget of "freeze" credits (default cap 3, 0..10; `0` disables the feature per user) earned automatically at each 7-day streak milestone; each freeze absorbs a single zero-meal day so a sick or travel day doesn't break the streak. Raw streak (`computeLoggingStreak`) is never overwritten — the protected value is a derived output of `computeProtectedStreak` so the UI can surface both. Factual UI copy only ("Freeze used (Tue)", never "Streak saved!"). (b) `WeeklyRecapCard` on the Progress dashboard surfaces at the end of the user's week (Sunday 18:00 for Monday-start, Saturday 18:00 for Sunday-start) summarising avg calories, avg protein + adherence %, streak + freezes, weight delta (suppressed when <2 weigh-ins), and best day. Share week uses `navigator.share` / clipboard on web, RN `Share` API on mobile. Dismiss writes `profiles.weekly_recap_last_seen_week_key` so the card never re-appears for the same week. (c) Mobile-primary local push via `expo-notifications` `WEEKLY` trigger at 18:00 — respects `profiles.weekly_recap_push_enabled` (default true). Stable `weekly-recap-v1` identifier → idempotent rescheduling, no duplicates. Web push deferred (no service-worker push infra yet). Shared pure helpers `src/lib/nutrition/streakFreeze.ts` + `weeklyRecap.ts` power both platforms; mobile re-exports via `apps/mobile/lib/streakFreeze.ts` + `weeklyRecap.ts`. Web `suppr/weekly-recap-card.tsx`, mobile `apps/mobile/components/WeeklyRecapCard.tsx`, scheduler `apps/mobile/lib/weeklyRecapPush.ts`. Migration `20260421170000` adds 5 profile columns. Analytics: `streak_freeze_used { dateKey, freezesRemaining }`, `streak_freeze_earned { newStreak }`, `weekly_recap_shown { weekKey }`, `weekly_recap_dismissed`, `weekly_recap_shared { weekKey, platform }`, `weekly_recap_push_sent { weekKey }`. Tests: 44 new (24 streak freeze + 20 weekly recap). **Freeze earned + used moments surfaced (2026-04-18 audit H7):** the earning + consuming side of the feature was invisible before — `streak_freeze_earned` fired but nothing confirmed it on-screen, and `computeProtectedStreak.protectedDateKeys` was never rendered. Two additive surfaces land on both platforms: (i) a one-time compact "You earned a freeze — N available" row with a "Got it" button under the streak insight card on Today (web `NutritionTracker`, mobile `app/(tabs)/index.tsx`) — gated by `newestEarnedAt > lastSeenEarnedAt`, dismissal writes today's ISO to `localStorage`/`AsyncStorage` key `suppr-last-seen-freeze-earned-at`, no migration; fires new `streak_freeze_earned_seen { earnedAt }` analytics event on dismiss; (ii) a ❄ glyph in the top-right of each `DayStrip` tile whose dateKey is in `protectedDateKeys` (web `src/app/components/DayStrip.tsx`, mobile `apps/mobile/components/charts/DayStrip.tsx`) with `aria-label` / `accessibilityLabel="Freeze used on {dateKey}"`. Parent components compute `protectedDateKeys = new Set(computeProtectedStreak(...).protectedDateKeys)` once and pass to both DayStrip instances (day view + week view). Uses the existing shared helpers — no math changed, cap untouched, factual copy, additive only (users who never earned a freeze see nothing extra). **Weekly recap push Settings toggle (2026-04-18 audit H6):** the push shipped in Batch 4.11 without an in-app opt-out — users could only escape via iOS OS push settings (store-review risk, dark-pattern adjacent). This lands a first-class toggle on both platforms. Web `Settings.tsx` Notifications section adds a shadcn `<Switch>` row ("Weekly recap", sub: "Sunday 18:00 (respects your week start)." or "Saturday 18:00 …" depending on `week_start_day`). Mobile `app/(tabs)/more.tsx` Connections section adds a `SettingsRow` ("Weekly recap", sub: current day + 18:00 or "Off · re-enable to get the Sun/Sat 18:00 nudge") that opens a bottom-sheet modal hosting a RN `Switch` (`accessibilityRole="switch"`, `accessibilityLabel="Weekly recap push notifications"`). Toggling off → `cancelWeeklyRecapPush()` clears the iOS queue immediately so no more pushes arrive; toggling on → `scheduleWeeklyRecapPush({ enabled: true, weekStartDay })` reinstalls the `WEEKLY` trigger. Both platforms write `profiles.weekly_recap_push_enabled` via the same server-owned column; on DB error the toggle reverts + surfaces a toast (web) / Alert (mobile). Default stays `true` (unchanged), push time + cadence unchanged, `weeklyRecapPush.ts` interface unchanged. New analytics event `weekly_recap_push_enabled_toggled { enabled }` registered in `AnalyticsEvents` and pinned in `tests/unit/analyticsEvents.test.ts`. Existing Progress-visit scheduler retained as defensive fallback so the OS queue reconciles on next launch regardless of which device made the change. `tests/unit/weeklyRecap.test.ts` gains a contract test that `shouldShowRecap` is independent of the flag (card still appears for opted-out users who open the app). | `data/schema.md`, `product/overview.md`, `product-roadmap.md`, `journeys/progress.md` (new), `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md`, `ux/patterns.md` |
| Apr 2026 | **Drag-drop meal between days + Save plan as template + Leftovers-aware plan (batch 3.10)** — three meal-planner upgrades shipping together. (a) Move a planned meal to another day or slot without regenerating the plan — web uses HTML5 drag-drop on each card plus a keyboard-accessible "Move" button as the a11y fallback (`day,slot` prompt); mobile uses the meal action sheet. Source and destination two-way swap; slot labels stay pinned. Analytics `meal_moved_in_plan { fromSlot, toSlot, crossDay }`. (b) Save a week (or any 1–7 day slice) as a named plan template ("Bulk week", "Vacation week") and re-apply it to any future week. Persists in `public.user_plan_templates` (case-insensitive unique per user, RLS owner-only). Empty-week saves **fail loudly** ("This plan has no meals to save."). Analytics `plan_template_created { dayCount, slotCount }` + `plan_template_applied`. (c) When a recipe yields > 1 serving, the remaining servings auto-fill matching subsequent empty slots as `🍱 Leftover of [recipe]`. Dinner leftovers go to next-day lunch/dinner; breakfast to breakfast/snack. Leftover macros equal parent scaled macros — flag is purely visual. Swapping a parent shows a factual confirm ("This will remove 2 leftover meals.") and clears downstream copies. Analytics `plan_leftovers_generated { parentCount, leftoverCount }`. Shared pure helpers `src/lib/nutrition/planTemplates.ts` + `leftoversPlanner.ts` + `planTemplatesClient.ts`; web dialog `suppr/plan-templates-dialog.tsx`; mobile sheet `apps/mobile/components/PlanTemplatesSheet.tsx`. Migration `20260421160000_plan_templates_and_leftovers.sql` introduces `user_plan_templates` and adds leftover columns to `meal_plan_days` + `meal_plan_meals`. Tests: 28 new (15 + 13). | `data/schema.md`, `product/overview.md`, `product-roadmap.md`, `journeys/meal-planning.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md` |
| Apr 2026 | **Custom foods with multiple serving sizes (batch 3.9)** — users can now create a food that isn't in USDA or OFF (homemade granola, local-bakery pastry), give it any number of named serving shortcuts (e.g. "1 bowl = 80g", "1 tbsp = 12g"), and log it with the portion picker automatically scaling macros linearly via the shared `scaleMacrosForGrams` helper. Shared pure helpers `src/lib/nutrition/customFoods.ts` + `customFoodsClient.ts` (Supabase CRUD); web `src/app/components/suppr/create-custom-food-dialog.tsx`; mobile `apps/mobile/components/CreateCustomFoodSheet.tsx`. **FoodSearch wire-up (2026-04-18, audit C1)** — `FoodSearch.tsx` (web) and `FoodSearchModal.tsx` (mobile) now accept optional `supabase` + `userId` props and when both are present: load the user's custom library on open, merge `searchCustomFoods(...)` matches at the top of the results list with a "Custom" badge, expose a persistent "+ Create custom food" entry row (with the typed query pre-filled as Name), auto-select the newly created food into the portion picker so logging is one extra tap, and offer Edit / Delete via the row overflow menu (web) or long-press action sheet (mobile). Two new shared helpers `customFoodToMacrosPer100g` + `buildCustomFoodPortions` keep the macro projection and portion-chip construction identical on both platforms. Custom-food logs write to `nutrition_entries` through the existing insert path. Migration `20260421150000_user_custom_foods.sql` introduces `public.user_custom_foods` (macros per `base_grams` default 100; `servings jsonb` bounded to 20; unique `(user_id, lower(name))` with client-side " (2)" … " (9)" suffix fallback) with owner-only RLS. Zero-macro save is allowed behind a soft "Macros not set" notice. Analytics: `custom_food_created { hasBrand, servingCount }`, `custom_food_updated`, `custom_food_deleted`, `custom_food_logged { servingLabel?, grams }`. | `data/schema.md`, `product/overview.md`, `product-roadmap.md`, `journeys/food-tracking.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md` |
| Apr 2026 | **Recipe notes + rating + cook timers + wake lock (batch 3.8)** — per-user personal 1-5 star rating + free-text notes on any saved/authored recipe, surfaced on `RecipeDetail` (web) and `recipe/[id].tsx` (mobile) with debounced autosave. Cook mode gains auto-detected inline timers parsed from instruction steps ("10 minutes", "1 hour 20 min", "5-10 min" → upper bound) — tappable pills start countdowns that persist across step navigation and fire a completion beep + haptic. Cook mode also requests a Wake Lock (web `navigator.wakeLock`) / `expo-keep-awake` (mobile) while open; releases on exit or tab-hide, re-acquires on visibility return. Shared pure helpers `src/lib/nutrition/recipeTimers.ts` (parsing + formatting) and `src/lib/nutrition/recipeNotesClient.ts` (CRUD). Web `suppr/recipe-notes-card.tsx` + updated `CookMode.tsx`. Mobile `apps/mobile/components/RecipeNotesCard.tsx` + updated `cook.tsx`. Migration `20260421140000` adds `public.user_recipe_notes` (per-user, per-recipe; notes, personal_rating, cook_count, last_cooked_at; RLS owner-only). Analytics: `recipe_note_saved`, `recipe_timer_started`, `recipe_timer_completed`, `cook_mode_opened`. | `data/schema.md`, `product/overview.md`, `product-roadmap.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md` |
| Apr 2026 | **Add ingredient + per-ingredient overrides (batch 2.7)** — imported recipes sometimes miss a row ("I also added cheese") or pick the wrong USDA match for one line. Users can now (a) add a new ingredient post-import via "+ Add ingredient" on web `RecipeDetail` and mobile `recipe/verify.tsx` — new rows run the same verify pipeline used during import and persist with `added_by_user: true`; and (b) pin manual label values on any row via an "Override nutrition" action — the override replaces the matched macros in recipe totals without losing the match metadata, with a visible "Override" badge + Reset affordance. Shared pure helper `src/lib/nutrition/ingredientOverrides.ts` (`effectiveMacros`, `recomputeRecipeTotals`, `sanitizeOverrideInput`). Web dialogs `suppr/AddIngredientDialog` + `OverrideIngredientDialog`; mobile sheets `AddIngredientSheet` + `OverrideIngredientSheet`; mobile persistence helpers `addUserIngredient` + `setIngredientOverride` in `apps/mobile/lib/verifyRecipe.ts`. Migration `20260421130000` adds `recipe_ingredients.override_macros jsonb` + `added_by_user boolean`. Analytics: `recipe_ingredient_added { recipeId, hasMatch }`, `recipe_ingredient_overridden` / `recipe_ingredient_override_cleared` with `{ recipeId, ingredientPosition }`. | `data/schema.md`, `product/overview.md`, `product-roadmap.md`, `journeys/verify-ingredients.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md` |
| Apr 2026 | **Saved meals / combos (batch 2.6)** — users save any slot with 2+ logged items as a named bundle (e.g. "My usual breakfast" = oats + berries + protein powder) and re-log the whole combo in one tap from a new "My meals" tab in the Quick Add panel. Distinct from recipes (no ingredients/instructions) and from single favourites. Shared pure helpers `src/lib/nutrition/savedMealsLogic.ts` + `savedMeals.ts` (CRUD). Web: `suppr/SaveMealDialog`, `suppr/SavedMealsTab`, wired into `QuickAddPanel` + `NutritionTracker`. Mobile: `apps/mobile/components/SaveMealSheet.tsx` + inline "My meals" tab + per-slot "Save combo" chip on the meal-slots card. Migration `20260421120000` introduces `public.user_saved_meals` (parent row) + `public.user_saved_meal_items` (ordered children) with owner-only RLS; listing sorts by `(last_logged_at desc nulls last, created_at desc)` so recently re-logged combos bubble up. Re-log goes through the same `addLoggedMealForDate` (web) / `setByDay` (mobile) path as manual logs. Analytics: `saved_meal_created`, `saved_meal_logged` with `{ itemCount, slot }`, `saved_meal_deleted`. **Amendment (2026-04-18, audit H4):** removed the `window.dispatchEvent("suppr:open-save-meal-dialog")` custom-event bridge that previously linked `NutritionTracker`'s "Save combo" chip to the dialog hosted inside `QuickAddPanel`. `SaveMealDialog` is now rendered by the host `NutritionTracker`; the panel exposes `onOpenSaveCombo(slot?, seedItems?)` + `savedMealsRefreshToken` props so the save-combo flow is prop-driven and testable. Behaviour preserved: same dialog, same seed items, same post-save tab switch. | `data/schema.md`, `product/overview.md`, `product-roadmap.md`, `journeys/food-tracking.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md` |
| Apr 2026 | **Hydration quick-add polish + Caffeine tracker + Alcohol tracker (batch 2.5)** — Today dashboard gained a three-row "Hydration & stimulants" card (Water / Caffeine / Alcohol) on web and mobile. Water chips expanded to 100/250/500/750 ml. Caffeine tracked against a default 400 mg/day FDA guideline with quick-add presets (Espresso / Coffee / Filter / Tea). Alcohol tracked weekly in grams of ethanol with default 0 (row hidden) and quick-add presets (Beer / Wine / Spirit / Cider). Over-target copy is factual and amber — "Over 400 mg" / "Over limit" — never red. Shared pure helper `src/lib/nutrition/hydrationStimulants.ts`; web component `suppr/hydration-stimulants-card.tsx`; mobile component `apps/mobile/components/HydrationStimulantsCard.tsx`. Migration `20260421110000` adds four profile columns. Apple Health: inbound caffeine import on the existing nutrition-import throttle, idempotent via `max(existing, imported)` per day; outbound caffeine written on `exportDayToHealth`. Alcohol is backlogged — Apple HealthKit exposes only a `NumberOfAlcoholicBeverages` count type outside the dietary save-food-sample path. Analytics events `hydration_logged` + `stimulant_logged`. **Amendment (2026-04-18, audit C3):** mobile card now threads `measurementSystem` from `profiles.measurement_system`; water progress + "from logged food" sub-line + quick-add chips render in `fl oz` for imperial users (4 / 8 / 16 / 20 fl oz chip scale). Storage stays ml. New shared helpers `formatWaterAmount(ml, system)` + `imperialWaterQuickAdds()` replace the per-card formatter on both platforms so units cannot drift. | `data/schema.md`, `product/overview.md`, `product-roadmap.md`, `journeys/food-tracking.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md` |
| Apr 2026 | **Copy meal / Duplicate day (batch 1.4)** — per-meal "Copy to another day…" and day-header "Duplicate day…" actions with single-date, quick-range, and date-range targets on both web and mobile. Shared helper `src/lib/nutrition/copyMeals.ts` (cloneMealWithoutId, expandDateRange, addDays, todayKey, sanitizeCopyTargets). New web components `src/app/components/suppr/copy-meal-dialog.tsx` + `duplicate-day-dialog.tsx`; new mobile sheets `apps/mobile/components/CopyMealSheet.tsx` + `DuplicateDaySheet.tsx`. New analytics events `meal_copied`, `day_duplicated`. All copied rows go through the same `nutrition_entries` insert primitive as manual logs — fresh ids, source row never mutated. | `product/overview.md`, `product-roadmap.md`, `journeys/food-tracking.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md` |
| Apr 2026 | **Week-start-day setting** — users choose Monday or Sunday in Settings (web) / More tab (mobile). Persisted as `profiles.week_start_day`. Affects DayStrip, NutritionTracker weekly view, ProgressDashboard weekly stats, and `weekSummaryWindow.ts` (rolling vs calendar_week). Migration `20260414210000`. **Analytics (2026-04-18, audit H5):** picker now fires `week_start_day_changed { from: "monday"\|"sunday"; to: "monday"\|"sunday" }` on committed change only — never on initial hydration from Supabase, never on no-op re-taps of the already-selected day. Registered in `AnalyticsEvents`; fires from web `Settings.tsx` and mobile `app/(tabs)/more.tsx`. | `data/schema.md`, `product-roadmap.md`, `journeys/food-tracking.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md`, `technical/components.md` |
| Apr 2026 | **Remaining macros bar + fit-this-in calculator** — compact kcal/P/C/F (+ fiber when tracked) row below the daily calorie ring on Today (web + mobile). Over-budget shows "+N over" in destructive colour. Food search portion pickers show a projected "after" row when macro context is available. Shared pure helper `src/lib/nutrition/remainingMacros.ts`; web component `src/app/components/suppr/remaining-macros-bar.tsx`; mobile component `apps/mobile/components/RemainingMacrosBar.tsx`. **Analytics (2026-04-18, audit H5):** portion pickers in `FoodSearch` (web) / `FoodSearchModal` (mobile) now fire `fit_this_in_previewed { fromSlot?, overCalories, kcalDelta }` once per distinct `(food, quantity, unit)` preview. Uses a ref-keyed last-emitted guard so slider drags and stepper holds never spam the event bus; never fires on initial mount of the modal. Registered in `AnalyticsEvents`. | `product/overview.md`, `product-roadmap.md`, `journeys/food-tracking.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md` |
| Apr 2026 | **Favourites + Frequent + Eat again (batch 1.3)** — tabbed Quick add picker (Favourites / Frequent / Recent) on both platforms. Star toggles persist to `public.user_favorite_foods` (RLS, unique on `user_id + lower(title) + round(cal)`). Today view shows "Eat again" banner when the current clock-time slot has a match on a prior day; one-tap logs, dismiss lasts until next local day. Shared helpers `src/lib/nutrition/foodHistory.ts` + `favoriteFoods.ts`; web component `src/app/components/suppr/quick-add-panel.tsx`; mobile panel now first-class in `apps/mobile/components/QuickAddPanel.tsx`. Migration `20260421100000`. **Mobile QuickAddPanel first-class component (2026-04-18 audit H1).** Inline re-implementation removed from `(tabs)/index.tsx`. Shared logic via `src/lib/nutrition/foodHistory.ts` + `favoriteFoods.ts` + `savedMealsLogic.ts` (plus `savedMeals.ts` CRUD). Mobile Today now renders `<QuickAddPanel />` with the same prop contract as the web component (`byDay`, `activeSlot`, `supabase`, `userId`, `onLog`, `onLogSavedMeal`, `onOpenSaveCombo`, `savedMealsRefreshToken`, `defaultTab`). AI-source detection centralised in `isAiSourcedFoodHistoryItem(item)` so both platforms light the AI badge from the same rule. Net -400 LOC on mobile Today. | `data/schema.md`, `product/overview.md`, `product-roadmap.md`, `journeys/food-tracking.md`, `technical/components.md`, `testing/test-plan.md`, `mobile_qa_uat_test_plan.md` |

## Rules
- Documentation must reflect reality
- Any meaningful code change must update docs
- No feature is complete without docs