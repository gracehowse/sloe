# Sloe v3 — prototype conformance backlog (ENG-1247)

Generated from the 14-agent forensic audit (workflow `sloe-v3-conformance-audit`). Canonical source: `docs/ux/redesign/v3/Sloe-App.html`. Drive to 100%: each surface → conform web+mobile in parity → SEE on sim + web → gate → commit → tick here.

> **Prototype developed 2026-07-16 (backlog end-state):** the canonical prototype now
> includes the ratified ENG-1525 Progress hierarchy (5 sections, tinted trajectory
> hero) plus ENG-992 / ENG-1274 / ENG-1518 / ENG-1535 deltas — see the README
> changelog. Any Progress-surface conformance line-item written against the old
> ~13-card stack is superseded; conform against the new §1–§5.

**Totals:** 79 surfaces · 21 need work · 21 conformant · 33 keep-current · 1 net-new build · 3 in progress. §B/§D ratified 2026-06-28 — see registry below.

**Completion framing (2026-06-28):** Launch-critical daily-loop + recipe spine conforms ship behind default-OFF flags on `main` (PR #609). §B (29) + §D (18) ratified 2026-06-28; remaining ⬜ = autonomous §A + explicit builds (CreatorProfile, import input-phase polish). See `docs/decisions/2026-06-28-eng1247-conformance-completion-status.md`.

Status legend: ⬜ todo · ✅ done · 🔄 in progress · 🔒 keep-current · 🆕 net-new (deferred)

## Progress log

- 🔄 **RecipeDetail (Recipes — viral hook)** — flag `recipe_detail_v3_conformance` (default OFF). **Shipped:** hero overlay, standfirst, borderless macros, sticky Cook/Log, v3 method steps + step-count note, import review banner (2026-06-28). **Flag backlog only:** CookMode dark aubergine theme — not a standalone ⬜ surface; ships with the same flag ramp. **🔒 keep-current:** solid Fits-your-day verdict banner (ENG-1085), footer yield stepper, image-tile ingredient grid.
- ✅ **Plan AdjustConstraints (B1)** — DONE (2026-06-28): sheet web+mobile + sliders button; leftovers toggle; **calorie floor wired into mealPlanAlgo (`calorieFloorMin`, ENG-1254)**.
- ✅ **BatchCook (B3 minimal v1, ENG-1255)** — DONE (2026-06-28): `batch-cook` route (mobile) + `BatchCookSheet` (web) + PlanTools 2-up row; scales shopping list to batch portions. **🔒 out of scope (Grace B3):** assign-portions day×meal planner + fridge pip tracker — not minimal v1; do not chase as conformance gap.
- ✅ **ConfirmFood P/C/F mix (A2b, ENG-1257)** — DONE (2026-06-28): 3-tile P/C/F row + serif kcal line in FoodSearchPanel preview; micro table kept below (web + mobile).
- 🔄 **Profile showcase (B6, ENG-1256)** — **mobile + web read showcase** behind `profile_showcase_v1` (default OFF). Legacy `Profile.tsx` editor remains on flag-off path.
- ✅ **Notifications** — DONE (grouping + toned plates shipped earlier; audit residual = header chrome 🔒keep). Today/Earlier overline groups + 38px toned `.notif-ic` plates on web + mobile.

- ✅ **Today header bell** — mobile shipped `c0ca0cd3`: extracted `TodayHeaderBar` (wordmark + bell + avatar), bell → `(tabs)/notifications` with unread dot. Prototype conform + web↔mobile parity (web already had a bell). Header **calendar deliberately omitted** (week-strip owns it — Grace "add bell only"). SEEN on sim.
- ✅ **Today quick-add recents row** — DONE both platforms, flag `today_quickadd_recents_v3` (default OFF), ready to ramp. Mobile `6de0ac5a` + web `d218f3c2`: `TodayRecentsRow` (recents one-tap re-log chips + "All"→LogSheet + empty state), reuses `logHistoryItemToSlot`/`logHistoryItem`; deleted the dead `TodayQuickLogStrip` (the audit's phantom "launcher strip") both platforms. SEEN on sim + web. Also fixed a peer's `_layout.tsx` v7 `sceneContainerStyle`→`sceneStyle` typecheck regression. Fast-follow ENG-1252 (LogSheet AI tooltip). _(Original resolution below.)_ **Phantom audit finding corrected:** the "Search/Voice/Snap/Scan launcher strip" (`TodayQuickLogStrip`) the audit flagged is **dead code — not rendered** on either platform (`canonicalTodayPhase2` already pins it out; confirmed in code + my Today SEEs). So this is **ADD** the prototype's recents row (net-new on Today) + delete the dead strip, not "replace".
  - **Decision (product-lead, conf 9/10):** recents one-tap re-log chips (icon + name-trunc-15 + bold kcal), first 6, most-recent-first; section-head "Quick add" + ghost **"All"** link → LogSheet (keep-current divergence vs prototype's "Search"); empty state = ghost "＋ Log your first meal". Placed after macros, before "Today's meals" (prototype). Flag-gate.
  - **Re-log wiring EXISTS:** `logHistoryItemToSlot(item, activeMealSlot)` (TodayScreen ~L2044) already maps `FoodHistoryItem`→`JournalMeal` carrying all macros + fibre + caffeine/alcohol + full micros panel — reuse it (nutrition-engine concern answered). Data: `recentFoodsForSearch`/`computeRecentMeals` already computed.
  - **Monetisation RESOLVED (monetisation-architect, conf 8/10):** AI-logging (Voice/Snap) Pro-discovery stays in the LogSheet (option c); removing the dead launcher strip is clean housekeeping (no flag, no conversion loss — it was already gone). **Fast-follow (separate Linear issue, NOT ENG-1247):** a first-session (1–3) AI-method tooltip inside `LogSheet.InputModeRow`, flag `logsheet_ai_method_tooltip`; watch `ai_paywall_sheet_viewed` (roll back if it drops >30% WoW).

- ✅ **Today calorie hero (de-card)** — DONE both platforms, flag `today_hero_decard_v3` (default OFF), ready to ramp. Mobile `877eca91` + web parity/numeral `16efd625`: bare hero (no card) + `RingStatusLine` below ring (sage under / red over) + 56px serif-medium numeral + `TodayHeroStats` extraction. SEEN on sim + web (empty + under-budget) — perfect parity, no slab.
- ✅ **Bulk 🔒 registry** — all ratified §B/§D rows marked 🔒 or ✅ in surface headers + resolution table below (2026-06-28).

- ✅ **MealEdit serif kcal hero** — shipped `957e5da9` (both platforms); expander + copy-row confirmed keep-current dupes.

## §B/§D ratified resolutions (2026-06-28)

Binding: `docs/decisions/2026-06-28-eng1247-section-b-ratified.md` + planning backlog §D. Every row below is **resolved** for 100% tally (✅ shipped · 🔒 keep-current · 🆕 scoped build).

| Ref | Surfaces marked 🔒/✅ | Outcome |
|-----|----------------------|---------|
| B1 | PlanHead | ✅ AdjustConstraints sheet + sliders button |
| B2 | AdaptiveTDEE, Progress | 🔒 inline adaptive; fix dead Why links |
| B3 | BatchCook, Plan tools | ✅ minimal v1 (scaling + shopping); 🔒 assign-portions + fridge pips beyond Grace scope |
| B4, §D4 | LogHub, SearchView, FoodRow, ConfirmFood inline | 🔒 search-first logging IA |
| B5 | Targets | 🔒 dashboard + delegated edit |
| B6 | Profile | 🔄 mobile read showcase (`profile_showcase_v1`); web showcase follow-up |
| B7 | Household, Plan household banner | 🔒 sharing grid |
| B8 | Paywall, ProLock | 🔒 Figma 284:2 canonical |
| B9 | Coach | 🔒 distributed coach; no unified screen |
| B10 | BurnDetail | 🔒 web inline panel |
| B12 | CreateRecipe (both) | 🔒 keep live source set |
| B13 | ScanLabel | 🔒 OCR in custom-food; standalone deferred |
| B14 | Meal action sheet | 🔒 web host-surface meal editing |
| B17, §D6 | ImportFlow | 🔒 route-out pipeline; B15 clear ✅ 2026-06-28 |
| B19, §D7 | MfpImport | 🔒 Settings-embedded CSV + auto mapping |
| B20 | GoPublic | 🔒 mobile attestation (already marked) |
| B22 | Digest | 🔒 weekly recap on Progress |
| B24 | PlanTemplates | 🔒 tabbed dialog (already marked) |
| B25 | DataExport | 🔒 two direct export buttons |
| B27 | Billing | 🔒 storefront-owned billing |
| B29 | Plan week strip | ✅ no CalendarPicker (already marked) |
| §D1 | WinMoment | 🔒 gold ring + transient player |
| §D2 | Calorie hero, Macros | ✅/🔒 Goal/Eaten/Bonus; macro toggle |
| §D3 | Today header | 🔒 bell only; week strip owns calendar |
| §D5 | ReportRecipe | 🔒 legal reason set (already marked) |
| §D10 | Settings | 🔒 web card IA carve-out |
| §D11 | HealthSync | 🔒 iOS-only (already marked) |
| §D12 | Net energy / Progress energy | 🔒 no fabricated adapt row |
| §D13 | RecipeDetail web | 🔒 single RecipeDetail.tsx (tabs 🔒) |
| §D14 | Fasting | 🔒 richer production surface |
| §D15 | Changelog | 🔒 asymmetric entry points |
| §D16 | Plan tools smart suggestions | 🔒 honest real-data nudges |
| §D17 | Grocery | 🔒 no Tesco/Ocado fiction (already marked) |
| §D18 | EraseEverything, DeleteAccount dialog | 🔒 web dialog IA; DeleteAccount sheet ✅ B26 |

**Still ⬜ (not keep-current):** autonomous §A cosmetic (Nutrients sort, Hydration sheet, CompleteDay, onboarding tokens, etc.) + CreatorProfile + import input-phase polish (single-line detect field, example chips — 🔒 route-out phases). **Shipped 2026-06-29:** B26 DeleteAccount 3-step sheet (`delete_account_sheet_v1`), B28 ResetPlan keep/clear sheet (`reset_plan_confirm_v1`), plan cooked state (diary match → strike-through + subline tally). **Shipped 2026-06-28:** B15 import clear button + B16 verify review banner (`unified-import-clear`, `RecipeImportReviewBanner`); B18 MFP reassurance strip (`mfp_tracker_reassurance_v1`, ENG-1258); B21 WeeklyRecap detail rows (`WeeklyRecapDetailRows`, ENG-1259). **Flag backlog (default-on, gate for rollback):** RecipeDetail CookMode dark theme (`recipe_detail_v3_conformance`). **Out of scope (not ⬜):** BatchCook assign-portions planner (Grace B3 minimal v1).



## Cluster: import-flows


### 🔒 ImportFlow — route-out paste sheet (§D6; B15 clear ✅ 2026-06-28)
- proto: `4515-4631 (+CSS L817-820, L1286-1294, L2141-2142, L2199)`
- mobile: apps/mobile/components/import/UnifiedImportSheet.tsx; apps/mobile/components/import/ImportDetectedChip.tsx; apps/mobile/components/import/ImportLoadingSkeleton.tsx; apps/mobile/lib/importRouting.ts
- web: src/app/components/suppr/unified-import-sheet.tsx; src/app/components/suppr/import-detected-chip.tsx; src/lib/recipe-import/classifyImport.ts; src/lib/recipe-import/importRoutingWeb.ts
- _The app's UnifiedImportSheet is ONLY the prototype's 'input' phase, and even that is reduced. The prototype ImportFlow is a self-contained multi-phase sheet (input → loading → single-recipe preview → multi-recipe picker → mfp → fail) that resolves results IN-SHEET. The app instead routes out to separate surfaces on Import, so the loading/single/multi/mfp/fail phases (and all their distinctive structure) are absent from this component. Within the input phase itself, multiple prototype elements are missing or restyled._
  - **[HIGH]** Container — Sheet vs Dialog (web): proto = <Sheet title="Import"> bottom sheet, height 74% (90% for single/multi); grabber handle; sheet-scroll body padding 0 20px 20px; a pushed-cta footer button bar. · app = web · _Web import is a centered dialog where the prototype is a bottom sheet with a docked CTA._
  - **[HIGH]** Input control — search field vs textarea/plain TextInput: proto = Single-line `.search` field (rounded pill input) with a LEADING detection icon (Ic2 n={det.ic}) that changes with the detected kind, and a trailing `.imp-clear` X button when text present. · app = both · _Prototype's signature single-line search field with leading live-detect icon + inline clear is replaced by a plain multiline box._
  - **[HIGH]** Detected chip — imp-detect row vs imp-sample pill: proto = On detection, a full-width `.imp-detect` ROW: 38px rounded icon plate (bg card, primary icon) + title 'Detected: {label}' (14px semibold) + sub line (12.5px) + trailing green success check. bg=accent-frost-mist, radius 14, padding 12/14. · app = both · _The prototype's prominent detect ROW with sub-copy + check is downgraded to a tiny pill with no sub-line._
  - **[HIGH]** Empty-state example samples (imp-samples / imp-sample): proto = When NOTHING is detected, an 'Or try an example' overline + a wrap of 5 `.imp-sample` pills (Reel / Recipe / Collection / Plan / MFP CSV) that populate the field on tap. · app = both · _Prototype's 5 example chips (a key first-run affordance) are not implemented._
  - **[HIGH]** Choose-a-file affordance (imp-file): proto = A dashed-border `.imp-file` button 'Choose a file (.csv, photo)' with a leading icon, below the input. · app = both · _No file-choose row; the prototype explicitly accepts a .csv/photo from this sheet._
  - **[LOW]** Intro copy wording: proto = 'Paste a link or choose a file — a single recipe, a reel, a whole collection, a meal plan, or a MyFitnessPal export. Sloe works out which and reads it for you.' · app = both · _Reworded; loses 'choose a file' and 'Sloe works out which' brand voice._
  - **[LOW]** Placeholder copy: proto = 'Paste a link, or choose a file…' · app = both · _Shorter placeholder._
  - **[HIGH 🔒keep?]** loading / single / multi / fail phases: proto = import-spin spinner + skeleton block; single-recipe import-preview (96px ip-photo + serif ip-name + review-banner warning card + detected-ingredients list with ver-dot confidence); multi-recipe exp-check picker list with 'Add N recipes' CTA; fail state with 'Try another' / 'Paste manually'. · app = both · _Routing out to dedicated, more-capable result surfaces is arguably better than a mock in-sheet preview — but it means none of the prototype's loading/single/multi/fail structure exists here; needs Grace's call on whether in-sheet preview is required for conformance._
  - **[MED]** Detection-driven CTA: proto = Docked pushed-cta: disabled when !det; label 'Import {label.toLowerCase()}' with sparkles icon, else 'Paste something to import'. · app = both · _CTA label logic conforms; missing the sparkles leading icon and the docked footer treatment._

### 🔒 MfpImport — Settings-embedded CSV (§D7; B18 MFP strip ✅ ENG-1258)
- proto: `5791-5872 (+CSS L857-885, L2068)`
- mobile: apps/mobile/components/imports/MfpCsvImportCard.tsx; apps/mobile/components/imports/CsvImportPreview.tsx
- web: src/app/components/imports/MfpCsvImportCard.tsx; src/app/components/imports/CsvImportPreview.tsx; src/lib/imports/useCsvImportFlow.ts
- _The prototype MfpImport is a full bottom Sheet ('Bring your history') with four phases: a 2-col SOURCE GRID of tracker tiles + 'Any other CSV' tile, a detecting spinner, a per-column MAPPING list (imp-detected banner + imp-map rows with confidence dots + chevrons), and a grid TABLE preview (mfp-table). The app implements an embedded CARD (in onboarding/settings) using a native OS file picker — no source grid, no column mapping phase, and the preview is a row-list, not the mfp-table grid. Functionally the CSV preview/commit is built (good), but the prototype's shell structure is largely absent._
  - **[HIGH]** Container — full Sheet vs embedded card: proto = Standalone <Sheet title="Bring your history" height 78%/90%> with its own pushed-cta footer ('Preview import' / 'Import 406 entries'). · app = both · _Different container paradigm: prototype is a dedicated sheet flow; app is an embedded card._
  - **[HIGH]** Source-picker grid (imp-srcgrid / imp-src / imp-anycsv): proto = Phase 'source': intro copy + a 2-column grid of IMPORT_SOURCES tiles (mark plate + name + day-count), an 'Any other CSV' dashed tile ('we fingerprint the columns'), then a 3-item assurance checklist card. · app = both · _The entire source-selection phase (the prototype's first screen) is missing._
  - **[HIGH]** Column-mapping phase (imp-detected banner + imp-map rows): proto = Phase 'mapping': `.imp-detected` success banner (source mark + 'N of M columns matched' + Recognised/Best-guess pill), then a card of `.imp-map` rows: csv header (mono) → chevR → mapped field pill with confidence dot + chevD, tappable to change; '← Choose a different source' link. · app = both · _The prototype's trust-building column-mapping review is entirely missing._
  - **[MED]** Preview — mfp-table grid vs row list: proto = `.mfp-table`: a bordered grid table with an uppercase header row (Meal / Food / kcal, mfp-th) and 80px/1fr/52px columns, a '+ N more entries' footer, and a `.lowconf-note` 'N entries already exist… will be skipped.' · app = both · _Preview content is equivalent but the structure (grid table + header row + footer) differs from mfp-table._
  - **[LOW]** Detecting / spinner phase: proto = Phase 'detecting': import-spin (search icon) + 'Reading {file}…' title + body ('Matching {source}'s export layout' / 'Fingerprinting your column headers') + skeleton lines. · app = both · _Reading state is far more minimal than the prototype's spinner + skeleton + descriptive body._
  - **[LOW]** CTA copy: proto = 'Preview import' then 'Import 406 entries' (with check icon). · app = both · _Entry CTA is the OS file picker, not the prototype's two-step Preview/Import._

### ⬜ VoiceLog (listening orb → parsed editable review) — H1 M3 L1
- proto: `6830-6875 (+CSS L837/974-983, L1083-1084, L2068)`
- mobile: apps/mobile/components/VoiceLogSheet.tsx; apps/mobile/components/AiLogReviewItem.tsx; apps/mobile/components/AiLogReviewSummary.tsx
- web: src/app/components/suppr/voice-log-dialog.tsx
- _The prototype VoiceLog is a full-screen pushed view (not a dialog/sheet) with two phases: a LISTENING state showing an animated `.voice-orb` (96-120px ping-ring mic orb) with a live transcript caption, then a REVIEW list. The app implements voice logging as a Dialog (web) / bottom Sheet (mobile) with an INPUT phase (press-hold mic + text field), a small parsing spinner, and a review list. The signature listening-orb phase and the serif 'voice-heard' transcript banner are absent; the review rows differ structurally._
  - **[HIGH]** Container — pushed full-screen vs Dialog/Sheet: proto = <div class="pushed full"> full-screen pushed view with a `.pushed-bar` (chevL back, 'Voice log' centered title) and a `.pushed-cta` docked footer. · app = both · _Prototype is a full-screen pushed flow; app is a dialog/sheet._
  - **[HIGH 🔒keep?]** Listening phase — voice-orb: proto = Phase 'listening' auto-runs: `.voice-orb` (round primary mic orb, 3 animated ping rings, 96-120px) + 'Listening…' title + the spoken sentence as state body. Auto-advances to review after ~1.9s. · app = both · _App's input-with-mic + text fallback is more honest/functional (real STT availability varies), but the prototype's distinctive listening-orb hero is absent. Needs Grace's call on whether the orb is required._
  - **[MED]** Heard transcript banner (voice-heard): proto = In review: a `.voice-heard` banner — frost-mist bg, serif ITALIC, with a mic icon, quoting back what was heard; plus a 'Parsed N items' overline with a 'Re-record' link. · app = both · _Prototype's serif 'voice-heard' quote-back banner + Re-record link are missing._
  - **[MED]** Review row structure (vrow + exp-check): proto = `.vrow`: a round `.exp-check` toggle (check when on, rows dim to .45 when off) + name with inline confidence chip (serif) + portion·kcal·P/C/F sub + a ghost pencil edit button. Items are TOGGLED on/off, not deleted. · app = both · _Different interaction model: prototype = toggle-include + pencil-to-edit; app = always-editable fields + remove. Arguably app is more capable, but structurally divergent._
  - **[LOW]** Low-confidence note: proto = `.lowconf-note` (warning-soft, info icon): 'One item is a rough estimate — tap the pencil to correct it.' · app = both · _Low-confidence guidance is per-row, not the prototype's single bottom note referencing the pencil._
  - **[MED]** Commit CTA copy: proto = Docked: 'Log {N} items · {total} kcal' (check icon), disabled when none chosen. · app = both · _Prototype CTA names the count + total kcal; app uses a generic 'Log all'/'Log anyway'._
  - **[LOW 🔒keep?]** Header — Pro accent mic + serif title: proto = pushed-bar centered plain 'Voice log' title (no mic glyph in the bar). · app = both · _App adds a Pro mic accent + serif title (consistent with LogSheet grammar) — a reasonable deliberate enhancement, not a regression._

## Cluster: moments-nutrients-hydration-notifs


### ⬜ CompleteDay — H3 M2 L1
- proto: `6773-6801`
- mobile: apps/mobile/components/today/TodayCompleteDayModal.tsx
- web: src/app/components/suppr/today-complete-day-dialog.tsx
- _Heavily divergent on both platforms. Prototype 'Complete today' Sheet = insight line ('Lock in Wednesday…'), a .cd-stats TRIO (1,642 kcal eaten / −158 vs target in success / 118g protein), a 'Weight projection' card with an SVG .trendline (dashed flat baseline + success projection curve + end dot) and 'now / in 6 wks' labels, a serif .coach line, and a 'Complete day' primary CTA that opens the WinMoment overlay. App = a simpler confirmation sheet: title 'Day logged!', a single check circle (80px, primary-soft), ONE projection sentence (no chart), and a 'View my progress' CTA — no cd-stats trio, no trendline chart, no coach line, and crucially NO WinMoment handoff._
  - **[HIGH]** Stat trio (.cd-stats): proto = Three .cd-stat cards: kcal eaten / vs target (success ±) / protein, serif 24px values · app = Absent — no eaten/vs-target/protein summary · _Whole summary block missing._
  - **[HIGH]** Weight-projection trendline: proto = Card with overline 'Weight projection' + SVG .trendline (dashed baseline + success curve + end dot) + 'X kg now / Y kg in 6 wks' tnum labels · app = No chart; a single text sentence 'projected weight in N weeks is X kg' · _Trendline visualisation replaced by one text line._
  - **[MED]** Coach line: proto = Serif .coach: '“A clean day — gently under, protein nailed. This is exactly the pace that holds.”' · app = Absent · _Coaching voice line missing._
  - **[HIGH]** CTA + WinMoment handoff: proto = 'Complete day' primary CTA (with check icon) → open('winmoment') celebration overlay · app = 'View my progress' CTA → navigates to Progress; no WinMoment trigger from this sheet · _CTA label, icon, and the celebration handoff all differ; the win-moment chain is broken here._
  - **[MED]** Title + intro: proto = Title 'Complete today'; intro 'Lock in Wednesday and we'll fold it into your trend. Here's how today lands.' · app = Title 'Day logged!' / 'Day complete'; no intro insight line · _Title and lead copy differ._
  - **[LOW]** Hero check circle: proto = No standalone check circle — the cd-stats + chart carry the surface · app = 80px primary-soft circle with a 40px check icon as the hero · _App adds a hero check the prototype omits._

### 🔒 WinMoment — gold ring + transient player (§D1)
- proto: `6802-6829`
- mobile: apps/mobile/components/ui/WinMomentPlayer.tsx
- web: src/app/components/ui/win-moment-player.tsx
- _Fundamentally different celebration on both platforms. Prototype WinMoment = a full overlay (.win-overlay, blurred scrim) with confetti and a .win-card containing a GREEN SUCCESS ring (stroke var(--success), 180px) that sweeps closed with a 56px check icon centred, an eyebrow 'Ring closed', a serif 28px headline '12-day streak', a .win-sub paragraph, then TWO CTAs — 'Keep it going' (primary) + 'Share my streak' (tertiary). The app WinMomentPlayer is a GOLD (Accent.win + win-gradient) ring-odometer PRIMITIVE: it sweeps a gold gradient ring, blooms a gold radial pulse, odometers a centre % up to 100, bursts gold confetti, shows a small label ('Goal hit'/'Streak!'), runs ~700ms, then onComplete unmounts. No win-card, no blurred overlay card, no eyebrow/serif-headline/subhead, no 'Keep it going' / 'Share my streak' CTAs, and gold not green._
  - **[HIGH 🔒keep?]** Ring colour: proto = Green success ring (stroke var(--success)) + 56px success check icon centred · app = Gold win-gradient ring (Accent.win / win-gradient), no check icon — centre shows odometer % / milestone numeral · _Win token is gold/damson by the three-role colour law (ENG-997); prototype uses success-green. Direct colour conflict — needs a Grace ruling on which wins._
  - **[HIGH]** Card + overlay chrome: proto = .win-card (26px radius, shadow-lg) over a blurred .win-overlay scrim · app = Bare full-bleed/inline player primitive; the overlay+card chrome is the caller's job, not part of the player · _Prototype's celebration card UI is not reproduced as one component._
  - **[HIGH]** Eyebrow + headline + subhead: proto = Eyebrow 'Ring closed' (success), serif 28px '12-day streak', .win-sub paragraph · app = Only a small Type.label ('Goal hit'/'Streak!'/'Logged') under the ring; no eyebrow, no serif headline, no subhead paragraph · _The textual celebration hierarchy is absent._
  - **[HIGH]** Dual CTAs: proto = 'Keep it going' (btn--primary btn--block btn--lg) + 'Share my streak' (btn--tertiary btn--block) · app = No CTAs in the player — it auto-dismisses after ~700ms · _Both the share affordance and the keep-going CTA are missing; the moment is non-interactive._
  - **[LOW]** Confetti palette: proto = Confetti in [primary, accent-frost, clay, success] · app = Confetti in win-gradient stops (gold family only) · _Multi-hue vs single gold family._

### ⬜ Nutrients — H2 M4 L1
- proto: `6700-6733`
- mobile: apps/mobile/components/today/FullNutrientPanelSheet.tsx (also legacy apps/mobile/components/today/TodayNutrientsModal.tsx — 2-col tile grid, even further from proto)
- web: src/app/components/suppr/full-nutrient-panel-sheet.tsx
- _Structurally divergent on BOTH platforms. Prototype Nutrients is a FULL pushed screen (.pushed.full) with a pushed-bar (Back · 'Nutrients' · a 'today' day-toggle icon-btn), an intro insight paragraph, three groups (Macronutrients / Vitamins / Minerals) rendered in fixed SOURCE order inside one flush card each, where every .nutr-row is name (left, '—' sub-rows indented) + amount with INLINE %DV on the right, then a full-width 5px .nutr-track bar BELOW. Footer: '%DV uses UK reference intakes…' with an info icon. App is a bottom-sheet Modal titled 'All nutrients' (mobile) / Dialog (web), rows %DV-SORTED DESCENDING (not source order), with a 3/4-column inline layout (name | amount | bar | %DV) rather than amount+inline-%DV over a full-width bar, no day-toggle, and different intro/footer copy. Bar colour ramp differs: prototype uses success(>=100)/warning(<50)/primary(else); app uses isLimit-based success/warning/destructive._
  - **[HIGH]** Container type: proto = Full pushed screen '.pushed.full' with pushed-bar header (Back, title, day-toggle icon-btn) · app = Bottom-sheet Modal (mobile) / Dialog (web), max-height 88%, X close, no day toggle · _Prototype is a full-screen detail with its own day toggle; app is a partial sheet._
  - **[HIGH 🔒keep?]** Section order / sorting: proto = Fixed source order within each group (Carbohydrate, fibre, sugars, Protein, Fat, sat, mono, Cholesterol, Sodium …) · app = %DV descending sort within each section (deficiencies bubble up) · _%DV-sort is a deliberate Cronometer-gap UX choice (documented in file). Arguably better, but a real structural divergence from the prototype's canonical nutrition-label order._
  - **[HIGH]** Row layout: proto = .nutr-row: top line = name (left) + amount with inline .nutr-dv %DV (right); full-width 5px .nutr-track bar BELOW the line · app = Single row: name | amount | inline progress bar | %DV column, all on one line · _Prototype stacks bar under the amount line; app inlines a short bar mid-row. Different hierarchy._
  - **[MED]** Sub-nutrient rows: proto = '— of which fibre/sugars', '— saturated', '— monounsaturated' as indented .nutr-name.sub (12.5px, secondary, pad-left 10) · app = Curated nutrient list does not render the dashed '— of which' sub-hierarchy under Carb/Fat · _Macro sub-breakdown nesting from the prototype is absent._
  - **[LOW]** Group labels: proto = 'Macronutrients' / 'Vitamins' / 'Minerals' as .overline · app = 'Macros' / 'Vitamins' / 'Minerals' (mobile section labels) · _Macronutrients abbreviated to 'Macros'._
  - **[MED]** Intro + footer copy: proto = Intro: 'Today vs. recommended daily values. Based on what you've logged…'; footer: '%DV uses UK reference intakes for an average adult. Tap Targets to personalise.' with info icon · app = Mobile subline 'N nutrients · sorted by %DV'; footer = DAILY_VALUES_SOURCE_LABEL only; no intro paragraph · _Intro paragraph missing; footer attribution differs; no 'Tap Targets to personalise' link._
  - **[MED]** Day toggle: proto = pushed-bar right icon-btn ('today') → toast 'Showing today.' · app = No day-scope toggle in the panel · _Prototype affords switching day scope; app shows current day only._
  - **[MED]** Bar colour ramp: proto = >=100% success, <50% warning, else primary · app = isLimit ? (>=100 destructive / >=80 warning / else success) : success; null → neutral · _App ramp is limit-aware (sodium/satfat go red), prototype is a generic 3-band. Different semantics._

### ⬜ Hydration — H0 M4 L2
- proto: `6734-6772`
- mobile: apps/mobile/components/HydrationStimulantsCard.tsx
- web: src/app/components/suppr/hydration-stimulants-card.tsx
- _Different interaction MODEL on both platforms. Prototype 'Hydration & stimulants' is a bottom Sheet with three stepper rows (icon + name + meal-sub + a -/value/+ .hyd-step control): Water (water-dots strip below, .hyd-btn 32px round), Caffeine (over-400 destructive copy), and a SELF-HIDING Alcohol row behind a dashed '.hyd-add' 'Track alcohol today' button; insight paragraph; single 'Save' primary CTA. App is an INLINE Today card grammar (SloeCard 'Hydration' + separate 'Stimulants' card, soft-lift), each tracked quantity rendered as a serif value over a full-width progress track plus a quick-add CHIP GRID (+250ml etc.) and an ellipsis reset menu — no +/- steppers, no Save CTA, no water-dots strip. Caffeine/alcohol self-hide via Settings TARGETS (caffeineMg/alcoholGWeekly === 0), not an in-sheet 'Track alcohol today' add button._
  - **[HIGH 🔒keep?]** Container / interaction model: proto = Bottom Sheet with -/value/+ steppers (.hyd-step, .hyd-btn 32px round) and a 'Save' CTA · app = Inline Today section card(s) with quick-add chip grids + progress tracks + ellipsis reset menu; no Save · _App's chip-grid + track model is more capable (presets, from-food sub-line, week-rolling alcohol). Deliberate divergence — but it is NOT the prototype's stepper sheet._
  - **[MED]** Water-dots strip: proto = .water-dots — 8 segment pills under the Water row (.on = macro-water) · app = No 8-dot strip; water shows a single continuous progress track + chips · _Prototype's discrete 8-glass dot strip is absent._
  - **[MED]** Alcohol self-hide affordance: proto = Dashed '.hyd-add' button 'Track alcohol today' reveals the alcohol stepper row inline · app = Alcohol row shows only if Settings target alcoholGWeekly > 0; no in-card 'Track alcohol today' add · _Different opt-in path (Settings target vs inline add button)._
  - **[LOW]** Caffeine over-limit copy: proto = '{caf} mg · over 400 mg limit' in destructive when caf>400; else '… of 400 mg' · app = 'Over {target} mg' in warning (amber) when over; '{n} / {target} mg' · _Copy + colour differ (destructive vs warning amber). Amber is the Grace-confirmed over-target treatment._
  - **[MED]** Stepper control: proto = .hyd-btn 32px round +/- buttons, .hyd-val 17px semibold tnum · app = No steppers — quick-add chips (+250 ml, +1 cup etc.) · _Increment UI fundamentally different._
  - **[MED]** Title + Save CTA: proto = Sheet title 'Hydration & stimulants'; bottom 'Save' btn--primary btn--block btn--lg; toast 'Hydration saved.' · app = Two card titles 'Hydration' / 'Stimulants'; no Save CTA (chips commit immediately) · _Single combined sheet + explicit Save vs two auto-committing cards._
  - **[LOW]** Insight footer copy: proto = 'Rows you don't use stay hidden — add only what you want to see. Nothing here counts against your calorie ring unless you log it as a drink.' · app = No equivalent insight paragraph in the cards · _Explanatory insight line missing._

### ✅ Notifications — H0 M2 L2 (core grouping + plates; header chrome 🔒keep)
- proto: `5469-5498`
- mobile: apps/mobile/app/(tabs)/notifications.tsx + apps/mobile/components/notifications/NotificationRow.tsx
- web: src/app/components/NotificationsCenter.tsx
- _Both platforms implement the v3 grouping correctly: Today/Earlier overline groups, ONE flush divided card per group, 38px toned icon plate (.notif-ic is-brand/good/neutral → primary-soft / success-soft / muted), 14px semibold title, 12.5px secondary, 11px tertiary time, 8px primary unread dot. Icon plate radius is the main token miss (prototype 11px vs app 12px/rounded-xl). Header chrome diverges: prototype = plain 'Notifications' title with an inline 'Mark all read' LINK (.link) in a spread row above the cards, with a separate 'Today' overline; the app puts Mark-all in a bordered button and web adds a net-new 'Clear' button + a primary icon-plate H1._
  - **[LOW]** Notif icon-plate radius: proto = .notif-ic border-radius:11px · app = mobile Radius.xl (12) / web rounded-xl (12px) · _1px off; prototype radius 11 isn't on the snap scale so 12 is the nearest token — arguably keep._
  - **[MED]** Mark-all-read affordance: proto = Inline text LINK '.link' ('Mark all read') in a spread row, paired with an 'overline' Today label; toast 'All caught up.' · app = mobile: bordered card-style button with label; web: bordered button with check icon · _Prototype treats it as a quiet ghost link, not a bordered button; web also pairs it with a Clear button._
  - **[MED]** Web Notifications header: proto = Pushed bar title only; body header is a plain 'overline' Today + link row, no big H1, no icon plate · app = Web renders a large icon-plate H1 ('p-2 bg-primary rounded-xl' + Icons.notification) with 'Notifications' headline + unread subline · _Web-only chrome the prototype's Pushed screen does not have._
  - **[LOW 🔒keep?]** Web 'Clear' button: proto = No clear-all control · app = Web adds a second bordered 'Clear' button (clearNotifications) · _Net-new capability (clear inbox) absent from prototype; arguably keep — but it breaks the prototype's single quiet-link header._
  - **[LOW]** Group/empty copy: proto = Header overline 'Today'; mark-all toast 'All caught up.'; sections Today / Earlier · app = Mobile subline 'N unread'/'No unread notifications'; web subline 'All caught up'; empty states reworded · _Wording drift in sub/empty copy; group labels Today/Earlier match._

## Cluster: settings-profile


### 🔄 Profile — mobile read showcase only (B6, ENG-1256); web follow-up
- proto: `5405-5466 (Profile fn); CSS L1245-1258(prof-head/prof-avatar/prof-name/prof-stats/prof-stat-v/prof-stat-l/prof-streak/streak-dots/mile-ic),524-539(segmented)`
- mobile: `apps/mobile/app/profile.tsx`, `apps/mobile/components/profile/ProfileShowcaseReadView.tsx` (flag `profile_showcase_v1`, default OFF)
- web: `src/app/components/Profile.tsx` — **legacy editor remains canonical**; read showcase not built
- _B6 ratified read showcase + Settings editor split. **Mobile:** showcase read view behind flag; editing moves to Settings when flag on. **Web:** follow-up only — still ships profile editor on `Profile.tsx`; parity gap is intentional until web showcase lands._
  - **[HIGH]** 3-stat strip: proto = prof-stats: Days logged / Recipes / Cooked, serif numerals, 1px hairline ::before dividers · app = Both apps show only 2 stats (Recipes + Streak). 'Days logged' and 'Cooked' absent · _Stat set and count differ._
  - **[HIGH]** Streak-dots card: proto = prof-streak card: 'Current streak' overline + flame success pill '12 days', 14 streak-dots (on/freeze/empty), '2 freeze tokens left' / 'Best: 31 days' · app = Streak surfaced as a single number tile; no 14-dot visual, no freeze-tokens/best line on this surface · _Distinctive streak-dots visualization missing from Profile._
  - **[HIGH]** Milestones card: proto = Divided card: '100-day cook' / 'First 5kg' / 'Recipe author' with mile-ic (is-done success) + check/'2 to go' · app = Absent on both · _net-new prototype block with no app equivalent._
  - **[MED]** Your recipes grid: proto = section-head 'Your recipes' + '+ Create' link + rgrid of rcards · app = Mobile shows a recipe count stat; web has a 'Published Recipes' row (L722) — not the prototype rgrid · _Recipe surfacing differs in shape and placement._
  - **[MED]** Identity header: proto = centered 84px prof-avatar gradient, serif prof-name, 'Cooking since March · Free plan' subline · app = Editorial identity card (mobile §3.2) / web header — avatar+name present but layout/subline copy differ, plus an editor entry row the prototype lacks · _Header present but not the centered display-only treatment + 'Cooking since' tenure copy._
  - **[LOW 🔒keep?]** Profile editing capability: proto = none (display only; Established/First-day is a demo toggle) · app = Both apps let you edit sex/age/height/weight/goal/pace via sheets · _keepCurrentCandidate — editing is net-new capability the product needs; the demo segmented in the prototype is not a real feature._

### 🔒 Targets — dashboard + delegated edit (B5)
- proto: `5142-5174 (Targets fn); CSS L524-539(segmented),539(field-label),1945-1948(tg-edit/tg-num/tg-step),2307-2308(tg-row/tg-v)`
- mobile: apps/mobile/app/targets.tsx
- web: src/app/components/Targets.tsx
- _Prototype Targets is a compact editor: an Auto(adaptive)/Manual segmented control, a card of tg-rows (Daily calories / Protein / Carbs / Fat / Fibre-with-stepper), a 'Why this number?' set-row (auto mode), an 'Other limits' card (Caffeine 400mg / Alcohol 14/wk), a 'Recalculate → Reset goals & targets' set-row, and a sticky 'Save changes' CTA. Both app implementations are MUCH larger (web 702L, mobile 1002L) and built around an adaptive-TDEE maintenance row, net-carbs lens, GoalPaceRetuneSheet, and macro tiles that deep-link to a manual /profile editor — they do NOT use the prototype's Auto/Manual segmented + inline editable macro rows + fibre stepper pattern._
  - **[HIGH]** Auto/Manual segmented control: proto = .segmented 'Auto (adaptive)' / 'Manual', full width, drives whether macro rows are read-only text or editable inputs · app = Absent on both — apps present adaptive maintenance/recalculate model instead of an explicit Auto/Manual toggle · _The prototype's defining Targets control is missing._
  - **[HIGH]** Editable macro rows + fibre stepper: proto = tg-row per macro; manual mode shows tg-num inputs; Fibre row has −/+ tg-step buttons ('aim 30g+'); inline editing · app = Macro tiles deep-link to manual /profile editor (Targets.tsx L132, mobile L777) — no inline tg-row editing, no fibre stepper here · _Editing model differs structurally; fibre +/- stepper not present on this surface._
  - **[MED]** Other limits card: proto = Card with 'Caffeine 400 mg' + 'Alcohol units / week 14' tg-rows · app = Caffeine/Alcohol limits live in mobile Settings 'Goals & targets' card (SettingsBundleContent L2132/2142), not on the Targets screen; web similar · _Present in product but relocated off the Targets surface._
  - **[MED]** Why this number? row: proto = set-row with sparkles icon (frost-mist bg), 'Why this number? / See how your target is calculated', auto mode only · app = Web has a 'Why this number' affordance (L583) — partial; mobile via maintenance row context · _Web closer; exact set-row treatment/copy not confirmed identical._
  - **[MED]** Reset goals & targets row: proto = set-row refresh icon under 'Recalculate' label, 'Reset goals & targets / Re-answer the questions and recalculate from scratch' → resetOnboarding · app = Mobile has Recalculate ghost CTA (L753) that re-runs adaptive recompute, NOT an onboarding-reset row with that copy · _Different action (recompute vs re-answer onboarding) and different copy/affordance._
  - **[LOW 🔒keep?]** Save changes CTA + offline error note: proto = Sticky pushed-cta 'Save changes' btn--primary btn--lg; lowconf-note destructive 'Couldn't save — you're offline' on error · app = Apps autosave / use sheets; no single sticky 'Save changes' bar with the offline retry note · _Different save model._

### 🔒 Settings — web card IA carve-out; mobile set-ic (§D10)
- proto: `5100-5141 (Settings fn); CSS L350,524-539,606,1628(avatar-lg),2298-2308(set-profile/set-name/pro-band/pglow/pro-h/pro-p/set-row/set-ic/set-ver),2403-2408`
- mobile: apps/mobile/app/(tabs)/settings.tsx + apps/mobile/components/settings/SettingsBundleContent.tsx
- web: src/app/components/Settings.tsx + src/app/components/settings/SettingsTwoPaneShell.tsx
- _Both platforms diverge HARD from the prototype's single-column 3-section IA (Account / Preferences / Your data) with a top profile row + gradient Pro band. WEB uses a two-pane desktop shell (sticky left sub-nav + right panel) with 4 different section groups (Account & billing / Preferences / Notifications / Privacy & data). MOBILE uses a search-first shell + an 11-section SettingsBundleContent (Personal/Membership/People/Goals & targets/Display/Connections/Reminders/Recipes/Account/Legal/Build/Danger zone). Neither matches the prototype's section names, row set, ordering, the avatar+name+plan profile header, the gradient Pro band card, or the 'Sloe v3.0 · nutrition values are estimates' version footer. The mobile search-first layout and web two-pane are net-new capability (Grace-blessed in the file comments) but the section taxonomy + profile header + Pro band are conformance gaps._
  - **[HIGH]** Section taxonomy / IA: proto = Three sections: Account / Preferences / Your data, each a flush divided card of set-row items · app = Web: 4 pane groups (Account & billing / Preferences / Notifications / Privacy & data). Mobile: 11 bundle sections (Personal/Membership/People/Goals & targets/Display/Connections/Reminders/Recipes/Account/Legal/Build/Danger zone) · _Both far exceed the prototype's 3-section set; rows are regrouped and renamed. Largest divergence in the cluster._
  - **[HIGH]** Profile header row: proto = .set-profile: 60px gradient avatar-lg ('G') + serif set-name 'Grace Howse' + 'grace@email.com · Free plan' muted subline at top of scroll · app = Mobile: no top set-profile row — identity lives inside the 'Personal' SettingsCard further down. Web: header handled by SettingsTwoPaneShell.header (different treatment) · _Prototype's distinctive avatar+name+plan header at top of Settings is absent/relocated on both._
  - **[HIGH 🔒keep?]** Sloe Pro band card: proto = .pro-band: full gradient-brand card with .pglow radial highlight, 'Sloe Pro' frost overline, serif 'Plan the whole week' headline, 'Unlimited imports, photo & voice logging, adaptive targets and body-composition trends.' subcopy, opens paywall · app = Mobile: Membership SettingsCard (flat white per ENG-1081 'flat white for now' — comment confirms gradient deliberately dropped). Web: billing under Account & billing pane · _keepCurrentCandidate on mobile flat-white treatment (ENG-1081 Grace call), but the prototype's gradient Pro upsell band as a discrete CTA card is structurally missing._
  - **[MED]** Version footer: proto = .set-ver: 'Sloe v3.0 · nutrition values are estimates' centered tertiary text below Sign out · app = Replaced by a 'Build' section card (mobile) / not the prototype string · _Prototype's single-line version+estimates footer not reproduced verbatim._
  - **[LOW 🔒keep?]** Sign out treatment: proto = .btn--tertiary btn--block ghost, destructive-coloured text, marginTop 16 · app = Mobile: neutral bordered card row with LogOut icon, text NOT destructive (deliberate — comment: red reserved for irreversible) · _keepCurrentCandidate — mobile intentionally makes Sign Out neutral (reversible action) vs prototype's destructive-red text._
  - **[LOW 🔒keep?]** Settings search: proto = none · app = Mobile has a pinned search field + settingsSearchIndex; prototype has no search · _keepCurrentCandidate — net-new capability (Wave-2 audit-vs-competitors), Grace-blessed._

### 🔒 Changelog — asymmetric entry points (§D15)
- proto: `6594-6615 (Changelog fn); CSS L1046-1053(chlog/chlog-head/chlog-v/chlog-new/chlog-date/chlog-list/chlog-dot)`
- mobile: apps/mobile/app/whats-new.tsx
- web: (web changelog surface not separately located — likely within Settings/help; not confirmed present)
- _Prototype Changelog ('What's new') is a simple stacked list: per-release chlog block with serif chlog-v version ('3.0'), a 'Latest' success chip, right-aligned date, and a chlog-list of bullet items with primary dot markers. Mobile whats-new.tsx is a richer data-driven screen (getLatestChangelog/kind-grouped section cards, release headline, attribution, Done pill, flag-gated full timeline) — same spirit but more structure than the prototype's bare bullet list. Web equivalent not located as a standalone surface._
  - **[MED 🔒keep?]** Release item layout: proto = chlog-head: serif chlog-v + 'Latest' chip + date; chlog-list flat bullets with chlog-dot · app = Mobile groups bullets into kind-based section cards with eyebrows; adds release headline + attribution + Done pill · _Mobile is structurally richer than the prototype's flat bullet list; copy/version sourced from data, not the prototype's hard-coded 3.0/2.8/2.6._
  - **[LOW 🔒keep?]** Version + content source: proto = Hard-coded 3.0 'June 2026' Latest with 3 specific bullets (Adaptive targets / Editorial recipe detail / Calm mode) · app = Data-driven from changelog source — different actual copy · _Expected — real app pulls live changelog data._
  - **[LOW]** 'Latest' chip: proto = chlog-new: 10px bold uppercase success, success-soft bg, pill radius · app = Mobile latestChip damson/plum soft chip (win family) — different colour family · _Prototype uses success-green; mobile uses damson/plum. Token mismatch._
  - **[MED]** Web changelog surface: proto = present · app = Not located as a standalone web screen · _Confirm whether web has a What's-new surface; if absent it's a web parity gap (net_new_absent on web)._

## Cluster: account-data


### 🔒 DataExport — two direct export buttons (B25)
- proto: `5909-5939`
- mobile: apps/mobile/components/settings/SettingsBundleContent.tsx (export rows, CSV/JSON flow ~L889-896)
- web: src/app/components/Settings.tsx (Privacy & Security section, ~L1922-2010; CSV button ~L1940 + 'Export everything' button data-testid=settings-export-everything-button ~L1960)
- _Prototype DataExport is a dedicated 'Export your data' Sheet (height 76%) with a 3-phase flow (choose -> preparing -> ready), a 4-item multi-select list (Food diary/Your recipes/Weight & body/Settings & targets) rendered as exp-row toggle cards with exp-ic plates and exp-check circles, an exp-foot counter ('N of 4 selected - GDPR-ready, machine-readable'), a 'Prepare export' primary CTA, a packaging spinner state, and a ready state (exp-ready-ic success circle + exp-file row 'sloe-export-18jun.zip - 1.4 MB - expires in 7 days' + 'Download .zip'). Neither app has this. Web renders two flat settings buttons (CSV download + JSON 'Export everything'); mobile renders equivalent CSV/JSON export buttons. No Sheet, no multi-select, no phase/packaging/ready states, no exp-* visual language._
  - **[HIGH]** Container shell: proto = Sheet title='Export your data', height='76%', sheet-scroll padding 0 20px 20px · app = Inline buttons inside the Settings Privacy & Security card (web) / settings bundle rows (mobile) — no dedicated export sheet · _Whole surface is a flat button list, not the redesigned selectable export sheet._
  - **[HIGH]** Selectable item list (exp-list / exp-row): proto = 4 toggleable rows: Food diary (418 entries - CSV), Your recipes (26 recipes - JSON), Weight & body (90 entries - CSV), Settings & targets (Preferences - JSON); each exp-row 1.5px border, radius 14px, on-state border var(--primary)+accent-frost-mist bg, 38px exp-ic plate (radius 11) that fills primary when on, 22px exp-check circle · app = No selectable list — export is all-or-nothing CSV or full JSON. No exp-row/exp-ic/exp-check elements exist.
  - **[HIGH]** Phase flow (choose/preparing/ready): proto = 3 phases with a 1900ms packaging spinner (import-spin) and a ready state with exp-ready-ic + exp-file card · app = No phased flow — direct download trigger with a toast
  - **[MED]** Footer meta (exp-foot): proto = 'N of 4 selected - GDPR-ready, machine-readable' with info icon, centered 12px fg-tertiary · app = Absent
  - **[MED]** Primary CTA copy: proto = 'Prepare export' (with chevD icon) then 'Download .zip' · app = 'Export nutrition log (CSV)' / 'Export everything' button labels

### ✅ DeleteAccount — H3 M2 L0 (ENG-1260 / B26, `delete_account_sheet_v1`, 2026-06-29)
- proto: `5942-5982`
- mobile: `apps/mobile/components/settings/DeleteAccountSheet.tsx` + `SettingsBundleContent.tsx` (flag-gated; legacy Alert chain in `else`)
- web: `src/app/components/settings/DeleteAccountSheet.tsx` + `Settings.tsx` (flag-gated; legacy `SettingsDialogs` two-stage in `else`)
- shared: `src/lib/settings/deleteAccountFlow.ts`, `fetchDeleteAccountLedger.ts`, `executeAccountDelete.ts`
- _Ledger owner-column fix (ENG-1270, 2026-06-29): the live "what will be removed" counts now read each table by its real owner column — `recipes.author_id` (was a non-existent `user_id`) + `saves.user_id` for the "saved & created recipes" row, and `profiles.weight_kg_by_day` (was a non-existent `weight_by_day`) for weight history. The pre-fix bug errored on the wrong columns and a single shared `catch` blanked the entire ledger to null, showing an empty consent step. Counts are now resolved per-query so one bad query degrades only its own row (logged loudly), never the whole ledger. Pinned by `tests/unit/fetchDeleteAccountLedger.test.ts`._
- _Prototype DeleteAccount is a 3-step Sheet (height 80%) with a del-steps progress bar (3 segments, destructive fill). Step 1: 'Why are you leaving?' del-stephd + 5 reason radios (ver-row + radio-dot) [Taking a break/Found another app/Privacy concerns/Too expensive/Other]. Step 2: del-warn circle (alertTri) + 'This can't be undone' + 'Export my data first' outline button + a set-row ledger of what's removed (418 diary entries / 26 recipes / 90 days weight / household membership) with destructive x icons. Step 3: type DELETE input (centered, letter-spaced, bold). Footer is a 2-button row: 'Keep my account' outline + 'Continue'/'Delete forever' (turns destructive when DELETE typed). The app has NONE of this multi-step structure. Mobile uses a native Alert chain (explainer Alert -> Alert.prompt type 'delete'); web uses a single ConfirmDialog with a one-line description. No reason capture, no progress steps, no removal ledger, no inline 'export first' affordance, no del-warn/del-step/ver-row/radio-dot visual language._
  - **[HIGH]** Container + structure: proto = Sheet height 80%, 3-step wizard with del-steps progress indicator · app = Mobile: native Alert + Alert.prompt chain. Web: single ConfirmDialog modal. No wizard, no steps bar.
  - **[HIGH]** Step 1 reason capture (ver-row + radio-dot): proto = 5 single-select reason radios under 'Why are you leaving?' serif heading · app = Absent on both platforms — no leaving-reason capture
  - **[HIGH]** Step 2 warning + removal ledger: proto = del-warn 56px destructive circle + 'This can't be undone' + 4-row set-row ledger of data removed (destructive x icons) + 'Export my data first' outline button · app = Mobile: plain Alert body text. Web: one-line description string. No ledger, no inline export-first CTA, no del-warn circle.
  - **[MED]** Step 3 type-to-confirm: proto = input typed 'DELETE' (centered, letterSpacing .2em, weight 700); Delete button turns destructive bg only when match · app = Mobile: Alert.prompt typed 'delete' (lowercased compare). Web: ConfirmDialog (no typed gate visible in description block). · _App has a typed-confirm on mobile but via native prompt, not the styled centered input; web delete may lack the typed gate the erase path has._
  - **[MED]** Footer CTAs: proto = 'Keep my account' outline + 'Continue'/'Delete forever' · app = Native Alert button labels ('Cancel' / 'I want to delete' / 'Delete account')

### 🔒 EraseEverything — web dialog IA (§D18)
- proto: `6618-6641`
- mobile: apps/mobile/components/settings/SettingsBundleContent.tsx (reset/erase combined Modal ~L2714-2900, erase-confirm-input typed 'RESET' ~L3030-3073)
- web: src/app/components/settings/SettingsDialogs.tsx (TypeToConfirmDialog typeToConfirm='RESET', title 'Delete your data and start fresh?' ~L200-210; ERASE_LEDGER ~L81-98)
- _Prototype EraseEverything is a focused 'Erase everything' Sheet (height auto) with: a del-warn block (alertTri + 'This keeps your account — but wipes your data' meal-name + explanatory meal-sub clarifying it's NOT account deletion); a 4-row set-row ledger (Food diary 418 entries / Weight & body 90 entries / Your recipes 26 recipes / Plans & templates All weeks) each with destructive set-ic icon + 'erased' trailing label; a 'Type RESET to confirm' field-label + input; footer = 'Cancel' outline + 'Erase everything' button that turns destructive only when RESET typed. The app implements an erase path with a RESET typed gate on both platforms (closest match of the four surfaces), but the structure differs: mobile bundles erase INTO a 'Refresh your plan' reset/erase combined bottom-sheet Modal (amber RefreshCw header, reset + erase in one modal), not a dedicated single-purpose 'Erase everything' sheet; web uses a generic TypeToConfirmDialog titled 'Delete your data and start fresh?' with a kept/cleared ERASE_LEDGER rather than the prototype's all-erased 'erased'-labelled ledger and del-warn 'keeps your account' framing._
  - **[HIGH]** Sheet scope/title: proto = Dedicated 'Erase everything' Sheet, height auto, single purpose · app = Mobile: combined into 'Refresh your plan' reset/erase Modal. Web: TypeToConfirmDialog titled 'Delete your data and start fresh?' · _Mobile merges reset+erase; prototype erase is standalone._
  - **[MED]** del-warn 'keeps your account' framing: proto = del-warn block: 'This keeps your account — but wipes your data' + sub explicitly distinguishing from account deletion · app = Web title 'Delete your data and start fresh?' + ERASE_LEDGER kept/cleared rows; mobile amber 'Refresh your plan' framing. Missing the explicit 'keeps your account, not the same as deleting' del-warn copy.
  - **[MED]** Erased ledger (set-row): proto = 4 rows, each destructive set-ic + title + sub + trailing 'erased' label · app = Web: ERASE_LEDGER with green-check kept / red-cross cleared rows (different model). Mobile: bullet list inside reset modal.
  - **[LOW 🔒keep?]** Type RESET gate: proto = input 'RESET', confirm button turns destructive bg on match (var(--destructive) else var(--muted)) · app = Present on both: mobile erase-confirm-input requires 'RESET' (border turns red, opacity gate); web TypeToConfirmDialog typeToConfirm='RESET' · _Typed-confirm semantics match the prototype; only the wrapping container/copy differs._

### 🔒 HealthSync
- proto: `5230-5249`
- mobile: apps/mobile/app/health-sync.tsx (full HealthSyncScreen)
- web: src/app/components/suppr/apple-health-card.tsx, src/app/components/suppr/today-apple-health-card.tsx (web has no full Health screen — iOS-only integration)
- _Prototype HealthSync is a minimal Pushed screen: a centered state block (60px destructive-soft heartPulse icon, title 'Connect Apple Health' / 'Health connected', body copy), and when connected a 4-row card (Steps Reading / Active energy Reading / Workouts Reading / Weight Writing) with success pills, plus a single 'Connect Health' (primary) / 'Disconnect' (outline) CTA. The mobile app implements a FAR more capable screen: CONNECT eyebrow + Apple Health card with 5 per-category rows (steps/weight/active energy/resting energy/workouts) each with a leading status dot and 'Synced X ago' timestamps, a NUTRITION card (import/share toggles, simple-labels, test-import/test-write probes, clear-imported), an error-recovery banner with retry/open-settings, unavailable-state handling, and Connect/Sync/Re-import CTAs. This is a deliberate, materially-more-capable divergence (real HealthKit wiring, per-category freshness, error recovery) — keep current. It is NOT the prototype's simple connect/disconnect toggle screen, but matches the prototype's intent at far higher fidelity. The header reads 'Health Sync' vs prototype 'Apple Health'._
  - **[HIGH 🔒keep?]** Screen scope / capability: proto = Minimal connect/disconnect state screen with a static 4-row reading/writing card · app = Full HealthKit screen: 5 category rows with live last-sync timestamps + status dots, nutrition import/share toggles, diagnostic probes, error-recovery banner · _App is intentionally more capable than the prototype (real HealthKit integration, per-category freshness, error recovery). Keep current — but the prototype's simple state-centered framing for the disconnected/connected hero is worth preserving as the visual register._
  - **[LOW 🔒keep?]** Header title: proto = 'Apple Health' (Pushed title) with centered state__title 'Connect Apple Health' / 'Health connected' · app = Header 'Health Sync'; card title 'Apple Health / Health Connect' · _App also targets Google Health Connect copy; lowercase/brand alignment minor._
  - **[MED 🔒keep?]** Connect/disconnect CTA: proto = single 'Connect Health' primary / 'Disconnect' outline toggle · app = 'Connect Health Data' primary, then 'Sync Now' primary + 'Re-import' ghost when connected (no explicit Disconnect — managed via iOS Settings) · _App routes disconnect through iOS Settings (correct platform pattern) rather than an in-app disconnect button._

## Cluster: onboarding


### ⬜ Onboarding flow architecture & step model — H2 M2 L0
- proto: `6096-6438`
- mobile: apps/mobile/components/onboarding/mobile-flow.tsx; apps/mobile/lib/onboarding.ts
- web: src/app/components/onboarding/web-flow.tsx; src/lib/onboarding/state.ts (STEP_IDS)
- _The app implements onboarding as a 14-step LINEAR funnel (welcome, app-choice, goal, sex, age, height, weight, activity, pace, diet, strategy, reveal, signup, data-bridges). The prototype is a smaller, differently-grouped flow: welcome(0) → goal+pace combined(1) → 'A little about you' combined body-stats(2: name+sex+gender+pronouns+age+height+weight+goalWeight+skip-weight all on ONE step) → activity(3) → diet(4) → allergens(5) → strategy(6) → calculating(7) → reveal(8) → signup(9) → activation-bridges(10) → motivation/why(12) → trial/paywall(14) → referral(15) → first-win(11). The app's per-question splitting (sex/age/height/weight as 4 separate steps) and absence of several prototype steps are the dominant divergences. Both web and mobile share this same 14-step architecture, so the diffs apply to BOTH surfaces._
  - **[HIGH 🔒keep?]** Step grouping — body stats: proto = Single 'A little about you' step (step 2) holds name + sex-at-birth + gender identity + pronouns + age + height + weight + goal-weight steppers + 'I'd rather not enter my weight' row · app = both · _One-question-per-screen is a deliberate, defensible funnel pattern (Cal AI / Yazio style) and arguably reduces per-screen friction. Flag for Grace's call rather than auto-revert, but it is a real structural divergence from the canonical prototype._
  - **[HIGH]** Goal + Pace coupling: proto = Goal and Pace live on ONE step (step 1): goal options + inline 'Pace — about X kg/week' segmented (Gentle/Steady/Ambitious) + aggressive-pace warning below · app = both
  - **[MED 🔒keep?]** Prototype-only step: Calculating (step 7): proto = A dedicated 'Building your plan…' interstitial with spinning sparkles icon (1.8s auto-advance) before the reveal · app = both · _The reveal's inline anticipation beat covers the same intent; a separate full-screen interstitial may be unnecessary._
  - **[HIGH]** Prototype-only step: Motivation / Why (step 12): proto = 'What's pulling you here?' driver list (Feel better/More energy/Health marker/Event/Get stronger) + obstacle chips (Evening snacking/Eating out/No time/Consistency/None) · app = both
  - **[HIGH 🔒keep?]** Prototype-only step: Trial / Paywall (step 14): proto = 'Start with everything on' — 7-day Pro trial card with feature list, trial-vs-free radio choice, 5-star social proof, 'No charge today / Cancel anytime' trust footer · app = both · _Onboarding paywall placement is a product/monetisation decision (Launch initiative) — likely intentional to keep the funnel value-first; confirm with Grace, do not silently add._
  - **[MED]** Prototype-only step: Referral / Invite (step 15): proto = 'Cooking's better together' — sloe.app/g/grace invite link + copy button + 'Invite someone' CTA + 'Maybe later' · app = both
  - **[MED 🔒keep?]** Prototype-only step: Guided First Log / First Win (step 11): proto = 'One quick win' — Breakfast / Coffee / Search-a-food chips + 'Take me to today' CTA · app = both · _The app's firstRun Today polish partially covers the first-win intent, but the explicit one-tap log chips are gone._
  - **[LOW 🔒keep?]** App-only step: app-choice (02): proto = No 'Coming from another app?' step · app = both · _Net-new MFP-refugee capture feature, flag-gated; deliberate addition._
  - **[MED]** Progress indicator placement & count: proto = ob-dots renders QN=6 segments and appears ONLY on the 6 question steps (1-6); welcome/calc/reveal/signup/activation/paywall have no progress bar · app = both · _Segment styling itself matches (h-1/4px, rounded-full, primary fill / muted track); the count and which-steps-show-it differ._

### ⬜ Goal step (03) — H2 M0 L3
- proto: `6119, 6147-6168; CSS .ob-opt/.ob-opt-ic 1144-1151`
- mobile: apps/mobile/components/onboarding/steps/goal.tsx (+ shared goalOptions)
- web: src/app/components/onboarding/steps/goal.tsx + src/lib/onboarding/goalOptions.ts
- _The goal options diverge from the prototype in count, copy, and leading visual. Prototype: 3 options with lucide ICONS in rounded-square plates (ob-opt-ic: 40x40, radius 11, --bg-secondary bg, primary icon → primary bg + white icon when selected). App: 4 options (adds a 'recomp / Just track what I cook' goal absent from the prototype) using circular FOOD-IMAGE thumbnails (FoodFallbackThumb size 56) instead of icon plates._
  - **[HIGH]** Leading visual on each option: proto = Icon in rounded-square plate (.ob-opt-ic, lucide: progress / today / flame) · app = both · _Different element type entirely — icon plate vs food image circle. Violates 'prototype icons must be exact'._
  - **[MED 🔒keep?]** Number of goal options: proto = 3 (lose / maintain / gain) · app = both · _'recomp' covers the just-track persona; arguably better capability, but it is a divergence._
  - **[LOW]** Step title: proto = “What brings you here?” · app = both
  - **[LOW]** Step subtitle: proto = “This sets the shape of your target. No judgement either way.” · app = both
  - **[LOW]** Option titles/subtitles: proto = 'Lose weight' / 'Maintain & eat better' / 'Build muscle' with terse subs · app = both
  - **[HIGH]** Inline pace control: proto = Goal step also renders the Pace segmented + aggressive warning · app = both · _Duplicate of the architecture-level goal+pace coupling diff._

### ⬜ Data-bridges / Activation step (prototype 10 / app terminal 14) — H1 M0 L2
- proto: `6337-6363; CSS .set-row/.set-ic 2303-2305, .ob-add 1203-1204`
- mobile: apps/mobile/components/onboarding/steps/data-bridges.tsx
- web: src/app/components/onboarding/steps/data-bridges.tsx
- _Both are 'bring your data' activation steps but structurally different. Prototype step 10 ('Set up in seconds') is a single flush card of 4 toggle ROWS (.set-row with .set-ic icon plate + .ob-add 'Add'/'Added' pill): MyFitnessPal/LoseIt/Cronometer import, Apple Health, Recipe-from-a-Reel import, Gentle reminders. The app renders a stack of richer CARDS with embedded forms: web = Manual targets paste-form + Notifications + Recipe URL + MFP CSV importer (no Apple Health on web by design); mobile adds an Apple Health card. The interaction model (inline forms vs simple add/added toggles) and the card-vs-row idiom differ._
  - **[HIGH]** Row vs card idiom: proto = .set-row toggle rows in one flush divided card, each with an 'Add'/'Added' pill (.ob-add) · app = both · _Fundamentally different interaction surface — lightweight toggles vs heavyweight inline forms._
  - **[MED 🔒keep?]** Bridge set: proto = MFP/LoseIt/Cronometer import · Apple Health · Recipe-from-Reel import · Gentle reminders · app = both · _App bridges are real, working importers (vs prototype's illustrative toggles); the manual-targets paste is a genuine MFP/MacroFactor refugee feature. More capability, but a set + framing divergence._
  - **[LOW]** Step title: proto = 'Set up in seconds' under 'Last thing' eyebrow · app = both
  - **[LOW]** Recipe-import framing: proto = 'Import a recipe from a Reel or link — Paste a TikTok / Instagram / web link → macros' (the viral hook front-and-centre) · app = both · _Prototype leans the viral Reel-import hook harder in onboarding copy._

### ⬜ Strategy / macro-split step (11) — H1 M0 L1
- proto: `6123, 6239-6255; CSS .ob-opt 1144-1151`
- mobile: apps/mobile/components/onboarding/steps/strategy.tsx
- web: src/app/components/onboarding/steps/strategy.tsx
- _Prototype step 6 ('How should we split your macros?') offers 3 strategies (Balanced / High-protein / Lower-carb) and crucially shows the RESULTING P/C/F split inline in each option subtitle (e.g. 'Even split — easy to live with · 150P / 200C / 60F') computed live via obTargets. The app offers 4 strategies (Balanced / High protein / High satisfaction / Low carb) with lucide ICONS but NO inline P/C/F preview in the option subtitles._
  - **[HIGH]** Inline P/C/F preview per option: proto = Each option subtitle appends live '· {p}P / {c}C / {f}F' · app = both · _Loses a key 'see the consequence before you pick' affordance from the canonical design._
  - **[MED 🔒keep?]** Strategy count & set: proto = 3 (balanced / high / lowcarb) · app = both · _'High satisfaction' is an extra Suppr strategy; more capability but a set divergence._
  - **[LOW]** Leading icon: proto = No icon plate on strategy options (.ob-opt without .ob-opt-ic) · app = both

### ⬜ Sex / body-stats steps (04-07) — H0 M5 L0
- proto: `6170-6197; CSS .segmented, .ob-chips/.ob-chip 1182-1193, .ob-stepper 1189-1191`
- mobile: apps/mobile/components/onboarding/steps/{sex,age,height,weight}.tsx
- web: src/app/components/onboarding/steps/{sex,age,height,weight}.tsx
- _The prototype's single 'A little about you' step (step 2) is fully decomposed in the app and the control idioms differ. Prototype: name text input; sex via a SEGMENTED control (Female/Male/Intersex-other); gender identity via CHIP selectors (Woman/Man/Non-binary/Self-describe/Prefer not to say); pronouns via CHIPS (She-her/He-him/They-them/Other/Prefer not); age/height/weight/goal-weight via inline +/- steppers all on one screen; 'I'd rather not enter my weight' skip row. App: sex is a vertical OptionCard list (Female/Male/Prefer not to say — 'Intersex/other' dropped); gender+pronouns collapse to ONE flag-gated free-text input; name moved to signup; age/height/weight each their own step._
  - **[MED]** Sex control idiom: proto = Segmented 3-up (Female / Male / Intersex / other) · app = both
  - **[MED]** Third sex option label: proto = 'Intersex / other' · app = both
  - **[MED]** Gender identity field: proto = 5 chip options (Woman/Man/Non-binary/Self-describe/Prefer not to say) · app = both · _Behind a default-OFF flag, so for most users it is absent entirely vs the prototype's always-on chips._
  - **[MED]** Pronouns field: proto = 5 chip options (She/her, He/him, They/them, Other, Prefer not) · app = both
  - **[MED]** Name capture location: proto = First-name text input on the body-stats step (step 2), optional · app = both
  - **[LOW 🔒keep?]** Stepper styling: proto = .ob-stepper: hyd-btn +/- with serif 21px value + small unit; multiple steppers stacked (.ob-metric rows) · app = both · _Stepper presentation is close; the divergence is mainly the per-step split, already captured at architecture level._

### ⬜ Reveal step (8 / app 12) — H0 M3 L1
- proto: `6266-6311; CSS .ob-macros 1170-1174, .ob-maths 1195-1201, .ob-proj 1202`
- mobile: apps/mobile/components/onboarding/steps/reveal.tsx
- web: src/app/components/onboarding/steps/reveal.tsx
- _Both platforms render a rich reveal — success check, 'Your daily target' eyebrow, CalorieRingDial with serif count-up, 3 macro tiles, BMR/TDEE summary, 'Show the maths' disclosure, and a goal blurb. This EXCEEDS the prototype in places (methodology note, 'What happens next' 3-step card). But it diverges on the title (no name echo) and the projection presentation (text sentence vs the prototype's SVG trend chart), and the macro tiles are styled differently (the app adds %/progress bars; the prototype shows just value+label)._
  - **[MED]** Title / name echo: proto = '{name}, here's your daily target' (echoes the name captured on step 2) under a 'Your plan is ready' eyebrow · app = both · _App captures name on signup (after reveal in the linear order), so the name isn't available to echo here — a consequence of the step reordering._
  - **[MED]** Projection trend: proto = Inline SVG polyline trend chart (.ob-proj) showing weight curve from now → goal with 'Projected trend · ~Xkg by {date}' and start/end markers · app = both · _The canonical reveal visualises the trajectory; the app states it in prose._
  - **[LOW 🔒keep?]** Macro tile content: proto = .ob-macros: just '{n}g' in macro colour + label, divider lines between, no bars/percentages · app = both · _App tiles are richer; arguably better but stylistically different from the prototype's minimal trio._
  - **[LOW]** Coach permission quote: proto = 'A guide, not a limit — Sloe nudges it as it learns how you actually eat.' · app = both
  - **[MED]** Reveal CTAs: proto = 'Looks good — save it' (→ paywall) + 'Adjust my details' ghost link (→ back to step 2) · app = both · _The 'Adjust my details' back-to-stats affordance is absent; the global Back chevron is the only edit path._
  - **[LOW 🔒keep?]** Net-new 'What happens next' card: proto = Not present · app = both · _Additive capability — keep._

### ⬜ Signup / Save-your-plan step (9 / app 13) — H0 M1 L1
- proto: `6312-6336; CSS .wob-prov/.ob-magic 1158-1163, 2642-2643`
- mobile: apps/mobile/components/onboarding/steps/signup.tsx
- web: src/app/components/onboarding/steps/signup.tsx
- _Prototype step 9 is a value-first 'Save your plan' brand screen (ob--brand) with an Apple primary button (.wob-prov-apple, white on plum) + 'Continue with email' → magic-link flow (email input → 'Email me a magic link' → 'Check your inbox' confirmation). The app's signup step exists and also leads with Apple (mobile is Apple-only per apps/mobile/CLAUDE.md); web adds email. The app additionally captures the user's NAME here (the prototype captures name on step 2). Detailed token/layout diffing of this step warrants a render pass, but the brand-ground + Apple-primary structure is present._
  - **[MED]** Name capture: proto = No name field on signup (name lives on the body-stats step) · app = both · _Consequence of moving name out of body-stats; means the reveal can't echo the name._
  - **[LOW]** Magic-link confirmation state: proto = Web email path shows an .ob-magic 'Check your inbox' card with check icon · app = web · _Mobile is Apple-only so the email path is web-specific; verify against .ob-magic._

### ⬜ Diet + Allergens (prototype steps 4 & 5) — H0 M0 L2
- proto: `6121-6122, 6217-6237; CSS .ob-chips/.ob-chip 1182-1185`
- mobile: apps/mobile/components/onboarding/steps/diet.tsx
- web: src/app/components/onboarding/steps/diet.tsx
- _The app MERGES the prototype's two separate steps — Diet (step 4) and Allergens (step 5) — into a single diet.tsx step on both platforms. The 14 regulated allergens are rendered in the same diet step rather than their own screen. Diet options and allergen chips otherwise map to the prototype's chip idiom._
  - **[MED 🔒keep?]** Step boundary: proto = Diet (step 4: 'Any way of eating?') and Allergens (step 5: 'Anything to avoid?') are TWO separate steps with Continue/Skip CTAs each · app = both · _Consolidating two short chip-pick steps into one is a reasonable funnel shortening; flag for Grace but defensible._
  - **[LOW]** Diet title: proto = 'Any way of eating?' (step 4) / 'Anything to avoid?' (step 5) · app = both
  - **[LOW]** Allergen selected-state colour: proto = .ob-chip.on.warn → --destructive bg, white text, with a check icon · app = both · _Needs a render check — prototype allergen chips go red (warn) when selected, distinct from diet chips which go primary._

### ✅ Step 0 — Welcome (ob--brand)
- proto: `6134-6145; CSS 1130-1178, 1620`
- mobile: apps/mobile/components/onboarding/steps/welcome.tsx
- web: src/app/components/onboarding/steps/welcome.tsx
- _Strong conformance on both platforms. Deep-plum --primary-deep ground, soft radial bloom (matches .ob--brand::before), lowercase Fraunces 'sloe' wordmark at 56px (md:64px web) light weight, italic Newsreader tagline in --accent-frost, white 'Get started' pill CTA, and 'Private by default · About a minute' trust footer with lock + clock icons. Verified tokens, layout, and copy all align._
  - **[LOW 🔒keep?]** Tagline copy: proto = “Cook what you love. Hit your goals anyway.” · app = both · _GLOBAL keep-current is the lowercase wordmark; this specific tagline wording is a Grace's-call exception noted in both files' header comments._
  - **[LOW]** 'About a minute' trust icon: proto = Ic n='sparkles' s=13 · app = both · _Prototype pairs sparkles with the 'About a minute' label; the app uses a clock. Minor icon mismatch, semantically defensible._
  - **[LOW 🔒keep?]** 'I already have an account' affordance: proto = Not present on the welcome step (sign-in deferred to step 9) · app = both · _Deliberate returning-user entry point (Grace 2026-04-20); more capability, keep._

### ✅ Activity step (08)
- proto: `6120, 6199-6215; CSS .ob-opt 1144-1151`
- mobile: apps/mobile/components/onboarding/steps/activity.tsx
- web: src/app/components/onboarding/steps/activity.tsx
- _Close conformance: 4 activity options (sedentary/light/active/very) as a vertical option list with title + subtitle + selected check, matching the prototype's .ob-opt list (these options carry NO icon plate in the prototype, consistent with the app). Title 'How active is a normal week?' and subtitles align in intent. No high-severity diffs found; verify exact copy wording in a visual pass._
  - **[LOW]** Copy wording: proto = Subtitles: 'Desk job, light errands' / 'On my feet a fair bit' / 'On your feet or training most days' / 'Hard training or a physical job' · app = both · _Confirm exact subtitle strings against the app's activity options data in a render pass._

## Cluster: household-batch-coach-fasting


### 🔒 Household — sharing grid + presets (B7)
- proto: `5585-5648`
- mobile: apps/mobile/app/household-settings.tsx
- web: src/app/components/HouseholdSettingsPage.tsx
- _App and prototype implement DIFFERENT household models. Prototype: a Shared/Just-me segmented toggle, member rows with per-person 'Eating in / Out' pills, a 'Match servings to who's eating' smart row, a Default-servings stepper, a Shared-shopping-list toggle, a 'Shared this week' meal list, and a Leave-household tertiary. App: a privacy-focused sharing system — a 7×4 day×meal sharing grid, named sharing presets (dinners/all/custom), a per-member share-targets privacy toggle, member rows routing to targets, and a member legend. The app is more capable on privacy/granularity but is MISSING most of the prototype's cooking-scale surface (servings stepper, eating-in toggles, shared-this-week list, segmented Shared/Just-me view)._
  - **[HIGH]** Shared / Just me segmented toggle (.segmented): proto = Full-width 2-button segmented at top switches between the shared household view and a solo empty-state ('Cooking for one?') · app = No segmented toggle. App shows a solo invite card when members.length<=1 but it's an inline card, not a view switch; the rest of the screen always renders · _Different solo-state pattern_
  - **[HIGH]** Member 'Eating in / Out' toggle (.hh-eat): proto = Each non-owner member row has a pill toggling 'Eating in' (sage success-soft) vs 'Out' (muted) — drives who the week scales to · app = Member rows have no eating-in toggle. Self row routes to /targets; other rows are non-interactive. Eating-status concept absent · _Core prototype interaction missing on both platforms_
  - **[HIGH]** Default servings stepper (.serv-step + .hh-set): proto = A −/value/+ stepper 'Default servings — Scales recipes & shopping' in both solo and shared views · app = No servings stepper anywhere in household settings on either platform · _Cooking-scale control absent_
  - **[MED]** 'Match servings to who's eating' smart row: proto = A set-row with sparkles icon that sets servings to the count of members eating in this week · app = Absent · _Smart-default affordance missing_
  - **[MED]** Shared shopping list toggle (.toggle-sw): proto = A switch 'Shared shopping list — Everyone sees the same basket' · app = No shared-list toggle in household settings. (A separate share_targets privacy switch exists but is a different concept — sharing macro targets, not the basket) · _Different toggle semantics_
  - **[MED]** 'Shared this week' meal list: proto = A card listing this week's shared meals with day + who (e.g. 'Miso salmon & greens — Tue · everyone') · app = Absent in household settings · _Weekly shared-meal summary missing_
  - **[MED]** Leave household action: proto = A tertiary destructive 'Leave household' button at the bottom · app = No leave-household action in the settings screen · _Missing destructive exit affordance_
  - **[MED 🔒keep?]** 7×4 sharing grid + named presets + privacy share-targets: proto = Not present in prototype household screen · app = App adds a full day×meal sharing grid, sharing presets (radio list), a member legend, and a per-member privacy 'share targets' toggle · _Net-new app capability — richer, legally-gated sharing model; keep, but reconcile with prototype's simpler servings/eating-in surface_
  - **[LOW 🔒keep?]** Owner avatar styling (.hh-av.is-owner): proto = Owner avatar filled plum with white initial; member avatars neutral; a 'you' inline pill · app = Member avatars use householdMemberAccent palette (per-index colors), a 'You' pill on self row · _Different but reasonable identity treatment_

### 🔒 Fasting — richer live surface (§D14)
- proto: `5499-5537`
- mobile: apps/mobile/app/fasting.tsx
- web: src/app/components/FastingTimer.tsx
- _App is a far richer production fasting surface than the prototype (animated SVG ring, 5 preset pills incl OMAD, live stages bar, narrative + autophagy disclosure, history list with delete). The prototype is a simpler concept: a static 16:8 ring landing, a dot-row stage indicator (not a progress bar), a coach quote, and a 7-day 'This week' bar chart. Most divergence is keep-current (app deliberately more capable), but two prototype elements are genuinely absent on both platforms: the 'This week' weekly bar chart, and the coach quote line. Ring size and stage labels also differ._
  - **[HIGH]** 'This week' 7-day fasting bar chart (.fast-week / .fw / .fw-bar): proto = Both landing AND active states show a row of 7 vertical bars (M T W T F S S) with heights scaled to hours fasted that day; rounded-top 6px bars in --primary, faded for zero-fast days · app = No weekly bar chart on either web or mobile. App shows a flat 'Recent fasts' list (date + duration rows) instead · _Prototype's signature weekly-rhythm visual is absent; the list is a different representation_
  - **[MED 🔒keep?]** Coach quote line (.coach): proto = Italic/semibold plum centered coach line in both states e.g. landing 'Ready when you are. Most people start their fast after dinner.' and active 'You're past the 12-hour mark — into the fat-burning window.' · app = App has a stage narrative (italic serif) only in the active state, and a plain 'Ready when you are' sub-label on landing — not the styled .coach plum line. Landing has no coach sentence · _App's stage narrative arguably covers the active-state coach intent; landing coach line is genuinely missing_
  - **[MED 🔒keep?]** Fasting stage indicator: proto = A .fast-stage horizontal DOT row — Fed / Early fast / Fat-burning / Ketosis — each a dot + label, 'on' state fills the dot plum. No progress bar · app = App renders a horizontal PROGRESS BAR with position markers and labels Fed / Fat burning / Ketosis / Deep (FASTING_STAGES). Different stage set ('Early fast' vs 'Deep') and different visual (bar not dot-row) · _App's stages bar is the richer, shared-lib stage model; deliberate divergence_
  - **[LOW 🔒keep?]** Ring geometry: proto = SZ=230, R=100, strokeWidth 14; serif elapsed fontSize 42 (.fast-time) · app = RING_SIZE=248, stroke 14; elapsed via Type.ringValue (mobile) / text-5xl (web) · _248 ring is the Sloe 305:2 spec already adopted product-wide; matches web/mobile parity_
  - **[LOW 🔒keep?]** Active center sub-line copy: proto = 'of 16:00 goal' under the 13:24 elapsed numeral; stage chip is the overline 'Elapsed' · app = 'elapsed · X:XX left' sub-line; a flame stage chip (e.g. 'Fat burning') above the numeral · _App's countdown framing is a reasonable improvement_
  - **[LOW 🔒keep?]** Start/End times: proto = A .spread row 'Started 8:06pm' / 'Ends 12:06pm' below the End-fast button · app = A Started/Goal two-column card slab above the End button (parity both platforms) · _Same info, card treatment instead of inline spread_

### ✅ BatchCook — minimal v1 (ENG-1255; B3 Grace scope)
- proto: `5651-5726`
- mobile: `apps/mobile/app/batch-cook.tsx`, `apps/mobile/components/plan/BatchCookSurface.tsx`
- web: `src/app/components/plan/BatchCookSheet.tsx` (via `MealPlanner.tsx` / `PlanToolsV3.tsx`)
- _Grace B3 (2026-06-28) ratified **minimal v1 only:** recipe picker → batch-size stepper → scaled shopping list → Save/Cook. Shipped web+mobile (ENG-1255). The prototype's second-phase **assign-portions** day×meal checklist and **fridge pip tracker** are intentionally **out of scope** — not a conformance gap; file separately if product wants full batch planner (historical deferral ENG-1225)._
  - **[HIGH 🔒keep] Assign portions + fridge pip tracker:** proto = day×meal slot checklist gated to batch size + `.batch-track` / `.bt-pip` "After cooking" tracker · app = not built · _Beyond Grace minimal v1 (B3); do not re-litigate under ENG-1247._
  - **[HIGH ✅] Core minimal flow:** proto intro picker + batch stepper + scaled shopping note + Save/Cook CTAs · app = shipped minimal v1 (recipe pick, portion stepper, scaled list, save/cook) · _B3 satisfied for launch conformance tally._

### 🔒 Coach — distributed pieces; unified screen superseded (B9, ENG-923)
- proto: `5727-5765`
- mobile: (none found)
- web: (none found)
- _No app equivalent on web OR mobile. Searched routes, components, and copy ('Your coach', 'What to eat next', 'coach-digest', 'Ask the coach') — zero matches outside the prototype. The prototype Coach is a full-screen daily digest: a 'Today's read' narrative card (remaining protein/kcal prose with a 'Coach' pill), a 'What to eat next' ranked list of saved-recipe suggestions (thumb, name, 'Best fit' badge, kcal·protein, a 'why' rationale), a low-confidence 'ranked from your saved recipes' note, and an 'Ask the coach' chip row. Entirely unbuilt. (Note: a separate post-log suggestion toast exists on Today, but it is not this screen.)_
  - **[HIGH]** Entire Coach / daily-digest screen: proto = Full-screen coach: narrative digest card + 'What to eat next' ranked suggestion list (with Best-fit badge + why rationale) + Ask-the-coach chips · app = Does not exist on web or mobile · _Net-new prototype-only screen; no route, component, or copy exists. Needs a build decision._

## Cluster: recipe-cook-detail


### 🔄 RecipeDetail — flag path partial (`recipe_detail_v3_conformance`)
- proto: `4315-4427`
- mobile: apps/mobile/app/recipe/[id].tsx + apps/mobile/components/recipe/{RecipeDetailHero,RecipeTitleBlock,RecipeMacroStrip,RecipeMethodSteps,RecipeServingsFooter,RecipeIngredientGrid,RecipeActionPills}.tsx
- web: src/app/components/RecipeDetail.tsx
- _RecipeDetail is still the Figma-era (332:2) layout on BOTH web and mobile and diverges structurally from the v3 prototype. The cluster note's claim that hero/standfirst/sticky-CTA conformed in PR #611 does NOT hold: there is no editorial standfirst anywhere, the hero carries no kicker/serif-title/meta overlay, and the sticky CTA is an outline 'Cook Mode' + servings-stepper bar, not the prototype's filled 'Cook this' + square '+Log'. Web additionally uses a TABBED body (ingredients/steps/nutrition) vs the prototype's single continuous scroll, which also breaks web<->mobile parity (mobile is single-scroll). The two platforms are otherwise in lockstep with each other._
  - **[HIGH]** Hero title/kicker/meta overlay (web RecipeDetail.tsx ~L1672-1759; mobile RecipeDetailHero.tsx): proto = Hero (.rd-hero.tall, height 360px) overlays a bottom title block: .rd-kicker overline (11px bold, .12em, uppercase, white .82) + serif h1 (var(--font-serif) 31px/1.06 white) + .rd-meta row (clock·time, flame·kcal, utensils·Serves N) all INSIDE the hero over a .rd-veil gradient. Controls are back (left) + a single heart fav (right) only. · app = Hero is photo-only (height 375px) with a top scrim and frosted circle controls back/bookmark/share(+more). Title, kicker and meta are NOT in the hero — title renders below in a separate RecipeTitleBlock; meta even lower in RecipeMetaRow. No kicker/overline at all. Extra share + more controls the prototype lacks; uses Bookmark glyph, prototype uses heart.
  - **[HIGH]** Editorial standfirst (drop-cap headnote): proto = .rd-standfirst — serif 17px/1.55 fg-secondary paragraph with CSS ::first-letter initial-letter:2 drop-cap in --primary. The cookbook 'why' sentence; sits between hero and macros. · app = Absent on both platforms (grep 'standfirst' returns nothing in app code). The recipe 'why'/description renders, if at all, as a plain card much lower, no serif treatment and no drop-cap.
  - **[MED]** Macro strip container (RecipeMacroStrip.tsx + web nutrition tab): proto = .rd-macros — borderless flex row, border-TOP + border-BOTTOM only (1px var(--border)), 1px vertical divider between cells. Cal value has no unit; protein/carbs/fat carry a 'g' suffix. Labels 'kcal / Protein / Carbs / Fat' (uppercase .05em 11px). Serif values 24px. · app = Rendered as a carded slab: backgroundSecondary fill + full 1px border + radius 16 + paddingVertical lg, borderLeft dividers. Labels CAL/PRO/CARB/FAT (Type.label). Serif values 24px and the no-unit/g-suffix split match, but the slab fill/border/radius and label abbreviations diverge.
  - **[HIGH 🔒keep?]** 'Fits your day' + allergen twin pills: proto = Two side-by-side sage pills under the disclaimer: .rd-fits (check + 'Fits your day · ≈N%') and .rd-allergen (sprout + 'No gluten-containing ingredients'), both success-soft bg / success fg, 11px/600, radius full, flex-wrap. · app = Single 'fits your day' rendered as a SOLID verdict banner (full-width white-on-success-solid, flag fit_verdict_banner_v1) inside RecipeTitleBlock — a different element (banner not paired pills) and position (in title block, above macros). Allergen is a separate TrustChip + multi-line disclaimer card lower. NOTE the verdict-banner itself is a Grace-ratified ENG-1085 'confident payoff' decision (arguably better); the PAIRED-pill structure + the sprout allergen pill are still missing.
  - **[LOW]** rd-disclaimer line: proto = .rd-disclaimer — single 12.5px/1.5 fg-tertiary line directly under the fits/allergen pills: 'Estimated from ingredient names — not a guarantee…' (or 'Verified by the recipe author…' when official). · app = Equivalent copy exists but as a gluten-chip caption (mobile styles.glutenDisclaimer) and/or import-source disclaimer elsewhere; no single rd-disclaimer line tied to the fits/allergen pills.
  - **[HIGH]** Claim-this-recipe / Official toggle card: proto = .claim-card — for owned/imported recipes: frost-mist icon plate + 'Claim this recipe'/'Official recipe' title + sub + a .claim-switch toggle (role=switch) flipping macros estimate→verified. Sits between disclaimer and Ingredients. · app = No inline claim toggle card on the detail body. Instead an 'Official version available' card (officialRecipeId path) that switches to a DIFFERENT recipe id — different intent (cross-recipe switch, not in-place claim toggle). The official-claim toggle UX is absent.
  - **[HIGH 🔒keep?]** Ingredients header + inline servings stepper: proto = .section-head with .rd-h2 'Ingredients' (serif 21px) left and a .serv-step (− / serif value / +, 32px icon-btns) INLINE right. Default list is .rd-ings (leader-dotted: name … dotted-leader … qty). · app = Header reads 'Ingredients' + 'For N servings'. The servings stepper is NOT here — it lives in the bottom sticky footer (RecipeServingsFooter, 'Yield'). Default rendering is a 4-up image-tile grid, not the dotted-leader list. NOTE the tile grid maps to the prototype's optional ing-tiles mode (richer Sloe image system, arguably better default); moving the stepper to the footer is the real structural divergence.
  - **[LOW 🔒keep?]** Ingredient tiles radius + FatSecret attribution: proto = .ing-tile-ph radius 16, inset 1px border, dot top:7/right:7 9px with 2px card ring; .rd-attrib 'Powered by FatSecret Platform API · dots show ingredient match confidence' beneath the grid. · app = App tiles use rounded-24 (per component doc) + a SourceDot; FatSecret attribution renders only when a line is FatSecret-sourced (FatSecretBadge), not the prototype's standing attribution line.
  - **[MED]** Method steps (RecipeMethodSteps.tsx + web steps tab): proto = .rd-steps — .section-head 'Method' (.rd-h2 serif 21px) + .rd-h2-note '{n} steps · {time} min'. Each .rd-step: serif index in --accent-frost (26px, width 30, NOT zero-padded: 1,2,3), 15px/1.55 body, 1px border-bottom dividers. · app = Heading 'Method' (Type.title ~24px, no '{n} steps · time' note). Index serif 30px, width 40, color textTertiary, ZERO-PADDED ('01','02'). Body 16px/26. No per-step border-bottom dividers (gap-only). Index colour (frost vs tertiary), zero-padding, sizes and the missing step-count note all diverge.
  - **[LOW]** Notes section / report link: proto = .section-head 'Your notes' + .rd-notes (Rating overline + .rd-stars in macro-carbs colour + .rd-notebox textarea) then .rd-report-link 'Report an issue with this recipe' (centered, 12.5px tertiary, flag icon). · app = RecipeNotesCard renders rating + notes (broadly equivalent). Report path exists (useRecipeReport) but different affordance/placement. Star colour and the centered tertiary report link styling not verified to match (likely diverges).
  - **[HIGH]** Sticky CTA bar: proto = .pushed-cta — gradient-fade bar (transparent→bg) holding [.btn--primary 'Cook this' filled, flex:1, utensils] + [.btn--outline square 54px '+Log']. Primary action = Cook this (filled); log is the secondary square. · app = RecipeServingsFooter — solid translucent-white bar (border-top) holding [Yield + servings stepper] left and a single OUTLINE 'Cook Mode' pill right. No filled 'Cook this', no square '+Log' in the footer. Log/Edit live in a separate mid-body RecipeActionPills row the prototype lacks. Primary CTA is an outline, contradicting the prototype's filled 'Cook this'.
  - **[MED]** Import 'needs review' banner: proto = .review-banner (warning-soft, .rb-ic 40px warning plate, alertTri) 'Imported from TikTok — nutrition needs review' + btn--primary 'Verify ingredients'; when conf==='review' the body collapses to just this banner. · app = A verify path exists (recipe/verify.tsx, auto-verify note) but the prototype's distinct top-of-body review-banner card (warning plate + collapse-to-banner behaviour) is not reproduced in this exact treatment.

### 🔄 CookMode — dark theme on flag backlog (`recipe_detail_v3_conformance`)
- proto: `4428-4514`
- mobile: apps/mobile/app/recipe/[id].tsx (inline Cook Mode modal ~L2336-2620) + apps/mobile/components/cook/{CookMiseEnPlace,CookStepSwipeSurface,CookStepTimerPills,CookRunningTimerStrip,CookStepPageIndicator,CookTimerPanel}.tsx
- web: src/app/components/CookMode.tsx
- _Feature parity is strong (mise-en-place, timers, swipe nav, step chips). **Remaining v3 conform work is visual only** and tracked under **`recipe_detail_v3_conformance`** alongside RecipeDetail — not a separate ⬜ audit row for ENG-1247 closure. Prototype = immersive aubergine (`--primary-deep`) + frost accents + serif step text; live = light theme both platforms (web↔mobile matched)._
  - **[HIGH]** Cook Mode surface theme: proto = .cookmode { background: var(--primary-deep); color:#efe9f2 } — dark aubergine immersive screen. cm-top labels #cabfd6, progress bar i in --accent-frost, timer/pill/chip surfaces white@8-14% on the dark ground. · app = Web root 'fixed inset-0 bg-background text-foreground' (light); mobile root backgroundColor: colors.background (light) with header 'COOK MODE' in accent.primary. Light theme throughout — no aubergine immersion; accents are bg-primary/10 + accent.primary rather than frost-on-deep.
  - **[MED]** Step text type: proto = .cm-step — serif (var(--font-serif)) 28px/1.25, A-/A+ steps through SIZES [23,28,34]. .cm-recipe is serif italic 14px in --accent-frost above it. · app = Step text renders sans on both platforms. No serif step body, no serif-italic recipe-name line in frost above the step.
  - **[MED]** Mise en place phase: proto = cm-mise: serif h (.cm-mise-h 23px white) + sub 'Get everything to hand before you start — N of M ready.' + .cm-check rows (24px frost checkbox, strike-through on check) on the dark ground; primary is .btn--onbrand 'Start cooking' (frost bg, deep text). · app = CookMiseEnPlace exists (flag cook_ingredient_checklist_v1) with checklist + continue button, but on a light surface; checkbox/strike-through and the frost 'Start cooking' onbrand button are not the prototype's dark-ground treatment.
  - **[MED]** Bottom nav buttons: proto = .cm-nav: [outline square 54px chevron-left, transparent + 1.5px white@20 inset, white] + [.btn--onbrand 'Next step' / 'Done — log it', flex:1, frost bg]. · app = Prev/Next exist (web 'Finish'/'Next') but as light-theme primary buttons, not the frost onbrand + white-outline-on-dark pair. Last-step label is 'Finish' (web) vs prototype 'Done — log it'.
  - **[LOW]** Step dots indicator: proto = .cm-dots: centered row of 6px dots, active dot widens to 18px in --accent-frost, on the dark ground. · app = CookStepPageIndicator renders an analogous dot row but on the light theme with primary tint (web bg-primary), not frost-on-deep.
  - **[LOW]** Timer pills + per-step chips: proto = .cm-tpill (white@10 on dark, frost icon, .done pulses success) and .cm-chips 'For this step' (uppercase #a99bbb label + white@8 chips). · app = Equivalent multi-timer pills (cook_multi_timers_v1) and 'For this step' chips (cook_step_ingredients_v1) exist and are functionally complete, but styled for the light surface (muted/border chips, primary accents) rather than the dark frost treatment.

## Cluster: create-recipe-creator


### ⬜ CreatorProfile — H4 M5 L0
- proto: `6517-6561, CSS 1035-1043, 569-572`
- mobile: apps/mobile/app/creator/[id].tsx
- web: app/creator/[id]/page.tsx (+ src/app/components/creator/CreatorRecipeList.tsx, CreatorFollowButton.tsx)
- _Prototype CreatorProfile: pushed-bar with back + share(link) icons; creator-top row (64×64 round serif-initial avatar LEFT, name+handle·spec RIGHT); a creator-stats CARD (1px border, radius 16, shadow-card) with THREE cells Recipes / Followers / Following separated by hairlines; a two-up [Follow (primary) | Message (outline)] button row; bio; a 'Recipes' section-head; an rgrid (2-col card grid) of rcards; and a 'Share your own recipes / Go public' promo card. Both app platforms ship a real data-driven profile that diverges structurally: avatar-centered vertical header (96px avatar), inline TEXT stats showing only Followers · Recipes (no Following, no stat card), a SINGLE Follow button (no Message), a vertical recipe LIST (not a 2-col grid), and NO 'Go public' promo card._
  - **[HIGH]** Stats presentation: proto = creator-stats CARD: 3 tappable cells Recipes / Followers / Following with serif numerals (20px) and hairline dividers, bordered+shadow card · app = Inline text 'N followers · M recipes' — only two stats, no Following, not a card, not tappable
  - **[HIGH]** 'Following' count: proto = Shows Following count as third stat · app = Absent on both web and mobile
  - **[HIGH 🔒keep?]** Avatar layout: proto = 64×64 round avatar, LEFT-aligned in a creator-top row with name/handle beside it; tint background, serif initial · app = 96×96 avatar, CENTER-aligned above a vertically-stacked name/handle/bio · _Centered avatar header is a common, defensible profile pattern; but it is a clear structural divergence from the prototype's left-aligned row_
  - **[MED]** Message button: proto = Two-up [Follow | Message] button row · app = Single Follow/Following toggle only — no Message button
  - **[MED]** Follow button style: proto = btn--primary (Follow) / btn--outline (Following), flex:1 half-width pair · app = Pill (radius 999) accent-filled / outline toggle, min-width 140 centered
  - **[HIGH]** Recipe layout: proto = rgrid — 2-column grid of rcards (image, name, 'kcal · Nm' meta) · app = Vertical LIST of rows (56px thumb + title + meta + chevron), wrapped in a bordered card
  - **[MED]** Section header: proto = section-head with serif 18px 'Recipes' h2 · app = Uppercase 11px tracked overline 'RECIPES' label
  - **[HIGH]** 'Share your own recipes / Go public' promo card: proto = Card with sparkles plate, 'Share your own recipes' title, body, and a 'Go public' btn--outline--sm that opens GoPublic · app = Absent on both platforms
  - **[MED]** Top bar: proto = pushed-bar with back chevron + centered 'Creator' label + share(link) icon · app = Mobile: bare back chevron, no title, no share. Web: server page header (back/avatar)
  - **[MED]** Name type: proto = creator-name serif 22px/600 · app = Mobile displayName 22px weight 700 sans (not serif)
  - **[LOW 🔒keep?]** Verified tick: proto = Not shown (prototype uses handle·spec) · app = App adds a verified checkmark-circle — net-new capability · _Verified badge is a real trust signal the prototype omits; keep_

### 🔒 CreateRecipe — source picker (B12; keep live set)
- proto: `5334-5356 (pick), CSS 1237-1242, 1238`
- mobile: apps/mobile/components/recipe/CreateRecipeActionSheetGrid.tsx
- web: src/app/components/RecipeUpload.tsx
- _Prototype 'pick' mode is a 2×2 create-grid of four source tiles [Write it out / Paste a link / Snap a photo / Scan a label], each a card (radius 18, shadow-card, border) with a 44×44 frost-mist icon plate (Radius 13), serif 16 title and 12.5px secondary subtitle. Mobile's CreateRecipeActionSheetGrid is the closest equivalent but diverges in tile order, labels, icon-plate size/colour, and adds Pro-lock + coming-soon + a redundant 'Or write from scratch' row. Web has no 2×2 source-picker grid at all — RecipeUpload is a single long form._
  - **[MED]** Tile order: proto = Write it out / Paste a link / Snap a photo / Scan a label (manual-first) · app = mobile
  - **[MED]** Tile labels & subtitles: proto = 'Write it out'+'Type ingredients & steps', 'Snap a photo'+'A cookbook page or your plate', 'Scan a label'+'Pull macros from packaging' · app = 'Create manually', 'Scan a photo', 'From a PDF', 'Paste a link' — no per-tile subtitle/description line at all
  - **[MED]** Per-tile subtitle (create-src-s): proto = Each tile shows a 12.5px secondary description line · app = Mobile tiles render label only — no subtitle line
  - **[MED]** Icon plate: proto = 44×44, Radius 13, background accent-frost-mist, colour --primary for all · app = 36×36, Radius.lg, tinted per-tile icon colour (success green / textSecondary), 10% icon-colour wash
  - **[MED]** Title type: proto = create-src-t = serif 16/500 · app = Type.headline (primary tile) / Type.bodyMuted (others) — sans, not serif
  - **[LOW 🔒keep?]** Tile card radius/shadow: proto = border-radius 18, shadow-card, 1px border, min-height 132 · app = Radius.xl, conditional border+shadow via cardElevation, minHeight 120; primary tile uses tinted fill (accentPrimary14) + tinted border instead of card+shadow · _Primary 'link-first' tile is a deliberate Julienne import-wedge emphasis (ENG-898); arguably better for the viral hook but diverges from prototype's uniform tiles_
  - **[MED]** Extra affordances not in prototype: proto = Four equal tiles, nothing else · app = Adds Pro-lock badge on photo, 'Coming soon' chip on PDF, and a separate 'Or write from scratch' chevron row below a divider
  - **[HIGH]** Web equivalent: proto = 2×2 source grid before the form · app = web

### 🔒 CreateRecipe — manual form (B12; keep live fields)
- proto: `5357-5401, CSS 1227-1236, 2139, 2143, 2035, 2068`
- mobile: apps/mobile/components/recipe/CreateRecipeWizard.tsx (apps/mobile/app/recipe/create.tsx)
- web: src/app/components/RecipeUpload.tsx
- _Prototype manual create is a SINGLE scrolling screen: dashed 'Add a photo' tile (128h), borderless serif 26px title input, Serves stepper row (top/bottom hairlines), Ingredients section-head + flush divided card of create-ing rows (name input + 74px recessed qty + ghost x), Method section-head + create-step rows (serif accent-frost step number + bordered textarea), an Estimated-nutrition card (per-serving macros + lowconf-note), and a sticky two-button pushed-cta [Save draft (outline) / Save recipe (primary)]. Mobile reimplements this as a multi-step WIZARD (one decision per screen, progress bar) — structurally different. Web is a single long combined upload+form. Neither matches the prototype's single-screen manual layout 1:1._
  - **[HIGH 🔒keep?]** Overall structure: proto = One scrolling screen, all sections visible, sticky dual CTA · app = mobile · _Wizard is a deliberate confidence-first guided path from a 2026-04-28 creation audit (docs/audits/2026-04-28-recipe-creation-audit.md); arguably better for first-time creators but is a structural departure from the prototype's single-screen form_
  - **[MED]** Add-a-photo tile: proto = create-photo: full-width 128h, 1.5px dashed border-strong, bg-secondary, column-centered camera + 'Add a photo' 13px semibold · app = Need to verify exact dashed-tile token match in wizard/web; prototype-specific dashed treatment likely not replicated
  - **[MED]** Title input: proto = create-title: borderless, serif 26px/500, no box · app = Wizard uses a labelled/boxed title field (per audit gap notes ~L738 'large in-body screen title serifSemibold 28')
  - **[LOW]** Serves stepper: proto = create-row with top+bottom hairlines, serv-step (32×32 icon-btns, serif 17 number) · app = Wizard serves step exists but layout/token match unverified against create-row hairline pattern
  - **[HIGH 🔒keep?]** Ingredients card: proto = card--flush divide; each create-ing row = borderless name input + 74px recessed (bg-secondary, radius 8, right-aligned) qty + 32×32 ghost icon-btn remove · app = Wizard ingredient rows are search-backed (tap to search food DB) rather than free-text name+qty — different interaction model · _Search-backed ingredient entry powers real nutrition matching vs prototype's free-text; nutrition-accuracy win, intentional_
  - **[MED]** Method steps: proto = create-step: serif accent-frost step number (rd-step-n, 26px) + bordered textarea (radius 12) · app = Verify wizard step-number is serif accent-frost + bordered textarea
  - **[MED 🔒keep?]** Estimated-nutrition card: proto = card with overline 'Estimated nutrition · per serving', rd-macros row (serif kcal + coloured macro values), lowconf-note 'Estimated from your ingredients. Verify to lock exact macros.' · app = Wizard shows live computed nutrition (serif heroValue per ~L819) — richer/real, but overline + lowconf-note copy/treatment likely differs · _Real computed macros from matched ingredients beat the prototype's static estimate_
  - **[MED]** Sticky CTA: proto = pushed-cta: [Save draft (btn--outline) | Save recipe (btn--primary, check icon)] side by side, fade-gradient bg · app = Wizard uses step Next/Back nav + final save; web has its own save button — not the prototype's two-up Save draft / Save recipe

### 🔒 GoPublic
- proto: `6564-6591, CSS 1083-1084, 2323`
- mobile: N/A — mobile is import-only by design (recipe Go-Public is web-only, Grace-confirmed)
- web: src/app/components/GoPublicDialog.tsx
- _Web GoPublicDialog faithfully implements the prototype's three-attestation gate (own / nutrition-estimate / photo-rights) with verbatim copy and a Publish CTA disabled until all three are ticked. It renders as an AlertDialog (web modal) rather than the prototype's bottom Sheet, and uses a native checkbox + disabled-hint copy instead of the prototype's custom exp-check plate — both deliberate (a11y/keyboard, dark-mode visibility, noted in-file). Mobile intentionally has no Go-Public surface (import-only). Treated as keep_current: structure + copy conform; the container/checkbox swaps are documented, defensible deltas._
  - **[LOW 🔒keep?]** Container: proto = Bottom Sheet (height auto) with sticky pushed-cta · app = AlertDialog centered modal · _Web modal pattern; mobile (which would use a sheet) is intentionally absent_
  - **[LOW 🔒keep?]** Checkbox affordance: proto = Custom exp-check round plate (22px, fills primary, check icon) via ver-row · app = Native <input type=checkbox> accent-primary, in a divided rounded-xl muted card · _Native checkbox kept for built-in a11y + keyboard + dark-mode visibility (documented audit decision)_
  - **[LOW]** Title copy: proto = Sheet title 'Go public'; body 'Publishing "X" to your profile. A couple of quick confirmations first.' · app = Title 'Publish this recipe?'; same body sentence
  - **[MED]** Closing note + CTA: proto = info note 'You can switch any recipe back to private anytime…' + 'Publish recipe' (check icon, btn--lg block) · app = 'Tick all three to enable Publish' hint + Cancel/Publish footer; omits the 'switch back to private' reassurance note

### 🔒 ReportRecipe
- proto: `6054-6076, CSS 2323 (ver-row), 1062-1063 (radio-dot), 2068 (lowconf-note)`
- mobile: apps/mobile/components/recipe/ReportRecipeSheet.tsx
- web: src/app/components/suppr/report-recipe-dialog.tsx
- _Prototype ReportRecipe is a simple single-step radio list (4 reasons via ver-row + radio-dot, copyright shows a lowconf info note, single 'Submit report' CTA disabled until a reason is picked). Both app platforms ship a legally-reviewed, materially richer flow: 4 icon-tile reasons (Copyright/Incorrect/Inappropriate/Something else), copyright routes to the real DMCA takedown form, everything else goes through a 'describe' phase → durable POST to /api/recipe-report (OSA/DSA queue) → sent/error states with mailto fallback. This is a deliberate, Grace/legal-confirmed superset (ENG-1225 #19) — keep_current. Copy intentionally differs from the prototype because a recipe isn't copyrightable per se._
  - **[MED 🔒keep?]** Reason rows: proto = ver-row text rows with trailing radio-dot (single-select radio) · app = Bordered icon-tiles (32×32 icon plate + label + hint), tap-to-route (no radio-dot) · _Icon tiles + immediate routing per reason type; richer + legally routed_
  - **[LOW 🔒keep?]** Reason set & copy: proto = Copyright/my content, Wrong or unsafe info, Spam or inappropriate, Something else · app = Copyright — this is my content, Incorrect nutrition or instructions, Inappropriate or unsafe, Something else · _Legal-reviewed copy: a recipe's facts aren't copyrightable, so wording was revised (ENG-1225)_
  - **[HIGH 🔒keep?]** Flow depth: proto = Single screen → Submit · app = choose → describe (free-text) → sending → sent/error phases; copyright deep-links to /dmca · _Honest routing — DMCA to formal review, others durably logged to OSA/DSA queue with ack; never a silent mailto. Materially better than the prototype stub_
  - **[LOW 🔒keep?]** Copyright note: proto = lowconf-note 'We'll ask for proof of ownership by email…' · app = Header line 'Copyright claims go to our DMCA team…' + actual DMCA form navigation · _Real DMCA form vs prototype's inline note_
  - **[LOW]** Container height: proto = Sheet height 76% · app = Mobile sheet maxHeight 85%; web centered Dialog max-w-sm

## Cluster: plan-grocery


### 🔒 Plan household banner (B7)
- proto: `4736-4743`
- mobile: apps/mobile/components/plan/PlanHouseholdBannerV3.tsx
- web: src/app/components/plan/PlanHouseholdBannerV3.tsx
- _Avatars + 'Cooking for N · names' + chevron (or 'M× — match' flag) present on both. But structurally diverges: prototype .plan-hh2 is a borderless divider row (background:none; only a 1px border-bottom; no radius), while the app renders a filled background-secondary card with a full 1px border + radius 12. Non-owner avatar bg is var(--muted) grey in the prototype vs Accent.successSolid green in the app. Avatar size 30px proto vs 26px app. Label color var(--fg-secondary) medium-weight proto vs foreground semibold in app. CRITICAL: tapping the banner opens the Templates sheet on both platforms instead of the household sheet (prototype open('household'))._
  - **[HIGH]** Household banner tap target: proto = open('household') — household management sheet · app = onOpenHousehold → setTemplatesOpen(true) (web PlanV3Connected.tsx L99; mobile planner.tsx L2710) · _Banner advertises household editing but opens Templates; household sheet unreachable from here._
  - **[MED]** Banner container treatment: proto = .plan-hh2 background:none; border-bottom only; no radius — a quiet divider row · app = filled background-secondary card, full border, radius 12 (RN borderRadius Radius.xl / web rounded-xl)
  - **[MED]** Non-owner avatar colour + size: proto = .plan-hh-av background var(--muted) grey, 30px · app = Accent.successSolid green, 26px
  - **[LOW]** Label weight/colour: proto = .plan-hh2-t var(--fg-secondary), fw-medium · app = foreground, font-semibold

### ⬜ Plan meal card (plan-card) — H1 M2 L0
- proto: `4786-4798`
- mobile: apps/mobile/components/plan/PlanMealCardV3.tsx
- web: src/app/components/plan/PlanMealCardV3.tsx
- _48px thumb (photo or tinted box + utensil glyph) + slot label + lock badge + kcal + name + Batch chip / queued note. Card structure + tokens conformant. Two real diffs on both platforms: (1) recipe name uses sans 13px(web)/14px(mobile) semibold, but prototype .plan-card-name is font-serif 16px medium (L1351); (2) the prototype's per-card shuffle 'Meal options' button (.plan-card-opt, L4796) is MISSING — there's no on-card affordance to open the action menu; the card's onPress instead routes to swap/recipe. Also the cooked state (.is-cooked opacity .72 + strike-through name + green check badge, L1359-1362) is not implemented (cookedCount never set)._
  - **[HIGH]** Meal options (shuffle) button on card: proto = .plan-card-opt icon-btn--ghost shuffle, 32px, opens the meal action sheet (L4796) · app = absent — no per-card options button on web or mobile; whole-card onPress routes to a single action · _The prototype separates 'tap card = open recipe' from 'tap shuffle = options menu'; the app collapses both into one tap._
  - **[MED]** Recipe name typography: proto = .plan-card-name font-serif, 16px, fw-medium, 2-line clamp · app = sans, 13px(web)/14px(mobile) semibold, 1-line truncate (Type.label / text-[13px])
  - **[MED]** Cooked state: proto = .plan-card.is-cooked opacity .72 + strike-through name + .plan-cooked-badge green check over thumb · app = ✅ diary match (`planCookedMeals.ts`) — strike-through + check badge when logged on that day (2026-06-29)

### ✅ PlanHead / PlanHeaderV3 — B1 AdjustConstraints shipped
- proto: `4707-4721, 4882-4886`
- mobile: apps/mobile/components/plan/PlanHeaderV3.tsx
- web: src/app/components/plan/PlanHeaderV3.tsx
- _Structure conformant — date overline + serif 'Your plan' title + 3 quiet 38px white-card action buttons (sparkles/sliders/bookmark) + verdict dot/headline/subline, all driven by shared computePlanWeekVerdict. Title 30px serif on mobile (matches proto .planx-title) but web uses 28px (off the proto 30px). Action-button gap is 4px (web gap-1 / mobile gap:4) vs prototype .planx-acts gap 7px. Prototype .planx-act icon color is var(--fg-secondary); app renders text/foreground color. CRITICAL: the middle 'Adjust constraints' (sliders) button is mis-wired on BOTH platforms — it opens the Templates sheet, not an adjust-constraints surface (prototype open('adjustconstraints'))._
  - **[HIGH]** Adjust-constraints (sliders) action button target: proto = open('adjustconstraints') — a dedicated constraints editor sheet · app = onAdjust → setTemplatesOpen(true): opens the Plan Templates sheet instead (web PlanV3Connected.tsx L97-98 wires onTemplates=onAdjust; mobile planner.tsx L2708-2709 wires both to setTemplatesOpen) · _Two distinct buttons (sliders + bookmark) both open templates; the prototype's adjust-constraints flow is unreachable. No keep-current rationale documented._
  - **[LOW]** Header title font size: proto = .planx-title font-size 30px serif medium · app = mobile Type.screenTitle (matches ~30) but web h2 is text-[28px]
  - **[LOW]** Action-button row gap + icon color: proto = .planx-acts gap 7px; .planx-act color var(--fg-secondary) · app = gap 4px (web gap-1 / mobile gap:4); icon color foreground/text not fg-secondary

### ✅ Plan tools / 'This week' section — BatchCook ENG-1255 + shopping (§D16 smart data 🔒)
- proto: `4802-4827`
- mobile: apps/mobile/components/plan/PlanToolsV3.tsx
- web: src/app/components/plan/PlanToolsV3.tsx
- _The prototype's 'This week' section is a 3-part block: an overline header 'This week', a flush divided card of smart-row suggestions (kcal-short nudge + fibre swap), then a 2-up .plan-toolrow of [Batch cook · '2 batches → 6 meals'] and [Shopping list · 'N items · for N']. The app's PlanToolsV3 ships ONLY the Shopping list row, full-width, with no 'This week' overline and no Batch tool. The smart suggestions DO exist but as a separate component (PlanSmartSuggestionsCard) rendered elsewhere in the planner, not inside this section. Tool icon box is rounded-full in the app vs prototype .plan-tool-ic radius 11._
  - **[HIGH]** Section structure: proto = 'This week' overline + smart-suggestions card + 2-up toolrow (Batch + Shopping) · app = single full-width Shopping list row only; no overline; smart suggestions live in a separate card · _Batch-cook is a tracked deferral (ENG-1225 — no batch sheet yet), but the missing overline + grid layout + co-located smart card are structural drift._
  - **[LOW]** Tool icon box radius: proto = .plan-tool-ic border-radius 11 · app = rounded-full (web) / Radius.full (mobile) 44px circle
  - **[LOW 🔒keep?]** Shopping subtitle copy: proto = '{17 + (servings-1)*6} items · for {servings}' · app = '{count} items · for {N}' or 'Build your basket · for N' when 0 — close; count source differs (real list count, acceptable) · _App uses the real shopping count — better than the prototype's hardcoded formula._

### ✅ ResetPlan sheet (ENG-1261 / B28, `reset_plan_confirm_v1`, 2026-06-29)
- proto: `6441-6467`
- mobile: `apps/mobile/components/plan/ResetPlanSheet.tsx` + `planner.tsx` (`requestLibraryGenerate` when plan has real meals)
- web: `src/app/components/plan/ResetPlanSheet.tsx` + `MealPlanner.tsx` (`requestRegenerate` when plan has real meals)
- shared: `src/lib/planning/resetPlanSheet.ts`
  - **[HIGH]** Reset-plan sheet: proto = keep/clear radio sheet + destructive note + Cancel/Reset CTA (L6441-6467) · app = absent on web and mobile; only a plain regenerate exists · _No keep-logged-vs-clear-and-rebuild choice anywhere; regenerate is destructive-by-default with no opt-out UI._

### ⬜ Plan day-detail band (plan-day) — H0 M1 L0
- proto: `4770-4783`
- mobile: apps/mobile/components/plan/PlanDayDetailBandV3.tsx
- web: src/app/components/plan/PlanDayDetailBandV3.tsx
- _Day name (serif 18) + bold day-total / target kcal, a 7px progress bar (sage under / amber over), gap subline, and the P/C/F macro pill row — all driven by shared computePlanDayDetail. Conformant on layout/tokens. Only gap: the prototype subline appends a cooked tally ('· N cooked') and the card name strikes through cooked meals; the app passes cookedCount={0} hardcoded (PlanV3Surface L170/171 web+mobile), so the cooked dimension never renders._
  - **[MED]** Cooked-meal tally in subline: proto = subline appends ' · {cookedCount} cooked' when meals marked cooked (L4778) · app = ✅ `countPlanDayCookedMeals` wired in PlanV3Surface (2026-06-29)

### ⬜ Plan empty / generating states — H0 M1 L0
- proto: `4669-4693`
- mobile: apps/mobile/components/PlanEmptyState.tsx
- web: src/app/components/MealPlanner.tsx
- _Prototype empty state offers a vertical 3-button stack: primary 'Generate my week' (sparkles), tertiary 'Import a plan' (link), tertiary 'Build it myself', under 'No plan for this week yet' + body copy. Mobile PlanEmptyState surfaces an import affordance ('Or import a plan you already have') but the canonical 3-button stack + exact copy ('No plan for this week yet', 'Generate my week', 'Build it myself') was not found verbatim, and the generating state (import-spin sparkles + 'Building your week…' + 3 skeleton blocks, L4687-4692) needs confirmation on both platforms._
  - **[MED]** Empty-state action stack + copy: proto = Generate my week / Import a plan / Build it myself + 'No plan for this week yet' · app = mobile shows a single import nudge; full 3-button stack + exact copy not confirmed

### ✅ Plan week strip (pweek)
- proto: `4724-4734`
- mobile: apps/mobile/components/plan/PlanWeekStripV3.tsx
- web: src/app/components/plan/PlanWeekStripV3.tsx
- _Verified conformant: 7-cell flex row, each cell = uppercase day letter (11px) + tabular date numeral + 3-state status ring (full=sage / part=amber / empty=hollow outline). Selected day fills navPrimary (plum) with white letter/date and white ring; today (unselected) tints letter navPrimary. Status derived from shared computePlanDayStatus. Radius xl (12), cell padding 10. Matches prototype .pweek/.pweek-d/.pweek-ring including the is-sel override (L1884-1889)._

### ✅ Plan meal-filter chips
- proto: `4745-4749`
- mobile: apps/mobile/components/plan/PlanMealFilterChipsV3.tsx
- web: src/app/components/plan/PlanMealFilterChipsV3.tsx
- _All / Breakfast / Lunch / Dinner / Snack chip row with 'All' shown as 'All meals', aria-pressed selection, horizontal scroll — matches prototype .plan-mealfilter. Selecting a specific slot switches the body to the across-week view (PlanMealSectionV3) as the prototype does._

### ✅ Plan empty slot (plan-empty)
- proto: `4786-4787, 4756`
- mobile: apps/mobile/components/plan/PlanEmptySlotV3.tsx
- web: src/app/components/plan/PlanEmptySlotV3.tsx
- _Dashed-border row with slot overline + '+ Add {slot}' primary-tinted action — matches prototype .plan-empty / .plan-empty-add. Tapping opens the add/log flow._

### 🔒 Meal action sheet (menu / portion / swap / move)
- proto: `4829-4877`
- mobile: apps/mobile/components/MoveMealSheet.tsx, SwapMealSheet.tsx, apps/mobile/components/plan/usePlanV3MealActions.ts
- web: src/app/components/suppr/plan-move-meal-dialog.tsx, plan-portion-dialog.tsx, plan-templates-dialog.tsx
- _The prototype unifies all meal actions into ONE sheet with a menu → submode pattern (menu rows: Log as planned / View recipe / Change portion / Swap / Move to another day / Lock / Mark cooked; then portion stepper, swap list, move-days grid, plus a destructive 'Remove from plan'). The app splits these into separate dialogs/sheets (MoveMealSheet, SwapMealSheet, portion dialog) reached via usePlanV3MealActions, and is missing the unified menu hub, the Lock toggle, the Mark-cooked toggle, and the 'Remove from plan' destructive action as a single coherent menu. Functionally most actions exist but the architecture and several rows (lock, cooked, remove) diverge._
  - **[HIGH 🔒keep?]** Action entry + unified menu: proto = shuffle button → one sheet, menu hub with 7 rows incl Lock + Mark cooked + Remove · app = separate per-action dialogs; Lock/Cooked/Remove rows not surfaced as a menu · _App's split-dialog model is an arguable product choice, but Lock + Mark-cooked + Remove are genuinely missing actions, not just a layout difference — flag for Grace's call._

### ✅ PlanImport sheet
- proto: `5540-5582`
- mobile: apps/mobile (PlanImport flow; A10 callout tokenized — task #10 done)
- web: src/app/components/PlanImport.tsx, src/app/components/plan-import/PlanImportReview.tsx
- _Import-a-plan flow with input → loading → review phases, seg-tabs (cookbook / MFP CSV / week's links), URL search field, recipe-pick checklist with pi-thumb + gr-check, and the 'Read the collection' / 'Add N recipes' pushed CTA. Task #10 (A10 PlanImport callout tokenize + radius snap) is marked complete; the MFP CSV import (tasks #1-6) is a richer real backend than the prototype's static list. Treated as conformant + already-conformed per the cluster note._

### 🔒 Grocery / Shopping list
- proto: `4888-4920`
- mobile: apps/mobile/app/shopping.tsx
- web: src/app/components/ShoppingList.tsx
- _Prototype Grocery is a simple sheet: gr-tools (share + clear), a gr-progress pill (bg-secondary, serif count in primary-active), category overline headers with per-cat counts, gr-item checkbox rows (gr-check 24px rounded-7 box, item name + recipe-source subline + qty), and a 'Send to Tesco · Ocado' primary CTA. The app ships a substantially richer real implementation — household-scoped lists, pantry-staples, aisle ordering, share/clear, progress bar — and on web a grid-cols-3 category-card layout rather than a flat list. Task #11 (overline headers + gr-check) is complete. Deliberately keep-current for the extra capability; the prototype's 'Send to Tesco · Ocado' grocery-partner CTA is net-new-absent (a prototype-only aspiration, no app equivalent)._
  - **[MED 🔒keep?]** Send-to-grocer CTA: proto = primary 'Send to Tesco · Ocado' button (cart icon) at list foot · app = absent on both platforms — no grocery-partner handoff · _Net-new prototype aspiration with no backing integration; correctly omitted until a real partner integration exists. Track if Grace wants it._
  - **[LOW 🔒keep?]** Web category layout: proto = flat single-column list of category sections · app = grid-cols-3 category cards (web) · _Web density choice; mobile stays single-column._

### 🔒 PlanTemplates sheet
- proto: `6470-6497`
- mobile: apps/mobile/components/PlanTemplatesSheet.tsx
- web: src/app/components/suppr/plan-templates-dialog.tsx
- _Prototype PlanTemplates: insight copy + flush divided card of template set-rows (bookmark/plan icon + name + meta + Apply outline-sm button + ghost trash), empty state, and a 'Save this week as template' primary CTA. The app ships a real, richer twin on both platforms — 'My templates' list with Apply + async Delete (with a delete-confirm target) + Save — matching the prototype's structure and exceeding it with real persistence. Conformant in shape; the per-row meta strings differ because the app uses real template data. Keep-current._
  - **[LOW 🔒keep?]** Template row meta: proto = hardcoded meta e.g. '21 meals · avg 142g protein' · app = real template meta from persisted data · _App uses real data — correct divergence._

## Cluster: today-core


### ✅ Calorie hero — de-card + Goal/Eaten/Bonus IA (§D2 ring toggle 🔒)
- proto: `3570-3601 (Today hero block); RingHero 2980-3275 (canonical 'arc'/'segments'); CSS 1426-1487, 2391-2400`
- mobile: apps/mobile/components/today/TodayHero.tsx; apps/mobile/components/today/TodayHeroRing.tsx; apps/mobile/components/today/TodayHeroRingGraphic.tsx; apps/mobile/components/charts/CalorieRingDial.tsx
- web: src/app/components/suppr/today-hero-ring.tsx; src/app/components/suppr/today-hero-stats.tsx; src/app/components/suppr/calorie-ring-dial.tsx
- _The biggest structural divergence in the cluster. (1) The prototype hero is DE-CARDED: a bare .ring-hero block (text-align:center; padding:2px 0 8px) sitting flush on the page — no fill, no border, no shadow. Both app surfaces wrap the entire hero (chip + ring + stats + coach) in a SupprCard with soft lift + hairline + #F6F5F2 fill. The web code itself flags this: 'the de-carded v3 hero layout lands in P2' (today-hero-ring.tsx L164-165) — a known, not-yet-done gap. (2) The prototype places a centered .ring-status line ('●  Under budget'/'Over budget') BELOW the ring; the app places a status CHIP (with lucide glyph) ABOVE the ring, left-aligned. (3) The prototype shows a .ring-switch-hint Remaining/Consumed toggle below the ring — the app retired this toggle on both surfaces (documented decision 2026-06-10). (4) The hero numeral: prototype .ring-big is 56px serif weight-medium(500); the v3 CalorieRingDial uses Type.ringValue = 48px weight-400 (a ringValueLg 56px token exists and is unused here). The GOAL/EATEN/BONUS stat trio order is the Grace-confirmed keep-current exception._
  - **[HIGH]** Hero card chrome (de-carded vs carded): proto = .ring-hero — no card: text-align:center; padding:2px 0 8px; bare on page (CSS 1429) · app = Entire hero wrapped in <SupprCard lift='soft'/elevation='card', radius lg, hairline, #F6F5F2 fill> on both surfaces (TodayHeroRing L281-291; today-hero-ring.tsx L170-182) · _Explicitly acknowledged as deferred to 'P2' in the web component comment (L164-165). The de-carded layout is the canonical v3 hero._
  - **[MED]** Status indicator placement: proto = .ring-status — centered line BELOW the ring: dot + 'Under budget'/'Over budget' (12.5px semibold, sage/warning) (L3575, CSS 1436-1439) · app = StatusChip with lucide CircleCheck/CircleAlert glyph ABOVE the ring, left-aligned in a justify-between row (TodayHeroRing L294-309; today-hero-ring.tsx L186-188) · _Different element (pill+glyph chip vs centered dot+text line) and different position (above vs below the ring). Symmetric on web + mobile._
  - **[MED 🔒keep?]** Remaining/Consumed switch hint: proto = .ring-switch-hint pill below ring ('↻ Remaining · tap to switch') + ring-tap toggles consumed⇄remaining (L3576, CSS 1443-1445) · app = Retired on both surfaces (props marked @deprecated 2026-06-10) · _Deliberately retired — it duplicated the Eaten stat below the ring (web ring parity 2026-06-10, documented in both components). Defensible keep-current._
  - **[MED]** Hero numeral size + weight: proto = .ring-big 56px serif, font-weight var(--fw-medium) ~500, letter-spacing -.02em (CSS 1433) · app = CalorieRingDial centre uses Type.ringValue = 48px serif weight 400 (theme.ts L708). ringValueLg=56px exists but is unused on this path · _8px smaller and a weight lighter than the canonical .ring-big. Switching the dial to ringValueLg (56/medium) would close it. Affects both surfaces (same CalorieRingDial / calorie-ring-dial)._
  - **[LOW 🔒keep?]** cal-row stat value type: proto = .cal-cell b — 18px sans-semibold value, 10.5px uppercase tertiary label, vertical 1px dividers (CSS 1448-1452) · app = Serif stat values — web RingStatCell 22px var(--font-headline); mobile Stat uses Type.statValue (serif 22) / Type.title 19. Labels are 11px secondary uppercase (statLabel) · _App uses a serif 22px stat value (richer, on the type ramp; ENG-119 lint-safe) vs the prototype's 18px sans. Label moved to secondary ink for AA (sub-AA tertiary in proto). Deliberate AA + type-ramp choice — keep._
  - **[LOW 🔒keep?]** GOAL / EATEN / BONUS stat order: proto = Eaten / Goal / Activity (L3583-3589) · app = Goal / Eaten / Bonus on both surfaces · _Grace-confirmed global keep-current exception. Not a gap._
  - **[LOW]** Coach guide-line: proto = .guide-line chip ('<icon> <bold head> · sub', frost-mist fill, success/warning glyph) below stats (L3592-3600, CSS 1482-1487) · app = coachLine slot rendered below stats via TodayDeficitInsight ('Room for {meal}…'), flag today_coach_in_hero_v1 (TodayScreen L5371-5380; TodayHeroRing L391) · _Present and positioned correctly on both surfaces. Verify the chip styling (frost-mist fill + glyph) matches .guide-line; content source differs (TodayDeficitInsight) but the slot conforms._

### 🔒 Today header — no day-chip / calendar in header (§D3)
- proto: `3537-3550 (t-head + t-greet); CSS 1402-1409`
- mobile: apps/mobile/app/(tabs)/_today/TodayScreen.tsx (L5105-5171); apps/mobile/components/today/TodayBrandBar.tsx; apps/mobile/components/today/TodayDateHeader.tsx
- web: src/app/components/suppr/today-brand-bar.tsx; src/app/components/NutritionTracker.tsx
- _The serif date hero (.t-greet) is a strong match on both surfaces: 'TODAY' eyebrow + hairline rule, Newsreader 36px day name, 13px tertiary date subline. The top header row (.t-head) is NOT a match: the prototype renders the wordmark plus THREE icon buttons — calendar, notification bell (with unread dot), and the avatar. Both app surfaces render only the wordmark + avatar (mobile routes the avatar to Settings; web brand bar is wordmark-only). The calendar and bell entry points are missing from the header._
  - **[HIGH]** Header icon buttons (calendar + notification bell): proto = .t-head has icon-btn Calendar, icon-btn bell with .t-bell-dot unread indicator, then avatar (L3540-3542) · app = Mobile renders only SloeHeaderWordmark + a single avatar Pressable→Settings (TodayScreen L5113-5131); web today-brand-bar is wordmark-only with no icon row · _Calendar + bell are real navigation affordances in the prototype header. Both are reachable elsewhere in-app (calendar via the week-strip icon; notifications via tab/route) so this is partly an IA choice, but the header structure diverges from the prototype on both surfaces._
  - **[LOW 🔒keep?]** Avatar fill + initial: proto = .avatar shows 'G' initial · app = GradientAvatar size 36, damson fill, white initial (mobile); parity on web · _App uses the live user initial + brand damson fill — richer/honest vs the static mock 'G'. Keep._
  - **[LOW 🔒keep?]** 'Day N' chip in greeting eyebrow: proto = .t-daychip 'Day 12' sits in the eyebrow next to 'Today' (L3547) · app = Omitted on both surfaces · _Deliberately omitted — mock text with no honest data source (Grace's call, documented in TodayScreen L5134-5138). Not a gap._

### ✅ Quick add row — recents chips (`today_quickadd_recents_v3`)
- proto: `3620-3628 (Quick add header + quickrow); CSS 1658-1662`
- mobile: apps/mobile/components/today/TodayQuickLogStrip.tsx; QuickAddPanel (inside TodayMealsSection)
- web: src/app/components/suppr/today-quick-log-strip.tsx
- _Concept divergence on both surfaces. The prototype 'Quick add' section is a horizontally-scrolling row of RECENT-FOOD chips — each chip is 'icon + food name + kcal' (e.g. clock 'Greek yogurt' 180), one tap logs it, with a 'Search' link in the section header. The app's TodayQuickLogStrip is instead a row of FOUR fixed ACTION chips (Search / Voice / Snap / Scan) — entry-method launchers, not recent foods. The recent-food quick-log lives in QuickAddPanel inside the meals section. So the prototype's recents-as-quick-add pattern has no direct equivalent at this position._
  - **[HIGH]** Quick add chip content (recent foods vs action launchers): proto = .quickrow maps RECENTS → .qchip 'icon + truncated name + bold kcal'; section header has a 'Search' link (L3621-3628) · app = TodayQuickLogStrip = Search / Voice / Snap / Scan action chips (mobile L77+; web L9). Recent-food one-tap logging is in QuickAddPanel within the meals section · _Different concept at the same screen position — the prototype's named-recent-food chips vs the app's entry-method launchers. Recents ARE reachable (QuickAddPanel) but not as the prototype's top-level horizontal quick-add row. Symmetric web + mobile._

### ⬜ Next-meal recipe hero (recipe-hero / Eat next) — H0 M2 L0
- proto: `3603-3615 (recipe-hero); CSS 1544-1554`
- mobile: apps/mobile/components/today/NorthStarBlock.tsx; NorthStarBlockHost.tsx; TodayScreen.tsx L5534-5553
- web: src/app/components/NutritionTracker.tsx (NorthStar host)
- _The prototype's .recipe-hero (a 176px photo card with 'Eat next · {Slot}' kick badge, eyebrow, serif title, kcal/protein meta) sits directly BELOW the hero ring (marginTop:24). The app implements the same concept as NorthStarBlock ('What to eat next'), but it is positioned BELOW the meals section (above Today's Meals per the comment, but after the macro section / context block), not immediately under the hero. The block is permanent (ENG-935) and photo-led, so the concept conforms; placement and the exact .rh-photo/.rh-kick/.rh-eyebrow/.rh-title/.rh-meta layout need a forensic match._
  - **[MED]** Recipe-hero position: proto = Immediately below the ring hero (marginTop:24), before the macro section (L3604-3615) · app = NorthStarBlockHost renders above the Meals section but AFTER the macro tiles + context block (TodayScreen L5534) · _Prototype order: ring → recipe-hero → macros. App order: ring → context block → macros → north-star. The 'eat next' module is one section lower than the prototype._
  - **[MED]** Card layout (.rh-photo / .rh-kick / .rh-body): proto = 176px cover photo with absolute 'Eat next · {Slot}' kick pill (top-left, blurred dark bg), then rh-eyebrow (uppercase primary-active) + rh-title (serif 21px) + rh-meta (12.5px tertiary) · app = NorthStarBlock uses RecipeHeroFallback + 'What to eat next' overline + CTA — needs side-by-side verification that the photo/kick/eyebrow/title/meta hierarchy matches · _Concept matches (photo-led permanent suggestion); the precise badge/eyebrow/serif-title/meta structure should be diffed against .recipe-hero on both surfaces._

### 🔒 Net energy card (NetEnergy diverging bar)
- proto: `3689-3692 (render); NetEnergy 3411-3453; CSS 1725-1741`
- mobile: apps/mobile/components/today/TodayActivityBonusCard.tsx (L336-616)
- web: src/app/components/suppr/today-activity-bonus-card.tsx
- _The prototype's standalone NetEnergy card is reproduced inside TodayActivityBonusCard on both surfaces with high fidelity: 'Net energy' overline + state pill (Maintenance/deficit/surplus), serif net headline + 'kcal {word}' unit, plain-language sub-line, the deficit→maintenance→surplus gradient track (sage→frost→amber) with a positioned knob, and the Burned/Eaten/Maintenance stat trio. The app also adds a maintenance-TDEE info popover (more capability). Two token nuances below. Consolidating NetEnergy + the burn-so-far card + WeeklySummary into one TodayActivityBonusCard is a defensible structural choice (one card vs the prototype's three sibling cards)._
  - **[MED]** Net headline numeral size: proto = .ne-big 40px serif (CSS 1729) · app = Net headline fontSize 52 serif (TodayActivityBonusCard L388-389) · _12px larger than the canonical .ne-big. Re-check against the prototype's 40px; 52 may over-weight the net figure relative to the hero ring._
  - **[LOW 🔒keep?]** Card consolidation (3 cards → 1): proto = Three separate sibling cards: NetEnergy, the burn-so-far .t-burncard, and WeeklySummary · app = All three folded into TodayActivityBonusCard (net hero + burn breakdown + weekly rollup), plus a maintenance info popover · _More capability (info popover, adaptive maintenance) and one coherent card. Defensible keep-current vs the prototype's three-card split._

### ✅ Macros section (MacroSection — tiles default)
- proto: `3281-3364 (MacroSection: tiles/bars/rings/split/donut); CSS 1513-1541`
- mobile: apps/mobile/components/today/TodayMacroSection.tsx; TodayDashboardMacroTiles.tsx; TodayDashboardMacroBars.tsx; TodayDashboardMacroRings.tsx
- web: src/app/components/suppr/today-macro-section.tsx; today-dashboard-macro-tiles.tsx; -bars.tsx; -rings.tsx
- _Strong match. The app ships the same switchable Tiles/Bars/Rings layouts (driven by macroDisplayStyle), mirroring the prototype's MacroSection variants. The Tiles variant is a faithful .mtiles implementation: 2-col hairline grid (top border on grid, bottom border per cell, right border on left column), asymmetric cell padding, lucide glyph + serif value + '/target g' + macro-coloured progress bar. Macro glyphs (protein=dumbbell, carbs=wheat, fat=droplet, fibre=leaf/sprout) and net-carbs lens match. App default is Bars (proto default tweak is Rings) but both expose the same set — a user preference, not a structural diff._
  - **[LOW]** Macro tile progress-bar track tone: proto = .mtile-track background var(--muted), 3px height, fill var(--macro-*) (CSS 1540-1541) · app = barTrackColor = Colors.light.border (#E8E2EC line) light / cardBorder dark; fill macroColorFor(macro) (TodayDashboardMacroTiles L113, 224) · _Minor track-tone nuance (line vs muted); both read as a recessed grey track. Within tolerance._

### ✅ Week date strip (day-strip)
- proto: `3553-3568; CSS 1411-1425`
- mobile: apps/mobile/components/today/TodayDateHeader.tsx (stripOnly mode); apps/mobile/components/charts/DayStrip
- web: src/app/components/suppr/today-date-header.tsx
- _The week strip is rendered in stripOnly mode on both surfaces: prev/next nav, 7 day cells with DOW + date + logged-dot, selected-state plum fill, today highlight, future days disabled. Structure and token treatment match the prototype .day-strip / .day-cell rules. Verified the chevron nav, selected-cell plum fill, and the success-coloured logged-day dot all map._

### ✅ Today's meals by slot (mslot / meal-row / meal-macros)
- proto: `3630-3659 (meals by slot); CSS 1665-1688, 1997-2001`
- mobile: apps/mobile/components/today/TodayMealsSection.tsx; MealRowSwipeable.tsx
- web: src/app/components/suppr/today-meals-section.tsx
- _The meals stack matches: a single flush divided card, per-slot header (overline + kcal total + chevron), per-slot meal-macros summary line (kcal + P/C/F/Fi coloured segments), meal rows (icon + name + source chip + Conf badge + kcal), and an 'Add {slot}' empty-row affordance. Section header 'Today's meals' + day kcal total present. Structure conforms on both surfaces._

### ✅ Steps & activity card (act-title + burn-stepbar)
- proto: `3661-3687 (Steps & activity); CSS 574-577, 915-916`
- mobile: apps/mobile/components/today/TodayActivityCard.tsx; TodayScreen L5744-5781
- web: src/app/components/suppr/today-steps-card.tsx; today-apple-health-card.tsx
- _Matches: 'Activity & energy' section header, a Steps & activity card with act-title (overline + info icon + 'Today' right), a Steps row (glyph + label + 'N / goal') over a burn-stepbar progress bar, a hairline divider, and an Active-energy row (flame glyph + kcal). When Health is disconnected the prototype shows a 'Connect Apple Health' outline button; the app shows a Connect-health affordance. Structure conforms on both surfaces._

### ✅ 7-day rolling summary (WeeklySummary / wk-row)
- proto: `3710-3711 (render); WeeklySummary 3456-3466; CSS 1745-1749`
- mobile: apps/mobile/components/today/TodayActivityBonusCard.tsx (L618-689)
- web: src/app/components/suppr/today-activity-bonus-card.tsx (weekly block)
- _Matches the .wk-row structure: 'Avg daily deficit', 'Weekly deficit', 'Projected weekly loss' rows (label-left, serif success-coloured value-right), plus the wk-foot hint line clarifying the denominator. Rendered as the weekly block of TodayActivityBonusCard. The app correctly flips deficit→surplus / loss→gain by sign (more honest than the prototype's loss-only mock)._

### ✅ Burn-so-far card (t-burncard)
- proto: `3694-3708 (t-burncard); CSS 929-933`
- mobile: apps/mobile/components/today/TodayActivityBonusCard.tsx (L540-615)
- web: src/app/components/suppr/today-activity-bonus-card.tsx (burn block)
- _Matches: a tappable burn card with flame glyph, serif 'N' burned-so-far headline + 'kcal burned so far' + info icon, an 'Active N    Resting N' sub-line, and a chevron leading to the burn-detail breakdown. Conforms on both surfaces._

### ✅ Hydration card (mini-metric / water-dots)
- proto: `3713-3719 (Hydration); CSS 1682-1688`
- mobile: apps/mobile/components/HydrationStimulantsCard.tsx; TodayScreen L5835-5883
- web: src/app/components/suppr/hydration-stimulants-card.tsx
- _The prototype's minimal Water mini-metric (overline + droplet glyph, 'N / 8' value, 8 water-dots) is superseded by the app's richer HydrationStimulantsCard (water + caffeine + alcohol, presets, measurement system). Section header reads 'Hydration & stimulants' (app) vs 'Hydration' (proto). The water sub-block conforms; the added stimulant rows are extra capability gated by Settings opt-in. Treating as conformant since the hydration concept + water row are present and the extra rows are a documented superset._
  - **[LOW 🔒keep?]** Section scope + header copy: proto = 'Hydration' — water only (mini-metric + 8 dots) · app = 'Hydration & stimulants' — water + opt-in caffeine + alcohol · _App is a documented superset (stimulant rows hide unless opted-in). Header copy differs accordingly. Keep._

### ✅ Today depth (All nutrients / Complete today set-rows)
- proto: `3721-3725 (set-rows: All nutrients + Complete today)`
- mobile: apps/mobile/components/today/TodayNutrientsModal.tsx; TodayCompleteDayButton.tsx; TodayDashboardMacroTiles (All nutrients link)
- web: src/app/components/NutritionTracker.tsx; today-complete-day-dialog.tsx
- _Both affordances exist. 'All nutrients' (35 vitamins & minerals · %DV) opens the full-nutrient panel — in the app it's surfaced as the right-aligned 'All nutrients ›' chip in the macro-tiles header (TodayDashboardMacroTiles L163-183) rather than a set-row in a depth card. 'Complete today' is a TodayCompleteDayButton that renders when viewing today with logged meals. Both lead to the same destinations as the prototype set-rows; placement of the nutrients link differs but the affordance conforms._
  - **[LOW 🔒keep?]** All-nutrients affordance placement: proto = .set-row inside a flush divided depth card near the foot of Today (L3723) · app = Right-aligned 'All nutrients ›' chip in the macro-tiles section header (TodayDashboardMacroTiles L163-183) · _Moved up to the macro section header so it reads as an action affordance rather than a floating orphaned link (Phase 4 Top-5 #2C). Documented. Keep._

## Cluster: progress-trends


### 🔒 Progress — D/W/M/6M/Y periods + Weight in-tab (§D8–9; B2 inline adaptive)
- proto: `4935-5099 (Progress); CSS prog-* L2249-2286, recap L2218-2222, trendline L2243`
- mobile: apps/mobile/app/(tabs)/progress.tsx (+ apps/mobile/components/progress/ProgressEnergyTriad.tsx, ProgressEnergyEquation.tsx, ProgressAverageAdherence.tsx, ProgressOnTargetRibbon.tsx, apps/mobile/components/today/ProgressHeadline.tsx, ProgressStoryGate.tsx)
- web: src/app/components/ProgressDashboard.tsx (+ src/app/components/suppr/progress-energy-triad.tsx, progress-average-adherence.tsx, progress-ontarget-ribbon.tsx, progress-headline.tsx, progress-period-control.tsx)
- _App and web are in tight mutual parity (same section ordering, shared period model, mirrored sub-components). Versus the prototype the app deliberately diverges in section ORDER (a ratified ENG-1247 / Grace-confirmed change — Adherence last) and is structurally MORE capable (real adaptive maintenance card, journey/projection, Apple Health, week digest). The genuine prototype-conformance gaps are: (1) the prototype's '1 · THIS WEEK' card is a narrative RECAP card with a 'Full recap' link + two explicit prog-chips ('High confidence', 'N/7 days on target'); the app renders ProgressHeadline which carries the ✦ THIS WEEK eyebrow + serif headline + a ConfidenceChip but has NO 'Full recap' link and NO 'N/7 days on target' chip. (2) The prototype has a standalone 'Projected weight' card (serif plum hero 'NN.N kg · in ~8 weeks'); the app folds projection into the Journey card instead and gates it behind progress_trajectory_box. (3) The prototype's 'Body composition · trends' Pro/locked card (locked veil + 'Unlock with Pro') has no app equivalent. (4) Energy-balance equation matches the prototype only when sloe_v3_energy_equation is ON (legacy triad otherwise) — with all flags forced ON for verification it conforms._
  - **[HIGH]** 'This week' recap card — 'Full recap' link: proto = L4966: <span overline>✦ This week</span> + <button class="link" onClick open('recap')>Full recap</button> on the same spread row · app = ProgressHeadline renders the ✦ THIS WEEK eyebrow + serif headline + body but has NO 'Full recap' link to open the weekly recap. The recap is reachable only via the WEEK DIGEST card lower down. · _The prototype's headline insight links directly to the shareable Full recap; app drops that affordance from the THIS WEEK card._
  - **[MED]** 'This week' card — prog-chips ('High confidence' + 'N/7 days on target'): proto = L4972: <div class="prog-chips"> sage is-good chip 'High confidence' + neutral chip 'N/7 days on target'. CSS .prog-chip pill: bg-grouped, 12px fw-semibold, 6px 11px pad, radius 9999; .is-good = success-soft/success. · app = ProgressHeadline shows only a single ConfidenceChip; there is no '/7 days on target' pill. Two distinct prototype chips collapsed to one. · _Missing the on-target-days chip; confidence chip present but as one chip not the two-chip prog-chips row._
  - **[LOW 🔒keep?]** Section ORDER (Adherence placement, energy-balance form): proto = Order: THIS WEEK recap → WEIGHT → ENERGY BALANCE equation → DAILY CALORIES → ADHERENCE → APPLE HEALTH → PROJECTED WEIGHT → BODY COMPOSITION. · app = Order: period control → THIS WEEK → WEIGHT → ENERGY triad/equation → DAILY CALORIES → AVERAGE ADHERENCE → ON-TARGET RIBBON → APPLE HEALTH → MAINTENANCE → JOURNEY → WEEK DIGEST. · _ENG-1247 (Grace 2026-06-26, commit de426021): Adherence is deliberately placed LAST, after Daily Calories. Ratified divergence — do not flag the reorder itself._
  - **[MED 🔒keep?]** Standalone 'Projected weight' card: proto = L5077-5084: card with plum overline+dot 'Projected weight', .prog-proj serif plum hero {projKg}<small>kg</small> + 'in ~8 weeks', insight body '7,700 kcal ≈ 1 kg. An estimate, not a promise.' · app = No standalone card. Projection is merged into the JOURNEY card body ('On that trend you'd reach NN kg in ~N weeks') and a separate TrajectoryCard gated behind progress_trajectory_box flag. · _App consolidates projection into Journey (more context: progress bar + days-to-goal). Arguably better but NOT the prototype's discrete plum-hero card; flag if strict conformance is wanted._
  - **[MED]** 'Body composition · trends' Pro/locked card: proto = L5086-5091: card.locked with 3 macro-tiles (Body fat / Lean mass / Trend, all '—', saturate .5) + locked__veil (blur + lock icon + 'Unlock with Pro'). · app = No equivalent on Progress (web or mobile). The body-fat figure exists in data (body_fat_pct selected on web) but there is no locked body-composition trends card. · _Prototype-only Pro upsell surface absent from both platforms._
  - **[LOW]** Energy balance: equation vs triad (flag-gated): proto = L5001-5018: single .prog-balance EQUATION row (Avg intake − Maintenance = Deficit/day) + 'How maintenance works' explainer with an at-row 2,260 → maintenance. · app = ProgressEnergyTriad renders the prototype equation (ProgressEnergyEquation) ONLY when isFeatureEnabled('sloe_v3_energy_equation'); otherwise a 3-cell AVG/TDEE/DEFICIT triad. Equation lacks the prototype's at-row 2,260→maintenance 'Why?' transition detail. · _Conforms with the v3-energy flag ON (all flags forced on for verification). The equation explainer omits the old→new maintenance at-row the prototype shows._
  - **[LOW 🔒keep?]** Energy equation term value type ramp: proto = .prog-bal-v: serif, 25px, fw-medium. · app = ProgressEnergyEquation termValue: serif 22px / 500 (deliberately 22 not 25 — comment cites ENG-119 ramp + 3-term overflow on narrow phones). · _Documented token-ramp choice (25 is off the ENG-119 ramp). Keep._
  - **[MED]** Daily calories chart — per-bar kcal labels + range band + goal-line label: proto = L5025-5045: each bar shows a tiny '(v/1000).toFixed(2)k' label above it; a success-soft target-RANGE band (lo..hi) behind the bars; a dashed goal line with a 'N.Nk' axis label at left. · app = Bars have NO per-bar kcal labels; uses a single goal DOT per bar (not a horizontal goal line + range band). Legend is 'On target / Over' (app drops the prototype's third 'Under' legend item). · _App's goal-dot-per-bar reads cleaner but diverges from the prototype's range-band + dashed-goal-line + per-bar-value treatment; and the 'Under' legend swatch (fg-tertiary) is missing._
  - **[MED]** On-target ribbon icon: proto = L5047 prog-win: sparkles glyph in a 34px accent-frost-mist rounded(10) plate, body 'N on-target days this week. Your most consistent week this month.' · app = ProgressOnTargetRibbon uses an Award (medal) icon in a 40px FULL-radius circle tinted sourceAi (damson), not the sparkles-in-frost-mist-square. Copy matches. · _Icon (Award vs sparkles), plate shape (full circle vs radius-10 square), tint (damson vs frost-mist) and size (40 vs 34) all diverge._
  - **[LOW 🔒keep?]** Average adherence — over-target macro qualifier + headline: proto = Adherence card has overall serif .prog-adh-big % + per-macro rows (label / pct / track-fill). · app = Matches structurally; ADDS an over-110% inverted headline ('11% over', amber via adherence_over_display flag) and a '· over' macro qualifier not in the prototype. · _Anti-shame / honest-overshoot additions (audit P1-3). Keep — strictly more correct than the prototype's raw >100%._

### 🔒 Digest — weekly on Progress supersedes daily screen (B22)
- proto: `6023-6050; CSS dg-* L1112-1123, coach-digest/narr L845-847`
- mobile: apps/mobile/components/Digest.tsx (+ apps/mobile/components/progress/DigestStoryCard.tsx)
- web: src/app/components/suppr/digest.tsx (+ digest-blended.tsx, digest-story-card.tsx)
- _Both platforms HAVE a digest surface. The prototype 'Daily digest' is a pushed screen: dg-eyebrow (date overline + time) → serif dg-head 'Good morning, Grace.' → serif dg-narr paragraph with bold numerals → two dg-chips (Log breakfast / See dinner, frost-mist pills) → 'At a glance' dg-grid 2x2 stat cards → coach-digest 'One thing for today' card → 'Digest delivery & timing' tertiary button. The app's Digest is rendered as a WEEK-recap card embedded INSIDE Progress (not a standalone morning push screen), driven by buildWeeklyRecap + headline/stats/narrative props. So the app surface is a week digest, structurally different from the prototype's daily morning narrative._
  - **[HIGH]** Daily morning digest vs week digest: proto = Daily digest: 'Good morning, Grace.' serif head + dg-narr daily narrative + Log breakfast / See dinner chips + At-a-glance 2x2 + 'One thing for today' coach card. · app = App Digest is a WEEKLY recap card embedded in Progress (week label, days/meals logged, streak, week stats, share/dismiss). No standalone daily-morning digest screen with the greeting head + action chips + 'One thing for today' coach card. · _The prototype's daily-narrative Digest (greeting, dg-chips, at-a-glance grid, coach card) has no app equivalent; the app reuses 'Digest' naming for a different (weekly) surface._
  - **[MED]** dg-chips action pills (Log breakfast / See dinner): proto = L6032-6035: two frost-mist pills with plus/utensils icons that open log + recipe. · app = Absent — the embedded week digest exposes share/dismiss/save-combo actions, not the daily Log-breakfast / See-dinner chips. · _Net-new daily-action chips missing._
  - **[MED]** 'One thing for today' coach card: proto = L6042-6045: card.coach-digest with overline 'One thing for today' + serif coach-narr advice line. · app = Absent in the embedded week digest. · _The single-focus coaching card is prototype-only._

### 🔒 AdaptiveTDEE — inline explainer only; no full screen (B2)
- proto: `5875-5906; CSS atde-* L888-891, at-row L2293-2295`
- mobile: (none — only lib/refreshAdaptiveTdee.ts engine; no screen/route)
- web: (none)
- _The prototype's full-screen 'Adaptive target' explainer (pushed view) has NO app equivalent on either platform. It is a strikethrough-old → success-new hero (atde-hero: 34px line-through fg-tertiary → 48px success), a coach quote, a 'Why it changed' divided card of 4 set-rows (Weight trend / Logged intake / Activity / The maths), a 6-week target trendline, and two CTAs ('Keep adapting automatically' / 'Set a fixed target instead'). The Progress Maintenance card surfaces adaptive vs formula inline, but the dedicated reasoning screen the prototype's 'Why?' link opens to (open('adaptive')) does not exist. The prototype Progress energy-explainer at-row even links to it (L5016 onClick open('adaptive')) — a dangling target in the app._
  - **[HIGH]** Adaptive target reasoning screen: proto = Full pushed screen: atde-hero old→new, coach quote, 4-row 'Why it changed' card, 6-week target trendline, keep-adapting / fixed-target CTAs. · app = absent · _Net-new prototype screen. The energy-balance 'Why?' / at-row CTA in the Progress prototype routes here; app has no destination._

### 🔒 BurnDetail — web inline panel; mobile screen (B10)
- proto: `6644-6697; CSS burn-* L910-936, set-row L2303-2305`
- mobile: apps/mobile/app/burn-detail.tsx (+ apps/mobile/components/burn/BurnDetailLoadingSkeleton.tsx)
- web: src/app/components/BurnDetailPanel.tsx
- _Both platforms HAVE an activity/burn detail surface that closely tracks the prototype: burn hero (flame plate + serif kcal + 'kcal burned · date'), a Breakdown divided card (Active energy / Resting energy / Steps with progress bar), an Activity-bonus card (Final burn / Maintenance estimate / Bonus earned + 'Add bonus to today's budget' toggle), and the info footnote. Differences are token/detail-level rather than structural. Need a pixel pass to confirm the burn-hero serif size (proto 56px), the warning-soft flame plate, and the workouts list card (proto has a separate divided card listing Tennis / strength-training rows with burn-wk-k warning kcal)._
  - **[LOW]** Title bar label: proto = pushed-bar title 'Activity summary'. · app = Need to confirm exact header copy matches 'Activity summary' on both (mobile uses a burn-detail-hero testID; header string not verified in this pass). · _Verify header copy = 'Activity summary' on web + mobile._
  - **[MED]** Workouts list card: proto = L6675-6679: a second card--flush divide listing per-workout rows (burn-wk: 26px frost-mist icon plate, name ellipsised, duration meal-sub, warning-tinted kcal right). · app = Mobile burn-detail.tsx structure shows hero + Breakdown + Activity-bonus; the discrete per-workout list card was not confirmed present in this pass. · _Confirm the per-workout rows card exists; if absent it is a structural gap. Pixel/Read pass needed._
  - **[LOW 🔒keep?]** Final-burn / Maintenance / Bonus copy nuance: proto = Rows: 'Final burn' (always), 'Maintenance estimate' (− value), 'Bonus earned' (+N kcal, serif warning). · app = Mobile branches 'Final burn' vs 'Projected burn' (isPast) and 'Bonus earned' vs 'Bonus' — a state nuance the prototype doesn't show (prototype is a fixed past day). · _App's past/projected branching is a real-data correctness addition; keep._

### ✅ WeeklyRecap detail rows (ENG-1259 / B21, 2026-06-28)
- proto: recap detail grid (weight / streak / most-cooked / protein)
- mobile: apps/mobile/components/recap/WeeklyRecapDetailRows.tsx + weekly-recap.tsx
- web: src/app/components/suppr/WeeklyRecapDetailRows.tsx + weekly-recap-dialog.tsx
- shared: src/lib/nutrition-core/weeklyRecapDetailRows.ts
  - **[LOW]** Footer domain string: proto = recap-foot … 'sloe.co'. · app = WeeklyRecapCard footer present; confirm the domain reads 'sloe.co' (some surfaces historically used getsloe.com). Mismatch if not 'sloe.co'. · _Verify exact domain string in the share card footer._

### ⬜ WeeklyRecap — share card footer domain (residual LOW)
- proto: `5985-6019; CSS recap-card L1097-1109, gradient L2403-2408`
- mobile: apps/mobile/app/weekly-recap.tsx (+ apps/mobile/components/recap/WeeklyRecapCard.tsx, WeeklyRecapShareButton.tsx)
- web: src/app/components/suppr/weekly-recap-card.tsx
- _Both platforms HAVE a shareable weekly-recap card matching the prototype's primary-deep gradient card: date overline (accent-frost) + serif headline, a 2x2 recap-stat-grid (serif values + frost labels with top/bottom hairlines), a 7-bar recap-spark sparkline (frost bars, white over-bars), and a footer wordmark 'sloe' + 'sloe.co'. Below it the prototype adds a coach quote + a 'The detail' divided card of 4 set-rows (Weight / Best streak / Most-cooked / Protein average) and Save-image / Share-recap CTAs. The shareable card itself is a strong match; verify the 'The detail' rows + coach quote + the wordmark domain string._
  - **[LOW]** Footer domain string: proto = recap-foot: wordmark 'sloe' (17px, white) + recap-foot-s 'sloe.co'. · app = WeeklyRecapCard footer present; confirm the domain reads 'sloe.co' (some surfaces historically used getsloe.com). Mismatch if not 'sloe.co'. · _Verify exact domain string in the share card footer._
  - **[LOW]** 'The detail' rows + coach quote below the card: proto = L6005-6011: coach quote, 'The detail' overline + divided card (Weight −0.6kg / Best streak 12 days / Most-cooked / Protein average). · app = weekly-recap.tsx is a larger interactive screen; confirm it surfaces the equivalent detail rows + coach line beneath the shareable card on both platforms. · _Verify the post-card detail list + coach quote exist; likely present given file size._

## Cluster: log-search-confirm


### 🔒 LogHub — search-first IA (B4, §D4)
- proto: `3768-3812`
- mobile: apps/mobile/components/today/LogSheet.tsx
- web: src/app/components/suppr/log-sheet.tsx; src/app/components/suppr/today-add-meal-dialog.tsx
- _App's log sheet is a search-first single sheet (2026-04-30 teardown, Grace-confirmed direction) vs the prototype's LogHub with method-grid + quick-action row + Recent/Saved-routine seg-tabs. Title differs ('Log a meal' vs 'Add to today'). Method affordances are a 4-cell icon row (Scan/Voice/Photo/Quick add) vs the prototype's 5-cell labelled method-grid (Barcode/Photo/Voice/Label/Describe). Prototype's loghub-quick row (Log usual / Copy yesterday / Duplicate day) and the Saved-tab routine rows with 'Routine' pills + save-row are absent. Web mirrors mobile structurally. Verified prop API and composition are identical web↔mobile._
  - ✅ **[LOW → RESOLVED ENG-1303]** Sheet title: proto = "Add to today" (Sheet title prop, L3780) · app = both · _Was a copy mismatch ('Log a meal' vs 'Add to today'). ENG-1303: the header reads "Add to today" behind `sloe_v3_log` (default-ON) on both platforms; the legacy 'Log a meal' stays in the flag-off else as the kill switch._
  - ✅ **[HIGH → RESOLVED ENG-1303]** Method set + layout (method-grid): proto = 5-cell labelled grid: Barcode, Photo, Voice, Label, Describe — each .method tile bg-secondary, radius 14px, icon (primary) + 11.5px/600 label below; photolock cell carries a .prolock lock badge (L3782-3788, .method L2047) · app = both · _ENG-1303 adopts the method-grid tile grammar behind `sloe_v3_log` (default-ON): 5 equal rounded tiles on the secondary surface (Scan / Photo / Voice / **Describe** / Quick add), icon in plum + 11/600 label, and a frost circular **lock badge** replacing the "PRO" text pill on locked AI methods. **Describe is a first-class tile** that expands the inline describe flow. **`Label` (scan nutrition label / OCR) is deliberately OMITTED — no scan-label flow exists yet; a follow-up ticket covers it (Grace 2026-07-02).** The legacy 4-cell circular icon chips stay in the flag-off else (kill switch). Tokens: tile bg `bg-secondary`/`colors.backgroundSecondary`, radius 12 (nearest legal to the prototype's 14), lock badge frost `bg-muted`/`colors.fillQuiet`._
  - **[HIGH]** Quick-action row (loghub-quick): proto = Three pill buttons in a row — 'Log usual' (sunrise), 'Copy yesterday' (refresh), 'Duplicate day' (plan) — .loghub-quick, card bg, 12px radius, 12.5px/600 (L3789-3793, CSS L2041-2043) · app = both · _Missing two of three quick actions ('Log usual', 'Duplicate day') and the surviving one is a different element (full-width row vs inline pill)._
  - **[LOW 🔒keep?]** Browse tabs (seg-tabs) labels + style: proto = seg-tabs: 'Recent' | 'Saved meals', 14px/600, gap 18px, bottom-border rail, active underline 2px primary (L3795, CSS L2051-2055) · app = both · _App superset (adds Favourites + My recipes tabs) is more capable — keep. Underline-rail treatment matches prototype._
  - **[MED]** Saved-tab routine rows + 'Routine' pill + save-row: proto = Saved tab renders ROUTINES with a .routine-pill ('Routine' uppercase chip, accent-frost-mist) inline in meal-name, plus a dashed .loghub-saverow 'Save a meal as a routine' footer (L3798-3807, CSS L2044-2046) · app = both · _Routine concept + 'Routine' pill + dashed save-row are prototype-specific and absent; app uses a plain ghost-button CTA instead of the dashed .loghub-saverow._
  - **[LOW 🔒keep?]** Search row affordance: proto = Tap-to-open readonly search that pushes to SearchView (L3781). Placeholder 'Search foods, e.g. yoghurt' · app = both · _App's inline search (no second screen) is the documented 2026-04-30 search-first improvement — keep. Placeholder copy differs ('…e.g. yoghurt' vs '…or scan')._

### ✅ ConfirmFood — P/C/F tiles + micro table (ENG-1257, A2b)
- proto: `3862-3911`
- mobile: apps/mobile/components/food-search/FoodSearchPanel.tsx; apps/mobile/components/food-log/ServingStepper.tsx
- web: src/app/components/food-search/FoodSearchPanel.tsx (inline portion preview)
- _Prototype ConfirmFood is a pushed screen: confirm-head (44px icon + name + source/Conf), optional trust-cue + lowconf-note, a card with Portion field (unit-row chips serving/grams/oz/cup/tbsp), portion stepper (−/value/+ with serif value), 3 macro-tiles, serif kcal-line, then a 'Meal' segmented (Breakfast/Lunch/Dinner/Snacks), and a sticky pushed-cta 'Add to {slot}' primary button. The app implements the equivalent INLINE in FoodSearchPanel's preview card (portion picker, unit list, nutrition lines, fit-hint, add) — capability is present and arguably richer (fit-this-in projection), but it's not a dedicated pushed screen and several specific elements (unit-chip set, macro-tiles, serif kcal-line, lowconf-note / trust-cue, segmented meal selector inside the confirm) need a 1:1 conformance check._
  - **[MED 🔒keep?]** Pushed confirm screen vs inline preview card: proto = <Pushed title='Add food'> dedicated screen with sticky bottom CTA (L3872, pushed-cta L2035/3908) · app = both · _Inline confirm (no extra screen) aligns with the search-first direction — likely keep, but it changes the confirm IA materially vs prototype._
  - **[MED]** Unit-row chips: proto = unit-row: serving / grams / oz / cup / tbsp chips, active = filled primary (.unit-chip L1300-1302) · app = both · _Verify the unit chip set + active style match prototype's .unit-chip (12.5px/600, radius 9, on=filled primary)._
  - **[MED]** 3 macro-tiles + serif kcal-line: proto = macro-tiles grid (3× recessed bg-secondary tiles, colour dot + value + label) and a centered serif kcal-line (26px Newsreader) below the stepper (L3896-3901) · app = both · _Confirm the confirm card renders the 3 macro-tiles + serif kcal-line, not a flat macro text line._
  - **[MED 🔒keep?]** Meal segmented selector inside confirm: proto = 'Meal' field-label + full-width .segmented Breakfast/Lunch/Dinner/Snacks, then 'Add to {slot}' CTA (L3904-3908) · app = both · _App hoists slot selection to the sheet header (visible before search) — arguably better, but diverges from prototype placing it in the confirm step. The header slotRow uses soft-tint pills (AA-safe, per Grace) vs prototype's pill .segmented._
  - **[MED]** lowconf-note + trust-cue: proto = If conf==='low': amber .lowconf-note 'Nutrition is estimated. You can edit any value.'; if src==='photo': success .trust-cue 'You corrected this before…' (L3881-3882) · app = both · _Verify the low-confidence estimated-nutrition note (amber, warning-soft) and photo trust-cue render in the confirm card._
  - **[LOW]** Confirm-head (icon + name + source/Conf): proto = confirm-head: 44px meal-ic + confirm-name (16px/600) + meal-sub 'sourceChip · srcLabel + Conf dot' (L3874-3880) · app = both · _Verify the confirm header composition (icon plate + name + source/confidence sub)._

### 🔒 FoodRow — search-first browse rows (B4)
- proto: `3813-3828`
- mobile: apps/mobile/components/food-search/FoodSearchPanel.tsx (and LogSheet BrowseRow apps/mobile/components/today/LogSheet.tsx)
- web: src/app/components/food-search/FoodSearchPanel.tsx
- _Prototype FoodRow = icon plate (.meal-ic 36px, radius 11) + name + sub line 'portion · kcal · NP NC NF + confidence dot' + round filled .addbtn (34px, primary, flips to green check on add). App rows exist (BrowseRow in LogSheet; result rows in FoodSearchPanel) but the browse-tab BrowseRow sub-line shows source-dot + kcal only — not the 'portion · kcal · P/C/F + Conf' composite — and uses ChevronRight/tap-to-portion rather than the inline round add+check button on every row._
  - **[MED]** FoodRow sub-line content: proto = .meal-sub: '{portion} · {kcal} kcal · {p}P {c}C {f}F' followed by a <Conf2> confidence dot (L3822) · app = both · _Browse rows drop portion + macro triplet + confidence from the sub-line the prototype specifies._
  - **[MED]** Per-row add button (.addbtn) with done-state: proto = Round 34px filled-primary add button on every row; on press flips icon plus→check and bg primary→success (.addbtn.is-done L2060-2061, FoodRow L3825) · app = both · _The prototype's signature one-tap round add button with the success-green check morph is missing from browse rows._
  - **[MED]** .meal-ic icon plate: proto = 36px square, radius 11, bg-secondary, primary-tinted source icon (L1669) · app = both · _Icon-plate treatment (rounded grey plate w/ source glyph) differs from the app's source-dot/thumbnail approach._

### 🔒 SearchView — inline search supersedes pushed screen (B4, §D4)
- proto: `3831-3859`
- mobile: apps/mobile/components/food-search/FoodSearchPanel.tsx; apps/mobile/components/today/LogSheet.tsx
- web: src/app/components/food-search/FoodSearchPanel.tsx; src/app/components/suppr/log-sheet.tsx
- _Prototype SearchView is a pushed full screen ('Search foods' back-nav) with a source filter chiprow (All/Branded/Whole foods/My foods), a 'Best matches · USDA, Open Food Facts, FatSecret' reslabel over FoodRows, and a no-results .state (offline-grey search icon, title, body, 'Create "q"' secondary btn). App renders results INLINE inside the sheet (no push, no back-nav), and the source-filter chiprow + the exact reslabel string + the create-custom-food empty state differ._
  - **[LOW 🔒keep?]** Push screen vs inline: proto = <Pushed title='Search foods'> — a full screen pushed onto the stack with a back arrow (L3836) · app = both · _Inline search (no second screen) is the documented search-first direction — keep._
  - **[MED]** Source filter chiprow: proto = chiprow: All / Branded / Whole foods / My foods, first aria-pressed (L3841, .chiprow L2063, .chip L507) · app = both · _The source-type filter chiprow is absent; app uses semantic result groups instead of a filter row._
  - **[LOW 🔒keep?]** Results label string: proto = reslabel 'Best matches · USDA, Open Food Facts, FatSecret' (L3851); plus a hint reslabel about trying 'xyz' (L3853, prototype-only demo text) · app = both · _App's data-source attribution is handled via FatSecretBadge + group labels; the demo hint reslabel is prototype-only scaffold (not a real gap)._
  - **[MED]** No-results state + create-custom-food: proto = .state with is-offline grey search icon (56px), title 'No matches for "q"', body 'Check the spelling, or add it as a custom food…', secondary btn 'Create "q"' → confirm (L3843-3849) · app = both · _Verify the no-results empty state matches the prototype's state__icon.is-offline + title/body copy + secondary 'Create "…"' button; app has the capability but likely different copy/layout._

## Cluster: billing-paywall


### 🔒 Billing — storefront-owned canonical (B27)
- proto: `5176-5228`
- mobile: (none - mobile billing is delegated entirely to Apple Subscriptions; no in-app Billing screen exists)
- web: src/app/components/settings/SubscriptionCard.tsx + src/app/components/settings/SettingsDialogs.tsx + /account/billing (Stripe Customer Portal shell)
- _Prototype Billing() is a full in-app subscription manager (status hero, banner states, plan-switch radio rows, Apple Pay/Restore/Billing-history rows, cancel, demo state switcher). The app splits this by platform per IAP/legal policy: mobile has NO in-app billing screen (Apple owns it), web has SubscriptionCard which DESCRIBES state and routes all mutation to the Stripe portal. Structure diverges from the prototype on both platforms - much deliberately (legal P0s), some addressable._
  - **[HIGH 🔒keep?]** Mobile in-app billing screen: proto = Full Billing() screen with plan switch, payment rows, cancel, state banners. · app = Absent on mobile. paywall covers purchase; management is Apple's native Subscriptions UI (cancelPath copy "Settings > Apple ID > Subscriptions"). · _Apple IAP policy (and legal MV-1/MV-2) forbids an in-app cancel/manage control for App Store subscriptions - the prototype Billing screen is conceptual and must NOT be built on mobile. Correct net-new-absent by design._
  - **[HIGH]** Status hero: proto = .bill-hero (gradient 135deg primary-deep->primary, white) with overline, bill-badge (Active/Past due/Grace/Ending), serif .bill-plan "Sloe Pro", and renews/active-until line. · app = Web SubscriptionCard is a flat SupprCard with a sparkles icon + "Subscription" h3 + a status paragraph. No gradient hero, no serif plan name, no pill badge. · _Web subscription surface has none of the prototype's status-hero visual language (gradient ground, badge pill, serif tier name). Structurally a plain card vs the prototype's branded hero._
  - **[MED 🔒keep?]** Plan-switch rows: proto = Two .bill-plan-row radio rows (Base/Pro) with .radio-dot, descriptions, and a "Current" pill; tapping switches plan. · app = No plan-switch UI. Web routes to Stripe portal for any change; Base is collapsed out of the SSOT (Free+Pro only). · _Base tier was removed per 2026-04-27 strategic direction (PR-01) and billing mutation must go through the Stripe portal (legal P0). Prototype's two-tier radio switch is obsolete. Keep-current._
  - **[MED]** Banner states (past-due / grace / canceled): proto = .bill-banner with tone-mapped bg/colour + icon + title + sub for pastdue (destructive), grace (warning), canceled (neutral), plus a contextual primary action button. · app = Web SubscriptionCard handles past_due (amber banner -> /account/billing) and canceled ("cancelled, access until [date]") but NOT a distinct grace-period state, and banner anatomy differs (single amber link-banner vs the prototype's icon+title+sub three-tone system). · _Web covers past-due + canceled with correct legal copy; grace-period as a distinct visual state is absent. Banner anatomy is flatter than the prototype._
  - **[MED 🔒keep?]** Payment + management rows: proto = card--flush divide with Apple Pay row (card +Update), Restore purchases row, Billing history row (chevR). · app = Web: no payment-method row, no restore (Stripe portal owns it), no billing-history row - all folded into the single "Manage or cancel subscription" button -> portal. Mobile restore lives only on the paywall. · _Stripe Customer Portal owns payment method, invoices, and cancellation (legal P0 - single managed path, no retention wall). Prototype's separate rows are demo-only. Keep-current._
  - **[LOW 🔒keep?]** Cancel control: proto = btn--tertiary block, destructive-coloured "Cancel subscription" (only when active). · app = Web: full-width SOLID bg-foreground "Manage or cancel subscription" button (legal P0 CC-5 requires equal-or-greater weight). Not a tertiary destructive ghost. · _Legal P0 mandates the cancel control have equal-or-greater visual weight than keep/upgrade - a tertiary ghost would violate it. App's solid button is correct over the prototype's ghost._

### 🔒 ProLock — Figma 284:2 paywall canonical (B8)
- proto: `4007-4099`
- mobile: apps/mobile/components/AiPaywallSheet.tsx (gate) + apps/mobile/components/PhotoLogSheet.tsx + VoiceLogSheet.tsx (capture->analyse->review)
- web: src/app/components/suppr/ai-paywall-dialog.tsx (gate) + photo-log-dialog.tsx + log-sheet-describe-flow.tsx
- _Prototype ProLock({kind}) is a single full-screen Pushed flow with 4 phases: intro (Pro gate with Try-it-now + See-Sloe-Pro + free-preview quota) -> capture -> analysing -> review. The app SPLITS this into a separate gate component (AiPaywallSheet/ai-paywall-dialog as a bottom-sheet/modal) and the capture/analyse/review in PhotoLogSheet/VoiceLogSheet. The gate's presentation and the free-preview quota copy diverge._
  - **[HIGH]** Gate presentation: proto = Full-screen Pushed intro: state__icon.is-locked (accent-frost-mist bg, primary-active glyph, 64x64), serif state__title, state__body, vertical action stack ("Try it now" primary + "See Sloe Pro" tertiary, width 248), free-preview line. · app = Mobile AiPaywallSheet is a bottom SHEET (Modal, rounded top, drag pill, PRO Badge, 20px bold title, body, "See Pro plans" primary + "Not now" ghost). Web ai-paywall-dialog is a centred modal. Neither is a full Pushed screen with the locked icon plate. · _Prototype gate is a full-screen pushed intro that lets the user "Try it now" (free preview) BEFORE paying; the app gate is a dismissable upsell sheet/modal with no try-now affordance at the gate - it routes straight to See Plans / paywall._
  - **[HIGH 🔒keep?]** Free-preview quota line: proto = "Free preview - 2 of 3 left today" under the action stack - names a remaining free-taster quota at the gate, AND a "Try it now" button to consume it. · app = Mobile photo_log copy: "You've used all 5 of your free photo logs this week." (5/week taster, shown only AFTER exhaustion). No remaining-count affordance or in-gate try-now; the gate appears once the quota is spent. · _App's free taster is 5/week (docs/decisions/2026-05-02-photo-log-free-taster.md) and the gate fires on exhaustion (403), not as a pre-gate with a try-now. Grace-ratified product decision that differs from the prototype's 3/day pre-gate model. Keep-current, but the quota numbers (3/day vs 5/week) and gate timing genuinely differ._
  - **[MED]** Locked icon treatment: proto = .state__icon.is-locked: background var(--accent-frost-mist), colour var(--primary-active), 64x64 with the feature glyph (mic/camera). · app = Mobile gate shows a PRO text Badge, not the locked icon plate. Capture phase in PhotoLogSheet has its own iconography but no accent-frost-mist locked plate at the gate. · _The prototype's frost-mist locked icon plate is a recognisable Pro-gate motif that the app's PRO text badge does not reproduce._
  - **[LOW 🔒keep?]** Capture / analysing / review phases: proto = Photo: photo-frame + grid + hint + shutter + 'Choose from library'. Voice: voice-orb + rings + hint + example. Analysing: scan / import-spin. Review: detected-items checklist (ver-row, gr-check toggle), lowconf-note, retake/say-again link, sticky Log CTA. · app = Implemented in PhotoLogSheet/VoiceLogSheet with equivalent capture->analyse->review (the app has a real AI pipeline, not the prototype's 1900ms setTimeout mock). Detailed token/structure parity of these sub-phases is a separate sheet audit. · _App has a real backend pipeline; phase structure broadly matches. Forensic per-phase token diff is out of scope here (lives with the PhotoLog/VoiceLog sheet surfaces)._

### ⬜ Verify — H0 M3 L3
- proto: `5251-5273`
- mobile: apps/mobile/app/recipe/verify.tsx
- web: src/app/components/RecipeDetail.tsx (inline verify section + /api/nutrition/verify-recipe) - no standalone Verify screen
- _Mobile Verify is structurally close to the prototype after the ENG-1247 A7 pass (chevron back + serif title done; 9px ver-dot done). But the row container, totals card, and the per-row action diverge from the prototype's lean card--flush list. Web has no standalone Verify screen - verification is inline in RecipeDetail._
  - **[LOW]** Header: proto = Pushed title="Verify ingredients" with chevron back. · app = Mobile VerifyTopBar: chevron-back + serif 18px "Verify ingredients" - MATCHES (ENG-1247 A7 already landed). · _Conformant. Verified the A7 chevron+serif rebuild is present._
  - **[MED]** Intro copy: proto = insight-b paragraph: "Check each ingredient so Sloe can calculate reliable macros for {recipe}." · app = Mobile shows recipe title (22px bold) + conditional subtitle "Some ingredients need review - tap to check the match". Different copy + a live totals card not present in the prototype. · _App leads with recipe name + a live totals card (per-serving kcal + macro trio); prototype leads with the instructional insight-b sentence and has NO totals card. Extra capability but structural divergence from the prototype's lean intro._
  - **[MED]** Ingredient row container: proto = Single .card.card--flush.divide wrapping .ver-row items (13/14px padding, hairline divider between rows, flat - no per-row border/radius). · app = Mobile renders each ingredient as a SEPARATE bordered card (styles.ingRow: card bg, Radius.md, 1px border, marginTop) - not a flush divided list. · _Prototype is one flush card with internal dividers; app is a stack of individually-bordered cards. Same card--flush-vs-bordered-rows pattern flagged across other v3 surfaces._
  - **[LOW]** Per-row confidence indicator: proto = .ver-dot (9px, is-high success / is-med warning / is-low destructive) at row start. · app = Mobile shows a 9px dot (confColor high/med/low) - token MATCHES (ENG-1247 A7), but placed at the bottom of the content stack (marginTop 6) rather than as a leading dot. · _Dot token matches; position differs (leading in prototype vs trailing-under-content in app)._
  - **[MED]** Low-confidence row action: proto = Low-confidence rows show a btn--primary btn--sm "Fix" button; matched/estimated rows show a success check glyph. · app = Every row shows a "Swap" pill (accent outline) + chevron + a calorie number; no contextual Fix/check split. needsReview rows get a leading alert-circle icon instead. · _Prototype splits the row affordance by confidence (Fix CTA on low vs check on matched); app uses a uniform Swap pill + alert-circle on needs-review. App is arguably more capable (always-swappable) but does not match the prototype's confidence-gated Fix/check._
  - **[LOW]** Bottom CTA: proto = pushed-cta: btn--primary btn--block btn--lg with check glyph "Calculate nutrition". · app = Mobile footer: confirmBtn (accent.primary, Radius.md) with checkmark-circle + "Calculate nutrition", PLUS a per-serving kcal label above it. Copy + icon match; footer also carries the totals label (extra). · _CTA copy/icon conformant; app adds a per-serving summary line above it._
  - **[MED 🔒keep?]** Web standalone Verify: proto = Verify is a discrete pushed screen. · app = Web has no standalone Verify screen - verification runs inline inside RecipeDetail via /api/nutrition/verify-recipe. · _Web verify is embedded in the recipe detail rather than a separate route. Likely a deliberate web IA choice, but no web surface matches the prototype Verify screen 1:1 - flag for a parity decision._

### 🔒 Paywall — Figma 284:2; v3 Paywall non-canonical (B8)
- proto: `5275-5307`
- mobile: apps/mobile/app/paywall.tsx + apps/mobile/components/paywall/{PaywallHero,PaywallValueGrid,PaywallComparison,PaywallPlanSelector,PaywallCta,PaywallTrustStrip,PaywallNoPaymentChip,PaywallPersonalisedPlanCard}.tsx
- web: src/app/components/suppr/upgrade-paywall-dialog.tsx (in-app upsell) + app/pricing/page.tsx (route paywall)
- _Both platforms ship a far more capable paywall than the prototype's static Paywall() (real RevenueCat/Stripe, CMA disclosure, promo codes, trial timeline, comparison matrix, personalised-plan lead). But several prototype-specified visual/structural elements diverge. Headline copy difference is keep-current (system-wide positioning copy)._
  - **[MED 🔒keep?]** Hero background: proto = .pw-hero uses --gradient-brand (linear-gradient 135deg #6a4b7a->#3b2a4d->#c9c2d6) with a .pglow radial highlight; flat plum brand wash, no photo. · app = Mobile PaywallHero renders a full-bleed finished-dish PHOTOGRAPH with an SVG transparent->bg fade (Figma 284:2). Web upgrade-dialog hero is a flat bg-card slab (no gradient, no photo). · _Mobile photo hero is the ratified Figma 284:2 direction (richer than the prototype plum wash). Web flat hero is the weaker of the three - matches neither prototype gradient NOR mobile photo. Worth a parity decision._
  - **[LOW 🔒keep?]** Hero headline copy: proto = "Everything in Sloe, unhurried." (serif, em on "unhurried") + "You've been tracking for a week. Pro turns that habit into a whole calm system." · app = "Cook what you love. Still reach your goals." (serif, em on "Still") - both mobile PaywallHero and web sloeHero variant. · _App uses the system-wide Sloe positioning headline (landing, onboarding welcome, login all share it). Deliberate brand-consistency choice, not drift._
  - **[MED 🔒keep?]** Region price chips: proto = .pw-region row of UK/EU/US/AU chips with flags switching cur/mo/yr/yrmo/tax inline; explicit "incl. 20% VAT" / "+ tax at checkout" lines. · app = No region selector on either platform. Mobile shows Apple-localised priceString (storefront handles currency+VAT); web shows GBP from PRICING_TIERS SSOT. VAT stated in the disclosure paragraph only. · _Region selection is a prototype demo affordance; production correctly defers currency/VAT to Apple storefront (mobile) and Stripe checkout (web). A manual toggle would fight real localisation. Keep-current, but web hardcoding GBP is a separate region-aware-pricing gap._
  - **[MED]** Primary CTA token: proto = .btn--clay (background var(--primary-deep) #241733, white text) labelled "Start 7 days free" with sparkles icon. · app = Mobile CTA uses accent.primary (or Accent.success when trial applies), label "Start free 7-day trial"; web uses bg-primary, label "Upgrade to Pro - price/period". Neither uses the primary-deep clay treatment. · _Prototype's hero-ground CTA is the deep-plum clay button; app paywall CTAs use the standard primary fill. Per project rules btn--clay is the on-brand-ground CTA - the trial-success-green override is a Grace-confirmed state colour rule but the non-trial path should consider clay on the photo/gradient ground._
  - **[LOW]** Secondary dismiss copy: proto = "Maybe later" (btn--tertiary). · app = Mobile "Continue for free"; web "Continue for free". · _Copy wording differs. App's "Continue for free" is arguably clearer (names the free outcome) - minor._
  - **[MED]** Feature list structure: proto = 4 .feat rows (plan / camera / link / progress) icon-plate (.feat-ic 40x40, radius 11, accent-frost-mist bg) + name + sub. · app = Mobile replaces the flat list with a 2x2 PaywallValueGrid + a FREE/PRO PaywallComparison matrix (Figma 284:2). Web upgrade-dialog renders a 6-row .feat-style list with round (rounded-full) icon plates, not the prototype's 11px-radius square plates. · _Mobile is an intentional richer rebuild (grid+matrix). Web icon-plate radius (rounded-full) diverges from prototype's radius-11 squircle; web list count (6) also differs from prototype's 4._
  - **[LOW]** Purchase-error state: proto = On CTA tap sets err -> inline .lowconf-note destructive banner "Purchase failed - your card was declined. No charge was made." · app = Mobile shows an Alert.alert("Purchase failed") modal; web shows alert(). No inline destructive banner in the scroll. · _Native Alert vs inline banner - acceptable platform pattern, but the prototype's inline no-charge reassurance copy is stronger and could be adopted._

## Cluster: scan-barcode


### 🔒 ScanLabel — OCR in custom-food; standalone deferred (B13, ENG-1004)
- proto: `3914-3954`
- mobile: apps/mobile/app/(tabs)/barcode.tsx (no ScanLabel path — only barcode scan + manual entry)
- web: src/app/components/suppr/today-barcode-dialog.tsx (manual entry only); no camera/OCR surface anywhere in src/app
- _Prototype ScanLabel is a dedicated full-screen camera flow with 3 phases (scan → reading → review): a dark .scanview, .scan-top header reading 'Scan a nutrition label', a .scan-frame.label-frame with .scan-corners + animated .label-lines placeholder bars, a 'Capture label' onbrand CTA, then a 'reading' phase with .scan-laser + .import-spin sparkles 'Reading the values…', resolving to a Pushed 'Check the label' review screen: a .lowconf-note ('Read from the label — tap any value to correct it before logging.'), a 2-col .label-grid of 4 large centred inputs (Calories/Protein/Carbs/Fat), a 'Food name' input, and a 'Log this food' primary CTA. NO equivalent exists in either app. Mobile's not-found state has an 'Enter manually' CTA that the inline comment says 'Routes to manual entry for now as the Label OCR path is deferred — see ENG-1004' — so the OCR flow is explicitly a tracked gap, not present. Web has no camera or label-OCR surface at all._
  - **[HIGH]** ScanLabel full-screen capture flow (scan→reading→review): proto = Dedicated .scanview camera flow: 'Capture label' → laser/sparkle 'Reading the values…' → Pushed 'Check the label' with 2-col label-grid of 4 editable per-serving inputs + Food name + 'Log this food' CTA · app = Mobile: absent — not-found 'Enter manually' routes to plain manual entry (OCR deferred, ENG-1004). Web: absent entirely. · _Prototype-only screen with no app equivalent on either platform. The label-OCR capture is the whole point of this surface._

### ⬜ Barcode scanner (scan + not-found + add-to-DB) — H0 M4 L1
- proto: `3957-4005`
- mobile: apps/mobile/app/(tabs)/barcode.tsx
- web: src/app/components/suppr/today-barcode-dialog.tsx
- _Prototype Barcode is a dark full-screen camera scan (.scanview) → 'New barcode' not-found state (error icon + community-DB copy: '...Add it once and it's saved for you — and everyone after you.') with 'Add this product' + 'Scan again' → Pushed 'Add product' form (.bc-chip showing the scanned code, Product name, 2-col per-serving grid, a .lowconf-note 'Shared anonymously to the community database…') → Pushed 'Added' saved state (.exp-ready-ic check, 'Saved to the database', 'Log it now'). MOBILE structurally matches the live-camera scan + found-product card + manual/correction overlays, and the community-contribution add-to-DB flow exists but is FLAG-GATED (barcode_community_contribution, off) via BarcodeShareOptIn — per the task note that opt-in is built and out of scope. Remaining diffs are visual/token and copy: mobile uses its own dark overlay card design (rgba literals, accent brackets, slot pills) rather than the prototype's .scan-result panel + onbrand buttons, and the not-found/saved STATES differ in framing. WEB has no camera scanner at all — only a typed-barcode dialog — so it diverges structurally from the prototype's scan-first flow._
  - **[MED]** Not-found state framing + copy: proto = Dark in-scanview .scan-result: .state__icon.is-error alert, title 'New barcode', body '5012345 678900 isn\'t in our database yet. Add it once and it\'s saved for you — and everyone after you.', buttons 'Add this product' (onbrand) + 'Scan again' · app = Mobile: AlertCircle + 'Product not found. Try a different barcode or enter it manually.' + 'Try again' + 'Enter manually' — no community 'saved for everyone' framing. Web: dialog 'We don\'t have this product yet.' / 'Add it to your library so the next scan recognises it.' · _Copy + tone diverge from prototype's community-database promise on both platforms; structure roughly analogous on mobile._
  - **[MED]** Add-product form chrome: proto = Pushed 'Add product' with .bc-chip (scan icon + monospaced code), 'Per serving' label, 2-col .label-grid (4 inputs), .lowconf-note 'Shared anonymously to the community database…', 'Save product' primary block CTA (disabled until name+kcal) · app = Mobile manual overlay: 'Add Item Manually' title, 'Barcode: {code}' sub (plain text, no .bc-chip pill), slot pills, single-column-ish input rows (name full-width then 2x2 grid), 'Log to {slot}' green CTA — no .bc-chip, no community-share note (flag-gated path aside). Web: typed-entry dialog, no add-product form matching this. · _Mobile manual-add form lacks the .bc-chip code pill and the community-share lowconf note; title copy differs ('Add Item Manually' vs 'Add product')._
  - **[MED]** Saved-to-database confirmation: proto = Pushed 'Added' state: .exp-ready-ic (green circle check), 'Saved to the database', body thanking the user for improving Sloe for everyone, 'Log it now' CTA · app = Mobile: native Alert ('{name} logged' / 'Added to today\'s tracker') with 'Scan another'/'Go to tracker'. Web: dialog just closes. Neither shows the celebratory 'Saved to the database / improving Sloe for everyone' state. · _Prototype's community-contribution success state is absent; the live flag-gated BarcodeShareOptIn covers community share but not this exact confirmation framing._
  - **[LOW]** Scan-frame reticle treatment: proto = .scan-frame: full rounded-20px frame on rgba(255,255,255,.04), .scan-corners 46px L-brackets in --accent-frost (#c9c2d6) at TL+BR only, animated .scan-laser glowing accent-frost line · app = Mobile: 4 corner brackets (all four corners) 24px, borderWidth 3, color accent.primary (clay/damson — NOT frost #c9c2d6), Radius.sm; no animated laser line; reticleContainer at top 25%/height 30%. Web: no reticle (no camera). · _Mobile reticle is 4 corners (proto has 2 diagonal), smaller (24 vs 46px), clay/damson not frost, and omits the animated scan-laser._
  - **[MED]** Scan header: proto = .scan-top: serif 17px white, centred title 'Scan a barcode', close (x) icon-btn left on rgba(255,255,255,.16), 40px spacer right · app = Mobile: floating circular X top-left on rgba(0,0,0,0.45) (not a header row); idle title 'Scan a barcode' is in the bottom overlay card (Type.navTitle serif) with a ScanLine icon + hint, not a top header. Web: dialog title 'Barcode (Open Food Facts)'. · _Mobile moves the title from a top serif header into the bottom overlay; web title copy 'Barcode (Open Food Facts)' is engineering-flavoured vs prototype 'Scan a barcode'._
  - **[LOW 🔒keep?]** Found-product result CTA language: proto = n/a — prototype Barcode flow has no 'found product' card (it demonstrates the not-found path); review is the add-product form with 'Save product' · app = Mobile found-product card: green 'Log to {slot}' (Accent.success) + 'Scan again' + 'This is wrong — edit and update'. Web: 'Add to diary'. These are app capability beyond the prototype. · _Found-product card + portion stepper + meal-slot pills + confidence chip + correction flow are real app capability the prototype doesn't model — keep current._
  - **[HIGH 🔒keep?]** Web barcode entry modality: proto = Camera-first full-screen scan · app = Web: typed 8-13 digit input in a Dialog (no camera). Mobile: real camera scan. · _Web cannot reasonably run a native camera barcode scanner in this dialog context; typed entry is a defensible web-platform divergence, but it is a genuine structural parity gap vs the prototype's scan-first flow — flag for Grace's call, lean keep-current for web._