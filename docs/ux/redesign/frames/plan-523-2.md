# Plan — Figma frame `523:2` spec

**Frame PNG (READ FIRST):** `docs/ux/redesign/frames/plan-523-2.png`
Source: Figma `B3UdOFup7ITersgNuoXh0l` node `523:2`.
NOTE: Grace's decision is Plan = **multi-day** (keep the app's multi-day structure). This frame shows the per-day VISUAL treatment to match — apply it to each day in the multi-day list; do NOT collapse the app to single-day.

## Tokens
Cream `#F6F5F2` · plum `#3B2A4D` · body grey `#6A6072` · clay `#C8794E` · sage check `#7C8466`/`#5E7C5A` · Newsreader headings/recipe names; Inter labels.

## Layout (top → bottom)
1. **"Meal plan"** (Newsreader ~30px plum) + filter/sliders icon top-right.
2. Subtitle "Week of June 3" (grey).
3. **Sub-tabs:** "This week" (active, plum underline) / "Shopping list" (grey).
4. **Plan summary card** (cream, rounded-24): "3 JUN – 9 · MEAL PLAN" eyebrow (grey, uppercase, tracked) + headline **"Hits your targets 3 of 7 days"** (Newsreader, **clay**) + paragraph "Some days run over target. Tap a meal to swap or adjust the portion." + two buttons: **"Generate"** (clay fill, white, refresh icon) + **"Adjust constraints"** (cream/outline, sliders icon).
5. **Day header row:** day name "Wed" (Newsreader) + "TODAY" chip (grey, uppercase) left; **"1,022 / 901 kcal"** right (clay when over). Below: macro check-row `P 94g ✓ · C 62g ✓ · F 42g +17 · Fi 11g −4` (Inter ~13px; ✓ sage when met, +N/−N clay/grey when off).
6. **Meal rows** (one per meal, cream rounded-24): **thumbnail** (left, ~56px rounded) + column: MEAL TYPE eyebrow (uppercase grey) + recipe **name** (Newsreader plum) + optional **"0.5× portion"** chip + **kcal** (grey) + a **swap/refresh** icon button (right, circular). Rows in frame: Breakfast · Cuban Ropa Vieja · 460 kcal; Lunch · Shakshuka with Eggs · 0.5× portion · 205 kcal; Dinner · Homemade Cream Cheese/Labneh · 67 kcal (no-photo → lilac placeholder tile); Snacks · Peruvian Ceviche · 290 kcal.
7. Tab bar (Plan active).

## Biggest deltas vs current app
Capture current `/planner` (deep link `suppr:///planner`) and diff. Apply the frame's: summary-card copy/structure (targets-hit headline + Generate/Adjust buttons), per-day header with the macro check-row, and the meal-row treatment (thumbnail + type eyebrow + serif name + portion chip + kcal + swap icon) to EACH day in the multi-day list. No-photo meals get a clean lilac/cream placeholder tile, not an empty box.

## Preserve (wired — never drop)
Multi-day structure, This week / Shopping list tabs, Generate, Adjust constraints, meal swap, portion scaling, move-meal (mobile), per-day + per-week macro totals, add-week-to-shopping.
