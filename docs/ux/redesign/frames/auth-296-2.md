# Auth / Sign-in chooser — Figma frame `296:2` spec

**Frame PNG (READ FIRST):** `docs/ux/redesign/frames/auth-296-2.png`
Source: Figma `B3UdOFup7ITersgNuoXh0l` node `296:2`.

## Tokens
White/cream bg · plum `#3B2A4D` · body `#6A6072` · Apple button = plum/near-black `#221B26` fill, white text · Newsreader for wordmark + headline; Inter for body/buttons.

## Layout (centered column)
1. **Close X** top-right.
2. **"Sloe"** wordmark — Newsreader, plum, centered.
3. **Headline** — Newsreader ~30px plum, two lines: "Cook what you love." / "*Still* reach your goals." ("Still" ITALIC). This is the brand positioning — keep exact.
4. **Subtitle** — "Create an account or log in — your recipes and plan sync everywhere." (Inter, grey, centered).
5. **Buttons** (full-width, rounded-full, ~56px, icon + centered label):
   - **Continue with Apple** — plum/near-black fill, white text, Apple glyph.
   - **Continue with email** — white/outline, envelope icon, plum text.
   - **(Google deferred — ENG-924):** the frame shows "Continue with Google", but per Grace's decision we ship **Apple + email only**. OMIT the Google button. Match the frame's button styling for the two we keep. (If a Linear note doesn't already exist for this divergence, add one.)
6. **Fine print** — "By continuing you agree to our Terms and Privacy Policy." (Terms + Privacy Policy underlined links).

## Biggest deltas vs current app
Capture current `/login` (deep link `suppr:///login`) and diff. Match: "Sloe" serif wordmark; the two-line positioning headline with italic "Still"; the sync subtitle; the Apple (plum fill) + email (outline) button treatment; the terms fine-print. Remove any email/password form fields from THIS screen — "Continue with email" leads to the email step (progressive disclosure), it is not an inline form here.

## Preserve (wired — never drop)
Apple Sign In flow (primary), continue-with-email flow, Terms + Privacy links, close/dismiss, any post-auth routing. Do not break the Supabase auth wiring.
