# Log sheet — canonical log entry path (moved)

**This file has moved.** The canonical, actively-maintained doc now lives at
[`docs/journeys/log-sheet.md`](./log-sheet.md) (renamed 2026-07-18).

Per the Genesis evergreen-doc convention (`docs/genesis/README.md` §4), a doc
that describes a permanent product primitive — not a dated event — drops its
date suffix. The `<LogSheet>` component is a permanent primitive (the single
canonical log entry point across web and mobile), so this file's content,
including the full phase history through 2026-06-27 (LogHub quick-action row),
moved to the undated path in the same change. Every inbound reference (README
table row, in-repo code comments) was updated to point at the new path in the
same change — nothing still resolves here on purpose.

Update any bookmark, external link, or note that still points at this
filename to [`docs/journeys/log-sheet.md`](./log-sheet.md) instead.
