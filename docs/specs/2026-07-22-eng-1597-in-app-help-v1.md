# ENG-1597 — In-app help scope pack (v1)

**Date:** 2026-07-22  
**Status:** Ratified defaults from leftovers wave → building v1 behind flag

## Scope (v1)

| Decision | Choice |
|----------|--------|
| First surface | Import → Verify → Save loop |
| Pattern | Contextual `?` hints / sheets — **not** a help center |
| Content | Short in-app copy keyed off `docs/journeys/import-recipe.md` |
| Flag | `in_app_help_import_v1` (default OFF) |
| Platforms | Web (`RecipeVerifyModal`) + mobile (`/recipe/verify`) |

## Out of scope (later)

- Full help center / searchable wiki mirror
- Other journey loops (Plan, Progress, onboarding)
- Linking out to the internal docs hub

## Shared module

`src/lib/help/importLoopHints.ts` — `IMPORT_VERIFY_HELP` topic consumed by both platforms.
