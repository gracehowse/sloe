# Backlog validation results — 2026-06-14

**Scope:** 33 launch-gating tickets (Gate 0/B/1/2 + a few Wave-3/4) that had coded work, each validated against its own acceptance criteria + the **real diff** (git diff/log + reading the code), not the status reports. **Method:** 33 parallel diff-grounded validators.

## Headlines
- At validation time the work was **uncommitted on `claude/eng-1099-tracker-tier`**; it has since been **committed** (`6f2eebe1`, `26933dda`, `13952d62`) and 21 items marked Done.
- **5 coded tickets are broken** — and **3 of those did the exact thing their brief warned against.** Post-commit re-check confirmed ENG-1134 + ENG-1132 are *still* broken (reopened); ENG-1108 is half-fixed (flagged); ENG-1102's CI wiring is invalid (reopened).
- **6 are genuinely not implemented** (match the open 'next 20').
- The board ran ahead of reality — Done was applied to broken/unapplied items. The reopens + **ENG-1190** (apply migrations to prod — was untracked) close that gap.

## Status summary
| Status | Count | Tickets |
|---|---|---|
| 🔴 Bug / regression | 5 | ENG-1102, ENG-1108, ENG-1132, ENG-1134, ENG-1162 |
| ⚪ Not implemented (genuine open work) | 6 | ENG-931, ENG-1051, ENG-1111, ENG-1122, ENG-1131, ENG-1133 |
| 🟡 Incomplete (mostly correct-but-was-uncommitted; small gaps) | 19 | ENG-33, ENG-123, ENG-774, ENG-776, ENG-805, ENG-973, ENG-1073, ENG-1077, ENG-1103, ENG-1104, ENG-1105, ENG-1106, ENG-1107, ENG-1109, ENG-1110, ENG-1123, ENG-1127, ENG-1161, ENG-1171 |
| ⚫ Ops, not code | 1 | ENG-667 |
| ✅ Correct & complete (code) | 2 | ENG-49, ENG-928 |

## 🔴 Bug / regression — must fix before it counts as done (5)

### ENG-1102 — rec: fix-then-close (confidence 9)
> **Post-commit status:** REOPENED → In Progress (5/5 core done; CI YAML invalid + targets prod)
- **Verdict:** Part A (local re-proof env vars + setup script) and the unconditional static CI contract guard are done, but the Part B CI automation has two real bugs (step-level `if:` gates on the `secrets` context which is invalid GitHub Actions syntax and breaks workflow validation; the live step targets the PRODUCTION Supabase URL, both explicitly forbidden by the brief), plus the required assertGate0VerifyAccountSafe unit test and the decision-doc 2026-06-14 re-proof record are missing. Ticket is marked Done but not correct/complete.
- **Gaps/bug:** 1) ci.yml:57 uses `secrets.*` in a step-level `if:` — invalid in GitHub Actions (only env/with/run see secrets); must mirror the env.* gate at ci.yml:88. 2) ci.yml:59 passes production NEXT_PUBLIC_SUPABASE_URL — AC requires a separate staging project and NEVER prod; no staging project exists. 3) Missing unit test for assertGate0VerifyAccountSafe (E2E_EMAIL + PERSONA_FORBIDDEN_EMAILS collision) — brief-required, absent. 4) docs/decisions/2026-06-11-gate0-db-security.md not updated with the 2026-06-14 5/5 re-proof + date (still claims 2026-06-11 + "live DB tests not runnable"). Minor: script now emits 7 checks (ENG-1103 additions) so prints 7/7 not the AC's 5/5.
- **Evidence:** .github/workflows/ci.yml:57 `if: ${{ secrets.GATE0_VERIFY_PASSWORD != '' }}` (secrets context invalid in step-level if — compare correct env.* gate at ci.yml:88, and the repo's own warning comment at ci.yml:33-36); ci.yml:59 uses production `secrets.NEXT_PUBLIC_SUPABASE_URL` (sam

### ENG-1108 — rec: fix-then-close (confidence 8)
> **Post-commit status:** Done but FLAGGED — numeric-cast fix only half-applied (:906 coerced, :193 stringifies); re-read pending
- **Verdict:** Both parts of the brief are implemented — the RPC-extension migration (Part A, resolving the prior review's verified_* metadata regression) and the web re-point to a single atomic RPC call (Part B) with the error result checked. But the web path introduces a NEW runtime regression: it now sends `amount` (a string, `""` for null-amount rows) through the RPC's `(v_ing->>'amount')::numeric` cast, which throws on `''`, aborting the atomic write for any owned recipe with a null-amount ingredient (common in imports) — so auto-verify silently fails and retries forever. Mobile avoids this because its amount is number/null. Required behavioral tests are also absent (only source-grep guards exist).
- **Gaps/bug:** (1) CRITICAL amount cast bug: RecipeDetail.tsx:906 passes string amount (`""` when null) into RPC `(v_ing->>'amount')::numeric` (migration ~line 58) → Postgres "invalid input syntax for type numeric" aborts the whole RPC for any owned recipe with a null-amount ingredient; old 2-step path never wrote amount, so this is a new regression. Fix: coerce to number-or-null (e.g. `ing?.amount ? Number(ing.amount) : null`, with NaN guard) before passing. (2) FatSecret branch (RecipeDetail.tsx:944-957) omits verified_at/confidence/source keys, so the RPC's `?` guard PRESERVES stale metadata instead of nulling it — HEAD's ZEROED_RECIPE_AGGREGATE nulled all three; minor re-verify edge case. (3) Required behavioral tests missing: no mock-supabase.rpc single-call/payload-shape test, no atomicity-reject test, no FatSecret-zeroing equality test, no RPC-level pgTAP — only 16 source-grep tests that cannot catch the amount bug. (4) Scope leak in same diff: badge label "Verified"→"Structured" (line 2343) plus inferAllergens/allRowsVerified additions are beyond ENG-1108.
- **Evidence:** supabase/migrations/20260614120200_eng1108_save_verified_metadata.sql (Part A: verified_* added with `case when p_recipe_update ? 'key'` guards, committed RPC file untouched) · src/lib/nutrition/saveVerifiedIngredientsRpc.ts (new web wrapper, checks error) · src/app/components/Re

### ENG-1132 — rec: fix-then-close (confidence 9)
> **Post-commit status:** REOPENED → Todo (still broken after commit: forbidden startOffset path persists)
- **Verdict:** Only the date line + confirmation copy were touched, and the date fix uses the brief's explicitly-forbidden `planCalendarDateForIndex(dayIdx, startOffset)` path — startOffset is unrestored live UI state (resets to 0 on plan load), so a plan saved with tomorrow/next-week still mis-dates to today+index, failing the core AC. The shared helper, start_date plumbing, the entire planned-vs-consumed read, and all required tests are absent.
- **Gaps/bug:** 1) Date math uses live UI startOffset (never restored from persisted start_date) → future-offset plans still mis-date (core AC fails). 2) DayPlan.startDate not plumbed; mapper drops fetched start_date. 3) Shared planDayCalendarDateKey helper never added (logic inlined). 4) Planned-vs-consumed read (2nd AC) entirely missing + no isFeatureEnabled gate. 5) Required tests (planDayCalendarDateKey unit, behavioural date_key assertion) absent.
- **Evidence:** apps/mobile/app/(tabs)/planner.tsx:4057 uses `dateKeyFromDate(planCalendarDateForIndex(dayIdx, startOffset))` — the EXACT approach the brief (step 3) flags as WRONG. startOffset is `useState(0)` (planner.tsx:653), mutated only by chip presses (lines 2982, 3925) and never restored

### ENG-1134 — rec: fix-then-close (confidence 9)
> **Post-commit status:** REOPENED → Todo (still broken after commit: leftover-skip retained → under-buy)
- **Verdict:** A working-tree implementation exists (helper + both call sites + one parity test) and uncommitted, but it triggers the exact trap the brief flagged in bold: both web and mobile still SKIP leftover slots while newly dividing the parent slot by recipe.servings, so the dominant case (a 4-serving recipe filling 1 parent + 3 leftovers) now buys base/4 instead of full base — an under-buy regression worse than the original over-buy.
- **Gaps/bug:** (1) Leftover-skip not removed on web (AppDataContext.tsx:1279) or mobile (planner.tsx:1950) — brief HOW steps 3 & 4 explicitly required removing it; with the new ÷servings this under-buys by a factor of `servings` whenever leftovers are distributed, violating AC "1 parent + 3 leftovers → full base yield, no regression". (2) Required unit tests missing: generateShoppingList.test.ts unchanged — none of the 4-serving→base/4, servings=1 unchanged, missing-servings unchanged, sum-across-2-slots, or the leftover-interaction non-regression test were added. The single new parity test (400g→100g) only exercises the helper math in isolation, so the suite is false-green and never touches the broken leftover path. (3) No mobile sim / web manual quantity verification per the brief. (4) Mobile diff also bundles an unrelated sibling change (planner.tsx:4054-4093, "Log as planned" date stamp).
- **Evidence:** src/context/AppDataContext.tsx:1279 (leftover-skip `!...leftoverOf` filter retained) + :1289 (now divides by servings via shoppingListIngredientMultiplier); apps/mobile/app/(tabs)/planner.tsx:1950 (`if (pm.leftoverOf) continue;` retained) + :1978 (now divides by servings); src/li

### ENG-1162 — rec: fix-then-close (confidence 9)
- **Verdict:** The deep-link routing was changed so suppr:///library returns a navigate action to /(tabs)/library, but the actual root-cause redirect identified in the brief — the empty-library useFocusEffect in library.tsx that bounces to /discover — was left untouched, so the audit's exact repro (fresh account, zero saved recipes) still lands on Discover. The fix does not resolve the bug.
- **Gaps/bug:** AC#1 fails: on an empty library, suppr:///library still redirects to Discover because library.tsx:163 router.replace("/(tabs)/discover") fires after the navigate lands — the brief's prescribed opt-out param (keep=1 read via useLocalSearchParams, gating the redirect) was never implemented. sitemap.md not updated. No new Maestro deep-link flow (12_library_deeplink.yaml absent; existing 12_library.yaml is a pre-audit tab-tap test). No RecipesTabChrome component test. No recipe-URL-wins test for suppr:///library?url=. Only the empty-library edge regresses; a user with >=1 saved recipe would land correctly.
- **Evidence:** apps/mobile/lib/deepLinkRouting.ts:56-64 (added library/discover to NAV_PATH_ALIASES → navigate action); apps/mobile/app/_layout.tsx:161-167 (navigate branch does router.replace(/(tabs)/library)); apps/mobile/app/(tabs)/library.tsx:161-167 (UNCHANGED useFocusEffect unconditionall

## ⚪ Not implemented (genuine open work) (6)

### ENG-931 — rec: finish (confidence 9)
- **Verdict:** ENG-931 (quiet "+" affordance on each search result row to log the primary serving instantly, while the row body still opens the preview) is not implemented on web or mobile. The result rows still route every tap into onPickResult → full preview; no per-row quick-log button, handler, or test exists. The uncommitted FoodSearchPanel diffs belong to other tickets (ENG-929 multi-add basket, micros sanitization, "Verified"→"Structured" relabel).
- **Gaps/bug:** Missing entirely: (1) the per-row "+" / quick-log affordance on web and mobile; (2) the handler that builds the primary-serving selection and calls onSelect/onAddMeal without opening the preview; (3) preserved row-body-tap → preview path is fine but the second "speed" doesn't exist; (4) tests asserting one-tap log of the default serving on both platforms. Ticket is correctly still in Backlog.
- **Evidence:** src/app/components/food-search/FoodSearchPanel.tsx:2547-2611 (result-row map: row body button onClick={() => onPickResult(item)} opens the full preview; trailing affordance is Icons.forward chevron + a custom-food "⋯" overflow menu — no quiet "+" quick-log button). apps/mobile/co

### ENG-1051 — rec: finish (confidence 10)
- **Verdict:** ENG-1051 (pantry/staples suppress-list for the shopping-list generator) is entirely unimplemented — no DB column, no shared-filter param, no web/mobile call-site wiring, no Settings or per-row management UI, no tests, no docs. Linear state is still Triage, consistent with no work done.
- **Gaps/bug:** Every acceptance criterion is unmet: (1) no `profiles.pantry_staples jsonb` migration; (2) no suppression on web (AppDataContext) or mobile (planner.tsx) regenerate paths; (3) no normalized/word-boundary matching logic; (4) no add/remove staples in web or mobile Settings; (5) no per-row "mark as staple / always have this" action on either ShoppingList.tsx (web) or shopping.tsx (mobile Swipeable); (6) no unit/component/Settings tests; (7) no doc note on the suppress-not-inventory scope. This is a beta-window P2 (Gate 2), not a launch-blocker per the brief's own downgrade.
- **Evidence:** No `pantry_staples`/`pantryStaples` token exists anywhere in src/, apps/mobile/, supabase/, tests/, or docs/ (grep returns 0 matches). `git log -S "pantry_staples" --all` returns 0 commits — never implemented in any commit. No migration file matching `*pantry*` in supabase/migrat

### ENG-1111 — rec: finish (confidence 10)
- **Verdict:** ENG-1111 (burn-aware measured-TDEE for the weekly check-in) is entirely unimplemented in the working tree — none of the 7 HOW steps were done and zero of the 10 acceptance criteria are met. Every cited file remains in its pre-ticket state; no estimator module, migration, flag, double-count guard, tests, or decision doc exist.
- **Gaps/bug:** Missing in full: (1) new pure module src/lib/nutrition/measuredTdee.ts (estimator + wear-completeness gate + median); (2) double-count mutual-exclusion suppressing computeActivityBonusKcal when measured wins; (3) `measured` branch + `measuredTdee`/confidence fields + `source: "measured"` in resolveMaintenance.ts; (4) activity_burn_by_day SELECT + estimator call + persistence in refreshAdaptiveTdee.ts; (5) measured source wired into buildWeeklyCheckinContent; (6) both call sites (mobile (tabs)/index.tsx, web NutritionTracker.tsx + AppDataContext.tsx); (7) supabase migration for measured_tdee/measured_tdee_confidence/measured_tdee_updated_at columns; (8) PostHog feature flag; (9) all required tests (measuredTdee.test.ts, resolveMaintenance double-count + measured-wins, weeklyCheckin/modal suites, flag-off regression); (10) docs/decisions doc + Notion/Linear mirror. Linear state is still Backlog.
- **Evidence:** No code exists: `grep -rl "measuredTdee/measured_tdee"` across the repo returns nothing; src/lib/nutrition/measuredTdee.ts, tests/unit/measuredTdee.test.ts, and docs/decisions/2026-06-14-measured-tdee-check-in.md all absent (ls: No such file). resolveMaintenance.ts:51 still `Main

### ENG-1122 — rec: finish (confidence 9.5)
- **Verdict:** ENG-1122 (web logged-meal edit to reach mobile parity) is unimplemented — no updateLoggedMeal hook primitive, no payload builder, no edit dialog, no kebab Edit item, no flag, no tests, and the stale "intentionally omitted" comment is still in place. Ticket is correctly in Backlog; the only adjacent change in the tree (mobile delete rollback) belongs to ENG-1123.
- **Gaps/bug:** Every part of the brief is missing on web: (1) updateLoggedMeal useCallback in useNutritionJournalState.ts with optimistic write + RLS-scoped .update().eq(id).eq(user_id) + rollback/toast; (2) src/lib/nutrition/nutritionEntryUpdatePayload.ts column-parity builder; (3) AppDataContext threading; (4) src/app/components/suppr/edit-meal-dialog.tsx with portion math + scaleLoggedMealFiberAndMicros + slot pills + flag-gated time field; (5) NutritionTracker/today-meals-section entry points; (6) web_logged_meal_edit flag; (7) deletion of stale comment at NutritionTracker.tsx:3557-3558; and all five required tests (hook, payload parity, portion-scaling, dialog component, mobile delete). The mobile reverse-parity delete-rollback sub-fix (step 7) IS present in the working tree but is attributed to ENG-1123, so it cannot be credited to this ticket.
- **Evidence:** No web edit primitive: `grep updateLoggedMeal src/` → NONE (hook src/context/appData/useNutritionJournalState.ts has only addLoggedMeal/removeLoggedMeal/copy/duplicate). Required new files absent: src/lib/nutrition/nutritionEntryUpdatePayload.ts, src/app/components/suppr/edit-mea

### ENG-1131 — rec: finish (confidence 9.5)
- **Verdict:** ENG-1131 (web Plan parity: move-meal, templates mount, portion stepper, smart suggestions, + moveMealInPlan fiber fix) is entirely unimplemented — the four affordances, the feature flag, the fiber fix, the hook extraction, the tests, and the doc corrections are all absent in HEAD and the working tree; the MealPlanner header comment still lists them as intentionally cut. Linear state is Backlog and matches reality.
- **Gaps/bug:** Nothing started. Missing: (1) plan-web-parity flag gating; (2) web Move action calling moveMealInPlan + setMealPlan; (3) mounting PlanTemplatesDialog (still dead code); (4) web portion stepper with fiber-inclusive rescale; (5) fiberG in moveMealInPlan totals reducer (leftoversPlanner.ts:286-294); (6) smart-suggestions ship-both-or-carve-out decision; (7) useMealPlanner() hook to avoid bloat; (8) all required unit/component/parity tests; (9) correcting the stale "Shipped (MVP)" smart-suggestions line in docs/mob-inspired-notes.md.
- **Evidence:** git diff HEAD on src/app/components/MealPlanner.tsx and src/lib/nutrition/leftoversPlanner.ts both empty; no recent commits for ENG-1131. Flag absent: grep "plan-web-parity" finds 0 refs. Move-meal absent: MealPlanner.tsx only has the carve-out comment (line 154) listing "Move di

### ENG-1133 — rec: finish (confidence 10)
- **Verdict:** ENG-1133 (expand the grocery categoriser lexicon to shrink the "Other" bucket) is completely unimplemented: category.ts remains the original 7-branch/~12-keyword function the ticket describes as the "before" state, none of the ticket-named keywords were added, no ordered keyword-table refactor was done, and the mandated tests/unit/groceryCategory.test.ts is absent.
- **Gaps/bug:** Every AC is unmet: (1) beans/lentils/tofu/herbs/spices/sauces still fall through to "Other"; (2) no expansion of existing roots; (3) no conversion to the ordered Array<{pattern,label}> first-match-wins structure; (4) no word-boundary patterns; (5) tests/unit/groceryCategory.test.ts (incl. the load-bearing SHOPPING_AISLE_ORDER-membership invariant) is missing; (6) docs not updated; (7) Linear issue not annotated re: deferred "reuse verified ingredient category" option. Ticket is in Backlog — no work started.
- **Evidence:** src/lib/planning/category.ts:1-13 is still the verbatim original 7-branch/~12-keyword implementation (chicken/milk/rice/spinach/berry/oil/chia → category, else "Other"); `git diff HEAD -- src/lib/planning/category.ts` is empty and last commit touching it is a9f78c92 ("Implement c

## 🟡 Incomplete (mostly correct-but-was-uncommitted; small gaps) (19)

### ENG-33 — rec: fix-then-close (confidence 9)
- **Verdict:** The flag-gated VAT-note plumbing on web is shipped and correct (PR #236), but ENG-33's actual remaining code deliverable — the US-automatic-tax fix (customer_update:{address:'auto'} when STRIPE_TAX_ENABLED=true) plus a test pinning it — is not implemented; the route still intentionally omits it and the existing test asserts the opposite. Verdict: incomplete (ticket correctly Blocked).
- **Gaps/bug:** AC#1 not done: checkout route does not pass customer_update:{address:'auto'} under the flag (route.ts:111 still says "intentionally omitted"; taxFields lacks it). AC#2 not done: no test pins the customer_update branch — stripeCheckoutRoute.test.ts:131 still asserts NOT to have it. The uncommitted route diff is unrelated EUR/GBP currency work (resolveProStripePrice), not the tax fix. AC#3 (dashboard Tax + tax_behavior=inclusive), AC#4 (flag flip in prod), AC#5 (UK/US manual checkout verify) are ops, hard-blocked on incorporation per ENG-199 dependency.
- **Evidence:** app/api/stripe/checkout/route.ts:111 (customer_update still "intentionally omitted"); :116-121 taxFields = {automatic_tax, billing_address_collection} only, no customer_update branch; tests/unit/stripeCheckoutRoute.test.ts:124-134 asserts payload.not.toHaveProperty("customer_upda

### ENG-123 — rec: finish (confidence 8)
- **Verdict:** The grandfather half (lifetime_pro never downgraded) is genuinely implemented and tested — but that's ENG-49's code, not ENG-123's. The ENG-123-specific Base→Free collapse half is only partially done: a migration script exists and the live base count is 0, but the required forward-only migration, the documented collapse decision, and Notion mirror are missing.
- **Gaps/bug:** (1) No docs/decisions/ note records the collapse policy (AC explicitly requires it); the 2026-04-27 strategic-direction doc only records Base's REMOVAL and explicitly defers the migration call to monetisation-architect. (2) The base→free cleanup is a one-off supabase/scripts/migrate_base_tier_to_free.sql, NOT a forward-only file under supabase/migrations/ as the AC specifies (though with 0 live rows it's a no-op, so a migration is arguably unnecessary — but then that should be stated, not left ambiguous). (3) The live count (0) is correct and I verified it, but it isn't recorded in any committed artifact. (4) No new test pins that a persisted 'base' resolves to 'free' (the floor tests are ENG-49's lifetime_pro tests; stripeTier.test.ts only gained select-mock plumbing). (5) Ticket still in Backlog; Notion Roadmap/Decisions mirror not done. Correct parts: base branches kept on BOTH web (tierFromPrice.ts) and mobile (purchases.ts) resolvers, webhook still maps legacy Base price IDs without throwing — the "wrong naive approach" trap was avoided.
- **Evidence:** supabase/scripts/migrate_base_tier_to_free.sql (one-off script, NOT in supabase/migrations/); live DB count via service role = 0 base rows ([{"count":0}], profiles?user_tier=eq.base); src/lib/stripe/updateProfileTier.ts + src/lib/tier/tierRank.ts + tests/unit/updateProfileTierFlo

### ENG-774 — rec: fix-then-close (confidence 8)
- **Verdict:** All four sub-changes are implemented with full web+mobile parity and are functionally correct: #3 (serving-no-mass flag) is committed + tested, and #1/#2 (macrosPerServing branch + picker "1 serving" mode) are present and correct in the uncommitted tree, scaling by serving count with no gram scaling and no commit-path data-loss. Marked incomplete because the new #1/#2 barcode scaling path has no dedicated tests (CLAUDE.md requires tests) and is not behind a feature flag despite comment #1 explicitly requiring flag-gate + sim-SEE for this visual change.
- **Gaps/bug:** 1) No test covers the new #1/#2 barcode servingNoMass→macrosPerServing×qty scaling (lookupBarcode/fetchProductByBarcode branch, scaleBarcodePreview, picker null-handoff); only the shared extractOffMacrosPerServing helper + servingNoMass detection are tested. 2) #1/#2 ship NOT behind a feature flag, contradicting comment #1's explicit "flag-gate + sim-SEE" plan and the CLAUDE.md visual-change flag rule (defensible since the branch only fires on a 0/100-rare row and the old path stays alive in the else, but it is a deviation). 3) No evidence of the required sim visual validation. Minor: committed product sets servingSizeG=grams(=serving count), a label-semantics oddity, not a macro-total bug.
- **Evidence:** #3 committed b21d7063, reconcileOne corrected-on-no-mass at src/lib/openFoodFacts/reconcilePer100g.ts:69-81; tests tests/unit/offReconcilePer100g.test.ts:110 + new extractOffMacrosPerServing test (14/14 pass). #1 uncommitted: apps/mobile/lib/verifyRecipe.ts:1795-1846 + src/lib/op

### ENG-776 — rec: fix-then-close (confidence 8)
- **Verdict:** A "Save a usual meal" CTA was added to the LogSheet Saved tab on both web and mobile (with refresh-token sync), but it routes into the same pre-existing >=2-slot-items gate the ticket exists to remove — so from the documented empty-state cold start it dead-ends in an alert/toast telling the user to go log 2 items first. The advertised create-from-scratch path (AC#2 "enter name + pick/scan a food") is not delivered: the save sheets have no in-sheet food picker.
- **Gaps/bug:** AC#2/#3/#4 unmet: from a fresh slot (no saved meals, <2 items logged) tapping the CTA shows "Log 2 or more items first" and never opens a composable sheet — the exact chicken-and-egg the ticket calls out ("saved meals never get seeded"). SaveMealSheet (mobile) and SaveMealDialog (web) cannot add foods from within — they only trim pre-seeded slot items, so even when opened they can't compose a new meal. No test covers the callback's behaviour, the gate, persist, or "new meal appears in picker -> tap to log". CTA copy ("Save a usual meal") also diverges from the AC's "Create a saved meal" but that's cosmetic.
- **Evidence:** Mobile CTA: apps/mobile/components/today/LogSheet.tsx SavedList (onCreateSavedMeal "Save a usual meal" button, empty-state + footer); wired in apps/mobile/app/(tabs)/index.tsx:6164-6168 -> openSaveMealSheetForSlot(activeMealSlot). That opener gates at index.tsx:1410-1416 ("if (sl

### ENG-805 — rec: fix-then-close (confidence 8)
- **Verdict:** The outstanding web half of ENG-805 IS implemented in the working tree and is behaviorally correct: cold-open is suppressed on both platforms, a non-blocking in-feed banner opens the dialog on demand, and the per-week dismissal key is byte-identical to mobile. But it is incomplete — every web test the brief requires (banner unit test, NutritionTracker suppression/render integration test, cross-platform dismissal-key parity assertion) is missing, failing the AC and the CLAUDE.md 'no feature complete without testing' non-negotiable.
- **Gaps/bug:** 1) BLOCKING: no web tests — brief mandates src/app/components/suppr/__tests__/weeklyCheckinBanner.test.tsx + a NutritionTracker cold-open-suppressed/banner-renders test + the explicit dismissal-key parity assertion; grep finds zero web test references. 2) Deviation from brief/CLAUDE.md: implemented as UNCONDITIONAL cold-open removal (no redesign_winmoment flag gate) on both platforms, removing the prescribed feature-flag safety net (the old cold-open path is gone, not preserved in an else). 3) Banner OPEN destination diverges (web→dialog, mobile→/weekly-recap route) — defensible given web has no /weekly-recap, dialog stays mounted. 4) Minor copy drift: web subcopy omits 'weight delta' vs mobile. 5) Ticket still Todo with an unanswered A-vs-B product question to Grace; the implemented option-A also contradicts the now-stale mobile WeeklyCheckinBanner docstring ('Mobile-only — web does NOT surface this banner').
- **Evidence:** Web cold-open suppressed: src/app/components/NutritionTracker.tsx:2077 (setWeeklyCheckinOpen(true) removed from eligibility effect). New web banner src/app/components/suppr/weekly-checkin-banner.tsx + dismissal src/lib/today/weeklyCheckinBannerDismissal.ts (key 'weekly_checkin_ba

### ENG-973 — rec: fix-then-close (confidence 8)
- **Verdict:** The Log-sheet promise row ("Barcode scan is free — always.") is implemented with genuine web+mobile parity via a correctly-shared constant, plus landing-copy updates. But the AC's second named surface — the paywall trust chip — is absent on BOTH platforms, and there are zero tests for the feature (CLAUDE.md "no feature complete without testing").
- **Gaps/bug:** 1) Paywall trust chip not implemented on mobile (apps/mobile/app/paywall.tsx) or web (upgrade-paywall-dialog.tsx) — an explicit AC surface. 2) No tests reference the promise (showBarcodeFreePromise / BARCODE_FREE / log-sheet-barcode-free-promise) anywhere. 3) BARCODE_FREE_FOREVER_DETAIL exported but never rendered (dead). 4) Promise is on the LogSheet search row, not the BarcodeScannerModal empty state the AC literally names (defensible reinterpretation, but worth noting).
- **Evidence:** src/lib/nutrition/barcodeFreePromise.ts:2-3 (new shared constants); apps/mobile/components/today/LogSheet.tsx:832-856 + src/app/components/suppr/log-sheet.tsx:829-839 (promise row, both platforms, gated by showBarcodeFreePromise && barcode?.onOpen); hosts wire it on: apps/mobile/

### ENG-1073 — rec: fix-then-close (confidence 9)
- **Verdict:** The flag-gate removal is implemented correctly across all 4 web+mobile sites, KNOWN_FLAGS, and the rewritten tests (all green), but two ACs fail: the stale flag-mentioning comments at the 4 sites (plus the shared helper + decision doc) were NOT rewritten and still describe a gate that no longer exists, and the hard time gate was violated — the cleanup was actioned 2026-06-14, 12 days before the mandated 2026-06-26 post-2-week-hold start date, so the no-regression precondition cannot have been met.
- **Gaps/bug:** (1) AC6 not met: all 4 const-site comment blocks still say "The flag gates ONLY the over branch / Else = raw {pct}%"; progress-hero-metric.tsx:194 JSX comment says "flag on"; adherenceDisplay.ts:36 and decision-doc:32 still describe the flag+else gate. These imply a gate the code no longer has (CLAUDE.md no-silent-deferral violation the brief explicitly warned against). (2) Hard time gate violated: brief/ticket say "do NOT start before 2026-06-26" (Linear due date); actioned 2026-06-14, only ~2 of the required 14 hold-days elapsed, so the "100% ≥2 weeks, no regression" precondition is unverifiable/unmet. (3) PostHog flag 714758 archival still pending (ops).
- **Evidence:** 4 sites de-gated correctly: progress-hero-metric.tsx:139, progress-average-adherence.tsx:99, apps/mobile ProgressHeroMetric.tsx:94, ProgressAverageAdherence.tsx:83 (overDisplay = adherencePct > 110 ? formatAdherenceHeadline(...) : null; isFeatureEnabled import removed in all 4). 

### ENG-1077 — rec: fix-then-close (confidence 8)
- **Verdict:** A per-micro plausibility clamp module was built and correctly wired into the WEB food-search preview for all vendors, but the MOBILE OFF path is left unsanitized — so the ticket's exact failure (absurd OFF potassium) still shows on iOS, breaking the non-negotiable web/mobile parity. Two bundled AC items (clamp inside the scalers + unify the ironMg 1dp/0dp rounding divergence) were also not done.
- **Gaps/bug:** 1) Mobile OFF/USDA preview paths not clamped (verifyRecipe.ts:1262, panel:768/726/751) — the primary OFF-potassium failure mode is unfixed on the primary surface; web↔mobile parity broken. 2) Rounding-unification AC skipped — ironMg still 1dp (parseOffMicros.ts:140) vs 0dp (scaleMicrosPerServing.ts:29); neither scaler touched. 3) Fixture test asserts sodiumMg, not potassiumMg per AC (minor).
- **Evidence:** New module src/lib/nutrition/microPlausibility.ts (ceilings + sanitizeMicrosPer100g); wired on web at src/app/components/food-search/FoodSearchPanel.tsx:498 (OFF), :573/:1486 (Edamam), :1525 (FatSecret). Mobile gap: apps/mobile/lib/verifyRecipe.ts:1262 builds OFF rows with RAW mi

### ENG-1103 — rec: fix-then-close (confidence 9)
- **Verdict:** The core DB fixes are present and correct — SUPPR_TEST_PREMIUM is deactivated by a committed migration and a DB-side per-user throttle was added to redeem_promo_code with all ENG-557/ENG-1043 machinery (SECURITY DEFINER, search_path, app.tier_writer GUC, tier_rank floor) preserved. But it's materially incomplete: no client error copy on web or mobile (throttle fails silently as a generic error — AC8), no CI grep guard (AC7), seed.sql still re-seeds the test promo active, threshold/error string diverge from AC (10/min + 'rate_limited' vs spec's 5/min + 'too_many_attempts'), and essentially all required tests are missing.
- **Gaps/bug:** AC8 not done: no 'rate_limited'/'too_many_attempts' user-facing copy in mobile messageForPromoError, web PromoCodeBlock messages map, or the web AppDataContext error allowlist — throttle collapses to rpc_error/generic (the exact silent-failure the brief forbade). AC7 not done: scripts/check-migration-drift.ts unchanged, so npm run ci won't catch a high-max_uses pro promo row. Step 2 not done: supabase/seed.sql:134 still inserts SUPPR_TEST_PREMIUM pro/100000 with no active=false. AC3 mismatch: implemented 10/min returning 'rate_limited' vs spec's 5/min returning 'too_many_attempts'. Tests almost entirely missing: no promoThrottleAndTestPremium migration-text test, no CI-guard unit test, no pricingPromoBlock/usePromoCode throttle-copy tests, no AppDataContext pass-through test. (Acceptable divergence: table named promo_redeem_throttle with a counter+window design instead of brief's promo_redemption_attempts row-per-attempt — functionally valid.) Note migration is uncommitted/unapplied, so AC1/AC2 can't be confirmed live until supabase db push --linked.
- **Evidence:** supabase/migrations/20260614120000_eng1103_deactivate_test_promo_throttle_redeem.sql:7-9 (deactivate), :44-66 (throttle before code lookup, 10/min, returns 'rate_limited'), :22-130 (full ENG-1043 RPC re-stated — security definer:25, search_path:26, app.tier_writer:103/127, tier_r

### ENG-1104 — rec: fix-then-close (confidence 8)
- **Verdict:** The six wrong-food micro rows (grapes/apple/brown-rice/greek-yogurt/sweet-potato/tomato) are corrected to accurate raw-USDA values and the three headline AC pins pass, so the data corruption is genuinely fixed — but the bake report on disk still contains the OLD wrong fdcIds/values (numbers were hand-spliced, violating the "from bake report, not hand-typed" AC), the required per-row physical-band plausibility test is absent (the audit test only string-matches comments), the backfill decision is unrecorded, and the audit doc isn't marked resolved.
- **Gaps/bug:** 1) AC "numbers from bake report not hand-typed" violated: scripts/out/generic-micros-bake.json was never re-baked with correct fdcIds (still shows 170959/169393/170051), corrected values hand-entered into .ts 4 min after stale report. 2) Required physical-band regression test missing — genericFoodMicrosAudit.test.ts asserts no numeric sodium/vitC bands and would not fail on a future wrong-food bake that updates the comment; the only band logic (microPlausibility.ts) is ENG-1077, a different ticket. 3) No explicit corrective-backfill decision recorded (brief step 5). 4) Audit doc P1-2/P2 not marked resolved (brief step 7).
- **Evidence:** src/lib/nutrition/genericFoodMicros.ts (diff HEAD): grapes→fdc 174683 sodiumMg=2, apple→fdc 171688 vitaminCMg=4.6/sodiumMg=1, brown-rice→169704, greek-yogurt→170894, sweet-potato→2346404, tomato→170457; all 36 rows pass physical bands (0 issues via tsx). scripts/out/generic-micro

### ENG-1105 — rec: fix-then-close (confidence 9)
- **Verdict:** The shared foodHistory bucket fix plus both Today-screen commit paths (mobile logHistoryItemToSlot, web logHistoryItem) are correctly implemented uncommitted, so Recents/Frequent/Eat-again re-logs now carry the full micro panel. But both FoodSearchPanel "Past logged"/recents shims (steps 3 + 5) are untouched — they still drop micros (mobile even keeps the sugarG:0/sodiumMg:0 literals), failing AC #2 and #7, and the required mobile/web behavioural + parity tests are absent.
- **Gaps/bug:** Two of five HOW steps unimplemented: (1) mobile FoodSearchPanel onSelectHistoryItem + recentFoods prop type don't carry/forward micros and still hardcode sugarG:0/sodiumMg:0 (L1207-1238, L287-299); (2) web FoodSearchPanel onSelectHistoryItem + recentFoods prop type omit micros entirely (L1386-1414, L296). Both leave the FoodSearchPanel "Past logged"/empty-query recents strip dropping sodium/sugar/vitamins on re-log — AC #2 and the web↔mobile parity AC #7 fail. Required tests missing: mobile behavioural (logHistoryItemToSlot→JournalMeal.micros/nutrition_micros), web behavioural (logHistoryItem→addLoggedMeal), and the parity test — only the shared compute-layer test exists.
- **Evidence:** DONE: src/lib/nutrition/foodHistory.ts (micros added to FoodHistoryItem L98-101; microsOf excludes caffeine/alcohol + drops <=0 L183-190; bucket microsSum/microsCount accumulate L290-295; finaliseBucket averages + attaches only when non-empty L345-353). apps/mobile/app/(tabs)/ind

### ENG-1106 — rec: fix-then-close (confidence 9)
- **Verdict:** Web path is fully wired end-to-end (migration, types, save→log→persist, export). But the mobile log-side re-map (the brief's CRITICAL step 7) was never implemented: both BuiltMealEntry→JournalMeal maps in (tabs)/index.tsx drop e.micros, so re-logging a saved meal on iOS still writes {} micros — the exact parity bug the ticket exists to fix, on the primary surface. Required client/export/parity tests are also missing.
- **Gaps/bug:** 1) Mobile re-map drops micros: add `if (e.micros && Object.keys(e.micros).length>0) jm.micros = e.micros;` at apps/mobile/app/(tabs)/index.tsx:1581 AND :1683 (JournalMeal.micros exists at nutritionJournal.ts:17 so it type-checks). Without it, iOS saved-meal re-log persists {} micros — fails the central AC "saved meal logs identical micros to source" and "holds on BOTH web and mobile". 2) Missing tests: source-vs-saved parity test + omits-when-absent case in savedMealsLogic.test.ts; itemToRow/rowToItem round-trip in savedMealsClient.test.ts; nutrition_micros in exportMeRoute.test.ts; mobile re-map guard. 3) (post-fix) needs supabase db push --linked by Grace; CLAUDE.md forbids MCP apply_migration.
- **Evidence:** apps/mobile/app/(tabs)/index.tsx:1568-1583 and 1670-1685 (BuiltMealEntry→JournalMeal re-maps copy e.source but NOT e.micros; `jm.micros` is never assigned anywhere in the file); apps/mobile/lib/nutritionEntryRow.ts:98,131 writes microsForWrite(meal.micros) → {} on mobile. Web is 

### ENG-1107 — rec: fix-then-close (confidence 9)
- **Verdict:** The production fix is implemented correctly and completely: all four web copy/duplicate call sites (plus web "Copy yesterday" via duplicateDay) re-anchor clones with reanchorMealEatenAt, giving date_key/eaten_at parity with mobile and no null-row regression; touched files typecheck clean. However the ticket's required test coverage is entirely missing — nutritionJournalBulkInsert.test.tsx was never extended with the re-anchor/date_key assertions the brief mandated, so the feature is not test-complete per its own acceptance criteria.
- **Gaps/bug:** Tests are missing entirely. The ticket's "## Tests" section explicitly required extending nutritionJournalBulkInsert.test.tsx to assert the web hook actually re-anchors: (1) copyMealToDate → date_key===target & dateKeyFromInstant(eaten_at)===target & localTime "08:15" preserved; (2) duplicateDay multi-meal + range variants per-target; (3) null-eatenAt fallback → date_key===target, eaten_at===null; (4) web "copy yesterday" → date_key===today. None added. The brief states the existing mealEatenAt.test.ts guard only proves the helper, "the new tests prove the web hook actually calls it" — that proof does not exist. AC "npm run typecheck && npm run test (web) green" cannot be the new tests since they're absent. No before/after screenshot manual validation evidence either.
- **Evidence:** src/context/appData/useNutritionJournalState.ts:14 (import reanchorMealEatenAt), L383-386 copyMealToDate, L417-420 copyMealToDateRange, L455-461 duplicateDay, L496-499 duplicateDayToDateRange — all wrap clones in reanchorMealEatenAt. Helper src/lib/nutrition/mealEatenAt.ts:140-15

### ENG-1109 — rec: fix-then-close (confidence 8)
- **Verdict:** The contrast fix is implemented and genuinely correct — every macro chip, aim line, and Log-usual pill on Today now clears WCAG AA 4.5:1 on web + mobile, light + dark (I recomputed all ratios). But it ships with NONE of the three required tests, and the slot -solid tokens the brief specified were never added (substituted with secondary-foreground, a passing but off-plan choice).
- **Gaps/bug:** (1) Zero tests added/updated — AC mandates a token-pair contrast census unit test (both platforms), a Playwright getComputedStyle e2e sweep, and a mobile snapshot assertion; no file references ENG-1109 and no contrast/Today test changed. Violates repo's testing non-negotiable. (2) Slot -solid tokens (--slot-*-solid web, SlotColors.*Solid mobile) were never created; pills use foreground-secondary instead — contrast-passing and consistent, but leaves the AC's "new slot -solid tokens exist across both platforms" unmet by design.
- **Evidence:** src/styles/theme.css:229/232/239 (+dark 493/496/502, @theme inline 678-688) add macro-*-solid; apps/mobile/constants/theme.ts:240-246 add proteinSolid/carbsSolid/fatSolid/fiberSolid; src/app/components/suppr/today-meals-section.tsx:164-179,230-234,710 (chips→-solid, pills/aim→for

### ENG-1110 — rec: fix-then-close (confidence 8.5)
- **Verdict:** Part A relabel (Verified→Structured) and Part B doc reconciliation (0.55/0.55/0.57) are implemented correctly with web/mobile parity and unchanged constants, but the ticket is incomplete: the in-code "Verified" comments the AC explicitly requires rewording are untouched on all three cited files, and two required tests are missing/partial (parity-label test not added; doc-vs-constant test under-asserts).
- **Gaps/bug:** 1) In-file doc comments still describe the chip/row as "Verified" at foodSearchRanking.ts:571/583/593, mobile FoodSearchPanel.tsx:1554/1556, and SearchResultConfidenceChip.tsx:23/39 — AC item unmet. 2) Required parity test extension to foodSearchConfidenceTierParity.test.ts not done (no cross-platform label-match assertion, no "Verified-gone" assertion). 3) verifyConfidencePolicy.test.ts doc-test does not import/assert the runtime constants (MIN_MATCH_CONFIDENCE/MIN_OFF_CONFIDENCE), doesn't assert "0.57" present, and scopes its negative check narrowly. Also: changes uncommitted and intermixed with unrelated ENG-929 basket + micros-plausibility edits in same files.
- **Evidence:** Label changed: src/app/components/food-search/FoodSearchPanel.tsx:1992 ("Verified"→"Structured"), apps/mobile/components/ui/SearchResultConfidenceChip.tsx:46 (defaultLabel.verified="Structured"); mobile panel renders chip w/o label override at apps/mobile/components/food-search/F

### ENG-1123 — rec: fix-then-close (confidence 9)
- **Verdict:** The common-case optimistic rollback + Alert is implemented (snapshot capture, idempotent restore, byDay dep), but the ticket's explicitly-flagged CRITICAL sub-requirement is unfixed: the HealthKit tombstone (markHealthSampleDeleted) still fires before the delete and is never cleared on failure, so a failed delete of an apple_health row leaves an orphan tombstone that suppresses a still-live sample — exactly the second silent inconsistency the brief warned against. No mobile test was added (AC requires one), and syncFailedRetryMessage was imported despite the AC forbidding it (Alert title is "Delete failed" not the specified "Couldn't delete").
- **Gaps/bug:** 1) AC#4 unmet: HealthKit tombstone not reordered after a confirmed delete (index.tsx:4309 still runs before :4311-4314) and not cleared in the error branch — orphan-tombstone data bug persists for apple_health rows. 2) AC#6 unmet: zero mobile test coverage (no deleteMealRollback.test.ts, journalSupabasePersistence.test.ts unchanged). 3) AC#5 violated: syncFailedRetryMessage added to mobile and imported (index.tsx:4325) contrary to the brief; Alert copy is "Delete failed" not "Couldn't delete". 4) Manual sim verification + mobile:typecheck/test-green not evidenced.
- **Evidence:** apps/mobile/app/(tabs)/index.tsx:4285 (removedMeal capture), :4318-4324 (idempotent restore), :4326 (Alert), :4330 (byDay in deps) — all uncommitted (git diff HEAD). BUT :4305-4314 HealthKit branch UNCHANGED from HEAD: markHealthSampleDeleted runs at :4309 BEFORE the delete at :4

### ENG-1127 — rec: fix-then-close (confidence 9)
- **Verdict:** Both Library filters were rewired onto recipeSearchMatch with creatorName added (fixes the primary mobile creator-search defect) and the inline title/creator filters removed, but the tag coverage is incomplete — only mealSlots is passed; dietaryFlags and source were dropped, so the AC's required "vegan" dietary-tag search fails — and none of the required web/mobile/shared tests were added.
- **Gaps/bug:** (1) dietaryFlags not passed to the matcher on either platform → "vegan"/"vegetarian" tag search fails, directly violating the AC ("searching by a seeded dietary term e.g. 'vegan' returns matching recipes on both platforms"). (2) source/sourceName not passed (brief + Discover both wire it). (3) Zero tests added or modified — brief required web (creator+tag), mobile (creator+tag), shared tags-only assertion, and an empty-query/category+sort regression guard; all absent, violating the "no feature complete without testing" rule. (4) Minor: web passes the pre-lowercased `q` local instead of raw `searchQuery` as the brief instructed (harmless/idempotent).
- **Evidence:** src/app/components/Library.tsx:164-174 and apps/mobile/app/(tabs)/library.tsx:243-253 both call recipeSearchMatch with only `tags: r.mealSlots ?? null` — dietaryFlags and source omitted. Brief (HOW step 1/2) required `tags: [...(r.mealSlots ?? []), ...(r.dietaryFlags ?? [])]` + s

### ENG-1161 — rec: fix-then-close (confidence 9)
- **Verdict:** A sticky bottom bar now renders a primary purchase CTA above the safe area, so the core "CTA in first viewport" fix is present — but the in-scroll CTA was NOT removed, so the same `primaryPurchaseCta` renders twice (L1155 + L1318), producing two filled CTAs and violating AC #7. The required feature-flag gate and both required tests are entirely absent.
- **Gaps/bug:** (1) Duplicate filled CTA: `{primaryPurchaseCta}` renders in-scroll (L1155) AND in sticky bar (L1318), unconditionally — violates AC #7 "only one filled CTA per screen" and brief step 5 (which said REMOVE the in-scroll block). (2) No feature-flag gate: missing `isFeatureEnabled("paywall-pinned-cta")` and the old footer-only `else` path — violates AC #8 and CLAUDE.md non-negotiable for visual changes. (3) No tests: neither `paywallPrimaryCtaAboveFold.test.ts` nor `paywallFirstViewport.test.tsx` created; no Maestro first-viewport capture — violates AC #10 and the Tests section. (4) No fresh after-fix sim screenshot (only the original BEFORE evidence shot exists). Note: restore was correctly demoted to a tertiary text link (AC #2 satisfied), scroll paddingBottom adjusted (L819, step 4), and all four CTA states preserved verbatim (AC #3).
- **Evidence:** apps/mobile/app/paywall.tsx — `primaryPurchaseCta` defined L991-1027 (shared CTA per step 1); rendered TWICE in the same unconditional render path: in-scroll at L1155 and inside the sticky bottom bar at L1318 (sticky View testID `paywall-sticky-primary-cta` L1305-1320). `git diff

### ENG-1171 — rec: fix-then-close (confidence 8)
- **Verdict:** The modal-over-consent stack is structurally resolved via the ENG-805 route the brief explicitly sanctioned as non-conflicting: the check-in modal now never cold-opens on web OR native — it only opens on tap of a new non-blocking in-feed WeeklyCheckinBanner — so it can never paint over the cookie banner. Functionally fixed with parity, but incomplete to the ticket's bar because none of the required tests exist and no test guards the single-overlay invariant.
- **Gaps/bug:** No ENG-1171-specific tests exist (weeklyCheckinSequencing.test.ts and verify-checkin-consent-sequence.spec.ts both missing); no test guards the "at most one overlay at cold open" invariant against regression. The cookieConsentRender.test.tsx additions cover only ENG-802 anchoring, not the consent/check-in sequence. Fix is uncommitted working-tree work bundled into a large multi-feature diff; ticket still in Backlog.
- **Evidence:** src/app/components/NutritionTracker.tsx:2077 ("// ENG-805 — never cold-open; the in-feed banner opens the dialog on tap." replacing the old setWeeklyCheckinOpen(true)); apps/mobile/app/(tabs)/index.tsx:2742 (same "never cold-open" change); new src/app/components/suppr/weekly-chec

## ⚫ Ops, not code (1)

### ENG-667 — rec: finish (confidence 8)
- **Verdict:** Code scaffolding (region→currency price resolver + checkout wiring + GBP fallback + unit test) is correctly built, but the ticket's acceptance criteria are Grace-only ops: no EUR SKU exists in Stripe or RevenueCat (env vars unset everywhere, project is pre-launch live:false), so EU users still see hardcoded GBP amounts; Stripe Tax inclusive mode and legal VAT-copy sign-off are also unconfirmed.
- **Gaps/bug:** No EUR price configured in Stripe/RevenueCat (env STRIPE_PRICE_PRO_*_EUR unset; no STRIPE_* in Vercel). Displayed price numeral is always hardcoded GBP (pricingTiers.ts) — regionCurrency only drives the checkout-button param + "coming soon" note, not the shown amount, so EU users are NOT quoted in €. Stripe Tax inclusive mode unconfirmed (STRIPE_TAX_ENABLED defaults false → VAT note suppressed). No legal sign-off recorded. No Playwright UK/EU checkout capture; stripeCheckoutRoute.test.ts not extended for the new currency branch (route currency routing only tested indirectly via the resolver unit test).
- **Evidence:** src/lib/stripe/resolveProStripePrice.ts:25-55 (new resolver, EUR→GBP fallback); app/api/stripe/checkout/route.ts:81-93 (detectRegion+currency wiring); app/pricing/page.tsx:257 ("EU pricing coming soon" gated on !isEurStripePricingConfigured); src/lib/landing/pricingTiers.ts:107-1

## ✅ Correct & complete (code) (2)

### ENG-49 — rec: fix-then-close (confidence 8)
- **Verdict:** The founding-cohort lifetime_pro comp path is fully built and committed (DB floor + GUC lockdown bypass + web/mobile resolvers + tests), and the EXPIRATION-downgrade trap the brief flagged is now guarded in updateProfileTierServiceRole (uncommitted) so no webhook can downgrade a lifetime_pro holder — both webhook rails route through it and all 46 tests pass. Creator-comp mechanism (RC granted entitlements) is decided and documented with the web-coverage caveat.
- **Gaps/bug:** No code gaps. Remaining items are ops/sign-off, correctly deferred: (1) FOUNDING100 promo_codes row not seeded — explicitly Grace's data action, not a migration; (2) end-to-end TestFlight redemption is a manual verification; (3) the ENG-49 floor guard (src/lib/stripe/updateProfileTier.ts, src/lib/tier/tierRank.ts, tests/unit/updateProfileTierFloor.test.ts) is uncommitted on branch claude/eng-1099-tracker-tier and needs committing.
- **Evidence:** supabase/migrations/20260611120200_redeem_promo_lifetime_pro_eng1043.sql (committed #384: tier_rank fn, GUC lockdown bypass, never-downgrade floor in redeem_promo_code); src/lib/supabase/serverAnonClient.ts:75,104 + src/types/recipe.ts:13 (web lifetime_pro→pro); apps/mobile/lib/p

### ENG-928 — rec: fix-then-close (confidence 8)
- **Verdict:** ENG-928 (surface time-of-day go-to foods atop the empty Log sheet) is implemented end-to-end and at parity: a shared slot-aware ranking module feeds a first-class "Go-tos" tab that auto-activates in the empty LogSheet on both mobile and web, slot derived from the current hour, with a passing unit test. Complete and correct; only minor gaps (no feature flag, header copy diverges from the Julienne brief, Linear still Backlog).
- **Gaps/bug:** Minor, non-functional: (1) the new "gotos" browse tab + its auto-default-active behaviour is a structural/visual change but is NOT behind isFeatureEnabled — CLAUDE.md mandates flag-gating such changes (web log-sheet uses flags elsewhere but not for goTos). (2) Copy/token diverges from the ticket's Julienne brief — implemented header is "Go-tos for this meal" (uppercase, textSecondary, 11px) vs. brief's calm "Around now"/"Your usual mornings" in textTertiary. (3) Linear state still "Backlog" — work lives only in the uncommitted working tree, untracked (?? slotGoToFoods.ts + test), nothing committed/mirrored. No data-loss/regression found; imports resolve on both platforms.
- **Evidence:** New module src/lib/nutrition/slotGoToFoods.ts:17-29 (computeSlotGoToFoods: filters byDay to active MealSlot, ranks via computeFrequentMeals, count>=2 floor); mobile wiring apps/mobile/app/(tabs)/index.tsx:4456-4466 (logSheetGoTos memo on slotForHour-derived activeMealSlot) + :605

## Post-validation actions taken (2026-06-14)
- **Reopened** ENG-1134, ENG-1132 (Done→Todo — verified still broken after commit), ENG-1102 (Done→In Progress — CI YAML invalid).
- **Flagged** ENG-1108 (re-verify the amount→numeric-cast path before close).
- **Filed ENG-1190** (Urgent/launch-blocker): apply the committed Gate-0 + base-tier migrations to prod (`supabase db push`) + re-run `verify:gate0` — was untracked.
- Each of the 5 bugs carries a code-grounded fix comment on its ticket.

## Note — items validated do NOT include the watch-athlete persona bugs
ENG-1188 (maintenance ignores activity_level → sedentary 1.2) and ENG-1189 (adaptive never engages despite met thresholds) were filed + DB-verified separately (Gate 1, Progress tab) — see their tickets.

*Untracked, for review. Verdicts are diff-grounded as of the working-tree state pre/post the 2026-06-14 commits.*