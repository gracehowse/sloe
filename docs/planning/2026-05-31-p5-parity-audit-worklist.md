# Redesign P5 — web↔mobile parity audit worklist

**Source:** 11-lane adversarial parity audit, 2026-05-31 (51 agents). Each candidate gap was re-verified against current code; 7 phantoms dismissed.
**Result:** 33 confirmed gaps (1 P1, 16 P2, 16 P3). Audit raw output was ephemeral; this is the durable record.

All web changes ship behind the named flag with the old path alive in the `else`, per the feature-flag non-negotiable. Parity = match the mobile reference exactly (platform-native deviations allowed: no haptics on web).

**2026-07-22 addendum:** gaps #2, #3, and #30 quote `today_log_usual_row_v2` as a live `isFeatureEnabled` read in `today-meals-section.tsx` — that flag was permanently collapsed on (ENG-1651), so the code citation is now stale (the read no longer exists; behavior is unconditional). The evidence below is left as the original point-in-time audit record, not updated in place.

## Confirmed gaps

### [1] P1 · Today win-moment celebration is entirely unwired on web — useWebWinMoment + WinMomentPlayer never mount on the live Today surface (NutritionTracker.tsx)
- **dimension:** winmoment-surface-parity
- **flag:** `redesign_winmoment`
- **web file:** `src/app/components/NutritionTracker.tsx`
- **evidence:** CURRENT CODE confirms the gap on every axis:

1. NutritionTracker.tsx IS the live web Today surface: mounted via `dynamic(() => import("./components/NutritionTracker.tsx")...)` in src/app/App.tsx:32-33 and rendered at App.tsx:445 and :581.

2. ZERO win-moment wiring inside NutritionTracker.tsx: `grep -n "WinMoment|winMoment|win_moment|win-moment|detectWinMoment|winSnapshot|redesign_winmoment|useWebWinMoment|confetti|celebration|pulse" src/app/components/NutritionTracker.tsx` returns nothing (exit 1). No snapshot built, no hook called, no player mounted, no `redesign_winmoment` gate.

3. The web hook exists, is built, but has NO live call site: `grep -rn "useWebWinMoment" src/ app/` returns only its own definition (src/lib/preferences/useWebWinMoment.ts:77 export) + doc-comments in 3 other files. `grep -rln "detectWinMoment"` on web returns only the hook itself + the shared math module — never a screen.

4. The web WinMomentPlayer's only LIVE mount is the weight win, a DIFFERENT landmark: ProgressDashboard.tsx:2038 mounts `<WinMomentPlayer celebration="goal-hit" .../>` for `weightWinActive`. The Today calorie/macro/streak landmark (detectWinMoment) has zero web coverage.

5. MOBILE fires it on Today: apps/mobile/app/(tabs)/index.tsx:2965-2987 builds `winSnapshot` (consumed: totals.calories, goal: effectiveCalorieGoal, streak: protectedStreakLength, macros protein/carbs/fat current+target); :2988-2997 calls `useWinMoment({ snapshot, dayKey, isToday, ready: hydrated })`; :5382-5389 mounts `<WinMomentPlayer celebration={winCelebration} onComplete={onWinComplete} fullBleed testID="today-win-moment"/>`.

6. The fix is well-scoped — NutritionTracker.tsx ALREADY computes every snapshot input: `totals` (calories/protein/carbs/fat, NutritionTracker.tsx:1677/2428), `effectiveCalorieTarget` (:2010), `effectiveMacroTargets` (:2012), `protectedStreakLength` (:722 via computeProtectedStreak), and `selectedDateKey === todayKey()` (:690) for isToday + dayKey.

7. TodayAtAGlance.tsx is dead code: `grep -rln "TodayAtAGlance"` returns only the file itself — referenced by no page, App, or test. Its ungated canvas-confetti is not the live web Today win moment and is not parity.
- **fix:** In NutritionTracker.tsx, mirror the mobile wiring (apps/mobile/app/(tabs)/index.tsx:2965-2997, 5382-5389):

1. Import `useWebWinMoment` from "../../lib/preferences/useWebWinMoment.ts" and `WinMomentPlayer` from "./ui/win-moment-player.tsx".

2. Build the snapshot from the EXISTING web vars (note the web var is `effectiveCalorieTarget`, NOT mobile's `effectiveCalorieGoal`):
   const winSnapshot = useMemo(() => ({
     consumed: totals.calories,
     goal: effectiveCalorieTarget,
     streak: protectedStreakLength,
     macros: {
       protein: { current: totals.protein, target: effectiveMacroTargets.protein },
       carbs:   { current: totals.carbs,   target: effectiveMacroTargets.carbs },
       fat:     { current: totals.fat,     target: effectiveMacroTargets.fat },
     },
   }), [totals.calories, totals.protein, totals.carbs, totals.fat, effectiveCalorieTarget, effectiveMacroTargets.protein, effectiveMacroTargets.carbs, effectiveMacroTargets.fat, protectedStreakLength]);

3. const { activeCelebration, onCelebrationComplete, pulse } = useWebWinMoment({ snapshot: winSnapshot, dayKey: selectedDateKey, isToday: selectedDateKey === todayKey(), ready: <today-hydrated flag> });

4. Render at the screen root: {activeCelebration ? <WinMomentPlayer celebration={activeCelebration} onComplete={onCelebrationComplete} fullBleed testID="today-win-moment" /> : null}

5. Thread the returned `pulse` boolean into the calorie ring stroke for the ~200ms green pulse (WEB_WIN_PULSE_MS), matching the web hook's documented contract.

The whole path is already flag-gated inside useWebWinMoment (returns null celebration + no pulse when `redesign_winmoment` is off), so the flag-off else is preserved with no extra branch. Separately, delete the orphaned src/app/components/TodayAtAGlance.tsx (referenced nowhere) — do NOT treat its ungated canvas-confetti as the web Today win moment; it bypasses the shared detectWinMoment landmark math and the once-per-day reservation, so it is a divergent/weaker trigger, not parity.

### [2] P2 · Web meal-management surface lacks the branded SupprMark + thumbnail/title/macro header (no `redesign_branded_sheets` flag on web)
- **dimension:** branded-sheets-parity
- **flag:** `redesign_branded_sheets`
- **web file:** `src/app/components/suppr/today-meals-section.tsx`
- **evidence:** CURRENT web today-meals-section.tsx — the meal "more actions" affordance is a bare Radix DropdownMenu with NO brand mark, NO thumbnail, NO macro header: lines 485-551 (`<DropdownMenu>` → `<DropdownMenuContent align="end">` with only three text items: "Copy to another day…" 496-498, "Share meal" 499-541, "Delete" 542-549). The only `isFeatureEnabled` call in the file is line 260 `today_log_usual_row_v2` — `redesign_branded_sheets` is never referenced. Confirmed `grep -rn "redesign_branded_sheets" src/` returns ZERO hits anywhere in web. The dialogs this surface feeds use bare chrome too: copy-meal-dialog.tsx line 97-102 (`<DialogHeader><DialogTitle ...>Copy meal to another day</DialogTitle>`), and the usual-meal picker Dialog in this same file (711-718) uses bare DialogHeader/DialogTitle/DialogDescription with no SupprMark.

MOBILE reference TodayMealsSection.tsx — the gated branded surface genuinely exists and renders exactly what web lacks: line 544 `const brandedSheets = isFeatureEnabled("redesign_branded_sheets")`; long-press dispatches to it at lines 1010-1016 (`if (brandedSheets) { …; setActionSheetMeal(m); return; }`) with the prior `Alert.alert` kept alive in the else (1017-1029). MealActionSheet (278-386) mounts: the quiet brand mark `<SupprMark size={16} … foreground={colors.textTertiary} />` (310-312, imported line 40); a header the stock sheet can't render — thumbnail `mealRowImageUrl(meal)` (288, 316-326), `recipeTitle` headline (328-330), and a kcal·P/C/F macro preview line `{macroDetail}` (331-333) fed by `formatMacroTrailer({calories,protein,carbs,fat})` at 1394-1399 — over a cream `colors.background` surface (304) with a grabber (`mas.handle`, 306).

The web SupprMark component the fix would mount already exists: src/app/components/ui/suppr-mark.tsx is present (4330 bytes) and is already imported elsewhere on web (desktop-sidebar.tsx, today-brand-bar.tsx as `SupprPlateMark`). So nothing blocks adding the branded variant on web.
- **fix:** Add `redesign_branded_sheets` to the web flag set via `isFeatureEnabled` (already imported at line 31). Gate a branded variant of the meal "more actions" surface in today-meals-section.tsx. Keep the desktop DropdownMenu interaction pattern — do NOT force a grabber bottom sheet on web — but when the flag is ON, apply the branded header chrome: mount `SupprMark` (from src/app/components/ui/suppr-mark.tsx, size ~16, muted/tertiary foreground) plus a header row showing the meal thumbnail (`mealRowImageUrl(meal)`, already imported line 32), `meal.recipeTitle`, and a `formatMacroTrailer({calories,protein,carbs,fat})` macro line (already imported line 15) — matching mobile's MealActionSheet header at lines 314-335. The cleanest web shape is a small branded Dialog/Popover (or a header band inside the DropdownMenuContent) that the kebab opens, with the same Edit/Copy/Share/Delete actions. Leave the current bare DropdownMenu alive in the `else` branch. Also apply the branded header to copy-meal-dialog.tsx (line 97) and the usual-picker Dialog (line 711) under the same flag. Prototype reference (mobile): docs/prototypes/2026-05-31-design-direction/surface-meal-actionsheet.html. Ship behind the flag per CLAUDE.md visual-change rule, with before/after web screenshots, and mirror mobile's mealActionSheetBranded test coverage on web. Note: mobile has NO "Edit entry" action on web's current menu (web kebab lacks Edit; mobile sheet row "Edit entry" 338-345 maps to onLongPressEdit) — decide whether web's branded variant should add an Edit action or remain Copy/Share/Delete to preserve current web behaviour.

### [3] P2 · Web copy-meal flow has no branded chrome (SupprMark + thumbnail/macro identity) that mobile's branded MealActionSheet copy action carries under redesign_branded_sheets
- **dimension:** branded-sheets-parity
- **flag:** `redesign_branded_sheets`
- **web file:** `src/app/components/suppr/copy-meal-dialog.tsx`
- **evidence:** WEB copy-meal-dialog.tsx imports only Dialog/DialogContent/DialogDescription/DialogFooter/DialogHeader/DialogTitle + Button (lines 4-12) — no SupprMark import. Header is plain: DialogTitle "Copy meal to another day" + DialogDescription `"${mealLabel}" — pick a day...` (lines 97-102). No thumbnail, no kcal·P/C/F macro row, no brand mark anywhere in the 173-line file. WEB invocation path: meal-row dropdown DropdownMenuItem "Copy to another day…" (today-meals-section.tsx:496-498) → setCopyMealTargetId (NutritionTracker.tsx:2553) → renders plain CopyMealDialog (NutritionTracker.tsx:3135-3158). The web DropdownMenu (today-meals-section.tsx:485-498) — the true structural counterpart of mobile's sheet — is a bare shadcn dropdown with no SupprMark and no meal identity. MOBILE MealActionSheet carries SupprMark (TodayMealsSection.tsx:310-312) + a header the native sheet "cannot render: thumbnail + name + macro preview" (lines 314-335: thumb 316-326, recipeTitle 328-330, macroDetail line 331-333), and the "Copy to another day" action lives inside that branded chrome (lines 346-353), gated by redesign_branded_sheets (lines 544, 1011). FLAG ASYMMETRY: `grep -rln redesign_branded_sheets src apps/mobile` returns only mobile files (TodayMealsSection.tsx, RootErrorBoundary.tsx, two tests) — zero references in src/. So web has no flag-on branded path at all.
- **fix:** The cited audit framing is slightly off: mobile's brand mark + thumbnail/title/macro header lives on the MealActionSheet (the menu the user opens before choosing Copy), whose web counterpart is the meal-row DropdownMenu in today-meals-section.tsx:485-498, not the copy-meal-dialog. The dialog is the step after Copy is chosen. The real, verifiable gap is that the web copy flow has zero branded chrome under redesign_branded_sheets (the flag does not exist in src/ at all). Two acceptable fixes: (A) Add the SupprMark + compact meal-identity row (thumbnail + kcal·P/C/F) to CopyMealDialog's header behind isFeatureEnabled("redesign_branded_sheets") from @/lib/analytics, keeping the existing plain DialogHeader (lines 97-102) in the flag-off else branch — this requires also threading thumbnail URL + macros into the dialog's Props (currently only mealLabel/sourceDayKey are passed, NutritionTracker.tsx:3142-3143). (B) The cleaner structural parity: render a branded meal-action surface on web (header with SupprMark + thumbnail + macro line wrapping the dropdown items) matching the mobile sheet, behind the same flag. Prefer (A) for the dialog-level title fix the audit names, but note the deeper parity is at the dropdown. Either way wire isFeatureEnabled and leave the current plain path alive in the else branch per the feature-flag rule.

### [4] P2 · Web error-recovery screens have no flag-gated branded variant to match mobile RootErrorBoundary.renderBranded()
- **dimension:** branded-sheets-parity
- **flag:** `redesign_branded_sheets`
- **web file:** `src/app/components/FeatureErrorBoundary.tsx`
- **evidence:** MOBILE (apps/mobile/components/ui/RootErrorBoundary.tsx) gates a branded recovery screen behind the flag: line 225 `branded = isFeatureEnabled("redesign_branded_sheets");` and line 229 `return branded ? this.renderBranded() : this.renderLegacy();`. renderBranded() (lines 82-148) mounts the brand mark at line 98 `<InlineBrandMark size={44} ring={c.text} />` (the SupprPlateMark ring motif per the line 44-46 comment), uses tokens `Colors.dark` (line 83), `Accent.primary`/`Accent.primaryForeground` (lines 124,131), `Type.title/body/headline/caption`, `Radius.xl` (line 127), and calm copy (lines 106,116-117). renderLegacy() (lines 150-217) is the hardcoded-hex flag-off fallback (e.g. `backgroundColor: "#0a0a0f"` line 158).

WEB (src/app/components/FeatureErrorBoundary.tsx) has NO flag import or call — there is no `isFeatureEnabled` and no `redesign_branded_sheets` reference in the file. render() returns a single bare slate card: line 53 `<div className="flex flex-col items-center justify-center p-8 rounded-2xl border border-slate-200/60 dark:border-slate-800/60 bg-white/70 dark:bg-slate-900/40 m-4">` with NO SupprMark/brand mark and no branded variant. A repo-wide grep for `redesign_branded_sheets` across src/ and app/ returns ZERO matches. The app-level boundaries are likewise unbranded slate cards: app/error.tsx line 28 `bg-white/70 dark:bg-slate-900/40` with plain `<h1>Something went wrong</h1>` (line 29) and no SupprMark; app/global-error.tsx line 23 identical slate card, `<h1>Suppr ran into a problem</h1>` (line 24), no brand mark. Web `<SupprMark>` exists (src/app/components/ui/suppr-mark.tsx) but is not used in any error path.
- **fix:** Under the web `redesign_branded_sheets` flag, add a branded fallback that mirrors mobile renderBranded(): mount the existing web `<SupprMark>` (src/app/components/ui/suppr-mark.tsx) plus brand-token chrome (brand background tone, primary CTA, calm "Something went wrong / The team has been notified / Try again" copy), keeping the current bare slate card alive in the flag-off `else` path. Since FeatureErrorBoundary is a class component, read flag state via a small functional wrapper (e.g. `useFlag("redesign_branded_sheets")` from @/lib/analytics) that passes a `branded` boolean prop into the class, rather than calling a hook inside the class. Apply the same flag-gated branded treatment to app/error.tsx and app/global-error.tsx so the app-level crash screens match the mobile branded recovery grammar; gate behind the same flag with the existing slate cards retained as the flag-off fallback. Per visual-validation rules, capture before/after on web behind the flag before shipping.

### [5] P2 · Web Today calorie/macro ring has no win-moment — useWebWinMoment + WinMomentPlayer are built but wired into zero Today components
- **dimension:** calorie-ring-win-analog
- **flag:** `redesign_winmoment`
- **web file:** `src/app/components/suppr/today-hero-stats.tsx (host) + src/app/components/NutritionTracker.tsx (composition root) + src/app/components/suppr/daily-ring.tsx (ring)`
- **evidence:** MOBILE (apps/mobile/app/(tabs)/index.tsx) wires the full win-moment: import useWinMoment L185, import WinMomentPlayer L186; winSnapshot built L2965-2987 ({consumed: totals.calories, goal: effectiveCalorieGoal, streak: protectedStreakLength, macros:{protein/carbs/fat {current,target}}}); useWinMoment called L2988-2997 ({snapshot, dayKey, isToday, ready: hydrated}); overlay rendered L5382-5389 (<WinMomentPlayer celebration={winCelebration} onComplete={onWinComplete} fullBleed testID="today-win-moment" />); success haptic in use-win-moment.ts L127 (Haptics.notificationAsync(Success)).

WEB analog EXISTS but is wired into ZERO Today components. grep across src finds useWebWinMoment ONLY in doc-comments of 3 sibling lib files (useWebWinMoment.ts self, useSettingsWinMoment.ts L9, winMomentLandmark.ts L23) — never imported/called by a component. The hook (src/lib/preferences/useWebWinMoment.ts) is complete: same shared detectWinMoment, redesign_winmoment gate (L83), returns activeCelebration/onCelebrationComplete/pulse (L68-75), WEB_WIN_PULSE_MS=200 (L30), prefers-reduced-motion honoured (L50-56,117). Web WinMomentPlayer (src/app/components/ui/win-moment-player.tsx) is wired ONLY in ProgressDashboard.tsx L65,L2038 for the unrelated weight-low landmark.

HOST today-hero-stats.tsx renders DailyRing (L128-138) + TodayHeroRing (L44) with NO win/pulse wiring. COMPOSITION ROOT NutritionTracker.tsx renders <TodayHeroStats> at L2342 but grep for useWebWinMoment|WinMomentPlayer|winSnapshot|activeCelebration|redesign_winmoment|pulse returns NONE. RING daily-ring.tsx has no pulse/win prop (props L91-106); stroke is fixed isEmpty?ring-bg:isOverBudget?destructive:success (L264-270) — no win-pulse branch.

CORRECTION to candidate: the three mobile *WinMoment* tests (settingsWinMoment, planWinMomentParity, logWeightSheetWinMoment) do NOT import the Today useWinMoment hook or shared detectWinMoment math (they cover settings/plan/weight). The shared detectWinMoment landmark math is untested on BOTH platforms — so "mirror the mobile use-win-moment tests" is inaccurate; the fix should add a new web hook test AND ideally a shared-math test since neither exists.
- **fix:** Wire the existing web analog into the Today ring behind redesign_winmoment — reuse useWebWinMoment, do NOT rebuild detection. (1) In NutritionTracker.tsx (web Today composition root, where <TodayHeroStats> renders at L2342), call useWebWinMoment with a snapshot mirroring mobile's winSnapshot: {consumed: totals.calories, goal: effectiveCalorieGoal, streak: protectedStreakLength, macros:{protein:{current,target},carbs:{...},fat:{...}}}, plus dayKey, isToday, ready/hydrated. (2) Thread the returned `pulse` down through TodayHeroStats → TodayHeroRing/DailyRing; add a `pulse` (or `win`) boolean prop to daily-ring.tsx (it currently has none) and, when true, swap the success-band stroke to the reserved win colour for WEB_WIN_PULSE_MS — the hook already suppresses pulse under prefers-reduced-motion. (3) Mount <WinMomentPlayer celebration={activeCelebration} onComplete={onCelebrationComplete} fullBleed/> as a pointer-events-none overlay on the Today screen, mirroring mobile index.tsx L5382 (gate the mount on activeCelebration != null; the hook already gates on the flag). (4) Add a vitest for useWebWinMoment AND a shared-math test for detectWinMoment (NOTE: contrary to the candidate, no mobile test currently covers the Today hook or shared math — neither platform tests detectWinMoment, so add the shared test once and enforce parity from it). Keep the entire path inert when redesign_winmoment is off so static behaviour is preserved.

### [6] P2 · Web macro-detail empty state has no "Log a meal" CTA, so the design_system_colours blue commit-CTA recolour never reaches web
- **dimension:** colours-cta-parity
- **flag:** `design_system_colours`
- **web file:** `src/app/components/MacroDetailPanel.tsx`
- **evidence:** CURRENT web MacroDetailPanel.tsx gates only one flag: line 102 `const redesignElevation = isFeatureEnabled("design_system_elevation");` — there is NO `isFeatureEnabled("design_system_colours")` anywhere in the file. The empty-state branch (lines 153-173) has two paths and NEITHER renders an action button: the redesignElevation path (155-168) renders a Salad icon chip + `<p>No meals logged yet` + `<p>Log a meal to see your {macro} broken down here.` (pure text, no button, no onClick); the else (170-172) renders a single `<p>No meals logged for this day.`. No `<button>`, no route-to-Today onClick, no CTA fill to recolour.

MOBILE reference proves the affordance exists: NutritionDetailEmptyState.tsx line 79 `const redesignColours = isFeatureEnabled("design_system_colours");` and line 81 `const ctaColor = redesignColours ? Accent.primary : ctaColorLegacy;` — flag ON recolours the CTA fill to blue Accent.primary, OFF uses the legacy macro hue. Lines 120-142 render that CTA as a filled PressableScale (`backgroundColor: ctaColor`) with label + Plus icon. macro-detail.tsx lines 174-184 wire it: `ctaLabel="Log a meal"`, `ctaIcon={Plus}`, `ctaColorLegacy={config.color}` (the saturated macro hue), `ctaA11yLabel="Log a meal on Today"`, `onPress={() => router.push("/(tabs)")}`. So mobile's macro-detail empty state offers a blue (flag-on) / macro-hue (flag-off) "Log a meal" button routing to Today; web offers only static text under either flag.
- **fix:** Add a "Log a meal" commit CTA to the web MacroDetailPanel empty-state branch (inside the redesignElevation block, after the subtitle `<p>` around line 167). Render a `<button>` with the Plus icon (lucide-react) + "Log a meal" label, gating only the FILL behind the new flag: `const redesignColours = isFeatureEnabled("design_system_colours");` then `className={redesignColours ? "bg-primary text-primary-foreground" : ""}` with `style={!redesignColours ? { backgroundColor: config.cssVar } : undefined}` to mirror mobile's `ctaColorLegacy={config.color}` (macro hue) when the flag is off and blue Accent when on. Because this surface is a Dialog (DialogContent, lines 138-140) rather than a full screen, wire onClick to first call `onClose()` (close the dialog) then navigate to Today's log entry point via `next/navigation` useRouter().push("/") (mobile routes to "/(tabs)" / Today). Keep a flag-off path alive (hue-filled CTA, matching mobile's legacy behaviour) so the visual change ships gated per the feature-flag rule. The shared component-extraction pattern mobile uses (NutritionDetailEmptyState) is worth mirroring on web if MacroDetailPanel's empty state grows, but a single gated CTA block is sufficient here. Add/extend the existing macro-detail-empty test to assert the CTA renders and that onClick closes + routes. If product instead decides a modal should stay CTA-less (web macro-detail is a dialog, mobile is a screen), record that explicitly as an intentional platform divergence rather than leaving it as silent drift.

### [7] P2 · Progress dashboard resting cards not flag-gated to design_system_elevation (web lags mobile)
- **dimension:** elevation-parity
- **flag:** `design_system_elevation`
- **web file:** `src/app/components/ProgressDashboard.tsx`
- **evidence:** CURRENT web ProgressDashboard.tsx hardcodes the legacy treatment with NO flag branching on every resting card: L1052 `className="rounded-xl bg-card border border-border p-3 min-h-[86px] card-elevated"` (skeleton tile), L1586 `...p-4 mb-6 card-elevated` (streak-freeze), L1639 `...p-4 card-elevated` (Daily Calories chart card), L1762 `...p-4 card-elevated` (Macro Adherence), L1830 `...p-4 mb-6 mt-6 card-elevated` data-testid="progress-maintenance-card" (Maintenance). A grep of the whole file for `design_system_elevation`, `elev-card-soft`, or an `isFeatureEnabled("design_system_elevation")` const returns ZERO hits — the file imports isFeatureEnabled (L63) and uses it only for redesign_winmoment/progress_digest_blend/progress_trajectory_box (L549/811/827), never elevation. theme.css L696-699 `.card-elevated { box-shadow: var(--shadow); border-color: transparent; }` is the always-on legacy paint; `--elev-card-soft` is defined (theme.css L239 light, L430 dark) but never referenced by this file. MOBILE reference proves the intended flag-aware behaviour: useCardElevation.ts L53-78 branches softElevation off→{Elevation.card, useBorder:true} / light→{Elevation.cardSoft, useBorder:false} / dark→{undefined shadow, liftBg cardElevated, useBorder:true}; DigestStoryCard.tsx L38 calls it and applies L54-63 (shadowStyle + liftBg + borderWidth: useBorder?1:0); TrajectoryCard.tsx L54/64-73 and TrendSummaryCard.tsx L49/79-87 do the same. The web child ProgressMetricDetail.tsx WAS wired (L44 `const elevated = isFeatureEnabled("design_system_elevation")`, L46 `border border-transparent shadow-[var(--elev-card-soft)]`), and Settings.tsx already ships the exact mirror pattern (L652 `const elevation = isFeatureEnabled("design_system_elevation")`, L654 `bg-card rounded-2xl border-0 shadow-[var(--elev-card-soft)]`, L655 `bg-card border border-border rounded-2xl card-elevated`, plus L1094) — so the parent Progress dashboard cards are a genuine miss against ENG-822's "Parity mobile + web".
- **fix:** Mirror the existing Settings.tsx pattern in ProgressDashboard.tsx. Add once near the component top (alongside the other isFeatureEnabled reads): `const elevation = isFeatureEnabled("design_system_elevation"); const cardClass = elevation ? "rounded-xl bg-card border-0 shadow-[var(--elev-card-soft)]" : "rounded-xl bg-card border border-border card-elevated";`. Then replace the hardcoded `rounded-xl bg-card border border-border ... card-elevated` substring with `${cardClass}` on the resting cards at L1052 (skeleton — keep the `p-3 min-h-[86px]`), L1586, L1639 (chart), L1762 (macro), and L1830 (maintenance — keep its data-testid and `mb-6 mt-6`). Keep each card's spacing/padding/data-testid classes appended after `cardClass`. The flag-OFF branch must equal today's render byte-for-byte (`border border-border card-elevated`) so the cold path is unchanged, matching the no-double-edge rule (border dropped only when the soft shadow carries separation). Note the dark-mode token already resolves: `--elev-card-soft` has a dark value (theme.css L430), so on web a single shadow class covers both themes (web does not need mobile's dark tonal-lift branch because RN-shadow-on-dark limitation is RN-only). Scope: this is a flag-gated visual change, so per the feature-flag non-negotiable it must ship behind the existing flag (it does) with the old path alive in the else — which this fix satisfies. After wiring, add/extend the ProgressDashboard test to assert flag-on yields `shadow-[var(--elev-card-soft)]`/`border-0` and flag-off yields `card-elevated`, mirroring the ProgressMetricDetail/Settings test coverage.

### [8] P2 · Plan tab cards (summary, empty-state, per-day) use legacy card-elevated, not flag-gated --elev-card-soft
- **dimension:** elevation-parity
- **flag:** `design_system_elevation`
- **web file:** `src/app/components/MealPlanner.tsx`
- **evidence:** Web MealPlanner.tsx has all three plan cards on the always-on legacy `card-elevated` utility with NO `design_system_elevation` gate: L645 summary card `className="rounded-2xl border border-border bg-card mb-4 card-elevated"`; L915 empty-state `className="flex flex-col items-center justify-center rounded-2xl border border-border bg-card card-elevated"`; L1007 per-day card `` className={`rounded-2xl border flex flex-col p-3.5 gap-2.5 card-elevated ${...}`} ``. A full-file grep for design_system_elevation / elev-card-soft / shadow-[var returns zero hits — the elevation flag is entirely unwired here. Mobile twin IS swept: planner.tsx L1130 `const cardElevation = useCardElevation();` applied to the summary card at L2556-2559 (`cardElevation.shadowStyle`, `borderWidth: cardElevation.useBorder ? StyleSheet.hairlineWidth : 0`, `liftBg`), and useCardElevation.ts L53 branches on `isFeatureEnabled("design_system_elevation")`. The canonical web pattern already exists in Settings.tsx L652-655: `const elevation = isFeatureEnabled("design_system_elevation"); const settingsCardClass = elevation ? "bg-card rounded-2xl border-0 shadow-[var(--elev-card-soft)]" : "bg-card border border-border rounded-2xl card-elevated";`. Token `--elev-card-soft` is defined in src/styles/theme.css L239 (light) + L430 (dark) and already consumed by RecipeDetail, ProgressMetricDetail, SubscriptionCard, and dialogs. Notably ENG-820's OTHER half (redesign_winmoment headline colour) IS already wired in this same file at L294-302, confirming the file was touched for ENG-820 but the elevation half was omitted.
- **fix:** Introduce a flag-gated card class at the top of MealPlanner (mirroring Settings.tsx L652-655): `const elevation = isFeatureEnabled("design_system_elevation"); const cardElevationClass = elevation ? "border-0 shadow-[var(--elev-card-soft)]" : "border border-border card-elevated";` Then apply it to all three cards by replacing the hard-coded `border border-border ... card-elevated` segments: L645 summary card, L915 empty-state card, and L1007 per-day card (keeping the per-day card's `isTodayCol` background/border-colour branch, which sets `border-primary/30` vs `border-border` — when the flag is ON those border-colour classes become no-ops under border-0, so the today-column distinction should fall back to the existing `bg-primary/10` tint, which it already has). Follow the Settings pattern (soft shadow + border-0 in BOTH light and dark on web) rather than replicating mobile's dark-only tonal-lift branch, since web's `--elev-card-soft` already has a dedicated dark value and the other web consumers apply it theme-uniformly. Leave the win-moment headline-colour half (already wired at L294-302) untouched.

### [9] P2 · Food-search soft elevation keyed off redesign_search_results, not design_system_elevation (no flag-off flat fallback) — diverges from mobile useCardElevation
- **dimension:** elevation-parity
- **flag:** `design_system_elevation`
- **web file:** `src/app/components/food-search/FoodSearchPanel.tsx`
- **evidence:** Web FoodSearchPanel applies the soft card shadow UNCONDITIONALLY inside the redesign_search_results block, with no design_system_elevation gate and no flat fallback:
- L1970 (category pills): style={isActive ? undefined : { boxShadow: "var(--elev-card-soft)" }}
- L2010 (grouped result cards): style={{ boxShadow: "var(--elev-card-soft)" }}
Both sit inside `searchResultsRedesign && results.length > 0` (L1948, L1988), where searchResultsRedesign = isFeatureEnabled("redesign_search_results") (L789). grep of the whole file shows design_system_elevation NEVER appears; isFeatureEnabled is only called for design_system_colours (L780) and redesign_search_results (L789).

The --elev-card-soft token is a static, always-defined CSS var (theme.css L239 light, L430 dark). Its own comment states it is "Applied only when `design_system_elevation` is on; the flat --elev-card above stays as the flag-OFF fallback" — i.e. gating is the CONSUMER's job, not enforced at token level. So var(--elev-card-soft) paints the soft shadow regardless of design_system_elevation state.

Every other web consumer honours that contract by wrapping the token in isFeatureEnabled("design_system_elevation"): ProgressMetricDetail.tsx L44/46, Settings.tsx L652/654, RecipeDetail.tsx L322/328, settings/SubscriptionCard.tsx L86/88, suppr/recipe-edit-dialog.tsx L183/185, suppr/add-ingredient-dialog.tsx L256/258, suppr/override-ingredient-dialog.tsx L116/118. Food-search is the sole consumer that skips the gate.

Mobile routes the identical surface through cardElevation = useCardElevation() (apps/mobile/.../FoodSearchPanel.tsx L318), consumed by the pills (L1916-1937: liftStyle = cardElevation.shadowStyle && !isActive ...) and the grouped cards (L1404-1426: cardElevation.liftBg/useBorder/shadowStyle, shadow on L1422). useCardElevation.ts L53 reads softElevation = isFeatureEnabled("design_system_elevation") and branches: flag-off → flat Elevation.card + hairline border (L55-62, the explicit flag-off fallback "must stay alive"); dark → tonal lift + hairline, no shadow (L64-71); light → soft shadow, no border (L73-78). Web has none of these three branches.

Net divergence: (1) web keys the soft shadow off redesign_search_results while mobile keys it off design_system_elevation — same visual, different flags, so if design_system_elevation is held off while redesign_search_results ramps, web shows soft shadows that mobile suppresses (flat) — a parity break; (2) web has no flat/hairline flag-off branch at all on this surface; (3) web ignores the dark tonal-lift branch mobile applies.
- **fix:** Gate the boxShadow on this surface behind isFeatureEnabled("design_system_elevation") IN ADDITION TO the redesign_search_results structural gate, mirroring the seven other web consumers (e.g. Settings.tsx L652-654, RecipeDetail.tsx L322-328) and mobile's useCardElevation. Derive a local `const elevated = isFeatureEnabled("design_system_elevation");` near L789, then:
- L1970 pills: style={isActive ? undefined : (elevated ? { boxShadow: "var(--elev-card-soft)" } : undefined)} and add a hairline border in the flat (non-elevated) class branch to match useCardElevation's useBorder=true fallback.
- L2010 grouped cards: when elevated → className "... border-0" + style={{ boxShadow: "var(--elev-card-soft)" }}; when flat → drop the shadow and add a hairline border (border border-border) so the flag-off path reads as today's flat list, matching the mobile flag-off branch.
Web cannot directly mirror mobile's dark tonal-lift branch the same way (RN renders dark shadows poorly; on web --elev-card-soft is already tuned per-theme in theme.css L430), so the two-branch elevated/flat split is the correct web analogue of the three-branch mobile hook. Add a one-line code comment documenting the two-flag relationship (redesign_search_results = structure; design_system_elevation = depth) so ENG-814/815 stay pixel-parity with the mobile sweep, and add/extend a web test asserting no shadow class/style when design_system_elevation is off (mirroring foodSearchRedesignResults.test.tsx on mobile).

### [10] P2 · Web Hydration & stimulants card uses 💧/☕/🍷 emoji as functional row icons instead of Lucide glyphs
- **dimension:** icons-parity
- **flag:** `none`
- **web file:** `src/app/components/suppr/hydration-stimulants-card.tsx`
- **evidence:** Current web code passes literal emoji as the three row icons on a daily-loop surface: line 246 `icon={<span aria-hidden>💧</span>}` (Water), line 283 `icon={<span aria-hidden>☕</span>}` (Caffeine), line 316 `icon={<span aria-hidden>🍷</span>}` (Alcohol). The Row component renders `{icon}` inside a styled <span> (lines 106-115). The ONLY Lucide import is `MoreHorizontal` (line 24) — no Droplet/Coffee/Wine. No feature flag gates this path. docs/decisions/2026-05-31-icon-strategy.md line 14 explicitly forbids this: "Eliminate functional emoji... Emoji used as UI... must NOT ship — they render differently per OS/font and look amateurish at the premium bar. Replace every functional emoji with a Lucide glyph." and line 32 "No functional emoji." Caveat confirmed: the mobile twin is NOT canonical either — it uses Ionicons not Lucide (apps/mobile/components/HydrationStimulantsCard.tsx line 89 types icon as `keyof typeof Ionicons.glyphMap`, line 106 renders `<Ionicons name={icon} size={16} color={COLORS[tone]} />`, with `water-outline` line 332, `cafe-outline` line 363, `wine-outline` line 386). So this is an icon-strategy violation on web (emoji-as-UI, the worse offender) AND a latent cross-platform icon divergence — neither platform is on Lucide for these three glyphs.
- **fix:** Web: import Droplet, Coffee, Wine from lucide-react and replace the three emoji spans. Line 246 -> `icon={<Droplet className="w-4 h-4" aria-hidden style={{ color: "var(--macro-water)" }} />}`; line 283 -> `icon={<Coffee className="w-4 h-4" aria-hidden style={{ color: "var(--stimulant-caffeine)" }} />}`; line 316 -> `icon={<Wine className="w-4 h-4" aria-hidden style={{ color: "var(--stimulant-alcohol)" }} />}`. The Row already wraps icon in an aria-hidden span, so the inner aria-hidden is belt-and-braces. To avoid creating a Lucide-vs-Ionicons divergence, file a PAIRED mobile ticket in the same change to swap apps/mobile/components/HydrationStimulantsCard.tsx from Ionicons (water-outline/cafe-outline/wine-outline, lines 332/363/386) to the same three Lucide glyphs via lucide-react-native (Droplet/Coffee/Wine), updating the Row `icon` prop type from `keyof typeof Ionicons.glyphMap` to a Lucide component. Per docs/decisions/2026-05-31-icon-strategy.md this is a visual change — gate behind isFeatureEnabled per the CLAUDE.md feature-flag rule, and capture before/after pixels on both platforms before claiming done. Do NOT land the web swap alone without tracking the mobile swap, or the row icons will render visibly different across platforms (Lucide line-weight vs Ionicons outline).

### [11] P2 · Web Estimated chip reuses the over-budget orange --warning token; mobile deliberately uses a warm-amber that avoids it
- **dimension:** search-results-parity
- **flag:** `redesign_search_results`
- **web file:** `src/app/components/food-search/FoodSearchPanel.tsx`
- **evidence:** Web (current code): FoodSearchPanel.tsx:1626-1630 styles the estimated chip as `tier === "verified" ? "bg-primary/10 text-primary" : "bg-warning/15 text-warning"` (data-testid `food-search-confidence-${tier}`, line 1625), behind the `redesign_search_results` flag (line 789: `const searchResultsRedesign = isFeatureEnabled("redesign_search_results")`). The web `--warning` token resolves to over-budget orange: theme.css:150 `--warning: var(--accent-warning)` → theme.css:109 `--accent-warning: #F78A32` (light) and theme.css:321 `#FAA45F` (dark). That exact token is the canonical over-budget colour — used for over-budget macros app-wide (e.g. remaining-macros-bar.tsx:22/151 "Over-budget → the number turns `--warning`") AND for the over-budget fat macro rendered in the SAME search row (FoodSearchPanel.tsx:1646 and 1658: `<span className="text-warning">F {headline.macros.fat}g</span>`). So in one row the same orange means both "estimated data" and "over-budget fat". Mobile reference (current code): SearchResultConfidenceChip.tsx:50-61 maps estimated to `fg: "#BF8324"` / `bg: "rgba(191, 131, 36, 0.14)"` with the explicit comment "the closest warm-amber to the prototype's #bf8324 without reaching for the orange `warning` slot which reads as 'over-budget'". Web also already ships a dedicated amber token (theme.css:117-126 `--accent-win: #F2A93B` mirroring mobile `Accent.win`) but the chip does not use it. Verified-tier styling matches across platforms (web `bg-primary/10 text-primary` vs mobile `Accent.primary`), so only the estimated tier diverges.
- **fix:** In FoodSearchPanel.tsx (line 1629), stop reusing the over-budget `--warning` orange for the estimated chip. Define a warm-amber chip pair matching mobile's #BF8324 — either reuse the existing `--accent-win` (#F2A93B) token already present (theme.css:117-126) via inline style `style={{ color: "var(--accent-win)", background: "color-mix(in srgb, var(--accent-win) 14%, transparent)" }}`, or add a dedicated `--confidence-estimated` amber token tuned to #BF8324 to match the mobile chip exactly. Keep the verified branch unchanged (`bg-primary/10 text-primary` already matches mobile `Accent.primary`). Confirm the amber meets contrast (>=4.5:1) per the contrast-audit harness, since #BF8324 is darker/more legible than #F2A93B at small text. Verify both light and dark themes.

### [12] P2 · No flag-forcing layer for web Playwright visual specs — only flag-OFF goldens are capturable
- **dimension:** visual-regression-readiness
- **flag:** `none`
- **web file:** `tests/e2e/utils/visual.ts`
- **evidence:** Mobile HAS a flag-force override: apps/mobile/lib/analytics.ts isFeatureEnabled() lines 201-207 — `if (typeof __DEV__ !== "undefined" && __DEV__) { const envKey = \`EXPO_PUBLIC_FLAG_FORCE_${flag.toUpperCase().replace(/-/g,"_")}\`; const override = process.env[envKey]; if (override === "true") return true; if (override === "false") return false; }`, mirrored in isFeatureDisabled() lines 233-239. It is genuinely wired/consumed (grep: apps/mobile/app/(tabs)/progress.tsx, (tabs)/planner.tsx, components/recipe/CreateRecipeActionSheet.tsx) and unit-tested (apps/mobile/tests/unit/isFeatureDisabled.test.ts). Web LACKS any equivalent: (1) src/lib/analytics/track.ts isFeatureEnabled() lines 30-37 reads ONLY `posthog.isFeatureEnabled(flag) === true` with no env/test override (isFeatureDisabled lines 52-58 same); grep for NEXT_PUBLIC_FLAG_FORCE/FLAG_FORCE in src/ returns NONE. (2) tests/e2e/utils/visual.ts (entire 30-line file read) exports only dismissVisualOverlays() and stabilizeForScreenshot() — no flag helper. (3) grep across tests/e2e for overrideFeatureFlags = NONE, addInitScript = NONE, onFeatureFlags/forceFlag = NONE; playwright.config.ts has no flag seeding; tests/e2e/auth.setup.ts produces the only fixture. (4) The committed fixture tests/e2e/.auth/user.json line 44 hardcodes `"$active_feature_flags":[]` and `"$enabled_feature_flags":{}`, so every authed visual spec (visual-regression-deep, visual-audit, visual-audit-authed, visual-regression-subpages, chromatic-e2e, visual-applitools — all import utils/visual) renders the flag-OFF path. A flags-ON golden is impossible to capture today.
- **fix:** Mirror the mobile contract on web. (a) Add an env override in src/lib/analytics/track.ts#isFeatureEnabled / isFeatureDisabled: gate on a dev/test signal (e.g. process.env.NODE_ENV !== "production" or NEXT_PUBLIC_E2E) then read `process.env["NEXT_PUBLIC_FLAG_FORCE_" + flag.toUpperCase().replace(/-/g,"_")]` returning true/false for "true"/"false" — identical mapping to mobile so a single flag key works on both platforms. This covers SSR/first-paint. (b) Add a `forceFlagsOn(page, flags: string[])` helper in tests/e2e/utils/visual.ts that calls `page.addInitScript` to seed PostHog's localStorage `$enabled_feature_flags`/`$active_feature_flags` before load (or calls `posthog.overrideFeatureFlags([...])` post-load then awaits `onFeatureFlags`) so client-side `posthog.isFeatureEnabled` also honours it. (c) Update the committed tests/e2e/.auth/user.json note or generate per-spec fixtures so the all-empty `$enabled_feature_flags:{}` is no longer the only available state, enabling flag-ON goldens. Add a unit test mirroring apps/mobile/tests/unit/isFeatureDisabled.test.ts. Note: this is test/CI infra (Maestro + manual sim capture remain the primary visual regime), so it is not user-facing — schedule rather than hotfix.

### [13] P2 · Web Settings redesign paths (design_system_elevation + web-subscription-card) have no flag-ON visual-regression baseline
- **dimension:** visual-regression-readiness
- **flag:** `design_system_elevation + web-subscription-card`
- **web file:** `tests/e2e/visual-audit.spec.ts (shell/settings-{vp}.png) + tests/e2e/visual-regression-deep.spec.ts (deep/settings-preferences-{vp}.png); gated code in src/app/components/Settings.tsx:652,1381 and src/app/components/settings/SubscriptionCard.tsx:86`
- **evidence:** The two web settings redesign paths are real and flag-gated: Settings.tsx:652 `const elevation = isFeatureEnabled("design_system_elevation")` → 653-658 swaps `card-elevated`+border (OFF) for `shadow-[var(--elev-card-soft)] border-0` (ON); Settings.tsx:1381 `{isFeatureEnabled("web-subscription-card") && userTier !== "free" ? (<SubscriptionCard .../>) : null}` — OFF renders nothing; SubscriptionCard.tsx:86-89 repeats the same elevation swap. isFeatureEnabled (src/lib/analytics/track.ts:30-37) returns `false` whenever `NEXT_PUBLIC_POSTHOG_KEY` is unset OR `posthog.isFeatureEnabled(...) !== true` — both true under Playwright, so every snapshot resolves the OFF branch. The only settings visual baselines are: shell/settings-{mobile,desktop}.png (visual-audit.spec.ts:13,39-42 — UNAUTHENTICATED `/settings`, no login, no flag bootstrap → never reaches the authed cards or subscription card) and deep/settings-preferences-{vp}.png (visual-regression-deep.spec.ts:21-29 — authed but scrolls to `settings-fasting-link` preferences band, no flag forcing). There is NO `addInitScript`/PostHog-bootstrap anywhere in tests/e2e (grep returned nothing), and tests/unit/settingsElevationFlag.test.ts is an explicit "Source-level structural check — no React rendering" (greps class strings, captures zero pixels). So the ramped-ON elevation treatment and the entire SubscriptionCard surface have no pixel baseline. Mobile's equivalent (SettingsBundleContent.tsx SettingsCard:194-235 via useCardElevation, Membership card:1399-1584) is covered by the Maestro visual layer per the three-layer model. Net: web's ramped redesign state can regress with green CI.
- **fix:** Add an authed redesign-spec test (or extend visual-regression-deep.spec.ts) that bootstraps both flags ON before navigation — e.g. `await page.addInitScript(() => window.localStorage.setItem('ph_<key>_posthog', JSON.stringify({ '$override_feature_flags': { design_system_elevation: true, 'web-subscription-card': true } })))`, or expose a test-only flag-override init hook the app reads in dev/CI. Run authed as a non-free (Pro) user so SubscriptionCard renders, scroll the full settings column, and snapshot e.g. redesign/settings-{mobile,desktop}.png. Note this is a web-only test-coverage gap, not a feature parity gap — both platforms already implement the redesign behind the flags; the missing artefact is the flag-ON golden so the ramped-up surface (soft-elevation cards + the billing SubscriptionCard) can't regress under green CI. Verify the snapshot before committing per the project's visual-validation rule.

### [14] P2 · Notifications has zero committed visual-regression baseline (authed subpage spec skips without E2E creds)
- **dimension:** visual-regression-readiness
- **flag:** `none`
- **web file:** `tests/e2e/visual-regression-subpages.spec.ts (route renders via src/app/App.tsx → NotificationsCenter)`
- **evidence:** visual-regression-subpages.spec.ts line 26 lists `{ name: "notifications", path: "/notifications" }` inside `authedSubpages`. The authed describe block is gated at line 53: `test.skip(!hasE2ECredentials(), "Set E2E_EMAIL and E2E_PASSWORD for authed subpage snapshots.")`, and line 63 writes `subpages/authed/${screen.name}-${vp.name}.png`. The committed snapshot dir tests/e2e/__snapshots__/visual-regression-subpages.spec.ts/ contains exactly 12 PNGs, ALL `subpages-public-*` (fasting/help/pricing/privacy/terms/whats-new × mobile/desktop); `ls | grep -c authed` returns 0. `git log --all -- '...*authed*'` returns nothing — an authed baseline was NEVER committed. So /notifications (and the other 4 authed subpages: profile, import, account/billing, create) have no PNG of any kind. The web UI itself is NOT missing — it renders via src/app/App.tsx:542-543 (`case "notifications": return <NotificationsCenter onOpenRecipe={openRecipeById} />`), and app/(product)/notifications/page.tsx returning null is intentional (the (product)/layout.tsx mounts HomePageClient/App.tsx which switches on pathname). The mobile reference apps/mobile/app/(tabs)/notifications.tsx is a full functional screen. So this is a test-coverage gap, not a feature-parity gap.
- **fix:** Two parts. (1) Make the authed specs actually run in CI: provide E2E_EMAIL/E2E_PASSWORD secrets so the `test.skip(!hasE2ECredentials())` guard (line 53) passes and the `subpages/authed/*.png` snapshots get generated and committed — this baselines notifications AND the other 4 starved authed subpages (profile, import, account/billing, create) in one go. (2) Drop the candidate's instruction to force design_system_elevation/design_system_colours ON for this surface: grep confirms NotificationsCenter.tsx consumes neither flag, so there is no redesign variant to baseline here (those flags live in ProgressMetricDetail, Settings, RecipeDetail, MacroDetailPanel, FoodSearchPanel only). Baseline the current single render path at both viewports (mobile 390×844, desktop 1440×900) using the existing dismissVisualOverlays + stabilizeForScreenshot helpers already in the spec.

### [15] P2 · Web has no per-meal / per-slot nutrition detail surface (mobile meal-nutrition.tsx has no web equivalent) — ✅ ADDRESSED 2026-05-31 (single-meal mode)
- **dimension:** visual-regression-readiness
- **flag:** `web_meal_nutrition_detail` (default OFF)
- **web file:** `src/app/components/suppr/today-meals-section.tsx (meal rows + slot headers have no nutrition-detail navigation); no app/.../meal-nutrition route or per-meal nutrition panel exists anywhere on web`
- **evidence:** MOBILE entry points (apps/mobile/app/(tabs)/index.tsx:5015-5019) wire TodayMealsSection to the dedicated screen: `onPressMeal={(id) => router.push(`/meal-nutrition?id=${...}`)}` and `onPressSlotSummary={(slot) => router.push(`/meal-nutrition?slot=${...}&date=${...}`)}`. The mobile screen apps/mobile/app/meal-nutrition.tsx renders a per-meal macro kcal-split bar (styles.macroBar / macroSeg, lines 587-597, 718-719), "% of kcal" labels (MacroStat, line 699), a per-meal/per-slot "Vitamins, minerals & more" micros table (line 614, microRows from listMicroNutrientsCompleteDisplay line 247-254), portion line (line 562), slot-aggregate line-item list with kcal-contribution bar (lines 445-552, "Combined macros" line 568), and an Edit action.

WEB has no equivalent. In src/app/components/suppr/today-meals-section.tsx the meal rows (lines 448-554) expose only a kebab DropdownMenu — "Copy to another day…" (496-498), "Share meal" (499-541), "Delete" (542-549) — and NO tap-to-open-nutrition; the row body div (449-459) has no onClick. The slot header onClick only toggles collapse: `onClick={() => onToggleSlot(sectionName)}` (line 335). The TodayMealsSectionProps interface (lines 64-119) contains no onPressMeal / onPressSlotSummary / nutrition-detail prop.

The web "all nutrients" sheet is DAY-level, not per-meal: src/app/components/suppr/full-nutrient-panel-sheet.tsx props doc reads "Day-summed micros — pass sumMicrosFromLoggedMeals(meals) directly" (lines 41-42); NutritionTracker.tsx feeds it `microSum={dayMicroSum}` (line 2474) where dayMicroSum = sumMicrosFromLoggedMeals(mealsForSelectedDate) (line 1704).

Programmatic confirmation: zero web consumers of macroSplitConfidence/macroSplitIncompleteCopy (grep over src + app returned nothing outside the lib def); listMicroNutrientsCompleteDisplay has no web consumer; "Combined macros", "of macro calories", "Portion ×" strings absent from web; NutritionTracker.tsx:2349 proteinPct is the day hero ring (totals.protein / effectiveMacroTargets.protein), not a per-meal split. No app/.../meal-nutrition route exists (app router dir listing has no such folder). No web baseline/golden/storybook for this surface exists.
- **fix:** This is a true web-parity gap, not merely a missing snapshot — the candidate's "rendered inline" assumption is false; the per-meal/per-slot surface is unreachable on web. Two-part fix: (1) Build the web meal-nutrition surface mirroring apps/mobile/app/meal-nutrition.tsx — either a new route under app/(product) or, more consistent with web's modal pattern, a MealNutritionDialog/Sheet (sibling to MacroDetailPanel.tsx / full-nutrient-panel-sheet.tsx) that reuses the already-shared libs (macroSplitConfidence, microNutrientDisplay, mealContributedFiberG, sumMicrosFromLoggedMeals). Render both modes: single-meal (id) macro kcal-split + per-meal micros + portion + Edit, and slot-aggregate (slot+date) line-item list + combined macros + combined micros. (2) Wire it: add onPressMeal and onPressSlotSummary props to TodayMealsSectionProps, make the meal-row body and slot-header (or a dedicated affordance) open it, and pass handlers from NutritionTracker.tsx. Per CLAUDE.md, gate the new structural surface behind an isFeatureEnabled flag with the old (no-detail) path alive in the else. Only after it exists should a flag-ON/flag-OFF golden be committed for visual-regression.
- **addressed (2026-05-31):** Built the **single-meal** mode as a Dialog (web's modal pattern won over a route — see `docs/decisions/2026-05-31-web-meal-nutrition-detail-parity.md`). New `src/app/components/suppr/meal-nutrition-dialog.tsx` reuses the shared `macroSplitConfidence` + `microNutrientDisplay` helpers and the newly-extracted shared `macroCalorieSplit` (`src/lib/nutrition/macroCalorieSplit.ts`, imported by web + mobile so the Hamilton rounding is single-sourced). Opener wired via a new `onOpenMealNutrition` prop on `TodayMealsSectionProps` → a "View nutrition" kebab item in each meal row; hosted in `NutritionTracker.tsx`, resolving the full LoggedMeal (with micros) from `mealsForSelectedDate` (no Supabase fetch). Gated behind `web_meal_nutrition_detail`; flag OFF = byte-identical to before (prop undefined → no item, dialog unmounted). Tests: `tests/unit/mealNutritionDialogWeb.test.tsx` (14) + the repointed shared-split mobile test. **Remaining:** the **slot-aggregate** mode (mobile `?slot=&date=` line-item list + combined macros) and the Edit action are not yet on web — Edit is intentionally read-only (no web meal-edit modal to route to); slot-aggregate is a follow-up if web ever needs a per-slot summary entry point. The flag-ON/flag-OFF visual-regression golden can now be captured.

### [16] P2 · redesign_search_results path on web FoodSearchPanel has no deterministic visual baseline (no Storybook story, no committed snapshot, no standalone route)
- **dimension:** visual-regression-readiness
- **flag:** `redesign_search_results`
- **web file:** `src/app/components/food-search/FoodSearchPanel.tsx`
- **evidence:** The web FoodSearchPanel DOES implement the redesign behind both flags — `design_system_colours` (line 780: `const commitCtaClass = isFeatureEnabled("design_system_colours") ? ...`) and `redesign_search_results` (line 789: `const searchResultsRedesign = isFeatureEnabled("redesign_search_results");`), with the segmented control rendered at lines 1948-1977 (`{searchResultsRedesign && results.length > 0 && (<div role="tablist" data-testid="food-search-category-tabs">...)`) and the Best/More sectioned-card body at lines 1988-2017 (`<div data-testid="food-search-results-redesign">` → `{ label: "Best matches", rows: sectionedResults.best }, { label: "More results", rows: sectionedResults.more }`). So the redesign code is NOT missing. The gap is visual-regression-readiness, and that IS real: (1) No story exists — `find ... -name "*.stories.tsx"` returns 26 stories (UI primitives + PlanSourceSelector + log-sheet) with ZERO `FoodSearch*` story; (2) `src/app/components/suppr/log-sheet.stories.tsx` line 29 stubs `search: { onOpen: () => {} }` and never renders FoodSearchPanel or sets a query, so the search body is in no Chromatic golden; (3) `tests/e2e/chromatic-e2e.spec.ts` lines 12-14 only snapshot `{ name: "landing", path: "/" }` and `{ name: "pricing", path: "/pricing" }` — not the authed Log flow; (4) the ONLY redesign coverage is `tests/unit/foodSearchPanelRedesign.test.tsx`, which asserts `toBeInTheDocument()` (lines 164-235) — DOM presence, not pixels; grep for `toHaveScreenshot|toMatchSnapshot` returns nothing touching this panel; (5) no `food-search`/`deep-redesign` PNG baselines exist on disk. Infra to fix exists: `.storybook/main.ts` + `@chromatic-com/storybook` addon + `package.json` `chromatic` scripts.
- **fix:** Add a deterministic Chromatic golden for the redesigned search-results body via Storybook (the infra already exists: `.storybook/main.ts` globs `../src/**/*.stories.@(...)`, the `@chromatic-com/storybook` addon is installed, and `npm run chromatic` is wired). Create `src/app/components/food-search/FoodSearchPanel.stories.tsx` that: (a) mocks `isFeatureEnabled` to return true for both `redesign_search_results` and `design_system_colours`, (b) mocks the search network layer (`customFoodsClient` + the `/api/usda|off|edamam|fatsecret/search` fetches, as `tests/unit/foodSearchPanelRedesign.test.tsx` already does) so the list renders without live network/search latency, and (c) seeds a fixed query + result set so Best matches / More results, the segmented control, and the Verified/Estimated chips all render deterministically. Add a flag-OFF story too so the legacy flat-list path keeps a golden. This pins the redesigned layout independent of network state. Mirror the same story on mobile (apps/mobile has its own `.storybook`) so both surfaces gain a golden, since neither currently has one.

### [17] P2 · Web App Router error/global-error screens are unbranded slate chrome, diverging from the ENG-799-rebranded mobile RootErrorBoundary
- **dimension:** web-only-chrome-cookie-and-error
- **flag:** `redesign_branded_sheets`
- **web file:** `app/error.tsx (and app/global-error.tsx)`
- **evidence:** CURRENT web is unbranded raw slate, no brand mark, divergent copy:
- app/error.tsx:27-31 — `bg-slate-50 dark:bg-slate-950` page, `bg-white/70 dark:bg-slate-900/40` card, `text-slate-900 dark:text-white` H1, `text-slate-600 dark:text-slate-300` body; copy line 31 "Try again, or go home and start fresh. If this keeps happening, let us know." No SupprMark/ring, no redesign tokens, no flag gate.
- app/global-error.tsx:22-26 — identical slate palette; copy line 24 "Suppr ran into a problem", line 25-26 "Try reloading the page. If this keeps happening, let us know." Single "Reload" button, no brand mark.

CURRENT mobile WAS rebranded today behind the flag:
- RootErrorBoundary.tsx:22-27 doc — "ENG-799 ... the recovery UI is rebuilt in the brand language ... Gated behind `redesign_branded_sheets`; flag-off renders the prior hardcoded-hex layout verbatim."
- renderBranded() :82-148 — `backgroundColor: c.background` (Colors.dark), `InlineBrandMark size={44} ring={c.text}` (lines 47-61 ring motif mirroring canonical SupprPlateMark), title "Something went wrong" (106), body "Suppr hit an unexpected error. The team has been notified. Tap Try again to recover, or restart the app if it keeps happening." (116-117), `backgroundColor: Accent.primary` CTA (124), force-quit hint after `resets >= 2` (133-145).
- render() :219-229 wraps `isFeatureEnabled("redesign_branded_sheets")` in try/catch, falling back to renderLegacy.

Flag is mobile-only: grep for `redesign_branded_sheets` across the repo returns only apps/mobile/* (RootErrorBoundary.tsx, TodayMealsSection.tsx, two mobile tests) — zero web usage.

Fix is feasible on web: isFeatureEnabled exported at src/lib/analytics/track.ts:30; redesign tokens exist in src/styles/theme.css (--background:64, --card:69, --muted-foreground:102); canonical ring SupprPlateMark at src/app/components/ui/suppr-mark.tsx:94. (Note SupprMark's ring is separately gated on `design_system_brandmark`, suppr-mark.tsx:66.)
- **fix:** Bring app/error.tsx and app/global-error.tsx to brand parity with mobile RootErrorBoundary, gated behind redesign_branded_sheets (register/wire it on web — currently mobile-only) with the existing slate layout kept alive in the else branch.

error.tsx: gate a branded path that uses redesign tokens (bg-background page, bg-card/border-border card, text-foreground title, text-muted-foreground body, primary CTA) and renders the canonical ring mark — SupprPlateMark from src/app/components/ui/suppr-mark.tsx (or gate the mark itself under design_system_brandmark to match the web mark's own flag). Align copy to the mobile string: title "Something went wrong", body "Suppr hit an unexpected error. The team has been notified. Tap Try again to recover, or restart the app if it keeps happening." Keep the "Go home" button.

global-error.tsx: same treatment but self-contained — it renders its own <html>/<body> and can fire before providers/theme CSS mount, so inline the ring SVG (identical to mobile's InlineBrandMark) and inline styles/CSS-var fallbacks rather than relying on utility classes that may not be generated; align copy with error.tsx.

Critical safety detail (mirror mobile render() :219-229): error/global-error boundaries can fire before the analytics/flag client is initialised, so wrap isFeatureEnabled("redesign_branded_sheets") in try/catch and fall back to the current slate layout on throw — never let the recovery UI itself crash. Add web tests mirroring apps/mobile/tests/unit/rootErrorBoundaryBranded.test.tsx (flag-on branded, flag-off legacy, throwing-flag falls back).

### [18] P3 · Web recipe-import "Paste a recipe link" card lacks the SupprMark that mobile leads with
- **dimension:** brandmark-parity
- **flag:** `design_system_brandmark`
- **web file:** `src/app/components/RecipeUpload.tsx`
- **evidence:** CONFIRMED GAP. Mobile reference apps/mobile/app/import-shared.tsx imports `SupprMark` (line 41: `import { SupprMark } from "@/components/SupprMark";`) and on the idle state renders it on the "Paste a recipe link" panel: line 1839 `<View style={styles.panelCard}>`, line 1840 `<SupprMark size={56} />`, line 1841 `<Text style={styles.panelTitle}>Paste a recipe link</Text>` — so the brandmark sits directly above the title. Web RecipeUpload.tsx renders the equivalent card with NO brandmark: lines 1343-1347 `<div className="bg-card border border-border rounded-2xl p-5"> <div className="flex items-center gap-2 mb-3"> <Icons.web className="w-5 h-5 text-primary" /> <h3 className="text-foreground text-sm font-semibold">Paste a recipe link</h3>` — only an Icons.web glyph, no SupprMark. A grep for `SupprMark|SupprPlateMark|design_system_brandmark|isFeatureEnabled|SupprLogo|brandmark` across the entire 2189-line RecipeUpload.tsx returns ZERO matches, so the mark is absent even behind the flag. The fix is low-risk because the web brand component already exists and is flag-ready: src/app/components/ui/suppr-mark.tsx exports `SupprMark`, which internally renders `SupprPlateMark` when `isFeatureEnabled("design_system_brandmark")` is true and falls back to the legacy S-glyph otherwise (lines 65-70), exactly mirroring mobile's apps/mobile/components/SupprMark.tsx (line 90 `if (isFeatureEnabled("design_system_brandmark"))`).
- **fix:** Import `SupprMark` from `./ui/suppr-mark` in RecipeUpload.tsx and render `<SupprMark size={44} className="mx-auto mb-3" />` above the "Paste a recipe link" heading (around lines 1343-1347), mirroring mobile's `<SupprMark size={56} />` on the idle panel. No explicit `isFeatureEnabled` wrapper is required at the call-site: per the ENG-797 design direction the gating is internal to the web `SupprMark` component (it already renders the canonical ring `SupprPlateMark` when `design_system_brandmark` is on and the legacy S-glyph when off, suppr-mark.tsx lines 65-70), so the flag-off path stays alive automatically. Severity is P3: this is brand chrome on a secondary card, not a functional/data parity gap — the import flow itself works identically on both platforms.

### [19] P3 · Web meal-action surface (kebab dropdown) lacks the quiet SupprMark mobile renders on its branded action sheet
- **dimension:** brandmark-parity
- **flag:** `design_system_brandmark`
- **web file:** `src/app/components/suppr/today-meals-section.tsx`
- **evidence:** Mobile renders the quiet brandmark inside its branded MealActionSheet at apps/mobile/components/today/TodayMealsSection.tsx:308-312: `{/* One quiet brand mark — ENG-797 ... */} <View style={mas.brandMark} pointerEvents="none"><SupprMark size={16} background="transparent" foreground={colors.textTertiary} /></View>`. SupprMark is imported at mobile line 40. This MealActionSheet is the meal long-press action menu (Edit/Copy/Share/Delete, lines 278-386), gated by `redesign_branded_sheets` (mobile line 544) replacing the raw Alert.alert.

On web, the equivalent meal-action surface is the per-row kebab DropdownMenu (web lines 485-551: "Copy to another day…", "Share meal", "Delete") — a Radix popover, NOT a custom sheet. It renders no brandmark. Web does not import SupprMark/SupprPlateMark at all (imports span lines 1-33; no brand-mark import present). The usual-meal picker Dialog (web lines 704-762) — which the candidate names as the equivalent — is actually the counterpart of the mobile usual-picker Modal (mobile lines 1261-1384), not of the action sheet; it likewise has no SupprMark (DialogHeader/DialogTitle/DialogDescription only, lines 711-718).

So both candidate-cited web surfaces (kebab dropdown + usual-picker Dialog) lack any brandmark, confirming the brandmark is web-absent.
- **fix:** This is a real but narrow and low-priority gap, and the candidate mis-maps the surface. The brandmark detail sits inside a larger structural divergence: mobile's whole branded `MealActionSheet` (cream bottom sheet with thumbnail + macro header + custom rows) is a brand-new mobile-only surface gated behind `redesign_branded_sheets` (ENG-799, dated today 2026-05-31), whereas web uses a Radix DropdownMenu popover for the same Edit/Copy/Share/Delete actions — a fundamentally different interaction pattern where a top-of-sheet SupprMark has no natural home (popovers are small anchored menus, not modal sheets).

Recommended: do NOT bolt a SupprMark onto the web kebab dropdown — it would look out of place in a compact anchored popover and is not the parity that matters. Instead, track the broader question (should web adopt a branded meal-action surface to match mobile's ENG-799 redesign?) as a follow-up under the redesign work, gated behind the web equivalent of `redesign_branded_sheets`. If/when web gains a meal-action *sheet/dialog* (not the dropdown), THEN add a flag-gated quiet brandmark at its header to match mobile's placement (size 16, transparent tile, tertiary-text foreground), behind a `design_system_brandmark` gate with the no-mark layout as the else path. Open a Linear issue (cross-platform parity for the ENG-799 branded meal-action surface) and reference it; per the no-silent-deferrals rule this should not be parked in a code comment.

### [20] P3 · Web root crash screen (global-error.tsx) has no brandmark; mobile branded crash screen renders an inline ring brand mark
- **dimension:** brandmark-parity
- **flag:** `none`
- **web file:** `app/global-error.tsx`
- **evidence:** app/global-error.tsx lines 19-37 render the entire crash UI with NO SVG/brand mark: line 24 `<h1 ...>Suppr ran into a problem</h1>` is text-only, line 25-27 a paragraph, line 29-31 a Reload Button. There is zero SVG/ring markup in the file. Mobile RootErrorBoundary.tsx defines `InlineBrandMark` (lines 44-61) — a self-contained ring SVG (`<Circle ... r="9.5" .../>` line 56, `<Circle ... r="5.5" .../>` line 57) with no theme/hook dependency — and renders it on the branded crash screen at line 98 `<InlineBrandMark size={44} ring={c.text} />`. So mobile's branded crash state shows a brand mark; web's crash state does not. NOTE: the mobile brand mark is flag-gated behind `redesign_branded_sheets` (line 225) — the legacy mobile path (`renderLegacy`, lines 150-217) also has NO brand mark, so the parity target is the redesign/branded state only, not baseline.
- **fix:** Add a small inline ring SVG above the `<h1>` in app/global-error.tsx (and app/error.tsx for consistency), mirroring the mobile InlineBrandMark motif: an outer ring at r=9.5 strokeWidth=2 and inner ring at r=5.5 strokeWidth=1 on a 0 0 32 32 viewBox. Hardcode colours inline (e.g. currentColor or a fixed neutral) since global-error.tsx renders its own `<html>/<body>` above the theme/provider tree and must not import hook- or flag-dependent components that could throw inside an already-crashed tree. No feature flag needed — this is web-only crash chrome with no shared visual-flag surface (flag: none). Caveat: since the mobile equivalent is gated behind `redesign_branded_sheets`, confirm the redesign brand-mark direction is the intended end state before shipping; if web is to match the *branded* mobile state, this is correct, and it brings web baseline crash chrome to where mobile lands when the flag reaches 100%.

### [21] P3 · Wordmark text-size ratio diverges: 0.72x mobile vs 0.64x web
- **dimension:** brandmark-parity
- **flag:** `design_system_brandmark`
- **web file:** `src/app/components/ui/suppr-mark.tsx`
- **evidence:** Web suppr-mark.tsx line 86 (SupprWordmark) and line 143 (SupprPlateWordmark) both: `style={{ fontSize: Math.round(size * 0.64), letterSpacing: "-0.02em" }}`. Mobile SupprMark.tsx line 113 (SupprWordmark) and line 173 (SupprPlateWordmark) both: `fontSize: Math.round(size * 0.72)` with `letterSpacing: -0.4`. At the shared size=28 default both wordmarks render Math.round(28*0.72)=20px on mobile vs Math.round(28*0.64)=18px on web — a real 2px size divergence in the shared brand wordmark, on both the canonical (Plate) and legacy paths, independent of the design_system_brandmark flag (the flag only swaps the mark glyph via SupprMark lines 65-70 / 89-94, not the wordmark text). Letter-spacing differs too (mobile numeric -0.4 vs web "-0.02em") but at ~18-20px those resolve to near-equal (~-0.36 to -0.4px), so it is not the load-bearing divergence.
- **fix:** Align the multiplier on one ratio across both platforms. Recommend raising web to 0.72 to match mobile: in src/app/components/ui/suppr-mark.tsx change line 86 and line 143 from `Math.round(size * 0.64)` to `Math.round(size * 0.72)`. Optionally normalise letter-spacing to a single representation (e.g. web "-0.02em" vs mobile -0.4 at default size are visually equivalent, so this can be left or harmonised). Confirm the intended design ratio with the design-director direction (ENG-797) before flipping. This is a pure visual change to the brandmark component; land it in the same design_system_brandmark gate context (note the wordmark text itself is not flag-gated today — only the mark glyph is — so the change affects both flag states equally, which is acceptable for a non-gated wordmark but worth flagging).

### [22] P3 · log-sheet.tsx sheet shadow hardcoded (not --elev-sheet token); inner-card claim misattributes the wrong web file
- **dimension:** elevation-parity
- **flag:** `design_system_elevation`
- **web file:** `src/app/components/suppr/log-sheet.tsx`
- **evidence:** PARTLY REAL, PARTLY MISATTRIBUTED.

REAL (hardcoded sheet shadow): log-sheet.tsx L345 hardcodes `"shadow-[0_-8px_32px_rgba(0,0,0,0.12)]"` on the DrawerPrimitive.Content shell. The canonical token already exists: src/styles/theme.css L240 `--elev-sheet: 0 -8px 32px rgba(0, 0, 0, 0.12);` (LIGHT — byte-identical to the hardcoded value) and L431 `--elev-sheet: 0 -8px 32px rgba(0, 0, 0, 0.5);` (DARK). Because the literal is light-only (alpha 0.12), in dark mode the web sheet renders a far-too-weak shadow vs the token's 0.5 alpha — a genuine dark-mode parity defect. Mobile reads the token: TodayEditMealModal L195 `style={[v2.sheet, Elevation.sheet, ...]}` and SavedMealPortionSheet L148 `style={[s.sheet, Elevation.sheet, ...]}`. suppr-card.tsx L71 confirms web maps `sheet: "var(--elev-sheet)"`. So swapping L345 → `shadow-[var(--elev-sheet)]` is correct and unifies light+dark, matching mobile Elevation.sheet.

MISATTRIBUTED (inner section cards): the candidate says "log-sheet.tsx inner cards stay flat while mobile's lift" and names TodayEditMealModal + SavedMealPortionSheet as the mobile reference. But those two mobile files are the EDIT-ENTRY and SAVED-MEAL-PORTION sheets — their web mirrors are `today-add-meal-dialog.tsx` (DialogContent L119) and `portion-picker.tsx`, NOT log-sheet.tsx. log-sheet.tsx is the search-first ADD/LOG sheet (header comment L4-43); it has essentially no resting "section cards" — browse rows use `bg-muted` hover (BrowseRow L1042-1048), the search row is `bg-muted` (L523), and the only `bg-card` surface is the barcode-manual-entry recovery form (L1095 `rounded-md border bg-card p-3`), which is not the inner-card pattern useCardElevation drives. The mobile elevation hook (useCardElevation.ts L48-79) drives `useBorder`/`liftBg`/`shadowStyle` on the NAME input (TodayEditMealModal L227-235), macro input wraps (L305-313), and the scaled-macro read-out card (SavedMealPortionSheet L226-235) — none of which exist in log-sheet.tsx. Confirmed: today-add-meal-dialog.tsx + portion-picker.tsx have ZERO `design_system_elevation`/`elev-card-soft` references (grep returned nothing), so the TRUE edit/portion web mirrors lack the gating; log-sheet.tsx is simply the wrong target for the inner-card half of the claim.
- **fix:** Scope down to what's actually real and split the two concerns:

1) log-sheet.tsx L345 — swap the hardcoded literal for the token: `"shadow-[var(--elev-sheet)]"`. This is a no-op in light (identical value) but fixes the dark-mode under-shadow (0.12 → 0.5 alpha), matching mobile Elevation.sheet. Sheets keep their shadow in both flag states — no flag gate needed here (the token itself already branches by .dark scope in theme.css). This is the only log-sheet.tsx change the candidate justifies, and it does NOT need design_system_elevation gating (it's a token-read swap, not a visual flag ramp).

2) Re-file the inner-card half against the correct web files. The real elevation-parity gap for mobile's two highest-traffic logging sheets (TodayEditMealModal, SavedMealPortionSheet) lives in their web mirrors: `today-add-meal-dialog.tsx` (the recipe/manual edit-entry dialog) and `portion-picker.tsx` (the portion read-out). Both currently use hardcoded `border border-border bg-card`/`bg-muted/40` surfaces with no `design_system_elevation` branch. Follow the established web pattern (recipe-edit-dialog.tsx L183-186): `const elevated = isFeatureEnabled("design_system_elevation"); elevated ? "bg-background border-transparent shadow-[var(--elev-card-soft)]" : "<current flat>"`. Do NOT gate log-sheet.tsx inner cards — it has none to gate (browse rows are bg-muted hover-only; the sole bg-card surface is the barcode-manual recovery form, not a resting elevation card).

Severity P3: dark-mode-only shadow weakness on a sheet that's only visible while open, plus a misfiled second half; cosmetic, behind no user-facing functional break.

### [23] P3 · today-add-meal-dialog.tsx is the only suppr dialog never swept onto design_system_elevation (recipe-edit / add-ingredient / override-ingredient all are)
- **dimension:** elevation-parity
- **flag:** `design_system_elevation`
- **web file:** `src/app/components/suppr/today-add-meal-dialog.tsx`
- **evidence:** CURRENT web today-add-meal-dialog.tsx has NO design_system_elevation gate at all: `grep -rn "design_system_elevation" src/` returns 8 files, none of them this dialog. L119: `<DialogContent className="bg-card border-border max-h-[90vh] ...">` is a static surface — no `isFeatureEnabled(...)` ternary, unlike its three direct siblings. The inner blocks are also static: L127 mode-toggle `flex rounded-lg border border-border p-1 bg-muted/50`; L192 recipe-portion `flex flex-wrap ... rounded-lg border border-border px-3 py-2 bg-muted/40`. The canonical web pattern these should follow is proven live in recipe-edit-dialog.tsx L183-186 (`const elevated = isFeatureEnabled("design_system_elevation"); const surfaceCls = elevated ? "bg-background border-transparent shadow-[var(--elev-card-soft)] max-w-lg ..." : "bg-card border-border max-w-lg ..."`), add-ingredient-dialog.tsx L256-258, and override-ingredient-dialog.tsx L116-118 — all three do the same bg-card→bg-background + --elev-card-soft + border-transparent swap under the flag. Tokens exist: src/styles/theme.css L239 `--elev-card-soft: 0 4px 12px rgba(28,25,22,0.07)` / L240 `--elev-sheet`, with dark variants L430-431. Dialog is still LIVE (not dead code): mounted in NutritionTracker.tsx L2906 on the `addOpen` state, separate from `<LogSheet>` (logSheetOpen, L3218). CORRECTION to candidate's mobile-reference framing: TodayEditMealModal.tsx is the EDIT-entry modal (slot/portion/macros of an already-logged meal, gated on `today-edit-entry-v2`, L92), and the web has NO edit-entry dialog counterpart at all (grep for editingMeal/editKcal/"Edit entry" across src/ returns nothing) — so TodayEditMealModal ↔ today-add-meal-dialog is not a 1:1 surface map. But the elevation primitive it cites (useCardElevation.ts L48-79 → design_system_elevation → Elevation.cardSoft/cardElevated; Elevation.sheet on the panel L195) is genuinely the mobile pattern the web `--elev-card-soft`/`--elev-sheet` system mirrors, so the elevation-parity gap itself is real.
- **fix:** Mirror the three sibling dialogs exactly. Add at the top of TodayAddMealDialog: `const elevated = isFeatureEnabled("design_system_elevation");` (import isFeatureEnabled from the web analytics module as recipe-edit-dialog does) and `const surfaceCls = elevated ? "bg-background border-transparent shadow-[var(--elev-card-soft)]" : "bg-card border-border";`. Replace L119's hardcoded `bg-card border-border` with `${surfaceCls}` while keeping the existing layout classes (`max-h-[90vh] min-h-[28rem] flex flex-col p-0 gap-0`). For the two inner blocks (L127 mode toggle, L192 recipe-portion), under flag-ON drop the hairline (`border-0`) and ride `shadow-[var(--elev-card-soft)]` on a tonal-lift bg; flag-OFF keeps current `border border-border bg-muted/*`. Leave the DialogFooter `border-t` (L310) as the sticky-footer separator regardless of flag. This is a low-priority cosmetic consistency fix, gated and reversible — fold it into the next elevation-sweep cleanup PR rather than a standalone change; it has no functional or accessibility impact (the dialog already renders with the base shadow-lg shell from dialog.tsx L60). Track as a one-line addition to the existing ENG-795 elevation-sweep scope.

### [24] P3 · Web photo-log "Plate total" row still renders 👉 emoji; mobile swapped to Lucide ArrowRight
- **dimension:** icons-parity
- **flag:** `none`
- **web file:** `src/app/components/suppr/photo-log-dialog.tsx`
- **evidence:** Web photo-log-dialog.tsx line 541 still renders the literal emoji as the leading glyph of the "Plate total" banner: `<span aria-hidden className="text-base">👉</span>` (inside the plate-total block, lines 537-544). It imports no Lucide arrow — only `Icons` from ../ui/icons (line 43), and the plate-total row uses the raw emoji span. Mobile PhotoLogSheet.tsx imports `ArrowRight` from lucide-react-native (line 55) and renders it on the identical "Plate total" row (lines 743-746): `<ArrowRight size={16} color={colors.text} style={{ marginRight: 6 }} />`, with a code comment on lines 740-742 documenting the exact swap: "2026-05-06 audit (F-107): swap 👉 emoji for lucide ArrowRight per project icon-registry rule." Both rows are the same primary-tinted "Plate total" banner showing `formatRangeKcal(totalKcal)`, so this is a direct mobile-switched/web-didn't divergence and a banned functional emoji on web.
- **fix:** In photo-log-dialog.tsx, replace the emoji span on line 541 with a Lucide arrow to match mobile. Add `import { ArrowRight } from "lucide-react";` to the imports, then change the plate-total leading glyph from `<span aria-hidden className="text-base">👉</span>` to `<ArrowRight size={16} aria-hidden className="text-foreground" />`. This matches mobile exactly (ArrowRight, size 16, foreground/text colour), completes the already-shipped F-107 mobile change, and removes the banned functional emoji. No flag needed — it is an icon-language parity fix on an existing row, not a structural/layout change. Note: the project codebase exposes a shared `Icons` registry (../ui/icons); if an ArrowRight glyph already exists there it should be preferred over a direct lucide-react import to stay consistent with the file's existing icon usage (e.g. Icons.camera, Icons.close).

### [25] P3 · Web creator recipe-list uses a 🍳 emoji as the missing-thumbnail placeholder instead of the canonical RecipeHeroFallback
- **dimension:** icons-parity
- **flag:** `none`
- **web file:** `src/app/components/creator/CreatorRecipeList.tsx`
- **evidence:** CreatorRecipeList.tsx lines 126-130 render the no-image branch as a bare emoji: `<div className="w-14 h-14 rounded-lg bg-muted flex items-center justify-center text-muted-foreground text-xs"> 🍳 </div>`. There is no import of RecipeHeroFallback anywhere in the file (imports are lines 3-12: react, next/link, supabase, normalizeRecipeTitle, creatorRecipePagination only). Meanwhile the canonical web fallback exists at src/app/components/suppr/RecipeHeroFallback.tsx (Lucide gradient+glyph, lines 11-38 import Salad/Beef/Fish/Pizza/Cookie/Soup/Wheat/Utensils) and the grep proves it is the product-wide no-image treatment: Library.tsx:41/418/555, RecipeDetail.tsx:244/1601, discover-recipe-image.tsx:92/115, today-eat-again-banner.tsx:40, north-star-block.tsx:262 all use it; the creator list is the lone holdout using an emoji. Mobile parity component (apps/mobile/components/RecipeHeroFallback.tsx) mirrors the same 8-glyph spec via @suppr/shared/recipe/recipeHeroFallback, confirming the canonical cross-platform pattern. The emoji is not flag-gated — there is no else branch with a flag, just the raw emoji at line 128.
- **fix:** Import the canonical fallback (`import { RecipeHeroFallback } from "../suppr/RecipeHeroFallback";`) and replace the lines 126-130 emoji branch with it. NOTE: RecipeHeroFallback renders `position:absolute; inset:0` (it is a fill overlay — see RecipeHeroFallback.tsx line 61), so it cannot self-size; wrap it in a relatively-positioned 56px box exactly like the other call sites (Library.tsx:418/555). i.e.: `<div className="relative w-14 h-14 rounded-lg overflow-hidden"><RecipeHeroFallback id={r.id} title={r.title} iconSize={20} /></div>`. Pass iconSize ~20 so the glyph fits the 56px row. This removes the divergent emoji and aligns with the canonical cross-platform fallback. (The audit's verbatim `className="w-14 h-14..."` directly on RecipeHeroFallback would mis-size it because of the absolute positioning — the relative wrapper is required.)

### [26] P3 · Web Today meals row renders an extra NutritionSourceBadge (with ✓/✎ dingbat glyphs) that mobile deliberately removed
- **dimension:** icons-parity
- **flag:** `none`
- **web file:** `src/components/NutritionSourceBadge.tsx`
- **evidence:** CURRENT web code confirms the dingbats: src/components/NutritionSourceBadge.tsx line 17 `verified: { label: "Verified", abbr: "✓", ... }` (literal U+2713) and line 19 `manual: { label: "Manual", abbr: "✎", ... }` (literal U+270E pencil). Line 28 renders them as text: `{cfg.abbr} {cfg.label}` inside a `<span>` — no lucide-react import anywhere in the 34-line file. The badge is rendered unconditionally (no feature flag) in src/app/components/suppr/today-meals-section.tsx line 481: `{meal.source && <NutritionSourceBadge source={meal.source} />}`; that same row ALSO renders <SourceDot> at line 473, so web shows BOTH a dot and the text glyph. Mobile reference SourceDot.tsx uses Lucide (line 3 `import { Sparkles } from "lucide-react-native"`, rendered line 51) for AI and a plain coloured View dot otherwise (lines 62-73) — never a dingbat. The candidate's premise that mobile's meal row renders SourceDot is itself WRONG: apps/mobile/components/today/TodayMealsSection.tsx imports SourceDot (line 41) but does NOT render it in the meal row — the row renders a static success dot (line 1066: `<View style={{ width: 6, height: 6, borderRadius: 3, backgroundColor: Accent.success ... }} />`), and lines 1095-1102 document an explicit Grace decision on 2026-05-22 to REMOVE the per-meal source badge ("Manual" / "FatSecret") from the row because it ate vertical space, moving provenance to the meal detail page. So mobile shows neither the badge nor SourceDot in the row.
- **fix:** Two distinct issues, ranked. PRIMARY (presence parity): mobile intentionally removed the per-meal text source badge from the meal row on 2026-05-22 (TodayMealsSection.tsx lines 1095-1102) — web still renders it at today-meals-section.tsx line 481. To restore parity, drop `{meal.source && <NutritionSourceBadge source={meal.source} />}` from the web row; the SourceDot at line 473 already conveys provenance, matching the leaner mobile row, and the full source label can live behind the row's detail affordance as mobile does. SECONDARY (cosmetic glyph, only if the badge is kept): replace the U+2713 (line 17) and U+270E (line 19) dingbats with inline lucide-react glyphs (`Check`/`ShieldCheck` for verified, `Pencil` for manual) sized to the 10px badge text, to match mobile's Lucide-only icon language. Both changes are cosmetic/structural-trim with no logic impact; the glyph swap needs no flag, but per CLAUDE.md the row-structure removal is a visible layout change and should ship behind an isFeatureEnabled gate with the old path retained.

### [27] P3 · Web log/edit/portion sheets lack the redesign_motion element→sheet open morph that mobile applies
- **dimension:** motion-parity
- **flag:** `redesign_motion`
- **web file:** `src/app/components/suppr/log-sheet.tsx`
- **evidence:** MOBILE (current) gates a real redesign_motion morph: TodayEditMealModal.tsx L139 `const motionEnabled = isFeatureEnabled("redesign_motion");` L140 `const { sheetStyle } = useSheetMorph(open && motionEnabled);` L172 `animationType={motionEnabled ? "none" : "slide"}` (Reanimated owns the curve) L191 `<Animated.View style={(motionEnabled ? sheetStyle : undefined) as StyleProp<ViewStyle>}>`. SavedMealPortionSheet.tsx mirrors it: L77-78, L125, L144. The mobile hook apps/mobile/lib/motion.ts L168-187 `useSheetMorph` springs the panel `translateY(sheetHeight)→0` with SPRING_DEFAULT and scales the trigger to SHEET_MORPH_SCALE with SPRING_SNAPPY.

WEB (current) lacks all of this. The shared source src/lib/motion.ts DOES export the CSS analogs — L234 `export function sheetTransition(open)` (transform translateY 100%→0 over SHEET_OPEN_MS=320ms with SPRING_EASE + paired `backdropTransition: opacity ${BACKDROP_FADE_MS}ms ease`), L249 `export function triggerMorphStyle(active)` (scale→SHEET_MORPH_SCALE=0.96 with SPRING_SNAPPY_EASE). But a repo-wide grep proves NO web file imports/consumes them: every match for sheetTransition/triggerMorphStyle/backdropTransition is inside src/lib/motion.ts itself; `grep redesign_motion src/` returns zero hits. The web LogSheet instead gates only the OLDER premiumMotion path: log-sheet.tsx L75 import isPremiumMotionV1Enabled, L317 `const premiumMotion = isPremiumMotionV1Enabled();`, L346-348 `premiumMotion ? "...slide-in-from-bottom duration-500 ease-[cubic-bezier(0.32,0.72,0,1)]" : ""` on DrawerPrimitive.Content — a plain slide with no trigger-element morph and no redesign_motion gate. Web isFeatureEnabled exists (src/lib/analytics/track.ts L30) so the gate is wireable.
- **fix:** Real but low-severity motion-polish parity gap (cosmetic; both platforms still animate the sheet open — web just uses the older premiumMotion slide rather than the new morph personality, and the trigger-element scale-down is absent). In log-sheet.tsx add a redesign_motion branch using the already-exported, currently-unconsumed shared helpers: when isFeatureEnabled("redesign_motion") is on, drive DrawerPrimitive.Content from sheetTransition(open) (transform + backdropTransition on the Overlay) instead of the premiumMotion slide branch, and apply triggerMorphStyle(active) inline-style to the element that opens the sheet (bottom-bar "+"/row) so it scales to 0.96 while opening. Keep the existing premiumMotion/else path alive for flag-off per the feature-flag rule. Note one caveat: vaul (DrawerPrimitive) already owns its own open transform, so a clean wire may need shouldAnimate/asChild handling or moving to a custom-positioned panel rather than naively stacking sheetTransition's translateY on top of vaul's. Web has no edit-entry / saved-meal-portion sheet equivalents at all (find src found none; today-meals-section.tsx has no edit dialog) — so the matching wiring for those mobile sheets has no web host yet; that is a separate, larger structural gap, not fixable inside log-sheet.tsx.

### [28] P3 · Web edit-entry / usual-meal dialogs use default Radix fade/zoom, not the redesign_motion sheet-morph mobile applies
- **dimension:** motion-parity
- **flag:** `redesign_motion`
- **web file:** `src/app/components/suppr/today-meals-section.tsx`
- **evidence:** CURRENT web code lacks the morph entirely. In today-meals-section.tsx the usual-meal picker is a plain centered Radix dialog: line 704 `<Dialog open={usualPicker != null} ...>` wrapping line 710 `<DialogContent className="bg-card border-border max-w-md">` — Radix's stock fade/zoom present animation, no sheet morph and no row→sheet transform. The file's only flag read is line 260 `const usualRowV2 = isFeatureEnabled("today_log_usual_row_v2");`; it never reads `redesign_motion`. Repo-wide checks confirm the gap: `grep -rn "redesign_motion" src/` returns nothing, and `grep -rn "today-edit-entry-v2" src/` returns nothing. The web morph helpers DO exist — src/lib/motion.ts:234 `export function sheetTransition(open: boolean)` and :249 `export function triggerMorphStyle(active)` — but `grep -rln "sheetTransition|triggerMorphStyle" src/` lists ONLY src/lib/motion.ts itself, i.e. zero web components consume them (not this dialog, not even LogSheet). By contrast the mobile reference gates and applies the morph: TodayEditMealModal.tsx:139-140 `const motionEnabled = isFeatureEnabled("redesign_motion"); const { sheetStyle } = useSheetMorph(open && motionEnabled);` then :191 `<Animated.View style={(motionEnabled ? sheetStyle : undefined) as StyleProp<ViewStyle>}>` plus :172 `animationType={motionEnabled ? "none" : "slide"}`; SavedMealPortionSheet.tsx mirrors this at :77-78 and :144. So the new motion vocabulary mobile applies to these two sheets is genuinely absent on web.
- **fix:** Gate behind isFeatureEnabled("redesign_motion"). When ON, route the usual-meal picker (and the web edit/portion equivalents) through the already-built src/lib/motion.ts helpers — apply `sheetTransition(open)` to the panel so it springs in with SPRING_EASE and `triggerMorphStyle(active)` to the tapped row — or convert it to a bottom-sheet Drawer variant consistent with LogSheet, matching mobile's TodayEditMealModal/SavedMealPortionSheet. Keep the current plain Radix DialogContent (line 710) alive in the flag-off else branch per the feature-flag rollout rule. NOTE the broader root cause to surface to Grace: the web motion helpers are wired into NO web component at all (LogSheet included), so `redesign_motion` is effectively unimplemented on web — fix the primary LogSheet cold-open surface first, then these secondary dialogs. Lower priority (P3) on its own: these are secondary edit/portion dialogs, not the cold-open log surface.

### [29] P3 · Web search-result confidence chip is inlined (no shared component) and the segmented filter control is result-count gated, unlike mobile
- **dimension:** search-results-parity
- **flag:** `redesign_search_results`
- **web file:** `src/app/components/food-search/FoodSearchPanel.tsx`
- **evidence:** CHIP INLINING (true): Web hand-rolls the Verified/Estimated chip inline in renderRedesignedRow, FoodSearchPanel.tsx lines 1624-1639: `<span data-testid={\`food-search-confidence-${tier}\`} className={\`... ${tier === "verified" ? "bg-primary/10 text-primary" : "bg-warning/15 text-warning"}\`}> {tier === "verified" ? <Icons.check.../> : <Icons.info.../>} {tier === "verified" ? "Verified" : "Estimated"} </span>`. No shared web chip exists: `grep -rn SearchResultConfidenceChip src` returns nothing; the only src confidence-chip.tsx is the unrelated neutral TDEE pill — its own header (lines 4, 18: `ConfidenceChip — neutral grey chip used for adaptive TDEE display ... low | medium | high`). Mobile by contrast factored it into apps/mobile/components/ui/SearchResultConfidenceChip.tsx and shares it across all three logging entry points: food-search (FoodSearchPanel.tsx:1278 `<SearchResultConfidenceChip tier=...>`), barcode (apps/mobile/app/(tabs)/barcode.tsx:30,719) and voice-log (apps/mobile/components/AiLogReviewItem.tsx:26,175 `<SearchResultConfidenceChip tier="estimated" testID="voice-confidence-chip"/>`). SEGMENTED-CONTROL GATING (true): Web gates the control on results presence — FoodSearchPanel.tsx:1948 `{searchResultsRedesign && results.length > 0 && (` ... `data-testid="food-search-category-tabs"` ... `)}` — so it disappears during loading and on the empty state. Mobile renders the category ScrollView unconditionally at the top of the main return (apps/mobile/.../FoodSearchPanel.tsx:1886 `<ScrollView horizontal ... testID="food-search-category-tabs">`), with only label/style changing under the flag (line 1915 `if (redesignSearch) {`); it is not wrapped in any results-length guard, so it persists across loading/empty/recents/results.
- **fix:** Two independent fixes. (1) Segmented-control gating — the real user-facing divergence: relax the web gate at line 1948 so the redesigned control mirrors mobile, i.e. render whenever `searchResultsRedesign` is on and the user has searched (e.g. gate on `query.trim()` / loading-or-results) rather than strictly `results.length > 0`, so the filter persists through loading and the category-empty state as it does on mobile. Keep it behind the redesign_search_results flag. (2) Chip drift — lower priority, maintainability only: extract a shared web SearchResultConfidenceChip (verified=blue/Check, estimated=amber/Info, matching the mobile tokens and the 2026-05-31 prototype) under src/app/components/ui/, consume it in renderRedesignedRow in place of the inline span at 1624-1639, and reuse it on the web barcode (today-barcode-dialog.tsx, currently no chip) and voice-log surfaces so the trust language cannot drift the way the mobile component prevents. Note the web voice-log dialog currently uses a different ConfidenceDot (low/medium/high), so unifying voice-log onto the Verified/Estimated chip is a deliberate product call, not a mechanical swap. Add a Playwright assertion that the control is visible in the loading/empty state under the flag.

### [30] P3 · Today: no committed flag-ON golden baseline for the four redesign flags (web visual-regression gap)
- **dimension:** visual-regression-readiness
- **flag:** `today-status-pills, today_log_usual_row_v2, today_log_again, log-sheet-slot-selector`
- **web file:** `tests/e2e/visual-audit.spec.ts (baselines), tests/e2e/visual-audit-authed.spec.ts, src/app/components/suppr/today-hero-stats.tsx, src/app/components/suppr/today-meals-section.tsx, src/app/components/NutritionTracker.tsx`
- **evidence:** The four flags are real and gate visual redesign paths on web Today: today-hero-stats.tsx:163 `{isFeatureEnabled("today-status-pills") &&`; today-meals-section.tsx:260 `const usualRowV2 = isFeatureEnabled("today_log_usual_row_v2");`; NutritionTracker.tsx:2570 `onLogAgain={isFeatureEnabled("today_log_again") ? logAgainSlot : undefined}`; NutritionTracker.tsx:3228 `isFeatureEnabled("log-sheet-slot-selector")`. The only committed Today goldens are shell-today-{desktop,mobile}.png from visual-audit.spec.ts, whose describe block uses the unauthenticated `chromium` project (no storageState) — `/today` 307s to /login, so they are flag-OFF AND unauthed (`git ls-files 'tests/e2e/__snapshots__/**/*.png'` shows only shell-* and subpages-public-*; zero tabs/ or tabs-redesign/). visual-audit-authed.spec.ts:33 writes `tabs/today-{vp}.png` but those baselines are NOT committed and the spec is skipped without E2E creds (line 23 `test.skip(!hasE2ECredentials(), ...)`). isFeatureEnabled (src/lib/analytics/track.ts:30-37) returns the live PostHog value with no test-side override, and grep found zero `addInitScript`/PostHog-bootstrap flag-forcing anywhere in tests/e2e/. So no committed baseline renders any of the four flag-ON paths. Mitigation that exists: log-sheet.stories.tsx bakes the slot-selector flag-ON appearance via a direct `slot` prop (flag-independent) for Storybook/Chromatic; and three states have unit tests (todayStatusPills.test.tsx, todayMealsSectionLogUsualRowV2.test.tsx, todayMealsSectionLogAgain.test.tsx, logSheetSlotHonouredWeb.test.ts) — but no Playwright golden image and no Storybook story for the status-pills / usual-row-v2 / log-again hero+meals composition.
- **fix:** This is a web visual-regression-readiness gap, not a web↔mobile feature divergence (mobile bakes these behaviours in and Maestro-captures them; the four web paths are unit-tested and the slot selector has a Storybook story). To close the pixel-golden gap, add an authed Playwright spec (e.g. tests/e2e/visual-regression-redesign.spec.ts, added to authedTestMatch in playwright.config.ts) that, in a page.addInitScript before navigation, stubs posthog.isFeatureEnabled to return true for the four flags (or seeds a PostHog bootstrap override), visits /today on a seeded day with logged meals + a populated slot, and writes tabs-redesign/today-{mobile,desktop}.png. Because the spec is creds-gated it will skip on CI without E2E_EMAIL/E2E_PASSWORD — so to get a baseline that actually runs in CI, prefer extending Storybook/Chromatic coverage instead: add today-hero-stats.stories.tsx (status-pills ON) and today-meals-section.stories.tsx (usualRowV2 ON + onLogAgain set) alongside the existing log-sheet.stories.tsx, since stories render the redesign markup flag-independently via props and Chromatic already captures stories. Track via a Linear issue rather than a code TODO.

### [31] P3 · Plan tab: no golden baseline captures the flag-ON (plan_source_selector + redesign_winmoment) state on web
- **dimension:** visual-regression-readiness
- **flag:** `plan_source_selector + redesign_winmoment`
- **web file:** `src/app/components/MealPlanner.tsx`
- **evidence:** MealPlanner.tsx gates two flag-ON visual paths that no committed golden renders: `const planSourceSelector = isFeatureEnabled("plan_source_selector");` (line 189) drives the PlanSourceSelector block (lines 781-790), and `const winMomentsEnabled = isFeatureEnabled("redesign_winmoment");` (line 294) drives the state-aware headline colour (`summaryHeadlineColor`, lines 296-302, applied at lines 656-666). Both flags default OFF in tests: `isFeatureEnabled` returns false when NEXT_PUBLIC_POSTHOG_KEY is absent (src/lib/analytics/track.ts:31), and no Playwright spec sets the flags. The public golden `visual-audit.spec.ts` visits `/plan` unauthenticated with no flag override (lines 30-43 → `shell/plan-{vp}.png`; committed baselines tests/e2e/__snapshots__/visual-audit.spec.ts/shell-plan-{desktop,mobile}.png both exist) so it always captures flag-OFF. The authed golden `visual-audit-authed.spec.ts` writes `tabs/plan-{vp}.png` (line 33) but (a) has NO committed baseline — `tests/e2e/__snapshots__/` contains only visual-audit.spec.ts and visual-regression-subpages.spec.ts directories, no authed dir — (b) skips entirely without E2E_EMAIL/E2E_PASSWORD (line 23), and (c) contains no addInitScript/flag override, so even when run it captures flag-OFF. PlanSourceSelector.stories.tsx exists and is matched by .storybook/main.ts glob (`../src/**/*.stories.@(js|jsx|mjs|ts|tsx)`), but its own docstring concedes the flag-ON Plan-tab appearance "can't be screenshotted from the real authed Plan tab without enabling the flag in PostHog" and only renders the selector in isolation. There is no MealPlanner.stories file, and redesign_winmoment has zero story/golden coverage on web. Mobile twin gates the identical flags: apps/mobile/app/(tabs)/planner.tsx:477 (plan_source_selector) and :1124 (redesign_winmoment).
- **fix:** Add a flag-ON visual golden for the Plan tab. Cheapest robust path: a Storybook story for MealPlanner (or extend the existing PlanSourceSelector story) that renders the flag-ON composition and let Chromatic (chromatic.config.json already present) capture it — this sidesteps the PostHog dependency entirely since stories render the presentational tree directly. If a full-page golden is wanted, add a dedicated Playwright spec that injects the flags via page.addInitScript (seed posthog feature-flag overrides / localStorage) before navigating to /plan authed, then snapshot tabs-redesign/plan-{vp}.png. Note this is visual-regression-readiness hygiene, not a missing user-facing feature — both flag-ON paths are implemented and parity-matched to mobile; the only gap is that no committed baseline locks their pixels. Keep flag-OFF goldens as-is.

### [32] P3 · Cookie consent banner uses raw slate chrome, not redesign tokens (no SupprMark / Lucide / elevation)
- **dimension:** web-only-chrome-cookie-and-error
- **flag:** `none`
- **web file:** `src/app/components/CookieConsent.tsx`
- **evidence:** Current CookieConsent.tsx renders raw slate chrome, confirming every claim. Container (line 70): `bg-white/95 dark:bg-slate-900/95 backdrop-blur border-t border-slate-200 dark:border-slate-800 shadow-lg` — not bg-card/border-border/elevation tokens. Body text (line 75): `text-xs text-slate-700 dark:text-slate-300` — not text-muted-foreground. Decline button (line 84): `bg-slate-100 dark:bg-slate-800 text-slate-700 dark:text-slate-300 ... hover:bg-slate-200 dark:hover:bg-slate-700` — not bg-muted. Only Accept (line 90) is token-aware: `bg-primary text-primary-foreground ... hover:bg-primary/90`. No SupprMark/SupprPlateMark import or render and no Lucide icon anywhere in the file (imports at lines 3-5 are only useEffect/useState, Link, usePathname). Comments confirm two prior LAYOUT-only audits — line 56 "Audit 2026-05-04 #24 + #36" and line 60 "Audit 2026-05-12 (premium-bar Phase 2)" — touching size/padding/placement, never tokens or brand. The redesign token system the fix should target exists: suppr-mark.tsx defines SupprPlateMark (line 94) using `--brand-mark-bg`/`--brand-mark-ring` (lines 106-123). Caveat: importing SupprMark pulls `isFeatureEnabled("design_system_brandmark")` (suppr-mark.tsx lines 3, 66) — the banner must paint pre-consent, so use SupprPlateMark directly (no flag) or a static Lucide glyph.
- **fix:** Pure chrome token swap, no flag gate (banner must render on first paint pre-consent). Container: `bg-white/95 dark:bg-slate-900/95 ... border-slate-200 dark:border-slate-800 shadow-lg` -> `bg-card/95 backdrop-blur border-t border-border shadow-lg` (or the project's card-elevation token). Body text: `text-slate-700 dark:text-slate-300` -> `text-muted-foreground`. Decline button: `bg-slate-100 dark:bg-slate-800 text-slate-700 dark:text-slate-300 hover:bg-slate-200 dark:hover:bg-slate-700` -> `bg-muted text-muted-foreground hover:bg-muted/80`. Keep Accept on `bg-primary text-primary-foreground` so UK/EU equal-prominence holds (both buttons stay inline at matching weight). Optionally prefix copy with `<SupprPlateMark size={16} />` imported directly from src/app/components/ui/suppr-mark.tsx (NOT SupprMark, which evaluates the design_system_brandmark flag) or a static Lucide Cookie/ShieldCheck glyph. After swap, verify the line-clamp-1 single-line body survives on the tightest viewport (line 75).

### [33] P3 · Plan 7/7 win moment on web lacks the rising-edge scale pulse mobile plays when the week first crosses into win
- **dimension:** winmoment-surface-parity
- **flag:** `redesign_winmoment`
- **web file:** `src/app/components/MealPlanner.tsx`
- **evidence:** WEB src/app/components/MealPlanner.tsx: tone colour is wired (L294-302: winMomentsEnabled = isFeatureEnabled("redesign_winmoment"); summaryTone === "win" ? "var(--accent-win)" : "progress" ? "var(--warning)" : "var(--muted-foreground)"), but summaryTone is computed plainly on every render (L295: `const summaryTone = planWeekHeadlineTone(summary);`) with NO rising-edge tracking — `grep -c useRef` returns 0 for the whole file, so no prevSummaryToneRef and no useEffect firing on the win edge. The only celebratory animation is a static colour ease: headline at L656-666 uses className "...transition-colors" with inline color, and the inline comment at L654-655 confirms this is deliberate — "The colour-only change animates via `transition-colors` so a regenerate that flips the tone eases rather than snaps." The comment at L292-293 acknowledges ONLY the haptic gap ("no Haptics API; the colour shift + the subtitle carry the payoff") — it neither mentions nor implements any visual rising-edge pulse. No @keyframes/scale pulse for the win exists anywhere (grep finds only `animate-spin` for the loading spinner at L697/L1418; theme.css has the --accent-win token but no win-pulse keyframe). MOBILE apps/mobile/app/(tabs)/planner.tsx L1240-1258 HAS exactly this: `summaryPulse` shared value + `prevSummaryToneRef` (L1240-1241), and a useEffect that fires ONLY on the rising edge into win (L1248: `if (summaryTone === "win" && prev !== null && prev !== "win")`) running a one-shot scale spring `withSequence(withSpring(1.06, SPRING_SNAPPY), withSpring(1, SPRING_DEFAULT))` (L1249-1252) plus `Haptics.notificationAsync(...Success)` (L1253). So mobile has a per-edge visual scale-pulse payoff that web does not.
- **fix:** In MealPlanner.tsx, add a rising-edge win pulse mirroring the mobile block, gated behind redesign_winmoment with the existing static-colour path as the flag-off/else behaviour. Concretely: (1) add a `prevSummaryToneRef = useRef<PlanWeekHeadlineTone | null>(null)` and a `useState` boolean `winPulse`; (2) in a `useEffect([winMomentsEnabled, summaryTone])` read the previous tone from the ref, update the ref, and when `winMomentsEnabled && summaryTone === "win" && prev !== null && prev !== "win"` set winPulse=true then clear it after ~300ms (matching mobile's one-shot spring duration); (3) on the headline `<p>` (L656-666) conditionally apply a one-shot CSS keyframe class (e.g. `win-pulse` -> `@keyframes win-pulse { 0%{transform:scale(1)} 40%{transform:scale(1.06)} 100%{transform:scale(1)} }` added to theme.css/globals) when winPulse is true, wrapped in a `@media (prefers-reduced-motion: reduce)` guard that disables the transform. Keep transition-colors for the steady-state colour. Haptics have no web analog (acceptable, already noted in the L292-293 comment). Add/extend the planner parity test (the one reading data-tone / data-testid="planner-week-summary-headline") to assert the pulse class is applied only on a non-win -> win tone transition, not on mount of an already-7/7 plan.

## Dismissed as phantom / out-of-scope (7)

- **Meal-nutrition full-nutrient sheet sections are flat — no elevation flag** (`src/app/components/suppr/full-nutrient-panel-sheet.tsx`) — The audit cites the wrong mobile reference. The web full-nutrient-panel-sheet.tsx is the DIRECT mirror of mobile apps/mobile/components/today/FullNutrientPanelSheet.tsx (web docstring L23: "Mirrors `apps/mobile/components/today/FullNutrientPanelSheet.tsx`"; both consume the same shared helper buildFullNutrientPanelRows; both export a component literally named FullNutrientPanelSheet) — NOT meal-nutrition.tsx, which is a different screen that mirrors MacroDetailPanel.tsx. The true mobile twin is ALSO flat and ALSO has no elevation flag: `grep -n "design_system_elevation\|useCardElevation\|SupprCard" apps/mobile/components/today/FullNutrientPanelSheet.tsx` returns EMPTY, and its SectionBlock (L213-216) renders a static flat card: `backgroundColor: colors.card, borderRadius: Radius.lg, borderWidth: StyleSheet.hairlineWidth, borderColor: colors.cardBorder`. That is the exact equivalent of the web SectionBlock's flat class on L118: `bg-card border border-border rounded-[14px] px-3 py-1 divide-y divide-border`. So web and its real mobile sibling are already in parity — both flat, neither flag-gated. The mobile meal-nutrition.tsx elevation work (useCardElevation L123; cardSurfaceStyle L370-375) and NutritionDetailEmptyState (softElevation branch L148-178) belong to a different screen whose web counterpart, MacroDetailPanel.tsx, already DOES gate on the flag (L102 `const redesignElevation = isFeatureEnabled("design_system_elevation")`; elevated empty-state L154-168). That lane is covered; this sheet is a separate lane.

- **Web full-nutrient-panel-sheet carries no branded SupprMark header where its mobile twin sits in the branded-sheet world** (`src/app/components/suppr/full-nutrient-panel-sheet.tsx`) — The mobile "reference" does NOT have branded chrome, so there is nothing for web to be missing. Mobile FullNutrientPanelSheet.tsx header block (lines 290-321) is a plain row: a `<Text>` "All nutrients" title (lines 300-302) plus a subtitle (lines 303-311) and a lucide `X` close button (lines 313-320) — no SupprMark, no flag. Its file-header comment (lines 23-52) never mentions branded sheets or `redesign_branded_sheets`. A grep across BOTH files for SupprMark|redesign_branded_sheets|isFeatureEnabled|brandedSheet returns "NO MATCHES IN EITHER FILE". Web (full-nutrient-panel-sheet.tsx) mirrors this with a plain DialogHeader/DialogTitle "All nutrients" + DialogDescription (lines 161-166). Both platforms use an identical plain header — they are in parity. The candidate's own text concedes this: "the mobile FullNutrientPanelSheet is not itself under the redesign_branded_sheets flag today — this is the next sheet to bring into the branded grammar, not a same-flag regression." A parity audit fires only when web lacks what mobile HAS; mobile has no branded header here. Adding SupprMark to web alone (per the suggested fix) would CREATE web-only divergence, the opposite of parity.

- **Web-only emoji/dingbat status glyphs: 🍽️ on recipe 404, ✓ verified-creator badge, ✓/✗ reset-modal bullets** (`app/recipe/[id]/not-found.tsx`) — Not a parity gap: all three surfaces are web-only with no mobile twin, and the "mobile reference" is a decision doc, not a screen. (1) app/recipe/[id]/not-found.tsx:8 `<span className="text-3xl">🍽️</span>` — a Next.js App Router not-found boundary; mobile has no equivalent route-level 404. (2) app/creator/[id]/page.tsx:156 renders a bare `✓` in the verified-creator badge; public creator profile pages are web-only (no mobile creator-profile screen). (3) src/app/components/Settings.tsx:1771 `{row.kept ? "✓" : "✗"}` with aria-label Kept/Cleared — web reset-confirmation modal chrome. The audit candidate itself concedes "None has a mobile equivalent, so none is a parity divergence." The icon-strategy doc (docs/decisions/2026-05-31-icon-strategy.md:14,32) sets a cross-cutting "Eliminate functional emoji / No functional emoji" RULE — not a mobile feature web lacks. That rule is already tracked: docs/planning/2026-05-31-redesign-implementation-plan.md:107 ("[P2] Eliminate functional emoji from UI components on both platforms; enforce via ESLint rule — folds ENG-808") and :1016 (design_system_icons workstream). So this is an already-ticketed code-quality cleanup, not a web↔mobile parity divergence.

- **Progress redesign flag-ON paths have no golden baseline (visual-regression coverage gap, not a feature-parity gap)** (`src/app/components/ProgressDashboard.tsx (rendered via src/app/App.tsx; app/(product)/progress/page.tsx returns null)`) — The web FEATURE code is fully present — this is NOT a case of web lacking what mobile has. ProgressDashboard.tsx gates and renders all three redesign paths: redesign_winmoment at track.ts callsite line 549 (`const winMomentEnabled = isFeatureEnabled("redesign_winmoment")`) with the WinMomentPlayer rendered at line 2038; progress_digest_blend at line 811 (`setDigestBlendEnabled(isFeatureEnabled("progress_digest_blend"))`) with the blended Digest gated at lines 1284/1383/1441; progress_trajectory_box at line 825 (`setTrajectoryBoxEnabled(isFeatureEnabled("progress_trajectory_box"))`) rendered at line 2172 `{trajectoryBoxEnabled && profileWeightSurfaceMode === "show" ? (<TrajectoryCard`. ProgressMetricDetail.tsx line 44 gates design_system_elevation (`const elevated = isFeatureEnabled("design_system_elevation")`). Mobile progress.tsx mirrors these exactly (lines 780-783 progress_digest_blend, 794-797 progress_trajectory_box, lines 264-268 + LogWeightSheet for redesign_winmoment). So feature parity HOLDS.

The candidate's actual claim — that no flag-ON golden baseline exists — is CORRECT but it's a test-infra gap. `find tests/e2e/__snapshots__ -iname "*progress*"` returns ONLY shell-progress-desktop.png and shell-progress-mobile.png (from visual-audit.spec.ts "public shell", which navigates /progress unauthenticated). Because isFeatureEnabled returns false for cold/unloaded PostHog (track.ts lines 30-37: `if (!process.env.NEXT_PUBLIC_POSTHOG_KEY) return false`), those shell baselines capture flag-OFF legacy chrome only. The authed spec visual-audit-authed.spec.ts writes `tabs/progress-${vp}.png` but is test.skip'd without E2E_EMAIL/E2E_PASSWORD AND forces no flags — and no `tabs/progress-*.png` file is committed. No `tabs-redesign/` dir exists anywhere (grep -rln "tabs-redesign" returns nothing), and grep for flag-forcing across tests/e2e/ returns nothing. So the redesign render paths have zero pixel-golden coverage. But mobile equally has none (Maestro is flow-based, not pixel-golden), so web does not "lack what mobile has."

- **Macro detail: no baseline (web panel, not a standalone route)** (`src/app/components/MacroDetailPanel.tsx`) — The candidate's own premise concedes the elevated variant is implemented ("reads design_system_elevation (line 102)"), and the current web code confirms full parity with the mobile reference — NOT a missing feature:

- MacroDetailPanel.tsx:102 `const redesignElevation = isFeatureEnabled("design_system_elevation");`
- :154-168 flag-ON branch renders the iconified, elevated empty card (rounded-xl + ambient shadow + Salad icon + "No meals logged yet" / "Log a meal to see your {macro} broken down here."), mirroring mobile macro-detail.tsx:174-184 (`NutritionDetailEmptyState`, icon=Salad, same title/subtitle).
- :169-173 flag-OFF else keeps the legacy one-line empty state alive (per the feature-flag rule).
- The elevated variant is ALREADY covered by unit tests: macroDetailPanel.test.tsx:94-106 asserts flag-ON renders a `div` with `rounded-xl` and the redesign copy; :84-92 asserts flag-OFF renders the legacy `<p>`.
- Cross-platform parity is enforced: apps/mobile/tests/unit/macroIngredientBreakdownParity.test.ts:31 reads the web panel file directly.

The claim reduces to "no committed pixel baseline / no standalone web route," which is a visual-regression-INFRA observation, not a behavior mobile has and web lacks. Verified: there is no `deep-redesign/` baseline directory anywhere in the repo (`grep -rln deep-redesign` → empty), no Playwright/visual spec touches the panel (only macroDetailPanel.test.tsx references it), and the `app/dev/macro-display/page.tsx` harness the fix proposes renders TodayDashboardMacroTiles/Bars (:19-20, :54-64) — it does NOT render MacroDetailPanel, so the suggested harness premise is factually wrong. Mobile equally has no committed `deep-redesign/macro-detail-*.png` baseline (mobile uses Maestro flows, not Playwright snapshots), so this is not a web-vs-mobile divergence.

- **Recipe detail: no flag-ON golden baseline (shared web+mobile coverage gap, not a parity divergence)** (`tests/e2e/visual-regression-deep.spec.ts (covers src/app/components/RecipeDetail.tsx; NOT app/recipe/[id]/page.tsx)`) — The three test-infra sub-claims are TRUE and verified: (a) NO committed deep snapshot dir — `ls tests/e2e/__snapshots__/` shows only `visual-audit.spec.ts/` and `visual-regression-subpages.spec.ts/`; `visual-regression-deep.spec.ts/` does not exist. (b) Skips without creds — visual-regression-deep.spec.ts:17-18 `test.beforeEach(() => { test.skip(!hasE2ECredentials(), ...) })`. (c) Runs flag-OFF — grep for `isFeatureEnabled|addInitScript|posthog|design_system|redesign_winmoment|flag` in the spec returns NOTHING, so RecipeDetail.tsx flags render default-off: RecipeDetail.tsx:321 `const redesignColours = isFeatureEnabled("design_system_colours")`, :322 elevation, :323 `const winFeedback = isFeatureEnabled("redesign_winmoment")` are uncovered by any golden. HOWEVER this is NOT a parity gap: mobile has the SAME gates — apps/mobile/app/recipe/[id].tsx:278 `const commitColours = isFeatureEnabled("design_system_colours")`, :285 `const winMomentFeedback = isFeatureEnabled("redesign_winmoment")`, :273 elevation — and mobile has NO flag-ON visual baseline either (Maestro 00e1_recipe_detail.yaml / 05_recipe_detail.yaml contain zero flag-forcing; no recipe .stories. file exists). Mobile is not ahead of web. Two candidate claims are also factually wrong: (1) app/recipe/[id]/page.tsx does NOT render RecipeDetail.tsx — grep for `isFeatureEnabled|design_system|RecipeDetail` in that route returns NONE; it is a flag-free public SSR page rendering inline JSX, and the deep spec reaches RecipeDetail.tsx via `/discover?recipe=...` (spec line 53). (2) app/dev/recipe-detail-redesign/page.tsx is a hand-coded static before/after mockup — its header says "Renders mock data only — no PII, no fetch" and it does NOT import RecipeDetail, so it cannot serve as a deterministic harness for the real component.

- **Mobile screen-diff goldens are captured flags-OFF — but so are web's; not a web-trails-mobile parity gap** (`tests/e2e/visual-regression-deep.spec.ts (+ visual-regression-subpages.spec.ts, visual-audit-authed.spec.ts, tests/e2e/utils/); src/lib/analytics/track.ts`) — Mobile claim verified true: the override hook is real — apps/mobile/lib/analytics.ts:203 `const envKey = `EXPO_PUBLIC_FLAG_FORCE_${flag.toUpperCase().replace(/-/g, "_")}`` (guarded by __DEV__ at :201), and the harness never sets it: maestro-screenshot-tour.mjs:62 `spawnSync("maestro", ["test", tourFlow], {cwd: mobileRoot, stdio: "inherit"})` injects no env; run-maestro-e2e.mjs:201-213 forwards only `EXPO_DEV_SERVER_URL`, `E2E_EMAIL`, `E2E_PASSWORD` via `-e` and buildChildEnv() (:64-74) merges only .env.local/.env/process.env. (Also: EXPO_PUBLIC_* is read by Metro at bundle time, started separately, so the Maestro child env can't reach the bundle anyway.) BUT the candidate's premise that WEB has flags-ON goldens is FALSE. grep for "FLAG_FORCE|forceFlag|posthog|redesign|featureFlags" across tests/e2e/ visual-regression-*.spec.ts + tests/e2e/utils returned ZERO hits. And web src/lib/analytics/track.ts:30-37 isFeatureEnabled has NO dev/E2E override path at all — `if (!process.env.NEXT_PUBLIC_POSTHOG_KEY) return false; ... return posthog.isFeatureEnabled(flag) === true`. There is no NEXT_PUBLIC_FLAG_FORCE_* anywhere in src/ or tests/. So web visual baselines are equally cold/OFF, and web actually LACKS the dev override hook mobile has (mobile is ahead, not behind).
