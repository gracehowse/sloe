appId: host.exp.Exponent
name: "Progress screen — stats, charts, weight journey"
---
- runFlow: shared/login.yaml

- tapOn: "Progress"
- assertVisible: "Progress"
- assertVisible: "Weekly report"

# Should see stat tiles (or empty state)
- assertVisible:
    text: "Avg Calories|Your progress will appear here"

# If data exists, check key elements
- runFlow:
    when:
      visible: "Avg Calories"
    commands:
      - assertVisible: "Protein Hit"
      - assertVisible: "Streak"
      # Scroll to see more
      - scrollUntilVisible:
          element: "Weight"
          direction: DOWN
