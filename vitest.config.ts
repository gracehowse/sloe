// @storybook/addon-vitest — entry for the Storybook Test addon UI and `npm run test:storybook*`.
// Unit tests live in vitest.unit.config.ts (`npm test`, `npm run test:coverage`).
export { default, STORYBOOK_UI_COVERAGE } from "./vitest.storybook.config.ts";
