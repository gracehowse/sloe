/**
 * /profile — profile drill-down from Settings (ENG-608).
 */
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Profile — Sloe",
  description: "Your Sloe profile and preferences.",
};

export default function ProfilePage() {
  return null;
}
