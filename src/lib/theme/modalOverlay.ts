/**
 * Modal/sheet backdrop scrim — shared between web and mobile (ENG-1013).
 *
 * `#00000066` ≈ 40% black. Documented anchor; do not drift per-surface.
 */
export const MODAL_OVERLAY_SCRIM = "#00000066" as const;
